# RULES — 규칙의 의미

`CLAUDE.md` 의 작업 규칙 하나하나가 **왜 있고 어떻게 지키는지**를 풀어 쓴 문서입니다.

규칙 목록 자체는 [CLAUDE.md](CLAUDE.md) 에 있습니다. 그쪽은 매 세션 처음부터 끝까지 읽히므로 짧게 유지하고, 긴 설명은 여기로 옮겼습니다. **규칙을 어떻게 적용할지 애매할 때 해당 절만 펴 보세요.** 전체를 읽을 일은 거의 없습니다.

| 규칙 | 무엇에 관한 것인가 |
|---|---|
| [1](#규칙-1의-의미) | **AI는 `.swift` 를 고치지 않는다.** 코드를 제시하고 사용자가 옮겨 적는다 |
| [2](#규칙-2의-의미) | 바뀐 위치를 짚기 |
| [3](#규칙-3의-의미) | ⭐️GUIDE⭐️ 로 용어 안내 |
| [4·5](#규칙-45의-의미) | 한글 개념 주석과 파일 최상단 요약 (10의 문서화 주석 포함) |
| [6](#규칙-6의-의미) | 막혔던 것을 Q&A.md 에 |
| [7](#규칙-7의-의미) | iOS·SwiftData·FoundationModels 의 함정 |
| [8·9](#규칙-89의-의미) | 최신 공식 문서 확인과 링크 |
| [12](#규칙-12의-의미) | PROGRESS.md 로 세션 잇기 |
| [13](#규칙-13의-의미) | 필요한 만큼만 읽기 |
| [14~19](#규칙-1419의-의미--만들면서-알아가기) | **만들면서 알아가기** — 무엇을 만들지 정하는 법, 만든 것에서 배우는 법 |
| [20](#규칙-20의-의미--문서의-세-층) | **`KCT.docc` 를 같이 고치기** — 문서의 세 층을 어긋나지 않게 |

---

### 규칙 1의 의미

이 저장소의 목적은 **앱이 완성되는 것이 아니라 사용자가 Swift 를 익히는 것**입니다. 그래서 AI 는 `.swift` 를 **직접 고치지 않습니다.** 코드를 제시하고, 사용자가 **손으로 옮겨 적습니다.**

옮겨 적는 동안에만 일어나는 일이 있습니다. 타입 이름을 다시 치면서 철자를 기억하고, 중괄호를 닫으면서 구조를 보고, 컴파일 오류를 스스로 만나 스스로 고칩니다. AI 가 파일을 써 주면 이 셋이 전부 사라지고 **결과만 남습니다.** 결과는 다음 세션에 남지 않습니다.

따라서 AI 는 다음을 **합니다.**

- **코드를 제시합니다** — 어느 파일 어느 위치인지 밝혀서 (규칙 2)
- **읽고 검색합니다** — `XcodeRead`·`XcodeGrep`·`XcodeLS` 로 현재 상태를 파악합니다
- **빌드로 검증합니다** — 사용자가 옮겨 적은 뒤 `BuildProject`·`XcodeRefreshCodeIssuesInFile` 로 확인하고, 오류가 나면 원인을 짚습니다
- **문서를 씁니다** — `.md` 는 **전부** AI 가 직접 고칩니다. 저장소 루트의 문서(PROGRESS·GUIDE·Q&A·RULES·CLAUDE·LEARNING_PLAN·STUDY_GUIDE)도, **`KCT/KCT.docc/` 안의 개념 아티클도** 마찬가지입니다

**경계는 파일이 아니라 「그 줄이 컴파일되는가」로 가릅니다.**

| | 누가 | 왜 |
|---|---|---|
| **실행되는 줄** — `.swift` 의 코드 전부 | **사용자가 옮겨 적는다** | 타입 이름을 다시 치고, 중괄호를 닫고, 컴파일 오류를 스스로 만난다 |
| **설명하는 줄** — `.swift` 안의 `//` · `///` · 최상단 요약 블록 | **AI 가 직접 고친다** | 주석을 손으로 치는 데서는 배울 것이 없다. 규칙 1의 근거가 성립하지 않는다 |
| `.md` — 루트 문서 · `KCT.docc/` 아티클 | **AI 가 직접 고친다** | 같은 이유 |

한때 이 경계를 **확장자**로 그었습니다. `.swift` 안이면 주석까지 사용자가, `.md` 면 전부 AI 가. 나눠 놓기는 쉬웠지만 규칙 20 이 노린 것을 못 지켰습니다 — **주석은 규칙 5·10 의 자리인데, 그것이 사용자 몫이면 코드와 같이 밀립니다.** 실제로 `record()` 를 셋으로 나눈 뒤 최상단 요약 넉 줄이 거짓인 채로 남았고, 그 거짓을 발견한 것은 규칙 21 의 리뷰였습니다.

그래서 지금은 **줄 단위**로 가릅니다. 손으로 쳐서 배우는 것은 코드이지 주석이 아닙니다.

**주석을 고칠 때 지키는 네 가지 — 하나라도 못 지키면 그 파일을 열지 않습니다.**

| | 안전선 | 왜 |
|---|---|---|
| ① | 고치는 줄은 반드시 `//` 로 시작한다 | 코드 줄을 건드리면 규칙 1 이 무너진다 |
| ② | **코드 줄을 한 줄도 더하거나 지우지 않는다** | 줄 수가 바뀌면 사용자가 보던 줄 번호가 어긋난다 |
| ③ | 무엇을 고쳤는지 **파일과 줄로 보고한다** | 규칙 2 — 모르는 사이에 바뀌면 학습이 일어나지 않는다 |
| ④ | 주석을 참으로 만들려면 **코드가 바뀌어야 하는** 상황이면, 고치지 말고 **코드를 제시한다** | 거짓 주석을 지우는 것으로 결함을 덮으면 안 된다 |

④ 가 가장 중요합니다. "`nextDueAt` 을 다시 잡는다" 가 거짓이 됐을 때, 주석만 고쳐서 참으로 만들 수도 있고 빠진 코드를 되살려서 참으로 만들 수도 있습니다. **어느 쪽인지는 AI 가 혼자 정하지 않습니다**(규칙 18) — 둘을 보이고 사용자가 고릅니다.

다음은 **하지 않습니다.**

- **`.swift` 의 코드 줄을 쓰거나 고치지 않습니다.** `XcodeWrite` 로 소스를 만들지 않습니다. 주석 줄은 위 안전선 안에서 고치되, **코드 줄은 한 줄도 건드리지 않습니다.** 사용자가 "직접 고쳐 줘" 라고 명시적으로 말한 경우에만 예외이고, 그때도 **무엇을 대신 해 주는지 한 줄로 밝힙니다**
- **`project.pbxproj` 를 직접 편집하지 않습니다.** 파일 추가·이동은 사용자가 Xcode 에서 합니다. 직접 고치면 프로젝트가 깨집니다
- **묻지 않고 커밋·푸시하지 않습니다.** 커밋 시점은 사용자가 정합니다
- **요청 범위를 넘어 손대지 않습니다.** 고쳐야 할 것이 눈에 띄면 고치지 말고 **말합니다**

**코드를 제시할 때의 크기가 중요합니다.** 한 번에 옮겨 적을 수 있는 양이어야 합니다. 100줄을 한꺼번에 내면 사용자는 읽지 않고 복사합니다 — 그러면 AI 가 직접 쓴 것과 같아집니다. **한 번에 하나의 함수, 하나의 판단.**

**설명은 코드가 아니라 판단을 담습니다.** "`SessionBuilder.swift:31` 의 `build()` 를 4개 함수로 나눕니다 — 한 함수가 복습 선별·신규 도입·슬롯 채우기·모드 배정을 모두 하고 있어서 알고리즘이 이야기로 읽히지 않습니다" 처럼, **왜 그 선택인지**가 본문입니다.

### 규칙 2의 의미

AI가 직접 고치더라도, 사용자가 **어디가 어떻게 바뀌었는지 모르면 학습이 일어나지 않습니다.** 변경을 설명할 때는 항상 다음을 밝힙니다.

- **파일 경로** — 저장소 루트 기준. 줄 번호가 있으면 `KCT/Session/SessionBuilder.swift:31` 형식으로
- **새 파일인지 기존 파일 수정인지** — 새 파일이면 어느 그룹(폴더)에 들어갔는지
- **무엇을 대체했는지** — 어떤 함수가 어떤 함수들로 갈라졌는지, 어떤 이름이 어떤 이름이 됐는지
- **최상단 요약 블록에 영향이 있으면 그 수정도 함께** (규칙 5)

**전문을 다시 출력하지 않습니다.** 파일 하나를 고칠 때 200줄을 되풀이하면 정작 바뀐 3줄이 묻힙니다. 바뀐 부분만, 필요하면 앞뒤 한두 줄만 보여줍니다.

이름을 바꿀 때는 **바꾼 이유를 한 줄로** 남깁니다. 이름 변경은 눈에 잘 보이지 않으면서 파일 전체에 퍼지므로, 이유가 없으면 다음 세션이 되돌리려 합니다.

> 예시
> `KCT/Progress/DifficultyMode.swift` → `AskingMode.swift` (타입 이름도 함께)
> 이유: 이 열거형은 *난이도*가 아니라 **묻는 방식**(2지선다·O/X·4지선다·직접입력)입니다. `Question.difficulty`(축 A)와 이름이 겹쳐 두 축이 헷갈렸습니다

### 규칙 3의 의미

사용자는 하고 싶은 것을 길게 풀어서 설명하지만, 그것을 부르는 정식 명칭을 모르는 경우가 많습니다. 용어를 모르면 검색도 질문도 어려워지므로, AI가 그 다리를 놓아줍니다.

**언제 붙이나**

- 사용자가 현상이나 원하는 결과를 길게 서술했는데 그것을 가리키는 용어가 따로 있을 때
- 설명 안에 여러 개념이 뒤섞여 있어 정리가 필요할 때
- 사용자가 쓴 표현과 Apple 공식 문서의 용어가 다를 때

짧고 명확한 요청에는 붙이지 않습니다. 매번 붙이면 잡음이 됩니다.

**형식**

답변 안에 다음과 같이 넣습니다. 용어와 그 뜻을 한 줄로 짝지어, 사용자가 그대로 검색할 수 있게 씁니다. 영문 API 이름이나 공식 용어가 있으면 함께 적습니다.

```
⭐️GUIDE⭐️
- **용어** — 사용자가 말한 그것이 무엇인지 한 줄 설명
- **용어** — ...
```

예시. 사용자가 "맞힌 문제는 좀 있다가 다시 나오고 틀린 건 바로 또 나오게 하고 싶어"라고 썼다면:

```
⭐️GUIDE⭐️
- **간격 반복 (spaced repetition)** — 복습 간격을 정답 여부에 따라 늘리거나 줄이는 학습법. 방금 설명하신 게 이겁니다
- **다음 출제 예정 시점 (due date)** — 그 간격을 저장하는 값. 이 앱에서는 `QuestionProgress.nextDueAt`
- **승급·강등 (promotion / demotion)** — 맞히면 한 단계 어려운 방식으로, 틀리면 쉬운 방식으로 옮기는 것
```

안내한 용어는 규칙 11에 따라 [GUIDE.md](GUIDE.md) 에도 누적합니다.

### 규칙 4·5의 의미

초보자에게 부족한 것은 문법 지식이 아니라 **전체 그림**입니다. 이 값이 어디서 와서 어디로 가는지, 이 함수가 누구의 부탁을 받아 무엇을 책임지는지가 안 보이면, 코드가 돌아가도 배운 게 남지 않습니다. 주석이 그 지도 역할을 합니다.

#### 파일 최상단 요약 (규칙 5)

모든 `.swift` 파일은 다음 네 블록으로 시작합니다.

- **역할** — 이 파일이 무엇을 하는 화면/타입인지 한두 줄
- **구성** — 주요 변수·함수의 이름과 각자의 책임. 계층을 들여쓰기로 표현
- **흐름** — 값과 호출이 오가는 경로. 화살표로 순서대로
- **연결** — 누가 이 파일을 부르는지, 이 파일은 무엇에 기대는지

```swift
//
//  SessionBuilder.swift
//  KCT
//
//  역할 : 이번 회차에 어떤 문제를 어떤 방식으로 낼지 자동으로 정한다
//  요점 : 어머니가 아무것도 고르지 않아도 되도록, 선택을 전부 여기서 대신한다
//
//  ── 구성 ──────────────────────────────────────────────
//  SessionBuilder            출제 계획을 세우는 주인
//  ├─ catalog                문제집 (어떤 문제가 있는지)
//  ├─ isUnlocked             스토리 모드 게이트. 지금은 항상 열림
//  ├─ build()                계획 전체를 지휘
//  │   ├─ reviewQueue()      복습 대상 줄 세우기 (예정 시각 이른 순)
//  │   ├─ introduceOrder()   신규 도입 줄 세우기 (쉬운 순 + 단원 번갈아)
//  │   └─ shapeRound()       첫·마지막을 격려용 2지선다로
//
//  ── 흐름 ──────────────────────────────────────────────
//  QuizView 가 세션 시작
//    → build(size:progressByID:) 호출
//    → 복습 + 신규를 이어 붙여 size 만큼 고름
//    → 각 문제에 진척이 기억한 모드를 붙임
//    → QuizItem 배열로 돌려줌
//    → QuizView 가 화면에 하나씩 표시
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuizView.startSession()
//  기대는 것    : QuestionCatalog(문제), QuestionProgress(진척), QuizItem(출제 항목)
//  건드리지 않는 것 : 진척 저장 — 채점 후 QuizView 가 record() 로 반영한다
//
```

`구성`에는 모든 변수를 다 적지 않습니다. 임시 변수나 반복문 카운터는 빼고, **그 파일을 이해하는 데 필요한 것만** 적습니다.

`연결`에 **건드리지 않는 것**을 한 줄 넣으면 책임 경계가 훨씬 선명해집니다. 무엇을 하는지보다 **무엇을 하지 않는지**가 역할을 규정합니다.

#### 본문 주석 (규칙 4)

한글로 개념을 풀되, **무엇을 하는지보다 왜 그렇게 하는지**를 우선합니다. 무엇을 하는지는 코드를 읽으면 보이지만, 왜는 보이지 않습니다. 특히 API의 함정이나 반직관적인 동작은 반드시 이유를 남깁니다.

```swift
// Swift 의 hashValue 는 실행할 때마다 값이 달라져 저장용으로 쓸 수 없다.
// 앱을 껐다 켜도 같은 값이 나와야 캐시가 맞으므로 SHA256 을 쓴다.
let digest = SHA256.hash(data: Data(text.utf8))
```

**용어는 한글과 영문을 함께** 적습니다. 한글만 있으면 검색이 안 되고, 영문만 있으면 뜻이 안 잡힙니다.

```swift
// 오답 보기(distractor) — 정답이 아닌 선택지
// 다른 문제의 정답에서 뽑아 오므로 문제집이 바뀌면 자동으로 따라 바뀐다
```

**자명한 것에는 달지 않습니다.** 모든 줄에 주석이 붙으면 정작 중요한 주석이 묻힙니다.

```swift
currentIndex += 1   // 다음 문제로 ← 불필요
```

**주석을 늘리기보다 함수를 쪼개 이름을 붙이는 쪽이 낫습니다.** 설명이 필요하다고 느껴지는 코드 덩어리는, 대개 이름 없는 함수가 숨어 있다는 신호입니다. 이름이 설명을 대신하면 주석은 저절로 줄어듭니다.

#### 문서화 주석으로 코드 밖에 두기 (규칙 10)

설명이 길어지는 것 자체는 문제가 아닙니다. 문제는 그것이 **코드와 같은 자리, 같은 무게로 깔려서** 코드를 밀어내는 것입니다. 그래서 설명은 `///` 문서화 주석(DocC)으로 씁니다. Xcode가 이를 문서로 인식해 `Option+클릭` 했을 때만 팝업으로 보여주므로, 화면에는 요약 한 줄만 남습니다.

**첫 문장은 요약, 빈 줄 뒤는 설명입니다.** Xcode가 이 둘을 구분해 다르게 취급하므로 순서를 지킵니다.

```swift
/// 문제별 학습 진척. 맞히면 한 단계 위 방식으로, 틀리면 아래로 옮깁니다.
///
/// 세션(날짜)을 넘어 유지되어야 하므로 SwiftData 로 영구 저장합니다.
/// 문제 내용(``Question``)과 분리되어 있어, 문제집을 갈아끼워도 기록은 남습니다.
@Model final class QuestionProgress { }
```

**API의 함정은 콜아웃으로 뺍니다.** `- Important:` 는 주황색, `- Note:` 는 회색 상자로 강조됩니다.

```swift
/// - Important: 문제 id 는 영구 고정입니다. 재사용하면 학습 기록이
///   엉뚱한 문제에 붙습니다.
```

**다른 타입은 백틱 두 개로 링크합니다.** ` ``QuestionProgress`` ` 처럼 쓰면 DocC 가 실제 링크로 만들어 줍니다. 이 링크들이 모여 문서에서 역할 관계를 따라갈 수 있게 됩니다.

**⚠️ `private` 멤버는 DocC 문서에 나오지 않습니다.** 좋은 설명을 private 함수에만 달아 두면 문서에서 사라집니다. 설명할 가치가 있는 로직이 private 안에 갇혀 있다면, 그것은 **별도 타입으로 꺼낼 때가 됐다는 신호**입니다.

**본문 인라인 `//` 주석은 "왜"만, 2줄 이내로 짧게** 남깁니다. 긴 설명은 위의 `///` 로 올립니다.

**무엇을 어디에 쓸지**

| 무엇을 | 어디에 |
|---|---|
| 파일 전체 지도 | 최상단 요약 블록 (규칙 5) |
| 타입·변수·함수 설명 | `///` — 코드엔 요약 한 줄, 나머지는 Quick Help |
| API 함정, 주의사항 | `///` 안의 `- Important:` / `- Note:` |
| 왜 이 줄이 이렇게 생겼나 | 인라인 `//` — 2줄 이내 |
| 개념 자체의 긴 설명 | [GUIDE.md](GUIDE.md) 또는 [Q&A.md](Q&A.md) |
| 설계 원칙, 단계별 계획 | [LEARNING_PLAN.md](LEARNING_PLAN.md) |

사용자에게 알려둘 것: 함수 위에서 `Control+클릭` → **Add Documentation** 으로 틀을 자동 생성할 수 있고, Product → **Build Documentation** 으로 `///` 주석을 모아 문서 사이트를 만들 수 있습니다.

#### 함께 지킬 것

- **`// MARK: -` 로 본문을 구획**합니다. Xcode 점프바에 목차가 생겨서 최상단 `구성` 블록과 실제 코드가 눈으로 연결됩니다.
- **코드를 고치면 최상단 요약도 같이 고칩니다.** 낡은 주석은 없느니만 못합니다.
- **처음 등장하는 타입이나 메서드**에는 한 줄 설명을 붙입니다. 두 번째부터는 생략합니다.
- **한 줄 계약** — 모든 타입의 `///` 첫 줄을 한 문장으로 못 쓰면, 그 타입이 일을 너무 많이 하고 있다는 뜻입니다. 쪼갤 신호로 씁니다.

### 규칙 6의 의미

같은 질문을 두 번 하지 않게 하는 것이 목적입니다. 특히 빌드 오류로 막혔던 경험은 시간이 지나면 원인은 잊고 증상만 남습니다.

기록은 저장소 루트의 **[Q&A.md](Q&A.md)** 에 남깁니다. 최신 항목을 위에 추가합니다. 형식은 이렇습니다.

- **제목** — 증상을 한 줄로. 나중에 검색할 때 걸리는 말로 씁니다
- **Q** — 무엇에 막혔는지 한 줄
- **A** — 원인과 해결. 코드 조각이 필요하면 최소한만
- **교훈** — 다음에 같은 실수를 피하는 한 줄 (있을 때만)
- **참고 문서** — 확인한 Apple 문서 링크 (규칙 9)

모든 대화를 기록하지 않습니다. **빌드가 막혔던 것, 개념을 오해했던 것, 문서와 실제가 달랐던 것**만 남깁니다.

### 규칙 7의 의미

KCT가 쓰는 프레임워크들은 각각 조용히 실패하는 방식이 있습니다. 컴파일은 되는데 런타임에 아무 일도 안 일어나는 부류라, 미리 알지 않으면 원인을 찾는 데 오래 걸립니다.

**FoundationModels (온디바이스 모델)**

- `SystemLanguageModel.default.isAvailable` 이 `false` 인 기기·시뮬레이터가 있습니다. **모델을 쓰는 경로에는 반드시 폴백이 있어야 합니다.** 이 앱에서는 규칙 기반 추출(`QuestionFocus`)과 결정적 채점(`RuleGrader`)이 그 역할입니다
- 모델은 **지어냅니다(hallucination).** 하이라이트처럼 "지문에 실제로 있는 문자열"이 필요한 경우 결과를 반드시 검증하고, 어긋나면 조용히 버립니다
- 실패는 화면에 노출하지 않습니다. 어르신에게 "모델 오류"는 아무 의미가 없습니다

**SwiftData**

- `@Query` 는 **즉시 갱신되지 않을 수 있습니다.** 지우고 바로 다시 만드는 흐름에서는 `@Query` 결과를 믿지 말고 직접 만든 값을 씁니다
- `@Attribute(.unique)` 로 중복을 막되, 그래도 조회할 때는 중복에 대비합니다
- `ModelContext` 를 만지는 코드는 `@MainActor` 경계를 지킵니다

**SwiftUI**

- **Combine 을 쓰지 않습니다.** `async`·`await`, `@Observable`, `.task`, `.onChange(of:)` 로 해결합니다
- 백그라운드 작업은 세션이 바뀔 때 **취소**합니다. `Task` 핸들을 보관해 두고 `cancel()` 합니다

**AVFoundation (낭독)**

- 무음 스위치가 켜져 있어도 들리게 하려면 `AVAudioSession` 카테고리를 `.playback` 으로 설정해야 합니다
- 고품질 한국어 음성은 **사용자가 따로 내려받아야** 존재합니다. 없을 때를 대비해 점수 기반으로 고릅니다

**Xcode 프로젝트**

- **file-system synchronized group** 방식입니다. `KCT/KCT/` 안의 파일은 확장자를 Xcode 가 모르면 **앱 번들 루트로 복사**됩니다. 문서·메모는 저장소 루트에 둡니다
- 번들 리소스는 폴더 구조와 무관하게 **번들 루트에 평평하게** 놓입니다. 그래서 `Content/questions.json` 을 `Bundle.main.url(forResource: "questions")` 로 찾을 수 있습니다

### 규칙 8·9의 의미

SwiftUI·SwiftData 는 해마다 API가 바뀌고, FoundationModels 는 아주 새로운 프레임워크입니다. 기억이나 검색 결과에 의존하면 없는 API를 만들어내거나 가용성을 착각합니다.

- 코드를 제시하기 전에 **`DocumentationSearch` 로 해당 심볼을 확인**합니다. 특히 **availability(어느 OS부터 되는지)** 를 봅니다
- 확인한 문서는 답변 끝에 **참고 문서** 목록으로 남깁니다
- 문서 페이지가 JavaScript로 렌더링되어 내용이 안 보이면, 주소 앞부분을 `https://developer.apple.com/tutorials/data/documentation/...` 로 바꾸고 끝에 `.md` 를 붙이면 원문을 얻을 수 있습니다

**모른다고 말하는 것이 지어내는 것보다 낫습니다.** 특히 새 API는 학습 데이터에 없을 수 있습니다. 프로젝트에 있는 코드가 낯설다면 그것은 틀린 코드가 아니라 **내가 모르는 새 API** 일 가능성이 높습니다. 지우거나 고치기 전에 먼저 찾아봅니다.

### 규칙 12의 의미

대화는 길어질수록 비싸지고, 세션이 바뀌면 사라집니다. 그래서 **"지금 어디까지 왔나"를 대화가 아니라 파일에 둡니다.**
[PROGRESS.md](PROGRESS.md) 가 그 파일입니다. 새 세션은 저장소를 훑는 대신 이 파일 하나를 읽고 곧장 이어서 작업합니다.

**언제 읽나** — 세션의 **첫 동작**입니다. 사용자가 무엇을 물어도 그 전에 읽습니다.

**언제 쓰나** — 다음 중 하나가 일어나면 그 자리에서 갱신합니다. 세션 끝까지 미루지 않습니다. 세션은 예고 없이 끊깁니다.

- 한 단계를 끝냈을 때 — 「지금」 네 줄과 「파일 지도」
- 새 `.swift` 파일이 생기거나 사라졌을 때, 이름이 바뀌었을 때 — 「파일 지도」
- 되돌리고 싶어질 만한 선택을 했을 때 — 「결정 기록」에 **이유와 함께** 한 줄
- 답을 못 찾고 넘어간 것이 있을 때 — 「열린 질문」

**무엇을 쓰지 않나** — 잘한 일의 목록이 아닙니다. **다음 사람이 모르면 막히는 것**만 씁니다.
개념 설명은 [GUIDE.md](GUIDE.md), 막혔던 것의 해결은 [Q&A.md](Q&A.md), 단계별 계획은 [LEARNING_PLAN.md](LEARNING_PLAN.md) 입니다. 같은 사실을 두 곳에 쓰지 않습니다. 중복이 생기면 낡은 쪽이 거짓말을 하게 됩니다.

**상한은 100줄입니다.** 넘으면 오래된 것을 각자의 집으로 보내고 여기서 지웁니다. 이 파일이 뚱뚱해지면 매 세션 그 비용을 다시 냅니다.

### 규칙 13의 의미

파일을 여는 것에는 값이 붙습니다. 이 저장소는 특히 그렇습니다. `.swift` 파일은 규칙 5 때문에 최상단 요약만 40줄이고, 화면 파일은 수백 줄입니다. 습관적으로 전체를 열면 정작 중요한 것을 볼 여력이 없어집니다.

**대신 이렇게 찾습니다.** 이 프로젝트는 Xcode 안에서 작업하므로 MCP 도구를 씁니다.

| 알고 싶은 것 | 여는 방법 |
|---|---|
| 이 파일이 무슨 일을 하나 | 최상단 요약만 — `XcodeRead(filePath:, limit: 40)` |
| 어떤 심볼이 어디 쓰이나 | `XcodeGrep(pattern:)` — 파일 전체를 열기 전에 먼저 |
| 어떤 파일이 있나 | `XcodeLS` / `XcodeGlob` — `ls`·`find` 를 쓰지 않습니다 |
| 이 폴더가 무슨 책임인가 | 열어보지 않습니다. `CLAUDE.md` 의 폴더 구조표로 나옵니다 |
| 지금 어디까지 왔나 | [PROGRESS.md](PROGRESS.md) 만 |
| 컴파일 되는지 빠르게 | `XcodeRefreshCodeIssuesInFile` — 전체 빌드보다 훨씬 쌉니다 |
| 저장소 전반의 넓은 조사 | 서브에이전트에 위임 — 중간 과정 말고 결론만 돌아옵니다 |

**⚠️ 셸 명령(`ls`, `find`, `cat`)을 파일 구조 파악에 쓰지 않습니다.** 사용자가 매번 승인을 눌러야 하므로 비쌉니다. MCP 도구가 같은 일을 더 빠르게 합니다.

규칙 5의 최상단 요약 블록은 이때를 위한 것입니다. **요약만 읽고 판단이 서면 본문은 열지 않습니다.**

**답변할 때도 같습니다.** 파일 하나를 고칠 때 전문을 다시 출력하지 않습니다. 규칙 2에 따라 **위치를 짚고 바뀌는 부분만** 보여줍니다.

**사용자에게 부탁할 것** — 빌드 로그나 오류 전문은 대화에 붙여넣기보다 파일로 저장하고 경로를 알려주는 편이 쌉니다. 필요한 줄만 찾아 읽겠습니다.

---

## 규칙 14~19의 의미 — 만들면서 알아가기

규칙 1~13 은 전부 **"코드를 어떻게 쓰나"** 입니다. 그런데 이 프로젝트에서 실제로 어려운 것은 코드가 아니었습니다. **무엇을 만들지 정하는 일**과 **만든 것에서 무엇을 배웠는지 붙잡는 일**이었습니다. 규칙이 그 자리를 비워 두었더니 이런 일이 일어났습니다.

> "다시 완벽 주의가 발동하는 기분이다. 여러 논문을 읽으면서 호기심이 발동해 이것 저것 찾아보는데 시간이 조금 많이 소요되고 있다. (…) 내가 실제로 체감하면서 리서치를 하는게 아니다보니, 가설 검증이 제대로 되지 않은채로 완벽한 프로토타입 계획안을 만들려는 것 같다."
> — CBL 기록, 8월 24일

규칙 14~19 는 이 문장에 대한 답입니다. 근거는 로렌 부크먼의 『Make to Know』이고, 핵심은 하나입니다 — **창작은 아는 것을 옮기는 일이 아니라, 만들면서 알아 가는 일이다.** 스티브 잡스가 애플 스토어를 만든 방식이 완벽한 계획이 아니라 **끊임없는 실행과 수정**이었던 것처럼.

### 규칙 14 — 만들기 전에 「무엇을 알게 될까」를 먼저 적는다

기능을 제안할 때 아래 셋을 함께 적습니다. 사용자가 CBL 기록(p.54)에 스스로 세운 검산 사다리입니다.

1. **이걸 지나간 어머니에게 무엇이 일어나야 하나** (결과)
2. **그러려면 무엇이 참이어야 하나** (전략)
3. **지금 하는 이 작업이 ②에 닿나** (전술)

> "결과 → 전략 → 전술의 사다리를 거꾸로 타서 검산한다. **어떤 전술이 어떤 결과에 닿는지 설명되지 않으면 그 전술은 버린다.**"

**셋을 못 적으면 그 기능은 아직 만들 것이 아닙니다.** 못 적는 이유는 대개 둘 중 하나입니다 — 결과를 정하지 않았거나, 그 기능이 실은 다른 이유(만들기 재미있어서, 남들이 하니까)로 떠오른 것입니다.

이것은 디자인 씽킹의 핵심이기도 합니다. **연구를 끝내고 제작에 들어가는 것이 아니라, 프로젝트의 모든 단계에 연구를 넣는 것.** 그래서 "리서치가 끝나면 만든다" 가 아니라 **"만들면서 계속 묻는다"** 입니다.

### 규칙 15 — 더하기 하나에 빼기 하나

어머니의 행동을 바꾸려 할 때, 추가안만 내지 않습니다. **뺄 것 하나를 반드시 짝지어** 냅니다.

이 규칙이 필요한 이유는 이미 관찰됐습니다.

> "무언가 추가하다보니 정보량이 많아졌다. 간소화할만한게 있을지 찾아보자." — CBL p.46
> "결과 화면에 정보량이 많아 다시 모든 내용을 확인하는 것이 힘들어보인다." — CBL p.36

어르신 화면에서 **더하기는 언제나 비용을 동반합니다.** 글자가 하나 늘면 읽을 것이 하나 늘고, 버튼이 하나 늘면 고를 것이 하나 늡니다. 그래서 책의 조언은 이렇습니다 — **"사람의 행동을 유도하기 위해서 무언가를 계속 추가하기보다, 때론 불필요한 게 다른 행동을 유발하는지 살펴보자."**

뺄 것을 정말 못 찾았다면 **"뺄 것을 못 찾았다"고 밝힙니다.** 조용히 생략하지 않습니다 — 그러면 이 규칙이 없는 것과 같아집니다.

### 규칙 16 — 어머니에게서 관찰된 것이 논문을 이긴다

근거를 제시하기 전에 **CBL 기록과 PROGRESS 에 같은 것이 관찰됐는지 먼저 봅니다.** 논문의 평균과 어머니의 관찰이 어긋나면 **어머니가 이깁니다.**

이유는 단순합니다. 논문의 효과크기는 **다른 사람 수백 명의 평균**이고, 우리에게 필요한 것은 **한 사람의 값**입니다. n=1 은 표본이 작은 게 아니라 **모집단이 한 명**인 것입니다.

실제로 이 프로젝트에서 여러 번 일어났습니다.

- 논문이 말하기 전에 **"질문을 소리내서 읽자 정답율이 틀린 문제도 다시 맞추게 되었다"**(p.36)가 두 번 관찰됐습니다
- 힌트 계단에 대해 **"힌트를 계속 주면 기다리면서 길들여진다"**(p.57)를 논문보다 먼저 적어 두었습니다
- 세션 길이 10분은 추정이 아니라 **두 번 잰 값**입니다(p.36·38)

그러므로 AI 가 논문을 가져올 때는 **"기록에 이미 있는가"를 먼저 확인**하고, 있으면 논문은 **뒷받침**으로만 씁니다. 없으면 그때 방향으로 씁니다 — **크기는 어머니에게서 다시 잽니다.**

### 규칙 17 — 모른다는 것을 지우지 않는다

빈칸을 "아마도" 로 메우지 않습니다. 근거가 없는 자리는 **비워 두고 비었다고 적습니다.**

특히 위험한 것은 **계획을 완결된 것처럼 보이게 만드는 것**입니다. 11단계짜리 표를 만들면 그 표가 확실해 보이지만, 실은 그중 절반이 아직 재지 않은 숫자 위에 서 있을 수 있습니다. 그러면 사용자는 **확실하지 않은 것을 확실한 것으로 착각한 채** 다음 단계로 갑니다.

- 답이 없는 것은 [PROGRESS.md](PROGRESS.md) 「열린 질문」에 그대로 남깁니다
- 추정값에는 **"이건 추정이다"** 를 붙입니다. 나중에 실측으로 갈아끼울 자리를 표시해 두는 것입니다
- 논문을 인용할 때 **어느 집단·어느 과제에서 나온 값인지**를 같이 적습니다. 조건 없는 효과크기는 이 저장소에서 쓰지 않습니다

> "불확실할 때 창조의 가능성이 열린다. 창작자는 불확실함의 공간에 들어간다."

불확실성은 없애야 할 결함이 아니라 **작업이 일어나는 자리**입니다. 지우면 그 자리도 같이 사라집니다.

### 규칙 18 — AI가 가장 잘 안다고 두지 않는다

> "좋은 교육을 지향한다면 '교사가 가장 잘 알고 있다' 라는 생각을 버린다."
> "배우는 데 옳고 그른 방법이란 것은 없어요. 사람들은 모두 달라서, 각자에게 맞는 방법과 수단이 필요하지요."

AI 는 빠르게 답을 냅니다. 그래서 그냥 두면 **사용자가 판단할 자리를 전부 가져갑니다.** 그러면 앱은 만들어지지만 사용자는 남지 않습니다.

- **판단이 갈리는 지점에서는 결론을 먼저 주지 않습니다.** 선택지와 **각각의 대가**를 보이고, 무엇을 고를지는 사용자가 정합니다
- **답변마다 「스스로 생각할 지점」을 최소 하나 남깁니다.** 사용자가 직접 요청한 것입니다 — *"클로드에게 내가 성장할 수 있도록 스스로 생각할 수 있는 공간도 마련해달라고 요청했다"*(CBL p.60)
- **AI 가 틀렸던 것을 기록합니다.** 이 프로젝트에서 이미 여러 번 있었습니다(효과크기를 조건 없이 옮긴 것, 3차에 없는 화면을 있다고 가정한 것). Q&A.md 에 남기면 **AI 의 말을 어느 정도로 믿을지**에 대한 감각이 쌓입니다

사용자가 스스로 정한 방향에서는 **사용자가 가장 잘 아는 사람**이어야 합니다. AI 는 그 방향을 대신 정하지 않습니다.

### 규칙 19 — 어머니는 사용자가 아니라 공동 창작자다

화면을 설계할 때마다 묻습니다. **"어머니가 이것을 평가하거나 고칠 자리가 있는가."**

이 규칙의 뿌리는 논문이 아니라 7월 31일의 관찰입니다.

> "핵심적으로는 **아들 앞에서 틀리면 초라해지는 두려움**에 어머니가 유능한 사람으로 느껴지게 할 것을 시도해보려고 한다."
> "어머니가 가르치는 시간을 만드세요. (…) 관계에서 '나는 아무것도 모르는 사람' 이라는 위치가 굳어지면 **공부 자리에 앉는 것 자체가 굴욕이 됩니다.** 하루에 한 번은 어머니가 유능한 사람인 시간이 있어야 합니다."

앱이 계속 묻고 어머니가 계속 답하기만 하면, 그 관계는 **매 세션 "나는 모르는 사람" 을 확인시킵니다.** 그래서 설계에 **역할이 뒤집히는 자리**를 하나는 둡니다.

- **평가하는 자리** — "이 설명 이상해요" 같은 버튼 하나. 눌린 문항은 실제로 고칩니다. 고쳐야 참여가 진짜가 됩니다
- **고르는 자리** — 오늘 무엇을 할지 **둘 중 하나**만. 셋 이상은 부담입니다
- **끝내는 자리** — "오늘 여기까지 할까요?" 종료 시점의 주도권
- **가르치는 자리** — 세션 시작에 "이따 하나 설명해 주실 수 있어요?" 한 줄. 비용은 문자열 하나이고, 효과의 대부분은 **가르치는 행위가 아니라 가르칠 것을 아는 상태**에서 옵니다

그리고 이 규칙은 **어머니에게만 적용되는 것이 아닙니다.** 사용자 본인도 이 앱의 사용자이고, AI 와의 관계에서 같은 일이 일어날 수 있습니다. 규칙 18 이 그 짝입니다.

---

## 규칙 20의 의미 — 문서의 세 층

이 저장소의 문서는 세 층입니다. **각 층이 잡는 것이 다릅니다.**

| 층 | 무엇을 설명하나 | 어디 있나 | 언제 낡나 |
|---|---|---|---|
| **최상단 요약** (규칙 5) | 파일 하나의 지도 | `.swift` 맨 위 | 함수가 생기거나 이름이 바뀔 때 |
| **`///` 주석** (규칙 10) | 심볼 하나의 계약 | 선언 바로 위 | 동작·조건이 바뀔 때 |
| **`KCT.docc` 아티클** (규칙 20) | **여러 파일에 걸친 개념** | `KCT/KCT.docc/` | **규칙 자체가 바뀔 때** |

앞의 둘은 **고치는 파일 안에 있습니다.** `record()` 를 고치려고 파일을 열면 그 위의 요약 블록과 `///` 가 눈에 들어옵니다. 그래서 잘 안 잊습니다.

**세 번째 층은 다른 폴더에 있습니다.** 코드를 고치는 동안 한 번도 화면에 뜨지 않습니다. 그래서 **아무도 눈치채지 못한 채로 낡습니다.** 규칙 20 이 필요한 이유가 이것 하나입니다.

### 낡으면 어떻게 되나

두 가지가 일어납니다. 둘 다 나중에 비싸집니다.

**① 죽은 심볼 링크** — ``QuestionProgress/record(correct:now:)`` 처럼 없어진 심볼을 가리키는 링크는 `xcodebuild docbuild` 에서 경고로 나옵니다. 경고가 쌓이면 **아무도 경고를 안 보게 됩니다.**

**② 낡은 설명이 사실로 읽힘** — 이쪽이 훨씬 나쁩니다. 다음 세션은 코드를 다 읽지 않습니다(규칙 13). 개념은 아티클에서 얻습니다. 아티클이 "격려용 슬롯은 축 B를 건드리지 않는다" 라고 적혀 있으면, **실제로는 한 칸 올리는 코드가 있어도 없는 것으로 판단합니다.**

### 누가, 언제 고치나

**AI 가, 코드를 제시하는 그 답변 안에서 직접 고칩니다.** 사용자에게 옮겨 적으라고 내밀지 않습니다 — 규칙 1의 이유(손으로 치면서 배운다)가 `.md` 에는 성립하지 않기 때문입니다. 마크다운을 옮겨 적는 데서 배우는 것은 없고, 비용만 있어서 **결국 미뤄집니다.**

"나중에 정리하겠습니다" 도 하지 않습니다 — 나중은 오지 않고, 와도 그때는 무엇이 바뀌었는지 기억나지 않습니다.

**AI 가 아티클을 먼저 고치면 순서가 뒤집힙니다.** 사용자가 아직 `.swift` 를 옮겨 적기 전이라 아티클이 없는 심볼을 가리키게 되는데, 이건 문제가 아니라 **쓸모입니다** — `docbuild` 경고 목록이 그대로 **"아직 안 옮겨 적은 것"의 목록**이 됩니다. 경고가 0이 되는 순간이 곧 반영이 끝난 순간입니다.

아래에 해당하면 아티클을 확인합니다.

- **심볼 이름이 바뀌었다** — 링크가 죽습니다. 반드시
- **동작 규칙이 바뀌었다** — "~하지 않는다" 가 "~한다" 가 되는 종류. 반드시
- **함수가 갈라지거나 합쳐졌다** — 흐름 설명이 어긋납니다
- 값만 바뀌었다 (상수·문구) — 아티클에 그 값이 적혀 있을 때만

### 어느 아티클을 여나

전부 열지 않습니다. **바꾼 심볼 이름으로 `XcodeGrep` 한 번**이면 됩니다.

```
XcodeGrep(pattern: "record\\(correct")   → 어느 아티클이 이 심볼을 말하는지
```

지금 있는 아티클과 각자의 담당은 이렇습니다.

| 아티클 | 담당 개념 | 이게 바뀌면 |
|---|---|---|
| `TwoAxes.md` | 축 A(문제 난이도) / 축 B(묻는 방식 사다리) | 승급·강등 규칙, 마스터 판정, 격려용 슬롯 |
| `SessionPlanning.md` | 회차 구성 4단계 | 줄 세우기, 칸 채우기, 모양 잡기 |
| `GradingPath.md` | 규칙 먼저, 안 되면 뜻으로 | 채점 경로, 폴백 |
| `FocusLayers.md` | 묻는 대상 하이라이트 3층 | 2·3층을 켜거나 끌 때 |
| `ElderAccessibility.md` | 어르신 접근성 원칙 | 글씨·대비·터치·부정 표현 |
| `KCT.md` | 랜딩 + Topics 목록 | **타입이 생기거나 사라질 때** |

`KCT.md` 의 `## Topics` 는 **새 타입을 추가하면 반드시** 넣습니다. 빠지면 문서에서 그 타입만 고아가 됩니다.

### 검증

`xcodebuild docbuild` 로 확인합니다. **심볼 링크 경고 0 이 통과 기준**입니다. 사용자에게 부탁할 것 — 이 명령은 시간이 걸리므로 매번이 아니라 **아티클을 고친 뒤 한 번** 돌리면 됩니다.

> 이 규칙은 실제로 낡은 뒤에 생겼습니다. `record(correct:now:)` 를 `countAttempt`·`moveLadder` 로 나눴을 때 `.swift` 두 파일은 같이 고쳤지만, `TwoAxes.md` 와 `SessionPlanning.md` 는 **사흘 동안 낡은 채로 있었습니다.** 그중 한 곳은 절 제목 자체가 틀린 말이 됐습니다 — 「격려용 슬롯은 축 B를 건드리지 않는다」.

---

## 규칙 21의 의미 — 반영을 확인하는 자리

규칙 1 때문에 이 저장소에는 **손이 둘**입니다. `.swift` 는 사용자가 치고, `.md` 는 AI 가 고칩니다. 나눠 놓으면 배우게 되지만, 대신 **아무도 전체를 본 적이 없는 상태**가 생깁니다. AI 는 자기가 제시한 것만 알고, 사용자는 자기가 친 것만 압니다.

규칙 21 은 그 둘이 만나는 유일한 지점입니다.

### 언제 시작되나

사용자가 **반영을 마쳤다고 말하는 순간**입니다. "다 넣었어", "코드 반영했어", "빌드 됐어" 같은 말이 신호입니다. 부탁을 기다리지 않습니다 — 다음 작업으로 넘어가기 전에 합니다.

### 기억으로 하지 않는다

**파일을 엽니다.** 대화 내용에 코드가 남아 있어도 그것은 *제시한 것*이지 *들어간 것*이 아닙니다. 옮겨 적는 과정에서 줄이 빠지고, 위치가 틀리고, 안내용 표시가 같이 딸려 들어갑니다.

### 무엇을 보나 — 넷

| | 보는 것 | 왜 |
|---|---|---|
| ① | 제시한 것이 **그대로** 들어갔나 | 한 줄이 빠지면 기능 하나가 통째로 죽습니다 |
| ② | 제시하지 **않은 것**이 딸려 들어갔나 | `← 추가` 같은 안내 표시, 옛 코드의 잔해 |
| ③ | 이번 변경으로 **거짓이 된 주석·DocC** | 규칙 5·10·20 의 세 층. 없어진 이름을 가리키는 주석이 가장 흔합니다 |
| ④ | 이제 **아무도 부르지 않는** 함수·인자 | 함수를 나누면 딸린 것이 남습니다. Swift 는 이걸 경고하지 않습니다 |

④ 를 따로 두는 이유가 있습니다. 컴파일러는 **쓰이지 않는 `private` 함수와 인자에 경고를 내지 않습니다.** 그래서 "빌드가 됐다" 는 반영이 끝났다는 뜻이 아닙니다. 죽은 코드는 조용히 남아, 나중에 읽는 사람에게 **아직 동작한다고 거짓말합니다.**

### 어떻게 보고하나

**「그대로 / 어긋남 / 누락」** 셋으로 나눕니다. 어긋남과 누락에는 **파일과 줄 번호**를 답니다.

그리고 **아무것도 없으면 "없음" 이라고 분명히 말합니다.** 리뷰라는 이름이 붙었다고 해서 지적을 만들어 낼 이유는 없습니다. 매번 무언가가 나오면 사용자는 이 절차를 믿지 않게 되고, 진짜 문제가 나왔을 때도 흘려듣습니다.

### 고치는 것은 여기서 하지 않는다

리뷰는 **보고까지**입니다. 발견한 것을 그 자리에서 고치기 시작하면 리뷰와 다음 작업이 섞여 무엇이 무엇을 바꿨는지 못 가립니다. `.md` 는 AI 가 바로 고치되 **고쳤다고 밝히고**, `.swift` 는 규칙 2 형식으로 다시 제시합니다.

> 이 규칙도 실제로 놓친 뒤에 생겼습니다. `record()` 를 셋으로 나눌 때 **`nextDueAt` 을 다시 잡는 한 줄이 사라졌는데**, 빌드는 됐고 앱도 돌았습니다. 아무도 눈치채지 못한 채 간격 반복이 **동작하지 않는 상태로 나흘**을 갔고, 그동안 `nextDue(...)` 는 정의만 된 채 한 번도 불리지 않았습니다. 발견한 것은 어머니가 "왜 같은 문제만 나오지" 를 몸으로 보여 준 뒤였습니다.
