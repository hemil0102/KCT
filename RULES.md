# RULES — 규칙의 의미

`CLAUDE.md` 의 작업 규칙 하나하나가 **왜 있고 어떻게 지키는지**를 풀어 쓴 문서입니다.

규칙 목록 자체는 [CLAUDE.md](CLAUDE.md) 에 있습니다. 그쪽은 매 세션 처음부터 끝까지 읽히므로 짧게 유지하고, 긴 설명은 여기로 옮겼습니다. **규칙을 어떻게 적용할지 애매할 때 해당 절만 펴 보세요.** 전체를 읽을 일은 거의 없습니다.

| 규칙 | 무엇에 관한 것인가 |
|---|---|
| [1](#규칙-1의-의미) | AI가 직접 수정하고, 무엇을 왜 바꿨는지 보고 |
| [2](#규칙-2의-의미) | 바뀐 위치를 짚기 |
| [3](#규칙-3의-의미) | ⭐️GUIDE⭐️ 로 용어 안내 |
| [4·5](#규칙-45의-의미) | 한글 개념 주석과 파일 최상단 요약 (10의 문서화 주석 포함) |
| [6](#규칙-6의-의미) | 막혔던 것을 Q&A.md 에 |
| [7](#규칙-7의-의미) | iOS·SwiftData·FoundationModels 의 함정 |
| [8·9](#규칙-89의-의미) | 최신 공식 문서 확인과 링크 |
| [12](#규칙-12의-의미) | PROGRESS.md 로 세션 잇기 |
| [13](#규칙-13의-의미) | 필요한 만큼만 읽기 |

---

### 규칙 1의 의미

이 저장소는 **동작하는 앱을 함께 키우는 곳**입니다. 문제 수가 늘고 화면이 바뀌고 리팩토링이 반복되므로, 파일을 손으로 옮겨 적는 방식으로는 앞으로 나아가지 못합니다. 그래서 AI가 Xcode 도구로 **직접 수정합니다.**

학습은 다른 방식으로 확보합니다. 코드가 **주석과 이름만으로 읽히게 만드는 것**(규칙 4·5·10), 그리고 **단계를 한 번에 하나씩 밟는 것**(`LEARNING_PLAN.md`)이 그 역할입니다.

따라서 AI는 다음을 **합니다.**

- `XcodeWrite`·`XcodeMV`·`XcodeMakeDir` 로 파일을 만들고 옮기고 고칩니다
- `BuildProject` 로 빌드해 스스로 검증합니다
- 바꾼 뒤 **무엇을·왜 바꿨는지** 사람이 읽을 수 있게 보고합니다

다음은 **하지 않습니다.**

- **`project.pbxproj` 를 직접 편집하지 않습니다.** 반드시 MCP 도구(`XcodeMV`, `UpdateTargetBuildSetting`, `AddInfoPlist`, `AddEntitlement`)를 씁니다. 직접 고치면 프로젝트가 깨집니다
- **묻지 않고 커밋·푸시하지 않습니다.** 커밋은 사용자가 시점을 정합니다
- **요청 범위를 넘어 손대지 않습니다.** 버튼을 추가해 달라고 했으면 다른 곳은 건드리지 않습니다. 고쳐야 할 것이 눈에 띄면 고치지 말고 **말합니다**

**보고는 코드가 아니라 판단을 담습니다.** "`SessionBuilder.swift:31` 의 `build()` 를 4개 함수로 나눴습니다 — 한 함수가 복습 선별·신규 도입·슬롯 채우기·모드 배정을 모두 하고 있어서 알고리즘이 이야기로 읽히지 않았습니다" 처럼, **왜 그 선택을 했는지**가 보고의 본문입니다.

### 규칙 2의 의미

AI가 직접 고치더라도, 사용자가 **어디가 어떻게 바뀌었는지 모르면 학습이 일어나지 않습니다.** 변경을 설명할 때는 항상 다음을 밝힙니다.

- **파일 경로** — 저장소 루트 기준. 줄 번호가 있으면 `KCT/Session/SessionBuilder.swift:31` 형식으로
- **새 파일인지 기존 파일 수정인지** — 새 파일이면 어느 그룹(폴더)에 들어갔는지
- **무엇을 대체했는지** — 어떤 함수가 어떤 함수들로 갈라졌는지, 어떤 이름이 어떤 이름이 됐는지
- **최상단 요약 블록에 영향이 있으면 그 수정도 함께** (규칙 5)

**전문을 다시 출력하지 않습니다.** 파일 하나를 고칠 때 200줄을 되풀이하면 정작 바뀐 3줄이 묻힙니다. 바뀐 부분만, 필요하면 앞뒤 한두 줄만 보여줍니다.

이름을 바꿀 때는 **바꾼 이유를 한 줄로** 남깁니다. 이름 변경은 눈에 잘 보이지 않으면서 파일 전체에 퍼지므로, 이유가 없으면 다음 세션이 되돌리려 합니다.

> 예시
> `KCT/Progress/DifficultyMode.swift` → `AskingMode.swift` (타입 이름도 함께)
> 이유: 이 열거형은 *난이도*가 아니라 **묻는 방식**(2지선다·O/X·4지선다·직접입력)입니다. `Question.difficulty`(축 A)와 이름이 겹쳐 두 축이 헷갈렸습니다

### 규칙 3의 의미

사용자는 하고 싶은 것을 길게 풀어서 설명하지만, 그것을 부르는 정식 명칭을 모르는 경우가 많습니다. 용어를 모르면 검색도 질문도 어려워지므로, AI가 그 다리를 놓아줍니다.

**언제 붙이나**

- 사용자가 현상이나 원하는 결과를 길게 서술했는데 그것을 가리키는 용어가 따로 있을 때
- 설명 안에 여러 개념이 뒤섞여 있어 정리가 필요할 때
- 사용자가 쓴 표현과 Apple 공식 문서의 용어가 다를 때

짧고 명확한 요청에는 붙이지 않습니다. 매번 붙이면 잡음이 됩니다.

**형식**

답변 안에 다음과 같이 넣습니다. 용어와 그 뜻을 한 줄로 짝지어, 사용자가 그대로 검색할 수 있게 씁니다. 영문 API 이름이나 공식 용어가 있으면 함께 적습니다.

```
⭐️GUIDE⭐️
- **용어** — 사용자가 말한 그것이 무엇인지 한 줄 설명
- **용어** — ...
```

예시. 사용자가 "맞힌 문제는 좀 있다가 다시 나오고 틀린 건 바로 또 나오게 하고 싶어"라고 썼다면:

```
⭐️GUIDE⭐️
- **간격 반복 (spaced repetition)** — 복습 간격을 정답 여부에 따라 늘리거나 줄이는 학습법. 방금 설명하신 게 이겁니다
- **다음 출제 예정 시점 (due date)** — 그 간격을 저장하는 값. 이 앱에서는 `QuestionProgress.nextDueAt`
- **승급·강등 (promotion / demotion)** — 맞히면 한 단계 어려운 방식으로, 틀리면 쉬운 방식으로 옮기는 것
```

안내한 용어는 규칙 11에 따라 [GUIDE.md](GUIDE.md) 에도 누적합니다.

### 규칙 4·5의 의미

초보자에게 부족한 것은 문법 지식이 아니라 **전체 그림**입니다. 이 값이 어디서 와서 어디로 가는지, 이 함수가 누구의 부탁을 받아 무엇을 책임지는지가 안 보이면, 코드가 돌아가도 배운 게 남지 않습니다. 주석이 그 지도 역할을 합니다.

#### 파일 최상단 요약 (규칙 5)

모든 `.swift` 파일은 다음 네 블록으로 시작합니다.

- **역할** — 이 파일이 무엇을 하는 화면/타입인지 한두 줄
- **구성** — 주요 변수·함수의 이름과 각자의 책임. 계층을 들여쓰기로 표현
- **흐름** — 값과 호출이 오가는 경로. 화살표로 순서대로
- **연결** — 누가 이 파일을 부르는지, 이 파일은 무엇에 기대는지

```swift
//
//  SessionBuilder.swift
//  KCT
//
//  역할 : 이번 회차에 어떤 문제를 어떤 방식으로 낼지 자동으로 정한다
//  요점 : 어머니가 아무것도 고르지 않아도 되도록, 선택을 전부 여기서 대신한다
//
//  ── 구성 ──────────────────────────────────────────────
//  SessionBuilder            출제 계획을 세우는 주인
//  ├─ catalog                문제집 (어떤 문제가 있는지)
//  ├─ isUnlocked             스토리 모드 게이트. 지금은 항상 열림
//  ├─ build()                계획 전체를 지휘
//  │   ├─ reviewQueue()      복습 대상 줄 세우기 (예정 시각 이른 순)
//  │   ├─ introduceOrder()   신규 도입 줄 세우기 (쉬운 순 + 단원 번갈아)
//  │   └─ shapeRound()       첫·마지막을 격려용 2지선다로
//
//  ── 흐름 ──────────────────────────────────────────────
//  QuizView 가 세션 시작
//    → build(size:progressByID:) 호출
//    → 복습 + 신규를 이어 붙여 size 만큼 고름
//    → 각 문제에 진척이 기억한 모드를 붙임
//    → QuizItem 배열로 돌려줌
//    → QuizView 가 화면에 하나씩 표시
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuizView.startSession()
//  기대는 것    : QuestionCatalog(문제), QuestionProgress(진척), QuizItem(출제 항목)
//  건드리지 않는 것 : 진척 저장 — 채점 후 QuizView 가 record() 로 반영한다
//
```

`구성`에는 모든 변수를 다 적지 않습니다. 임시 변수나 반복문 카운터는 빼고, **그 파일을 이해하는 데 필요한 것만** 적습니다.

`연결`에 **건드리지 않는 것**을 한 줄 넣으면 책임 경계가 훨씬 선명해집니다. 무엇을 하는지보다 **무엇을 하지 않는지**가 역할을 규정합니다.

#### 본문 주석 (규칙 4)

한글로 개념을 풀되, **무엇을 하는지보다 왜 그렇게 하는지**를 우선합니다. 무엇을 하는지는 코드를 읽으면 보이지만, 왜는 보이지 않습니다. 특히 API의 함정이나 반직관적인 동작은 반드시 이유를 남깁니다.

```swift
// Swift 의 hashValue 는 실행할 때마다 값이 달라져 저장용으로 쓸 수 없다.
// 앱을 껐다 켜도 같은 값이 나와야 캐시가 맞으므로 SHA256 을 쓴다.
let digest = SHA256.hash(data: Data(text.utf8))
```

**용어는 한글과 영문을 함께** 적습니다. 한글만 있으면 검색이 안 되고, 영문만 있으면 뜻이 안 잡힙니다.

```swift
// 오답 보기(distractor) — 정답이 아닌 선택지
// 다른 문제의 정답에서 뽑아 오므로 문제집이 바뀌면 자동으로 따라 바뀐다
```

**자명한 것에는 달지 않습니다.** 모든 줄에 주석이 붙으면 정작 중요한 주석이 묻힙니다.

```swift
currentIndex += 1   // 다음 문제로 ← 불필요
```

**주석을 늘리기보다 함수를 쪼개 이름을 붙이는 쪽이 낫습니다.** 설명이 필요하다고 느껴지는 코드 덩어리는, 대개 이름 없는 함수가 숨어 있다는 신호입니다. 이름이 설명을 대신하면 주석은 저절로 줄어듭니다.

#### 문서화 주석으로 코드 밖에 두기 (규칙 10)

설명이 길어지는 것 자체는 문제가 아닙니다. 문제는 그것이 **코드와 같은 자리, 같은 무게로 깔려서** 코드를 밀어내는 것입니다. 그래서 설명은 `///` 문서화 주석(DocC)으로 씁니다. Xcode가 이를 문서로 인식해 `Option+클릭` 했을 때만 팝업으로 보여주므로, 화면에는 요약 한 줄만 남습니다.

**첫 문장은 요약, 빈 줄 뒤는 설명입니다.** Xcode가 이 둘을 구분해 다르게 취급하므로 순서를 지킵니다.

```swift
/// 문제별 학습 진척. 맞히면 한 단계 위 방식으로, 틀리면 아래로 옮깁니다.
///
/// 세션(날짜)을 넘어 유지되어야 하므로 SwiftData 로 영구 저장합니다.
/// 문제 내용(``Question``)과 분리되어 있어, 문제집을 갈아끼워도 기록은 남습니다.
@Model final class QuestionProgress { }
```

**API의 함정은 콜아웃으로 뺍니다.** `- Important:` 는 주황색, `- Note:` 는 회색 상자로 강조됩니다.

```swift
/// - Important: 문제 id 는 영구 고정입니다. 재사용하면 학습 기록이
///   엉뚱한 문제에 붙습니다.
```

**다른 타입은 백틱 두 개로 링크합니다.** ` ``QuestionProgress`` ` 처럼 쓰면 DocC 가 실제 링크로 만들어 줍니다. 이 링크들이 모여 문서에서 역할 관계를 따라갈 수 있게 됩니다.

**⚠️ `private` 멤버는 DocC 문서에 나오지 않습니다.** 좋은 설명을 private 함수에만 달아 두면 문서에서 사라집니다. 설명할 가치가 있는 로직이 private 안에 갇혀 있다면, 그것은 **별도 타입으로 꺼낼 때가 됐다는 신호**입니다.

**본문 인라인 `//` 주석은 "왜"만, 2줄 이내로 짧게** 남깁니다. 긴 설명은 위의 `///` 로 올립니다.

**무엇을 어디에 쓸지**

| 무엇을 | 어디에 |
|---|---|
| 파일 전체 지도 | 최상단 요약 블록 (규칙 5) |
| 타입·변수·함수 설명 | `///` — 코드엔 요약 한 줄, 나머지는 Quick Help |
| API 함정, 주의사항 | `///` 안의 `- Important:` / `- Note:` |
| 왜 이 줄이 이렇게 생겼나 | 인라인 `//` — 2줄 이내 |
| 개념 자체의 긴 설명 | [GUIDE.md](GUIDE.md) 또는 [Q&A.md](Q&A.md) |
| 설계 원칙, 단계별 계획 | [LEARNING_PLAN.md](LEARNING_PLAN.md) |

사용자에게 알려둘 것: 함수 위에서 `Control+클릭` → **Add Documentation** 으로 틀을 자동 생성할 수 있고, Product → **Build Documentation** 으로 `///` 주석을 모아 문서 사이트를 만들 수 있습니다.

#### 함께 지킬 것

- **`// MARK: -` 로 본문을 구획**합니다. Xcode 점프바에 목차가 생겨서 최상단 `구성` 블록과 실제 코드가 눈으로 연결됩니다.
- **코드를 고치면 최상단 요약도 같이 고칩니다.** 낡은 주석은 없느니만 못합니다.
- **처음 등장하는 타입이나 메서드**에는 한 줄 설명을 붙입니다. 두 번째부터는 생략합니다.
- **한 줄 계약** — 모든 타입의 `///` 첫 줄을 한 문장으로 못 쓰면, 그 타입이 일을 너무 많이 하고 있다는 뜻입니다. 쪼갤 신호로 씁니다.

### 규칙 6의 의미

같은 질문을 두 번 하지 않게 하는 것이 목적입니다. 특히 빌드 오류로 막혔던 경험은 시간이 지나면 원인은 잊고 증상만 남습니다.

기록은 저장소 루트의 **[Q&A.md](Q&A.md)** 에 남깁니다. 최신 항목을 위에 추가합니다. 형식은 이렇습니다.

- **제목** — 증상을 한 줄로. 나중에 검색할 때 걸리는 말로 씁니다
- **Q** — 무엇에 막혔는지 한 줄
- **A** — 원인과 해결. 코드 조각이 필요하면 최소한만
- **교훈** — 다음에 같은 실수를 피하는 한 줄 (있을 때만)
- **참고 문서** — 확인한 Apple 문서 링크 (규칙 9)

모든 대화를 기록하지 않습니다. **빌드가 막혔던 것, 개념을 오해했던 것, 문서와 실제가 달랐던 것**만 남깁니다.

### 규칙 7의 의미

KCT가 쓰는 프레임워크들은 각각 조용히 실패하는 방식이 있습니다. 컴파일은 되는데 런타임에 아무 일도 안 일어나는 부류라, 미리 알지 않으면 원인을 찾는 데 오래 걸립니다.

**FoundationModels (온디바이스 모델)**

- `SystemLanguageModel.default.isAvailable` 이 `false` 인 기기·시뮬레이터가 있습니다. **모델을 쓰는 경로에는 반드시 폴백이 있어야 합니다.** 이 앱에서는 규칙 기반 추출(`QuestionFocus`)과 결정적 채점(`RuleGrader`)이 그 역할입니다
- 모델은 **지어냅니다(hallucination).** 하이라이트처럼 "지문에 실제로 있는 문자열"이 필요한 경우 결과를 반드시 검증하고, 어긋나면 조용히 버립니다
- 실패는 화면에 노출하지 않습니다. 어르신에게 "모델 오류"는 아무 의미가 없습니다

**SwiftData**

- `@Query` 는 **즉시 갱신되지 않을 수 있습니다.** 지우고 바로 다시 만드는 흐름에서는 `@Query` 결과를 믿지 말고 직접 만든 값을 씁니다
- `@Attribute(.unique)` 로 중복을 막되, 그래도 조회할 때는 중복에 대비합니다
- `ModelContext` 를 만지는 코드는 `@MainActor` 경계를 지킵니다

**SwiftUI**

- **Combine 을 쓰지 않습니다.** `async`·`await`, `@Observable`, `.task`, `.onChange(of:)` 로 해결합니다
- 백그라운드 작업은 세션이 바뀔 때 **취소**합니다. `Task` 핸들을 보관해 두고 `cancel()` 합니다

**AVFoundation (낭독)**

- 무음 스위치가 켜져 있어도 들리게 하려면 `AVAudioSession` 카테고리를 `.playback` 으로 설정해야 합니다
- 고품질 한국어 음성은 **사용자가 따로 내려받아야** 존재합니다. 없을 때를 대비해 점수 기반으로 고릅니다

**Xcode 프로젝트**

- **file-system synchronized group** 방식입니다. `KCT/KCT/` 안의 파일은 확장자를 Xcode 가 모르면 **앱 번들 루트로 복사**됩니다. 문서·메모는 저장소 루트에 둡니다
- 번들 리소스는 폴더 구조와 무관하게 **번들 루트에 평평하게** 놓입니다. 그래서 `Content/questions.json` 을 `Bundle.main.url(forResource: "questions")` 로 찾을 수 있습니다

### 규칙 8·9의 의미

SwiftUI·SwiftData 는 해마다 API가 바뀌고, FoundationModels 는 아주 새로운 프레임워크입니다. 기억이나 검색 결과에 의존하면 없는 API를 만들어내거나 가용성을 착각합니다.

- 코드를 제시하기 전에 **`DocumentationSearch` 로 해당 심볼을 확인**합니다. 특히 **availability(어느 OS부터 되는지)** 를 봅니다
- 확인한 문서는 답변 끝에 **참고 문서** 목록으로 남깁니다
- 문서 페이지가 JavaScript로 렌더링되어 내용이 안 보이면, 주소 앞부분을 `https://developer.apple.com/tutorials/data/documentation/...` 로 바꾸고 끝에 `.md` 를 붙이면 원문을 얻을 수 있습니다

**모른다고 말하는 것이 지어내는 것보다 낫습니다.** 특히 새 API는 학습 데이터에 없을 수 있습니다. 프로젝트에 있는 코드가 낯설다면 그것은 틀린 코드가 아니라 **내가 모르는 새 API** 일 가능성이 높습니다. 지우거나 고치기 전에 먼저 찾아봅니다.

### 규칙 12의 의미

대화는 길어질수록 비싸지고, 세션이 바뀌면 사라집니다. 그래서 **"지금 어디까지 왔나"를 대화가 아니라 파일에 둡니다.**
[PROGRESS.md](PROGRESS.md) 가 그 파일입니다. 새 세션은 저장소를 훑는 대신 이 파일 하나를 읽고 곧장 이어서 작업합니다.

**언제 읽나** — 세션의 **첫 동작**입니다. 사용자가 무엇을 물어도 그 전에 읽습니다.

**언제 쓰나** — 다음 중 하나가 일어나면 그 자리에서 갱신합니다. 세션 끝까지 미루지 않습니다. 세션은 예고 없이 끊깁니다.

- 한 단계를 끝냈을 때 — 「지금」 네 줄과 「파일 지도」
- 새 `.swift` 파일이 생기거나 사라졌을 때, 이름이 바뀌었을 때 — 「파일 지도」
- 되돌리고 싶어질 만한 선택을 했을 때 — 「결정 기록」에 **이유와 함께** 한 줄
- 답을 못 찾고 넘어간 것이 있을 때 — 「열린 질문」

**무엇을 쓰지 않나** — 잘한 일의 목록이 아닙니다. **다음 사람이 모르면 막히는 것**만 씁니다.
개념 설명은 [GUIDE.md](GUIDE.md), 막혔던 것의 해결은 [Q&A.md](Q&A.md), 단계별 계획은 [LEARNING_PLAN.md](LEARNING_PLAN.md) 입니다. 같은 사실을 두 곳에 쓰지 않습니다. 중복이 생기면 낡은 쪽이 거짓말을 하게 됩니다.

**상한은 100줄입니다.** 넘으면 오래된 것을 각자의 집으로 보내고 여기서 지웁니다. 이 파일이 뚱뚱해지면 매 세션 그 비용을 다시 냅니다.

### 규칙 13의 의미

파일을 여는 것에는 값이 붙습니다. 이 저장소는 특히 그렇습니다. `.swift` 파일은 규칙 5 때문에 최상단 요약만 40줄이고, 화면 파일은 수백 줄입니다. 습관적으로 전체를 열면 정작 중요한 것을 볼 여력이 없어집니다.

**대신 이렇게 찾습니다.** 이 프로젝트는 Xcode 안에서 작업하므로 MCP 도구를 씁니다.

| 알고 싶은 것 | 여는 방법 |
|---|---|
| 이 파일이 무슨 일을 하나 | 최상단 요약만 — `XcodeRead(filePath:, limit: 40)` |
| 어떤 심볼이 어디 쓰이나 | `XcodeGrep(pattern:)` — 파일 전체를 열기 전에 먼저 |
| 어떤 파일이 있나 | `XcodeLS` / `XcodeGlob` — `ls`·`find` 를 쓰지 않습니다 |
| 이 폴더가 무슨 책임인가 | 열어보지 않습니다. `CLAUDE.md` 의 폴더 구조표로 나옵니다 |
| 지금 어디까지 왔나 | [PROGRESS.md](PROGRESS.md) 만 |
| 컴파일 되는지 빠르게 | `XcodeRefreshCodeIssuesInFile` — 전체 빌드보다 훨씬 쌉니다 |
| 저장소 전반의 넓은 조사 | 서브에이전트에 위임 — 중간 과정 말고 결론만 돌아옵니다 |

**⚠️ 셸 명령(`ls`, `find`, `cat`)을 파일 구조 파악에 쓰지 않습니다.** 사용자가 매번 승인을 눌러야 하므로 비쌉니다. MCP 도구가 같은 일을 더 빠르게 합니다.

규칙 5의 최상단 요약 블록은 이때를 위한 것입니다. **요약만 읽고 판단이 서면 본문은 열지 않습니다.**

**답변할 때도 같습니다.** 파일 하나를 고칠 때 전문을 다시 출력하지 않습니다. 규칙 2에 따라 **위치를 짚고 바뀌는 부분만** 보여줍니다.

**사용자에게 부탁할 것** — 빌드 로그나 오류 전문은 대화에 붙여넣기보다 파일로 저장하고 경로를 알려주는 편이 쌉니다. 필요한 줄만 찾아 읽겠습니다.
