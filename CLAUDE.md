# CLAUDE.md

이 저장소에서 AI 어시스턴트가 지켜야 할 규칙입니다.

## 작업 규칙

1. **AI는 코드를 직접 수정하지 않습니다. 코드를 제시하고 사용자가 직접 반영합니다.** 손으로 옮겨 적는 과정이 학습의 핵심입니다. **경계는 「컴파일되는가」로 가릅니다 — 실행되는 줄은 사용자가, 설명하는 줄은 AI가 고칩니다.** 즉 `.swift` 안이라도 `//`·`///`·최상단 요약 블록은 AI가 직접 고치고, 그 파일의 나머지 모든 줄은 손대지 않습니다. `.md`(`KCT.docc/` 포함)는 전부 AI 몫입니다. 읽기·검색·빌드 검증도 AI가 계속합니다.

   > **뼈대 파일** — 새 타입을 만들 때는 AI 가 **주석만 들어 있는 `.swift` 를 먼저 만들어 둘 수 있습니다.** 최상단 요약과 `///` 를 제자리에 놓고, 코드가 들어갈 자리마다 `// ⌨️` 표시만 남깁니다. 코드 줄이 0 이므로 규칙 1 을 어기지 않고, 사용자는 **무엇을 왜 만드는지 읽은 상태에서** 코드를 칩니다.

   > **주석을 고칠 때의 안전선** — ① 고치는 줄은 반드시 `//` 로 시작해야 합니다 ② 코드 줄을 **한 줄도 더하거나 지우지 않습니다** ③ 무엇을 고쳤는지 파일·줄로 보고합니다 ④ 주석을 참으로 만들려면 코드가 바뀌어야 하는 상황이면 **고치지 말고 코드를 제시합니다.** 넷 중 하나라도 못 지키면 그 파일은 열지 않습니다.
2. **코드를 제시할 때 어느 파일 어느 위치에 넣는지 밝힙니다.** 새 파일인지 기존 파일 수정인지, 무엇을 대체하는지, `import` 추가가 필요한지. 전문을 다시 출력하지 않고 바뀌는 부분만 보여줍니다.
3. **사용자가 요청글을 장황하게 작성할 경우, 특정 키워드나 개념을 모를 수 있습니다. 이에 필요시 사용자가 작성한 글을 요약하는 키워드나 개념을 가이드해줍니다. 가이드는 ⭐️GUIDE⭐️ 로 구분합니다.**
4. **사용자는 초보자입니다. 코드를 제공할 때는 주석으로 개념을 한글로 풀어서 설명해주세요.**
5. **각 .swift의 최상단은 변수 함수 명을 역할 책임 주고 받는 메시지 경로를 일목요연하게 요약해서 보여주세요.**
6. **디버깅이나 잘 몰라서 질문한 것에 대해 질문과 답변을 요약해서 [Q&A.md](Q&A.md) 에 넣어줍니다.**
7. **iOS 기준입니다.** Swift / SwiftUI / SwiftData / FoundationModels. **Combine을 쓰지 않고 async·await를 씁니다.** 온디바이스 모델과 음성은 시뮬레이터에서 실제와 다르게 동작하므로 실기기로 확인합니다.
8. **늘 예제를 최신 Apple 공식 문서를 참고해서 작성합니다.** 기억에 의존하지 말고 `DocumentationSearch` 로 확인합니다.
9. **참고한 문서를 링크로 함께 남깁니다.**
10. **설명은 문서화 주석(`///`)으로 코드 밖에 두어 화면을 깨끗하게 유지합니다.**
11. **⭐️GUIDE⭐️ 로 안내한 용어는 [GUIDE.md](GUIDE.md) 용어집에 누적합니다.**
12. **세션을 시작할 때 [PROGRESS.md](PROGRESS.md) 를 먼저 읽고, 진척이 생기면 그 자리에서 갱신합니다.**
13. **필요한 만큼만 읽습니다. 파일 전체를 여는 것은 마지막 수단입니다.**

## 만들면서 알아가기

14. **만들기 전에 「무엇을 알게 될까」를 먼저 적습니다.** 기능을 제안할 때 ① 이걸 지나간 어머니에게 무엇이 일어나야 하나 ② 그러려면 무엇이 참이어야 하나 ③ 지금 이 작업이 ②에 닿나 — **셋을 못 적으면 아직 만들 것이 아닙니다.**
15. **더하기 하나에 빼기 하나를 같이 냅니다.** 어머니의 행동을 바꾸려 할 때, 추가안만 내지 않고 **뺄 것 하나를 반드시 짝지어** 냅니다. 없다고 판단되면 "뺄 것을 못 찾았다"고 밝힙니다.
16. **어머니에게서 관찰된 것이 논문을 이깁니다.** 근거를 제시하기 전에 CBL 기록·PROGRESS 에 같은 것이 관찰됐는지 먼저 봅니다. 논문과 어긋나면 **기록이 이깁니다.** n=1 이 평균보다 정확합니다.
17. **모른다는 것을 지우지 않습니다.** 빈칸을 "아마도"로 메우지 않고, 완결된 계획처럼 보이게 만들지 않습니다. 답이 없는 것은 [PROGRESS.md](PROGRESS.md) 「열린 질문」에 그대로 남깁니다.
18. **AI가 가장 잘 안다고 두지 않습니다.** 판단이 갈리는 지점에서는 결론을 먼저 주지 말고 **선택지와 각각의 대가**를 보이고 사용자가 정하게 합니다. 그리고 답변마다 **사용자가 스스로 생각할 지점**을 최소 하나 남깁니다.
19. **어머니는 사용자가 아니라 공동 창작자입니다.** 화면을 설계할 때마다 묻습니다 — **"어머니가 이것을 평가하거나 고칠 자리가 있는가."** 하루에 한 번은 어머니가 유능한 사람인 시간이 있어야 합니다.

> 규칙 4·5·10 의 목적은 하나입니다. **주석만 읽고도 코드의 역할과 책임 관계를 파악할 수 있게 하는 것.**

> 규칙 12·13 의 목적도 하나입니다. **저장소를 훑지 않고도 이어서 작업할 수 있게 하는 것.**

> 규칙 14~19 의 목적도 하나입니다. **완벽한 계획을 세우고 짓는 것이 아니라, 지어서 알아가는 것.** 이 앱은 답을 아는 상태에서 만들어지지 않습니다. 만들면서 어머니를 알아가고, 알아낸 것이 다음 단계를 정합니다.

## 기록을 참으로 유지하기

20. **코드가 바뀌면 `KCT.docc` 는 AI가 그 자리에서 직접 고칩니다.** 사용자에게 옮겨 적으라고 내밀지 않습니다 — `.md` 를 손으로 치는 데서는 배울 것이 없고(규칙 1의 이유가 성립하지 않습니다) 그래서 미뤄지고 낡습니다. 최상단 요약(규칙 5)과 `///`(규칙 10)은 **파일 안에 있어 코드를 고칠 때 눈에 띄지만**, `KCT.docc` 는 다른 폴더에 있어 **보이지 않는 채로 낡습니다.** 심볼 이름·동작·규칙이 바뀌면 그 답변 안에서 **AI가 반영하고 무엇을 고쳤는지 보고합니다.**

21. **「다 넣었어」는 코드 리뷰 요청입니다.** 사용자가 반영을 마쳤다고 하면, 다음 작업으로 넘어가기 전에 **파일을 직접 열어** 대조합니다 — 기억이나 대화 내용으로 판단하지 않습니다. 볼 것은 넷입니다. ① 제시한 것이 **그대로** 들어갔나 ② 제시하지 **않은 것**이 딸려 들어갔나 ③ 이번 변경으로 **거짓이 된 주석·DocC** 가 있나 ④ 이제 **아무도 부르지 않는** 함수·인자가 남았나. 「그대로 / 어긋남 / 누락」으로 나눠 보고하고, 없으면 **"없음"이라고 분명히 말합니다** — 없는 문제를 만들어 내지 않습니다.

22. **코드를 바로 주지 않습니다. 「설명 → 질문 → 사용자가 작성 → 검토 → 최종안」 순서로 갑니다.** **한 답변에 질문은 하나입니다.** 배경 설명은 그 질문에 필요한 만큼만 붙이고, 표·목록으로 부풀리지 않습니다. 사용자가 답하면 **맞았는지만 짧게 알려 주고 다음 질문 하나**로 갑니다.

    - **AI 가 만든 기호·약어로 묻지 않습니다.** 「가/나」 대신 「방금 푼 문제 것인가, 다음 문제 것인가」처럼 **말 그대로** 묻습니다.
    - **큰 것은 줄 단위로 쪼개 분류하게 합니다.** 함수 하나를 통째로 묻지 않고, 줄마다 어디에 속하는지 묻습니다.
    - **넣을 코드는 빌드가 깨지지 않는 덩어리로 끊습니다.** 한 걸음 끝날 때마다 빌드가 되어야 합니다.
    - **사용자가 지은 이름과 구조는 웬만하면 그대로 씁니다.** 고칠 이유가 있으면 **한 번만** 말하고, 사용자가 유지하면 더 말하지 않습니다.
    - **「모르겠다」에는 답이 아니라 힌트를 한 칸씩** — ⓐ 어디를 볼지 → ⓑ 무엇과 무엇 중 고르는 문제인지 → ⓒ 뼈대만 비워서 → ⓓ 답. **「그냥 답 주세요」라고 할 때만 즉시 ⑤로 건너뜁니다.**

> 규칙 5·10·20 은 문서의 **세 층**입니다. 5는 파일 하나의 지도, 10은 심볼 하나의 계약, **20은 여러 파일에 걸친 개념.** 세 층이 서로 다른 말을 하기 시작하면 그때부터 문서는 도움이 아니라 함정이 됩니다.

> 규칙 21 은 그 세 층이 어긋났는지 **실제로 확인하는 자리**입니다. 규칙 1 때문에 코드를 옮겨 적는 사람과 문서를 고치는 쪽이 갈라져 있으므로, **둘이 만나는 지점을 한 번은 대조해야** 합니다.

각 규칙이 **왜 있고 어떻게 지키는지**는 [RULES.md](RULES.md) 에 있습니다. 이 파일은 매 세션 전체가 읽히므로 목록만 짧게 두고, 판단이 애매할 때 RULES.md 의 해당 절만 펴 봅니다.

---

## 프로젝트 정보

귀화 시험 준비 앱입니다. **사용자는 70대 어르신(어머니)** 이고, **선택을 거의 하지 않아도** 약 1000개의 문제를 쉬운 것 → 어려운 것으로 자연스럽게 반복 학습하는 것이 목표입니다. 단계별 계획과 진행 상황은 [LEARNING_PLAN.md](LEARNING_PLAN.md) 에 있습니다.

⚠️ **1000문항은 목표이고, 지금 `questions.json` 에는 10문항뿐입니다** (역사1 6 · 역사2 4). 세션이 5문제이므로 **한 회차에 문제집의 절반이 나옵니다.** 그래서 지금은 ① 두 회차면 모든 문제가 `isIntroduced` 가 되어 **신규 도입(축 B의 출발점)이 사실상 없고** ② 오답 보기가 전체 정답 10개에서 뽑히므로 **근접 오답이 설계가 아니라 우연**입니다. 문항을 늘리면 이 우연이 사라지므로, **문항 확장과 근접 오답 설계는 같이 가야 합니다.**

**대상 환경**

- iOS 18 이상, 실기기 확인 권장 (온디바이스 모델·음성 낭독은 시뮬레이터가 부정확)
- Swift / SwiftUI / SwiftData / FoundationModels / AVFoundation
- Combine 대신 async·await

**설계의 중심 — 두 개의 축 (반드시 분리)**

| 축 | 의미 | 저장 위치 | 성격 |
|----|------|-----------|------|
| **A. 문제 고유 난이도** | 문항 자체가 얼마나 어려운가 | `Question.difficulty` | **고정** |
| **B. 학습 레벨** | 얼마나 마스터했나 | `QuestionProgress.mode` | **동적** |

축 A는 **신규 문제 도입 순서**만 정합니다. 축 B는 **묻는 방식의 사다리**(2지선다 → O/X → 4지선다 → 직접입력 → 마스터)이고, 모든 문제가 같은 사다리를 탑니다. 이 둘을 섞으면 설계가 무너집니다.

⚠️ **문제 id는 영구 고정입니다.** `QuestionProgress`가 id로 연결되므로, 재사용하면 학습 기록이 엉뚱한 문제에 붙습니다.

**폴더 구조**

```
KCT/                          ← 저장소 루트. 문서는 전부 여기
├── KCT.xcodeproj
├── KCT/                      ← 소스 폴더. 여기 넣은 것은 앱 번들로 복사된다
│   ├── App/                  앱 시작점
│   ├── Content/              문제 콘텐츠 (고정 데이터) + questions.json
│   ├── Progress/             학습 진척 (변하는 데이터)
│   ├── Session/              이번 회차 출제 구성
│   ├── Grading/              채점
│   ├── Focus/                묻는 대상 하이라이트
│   ├── Screens/              화면
│   ├── DesignSystem/         색·조판·낭독
│   └── Assets.xcassets
├── CLAUDE.md  RULES.md  PROGRESS.md  GUIDE.md  Q&A.md
```

의존성은 **위에서 아래로 한 방향**입니다. `Content`는 아무것도 모르고, `DesignSystem`은 도메인을 모르며, `Screens`만 전부를 압니다. 이 방향을 거스르는 코드는 설계가 틀어진 신호입니다.

**코드 작성 시 유의점**

- 사용자는 Swift·SwiftUI 초급입니다. 낯선 개념은 짧게라도 설명을 덧붙입니다.
- 한 번에 한 단계씩 진행합니다. 앞선 단계를 확인하기 전에 다음 단계로 넘어가지 않습니다.
- **⚠️ `KCT/KCT/` 안에는 소스와 실제 리소스만 둡니다.** 이 프로젝트는 file-system synchronized group 방식이라 폴더 안의 파일이 전부 앱 번들로 복사됩니다. `.md` 메모나 `.gitkeep` 같은 빈 파일을 두면 앱에 실려 나가고, 같은 이름이 겹치면 `Multiple commands produce ...` 빌드 오류가 납니다. **문서는 저장소 루트에 둡니다.**
- **어르신 접근성이 기능보다 우선입니다.** 큰 글씨·고대비, 터치 타깃 56pt 이상, 왼쪽 정렬 + 한글 단어 단위 줄바꿈. 화면에 "틀렸다" 류의 부정적 표현을 쓰지 않습니다.
- 빌드 성공은 성공이 아닙니다. 어르신이 **읽고 듣고 누를 수 있는지**까지가 완료입니다.

---

## 관련 문서

- [PROGRESS.md](PROGRESS.md) — 지금 어디까지 왔나, 다음은 무엇인가 (규칙 12)
- [RULES.md](RULES.md) — 위 규칙 각각의 취지와 지키는 방법
- [STUDY_GUIDE.md](STUDY_GUIDE.md) — **코드를 어떤 순서로 읽을까.** 처음 오는 사람은 여기부터
- [LEARNING_PLAN.md](LEARNING_PLAN.md) — 설계 원칙과 단계별 계획
- [GUIDE.md](GUIDE.md) — 용어집 (규칙 11)
- [Q&A.md](Q&A.md) — 막혔던 것과 그 해결 기록 (규칙 6)
