# HISTORY — 무엇을 왜 그렇게 만들었나

**이 파일은 지나온 길입니다.** 지금 상태는 [PROGRESS.md](PROGRESS.md), 앞으로 할 일은
[6차_계획_지식층.md](6차_계획_지식층.md) 에 있습니다.

여기에는 **결정과 그 이유**만 남깁니다. 코드는 이미 `.swift` 에 들어갔으므로 옮겨 적지 않고,
「그때 왜 그렇게 정했나」와 「무엇이 틀렸다가 어떻게 바뀌었나」를 적습니다.

> 4차·5차·6차의 단계별 `.md` 열두 장을 이 한 장으로 합쳤습니다(2026-09-10).
> 원본은 `_to_delete/` 에 있습니다.

---

## 한 줄 연표

| 차수 | 언제 | 무엇을 알아냈나 |
|---|---|---|
| **3차** | ~8/26 | 같은 다섯 문제만 나왔다. 로그가 없으면 아무것도 모른다 |
| **4차** | 8/31~9/6 | 모달을 만들었고 **해설이 사실을 지어낸다**는 것을 봤다 |
| **5차** | 9/6~9/9 | 「사실은 우리가 주고, 문장은 모델이 만든다」로 방향을 잡았다 |
| **6차** | 9/9~9/10 | **글자는 코드가, 뜻은 모델이.** 채점을 세 층으로 갈랐다 |

---

## 3차 — 로그가 없으면 아무것도 모른다

**증상** — 회차를 세 번 돌려도 같은 다섯 문제만 나왔다.

**원인** — `nextDueAt` 이 한 번도 채워지지 않아 정렬이 늘 `(난이도, id)` 순으로 떨어졌다.
`lastSeenAt` 을 두 번째 기준으로 넣어 고쳤다.

**여기서 정한 것** — 화면에서 눈으로 보고 고치는 방식은 한계가 있다.
**무슨 일이 있었는지 서버에 남긴다**(`ObsRecord` · Supabase).
표는 **넣기만 되고 읽기는 대시보드에서** — 앱에 든 키는 공개된 것이라 RLS 가 유일한 방어다.

---

## 4차 — 틀렸을 때 창을 띄우다

### 1단계 · 채점을 문항마다로

회차 끝에 한꺼번에 채점하던 것을 **문항마다**로 옮겼다. 틀린 그 자리에서 창을 띄우려면
그 시점에 정오답을 알아야 했다.

### 2단계 · 해설을 만들되 화면에는 안 띄운다

`CommentaryWriter` 를 만들고 결과를 `ObsRecord.explanation` 에만 남겼다.
**눈으로 스무 줄을 검수한 뒤에** 화면에 올리기로 했다 — 어머니에게 먼저 보이면 되돌릴 수 없다.

### 3단계 · 모달

`alert` 을 `sheet` 로 바꾸고 `FeedbackSheet` 를 만들었다.

> **여기서 배운 것** — 「다음 문제」를 한 번 눌렀는데 두 문제가 넘어갔다.
> 버튼과 `Binding` 의 `set` 이 **같은 일을 두 번** 시키고 있었다.
> 고친 방법은 「누가 부르는가」를 맞히는 것이 아니라 **몇 번 불려도 결과가 같게** 만드는 것이었다.
> 화면이 부르는 상태 변경 함수의 원칙이 되었다.

### 4·5단계 · 해설을 종류에 맞게

「안익태」와 「고조선」에 같은 말투가 붙었다. **사람 이름에는 글자 풀이가 안 되고,
제도 이름에는 그것이 가장 잘 통한다.** `AnswerKind` 를 만들어 문항이 자기 종류를 들고 다니게 했다.

### 보완 · 예시를 베낀다

로그에서 세 가지가 나왔다.

| 증상 | 원인 |
|---|---|
| 「~는 관련이 없습니다」 | **금지하려고 인용한 문장**을 그대로 따라 썼다 |
| 「주몽과 애국가는 늘 같이 나옵니다」 | 예시의 꼬리 문장을 복제했다 |
| 「정답은 …입니다」가 22줄 중 16줄 | 프롬프트에 「정답」이 두 번 나왔다 |

> **여기서 배운 것** — **모델에게 「하지 마라」를 인용해서 알려 주면 그 말을 쓴다.**
> 금지어를 적지 않고 **할 일을 긍정형으로** 적는다.

### 관찰 (9/5) — 이 프로젝트에서 가장 값어치 있던 하루

75문항 · 93% · 14분. 8/26 에는 다섯 문제만 나왔는데 이번엔 25문항이 모두 나왔다.

```
2지선다  98% (n=47)      O/X   88% (n=17)
4지선다 100% (n=7)       직접입력 50% (n=4) · 중앙값 17.9초
```

가장 큰 발견은 정답률이 아니었다. **오답 다섯 중 셋의 해설이 사실과 달랐다.**

```
「고조선은 크고 높은 나라」          거짓
「태극기는 붉은색과 하얀색의 띠」     거짓
「한글은 하늘에서 내려온 글」        거짓
```

**어머니가 틀린 자리에서 창이 또 틀린 것을 알려 줬다.**

그리고 한 문항에서 58.3초를 기다리셨다. 8/26 에 「모르면 그냥 찍음」이라고 하셨던 분이
**이제 기다린다.** 최대 30초였던 것이 두 배가 됐다.

### 7단계 · PCC — 보류

해설을 Private Cloud Compute 로 옮기려 했으나 **200만 다운로드까지만 무료이고 그 뒤 유료 등급이 없다.**
넘으면 접근이 끊긴다. `explanation_source`(`device`/`cloud`) 칸만 만들어 두고 보류했다.

---

## 5차 — 사실은 우리가 주고, 문장은 모델이 만든다

### 환각은 한 가지가 아니다

로그를 나눠 보니 **셋이 섞여 있었다.**

| 종류 | 예 | 고치는 법 |
|---|---|---|
| 모르는 것을 지어냄 | 「고조선은 크고 높은 나라」 | **사실을 미리 준다** |
| 예시를 베낌 | 「늘 짝으로 나옵니다」 | 예시를 고친다 |
| 지침을 안 지킴 | 「정답은 …입니다」 | 지침을 고친다 |

**앞의 하나는 지침으로 못 고친다.** 모델이 모르는 것이기 때문이다.
그래서 `Question.facts` 를 만들기로 했다 — 6차 4단계의 씨앗이 여기서 나왔다.

### 함께 고친 것들

- 직접입력도 다른 문항처럼 **모달을 먼저** 띄운다
- 채점 뒤에 문제를 다시 읽어 주지 않던 것 — `.onChange` 감시자가 화면 안에 있어 놓쳤다
- 회차 끝에 **3초 박자**(`isWrappingUp`)를 두어 마지막 답을 누른 손이 결과 화면 버튼을 안 누르게

> **여기서 배운 것** — `.onChange` 를 **바뀌는 뷰 안**에 두면 그 뷰가 사라질 때 변화를 놓친다.

### 안전 필터를 처음 만났다

```
❌ 해설 실패: May contain unsafe content
```

원인은 **프롬프트에 넣은 문제 지문**이었다 — 「일제의 지배」·「목숨을 바친」.
그리고 지침에 있던 **「실생활」이 「성생활」로 읽히는** 일도 있었다.

---

## 6차 — 글자는 코드가, 뜻은 모델이

### 1단계 · 읽는 길을 하나로

`QuestionSource`(프로토콜) · `BundledQuestionSource` · `StoredQuestionSource` 셋을
**`ContentFile<Payload>` 하나로** 합쳤다.

**이음새가 다른 자리로 옮겨갔기 때문이다.**

```
옛 생각   서버에서 곧바로 읽는다        → 「어디서 읽나」가 갈림길
새 설계   서버에서 받아 파일로 저장한다  → 읽는 것은 늘 파일. 「어느 파일이냐」만 다르다
```

낱말 사전도 같은 모양이라 같은 타입을 쓴다.

> **여기서 배운 것 (가장 비쌌던 것)** — `Question` 에 `var tags: [String] = []` 를 더하고
> 앱이 안 켜졌다. **Swift 가 자동으로 만들어 주는 `Codable` 은 기본값을 쓰지 않는다.**
> 자동 생성 코드는 `decodeIfPresent` 가 아니라 `decode` 라서, 파일에 그 칸이 없으면 던진다.
> 해독기를 손으로 적어야 했다. → [Q&A.md](Q&A.md)

### 2단계 · 코드가 글자를 본다

`AnswerMatcher` 를 만들어 **확실한 것을 먼저 걸렀다.**

여기서 **축이 둘이라는 것**을 알게 됐다.

| 축 | 의미 | 누가 쓰나 |
|---|---|---|
| `AnswerShape` | 답이 **어떻게 생겼나** | 채점 |
| `AnswerKind` | 답이 **무엇인가** | 해설 |

「고구려, 백제, 신라」는 **목록이면서 동시에 `place`** 다. 한 열거형에 넣으면 해설이 주제를 잃는다.
`AnswerKind` 에서 `list`·`meaning` 을 빼 아홉으로 줄이고, 모양은 따로 두었다.

목록도 둘로 나뉘었다 — `closedList`(다 대야 함, 32) · `openList`(「등」으로 끝남, 셋 이상, 13).

### 3단계 · 모델은 마지막 구제 절차

`MeaningGrader` 를 `AnswerChecker` 로 바꿨다. **뜻을 보던 것이 글자를 보게 됐고,
이제는 코드가 못 가른 것만 본다.**

판정 근거를 `basis` 로 로그에 남기기 시작했고, 열거형을 **둘로 나눴다** —
`MatchBasis`(코드) · `CheckBasis`(모델). 섞으면 「코드가 일하고 있다」는 확인이 거짓이 된다.

### 🔴 여섯 번의 실패로 알아낸 것

한 글자 오타가 계속 통과했다. 지침을 여섯 번 고쳤다.

```
고죠선  → 고조선    2번째 글자   통과
태국가  → 애국가    1번째 글자   통과
단검신화 → 단군신화  2번째 글자   통과
단검왕검 → 단군왕검  2번째 글자   통과
```

마지막에 모델이 이렇게 적었다 — **「정답과 답변이 글자 하나씩 모두 같아 정확히 일치합니다.」**
시킨 대로 하려고는 했는데 **실제로 대조하지 못했다.**

**모델은 한글을 글자가 아니라 조각(token)으로 읽는다.**

```
사람이 보는 것    고 / 죠 / 선
모델이 보는 것    [고죠] [선]      ← 「죠」를 따로 꺼낼 수가 없다
```

「몇 번째 글자가 다른가」는 **물어볼 수단 자체가 없다.** 코드로 옮기니 한 번에 멈췄다.

반대로 **아예 다른 답**(「홍길동/안익태」·「매화/무궁화」)은 **10건 모두** 걸렀다.

> **그래서 정한 기준** — **답이 하나로 정해지는 일은 모델에게 맡기지 않는다.**
> 95%를 맞혀도 못 쓴다. 나머지 5%가 언제 나올지 모르고, 어제 맞힌 것을 오늘 틀려도
> 원인을 찾을 수 없다. → [GUIDE.md](GUIDE.md)

### 4단계 · 해설이 지어내지 않는다

`Question.facts` 에 사실을 미리 넣고, 모델에게는 **그 재료만** 준다.
모델의 일이 「기억해 내기」에서 **「고쳐 쓰기」로** 바뀌었다.

프롬프트에서 **문제 지문을 뺐다** — 글자를 설명하는 데 필요 없고 안전 필터에 걸린다.

> **여기서 배운 것** — `guide(for:)` 에 있던 `[구조]` **문장 틀이 로그에 글자 그대로
> 복사되어** 나왔다. 「단군왕검은 … 고마운 사람입니다」·「설날은 문제에 나온 설명처럼 …」.
> **틀을 주면 모델은 틀을 베낀다.** 이제 무엇을 말할지는 재료가 정하고,
> `tone(for:)` 은 말투 한 줄만 정한다.

재료를 채우는 규칙은 [해설_재료_규칙.md](해설_재료_규칙.md) 에 따로 두었다.

### 그리고 모달을 다시 만들었다

**고른 답도 어딘가의 정답이다.** 추석 문제에 「개천절」을 고르셨다면 개천절도 설명해 드린다.
**지어내지 않고 그 문항의 `facts` 를 조회해서** 만든다.

두 박자로 나눈 것은 — 정답이 처음부터 보이면 고른 답을 읽지 않기 때문이다.
자세한 것은 DocC 의 <doc:FeedbackModal>.

---

## 지금까지 쌓인 원칙

되돌리고 싶어질 때 여기를 먼저 본다.

| 원칙 | 어디서 나왔나 |
|---|---|
| **답이 하나로 정해지는 일은 코드가, 여럿인 일은 모델이** | 6차 3단계, 여섯 번의 실패 |
| **사실은 우리가 주고, 문장은 모델이 만든다** | 4차 관찰, 5차 계획 |
| **금지어를 인용하지 않는다. 할 일을 긍정형으로** | 4차 보완 |
| **틀을 주면 모델은 틀을 베낀다** | 6차 4단계 |
| **축을 섞지 않는다** (난이도/사다리, 모양/주제) | 3차, 6차 2단계 |
| **화면이 부르는 함수는 두 번 불려도 결과가 같게** | 4차 3단계 |
| **실패를 조용히 두지 않는다** — 셀 수 있게 남긴다 | 3차 로그, 6차 `basis`·`ModelFailure` |
| **어르신에게 「오류」는 뜻이 없다. 다음에 할 일을 알려 준다** | 4차 3단계 |

---

## 아직 못 한 것

- **어머니 관찰** — 9/5 이후 없다. 그 사이 채점·해설·모달이 전부 바뀌었다
- 문제집 1,130문항을 `questions.json` 으로 (지금 30문항)
- 낱말 사전을 화면에 ([낱말_구상.md](낱말_구상.md))
- 스토리 모드 ([스토리모드_구상.md](스토리모드_구상.md))
- 서버 갱신 · 대화형 힌트 (6차 6·7단계)
