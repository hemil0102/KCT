# 4차 프로토타입 · 1단계 — 채점을 문항마다로 옮기기

> 지금까지 : 다섯 문제를 다 풀고 **끝에서 한꺼번에** 채점했다 (`gradeAll()`)
> 여기서 : **한 문제를 낼 때마다** 채점하고, 틀리면 창을 띄울 준비를 한다
>
> 창(모달) 자체는 3단계에서 만든다. 1단계는 **창을 띄울 신호(`feedback`)까지**다.

---

## 왜 옮기는가

창을 띄우려면 「지금 이 문제가 틀렸다」를 **그 자리에서** 알아야 한다.
회차 끝에 몰아서 채점하면 그 순간이 이미 지나가 있다.

그래서 `gradeAll()` 은 사라지고, `gradeCurrent(_:answer:)` 하나로 바뀐다.

```
「다음」을 누름
   │
 submitCurrent()              ← 문항마다 한 번
   ├ 답 기록  submittedAnswers[item.id] = trimmed
   ├ 시간 기록 recordTiming(for:)
   └ Task { await gradeCurrent(...) }
                │
        ┌───────┴────────┐
      맞음              틀림
        │                │
 moveToNextQuestion()   feedback = IncorrectCommentary(...)
                                 │
                          「다음 문제」 누름
                                 ▼
                          dismissFeedback()
                                 └ moveToNextQuestion()
```

---

## 고칠 파일은 둘

| 파일 | 무엇을 |
|---|---|
| `KCT/Screens/QuizSession.swift` | ①~⑤ |
| `KCT/Screens/QuizView.swift` | ⑥, ⑦ |

---

## QuizSession.swift

### ① `submitCurrent()` 를 통째로 교체

지금 있는 `submitCurrent()` 를 지우고 이걸 넣는다.

```swift
    func submitCurrent() {
        guard let item = current else { return }
        guard !isGrading else { return }

        let trimmed = rawAnswer.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return }

        submittedAnswers[item.id] = trimmed

        recordTiming(for: item)

        Task { await gradeCurrent(item, answer: trimmed) }
    }
```

- `guard !isGrading` — 채점하는 1~3초 동안 「다음」을 또 누르면 같은 문제가 두 번 채점된다. 그걸 막는다.
- `Task { }` — `gradeCurrent` 가 `async` 라서. `submitCurrent()` 는 버튼이 부르는 함수라 `async` 가 될 수 없다.
- **`if isFinished { Task { await gradeAll() } }` 는 여기서 사라진다.** 이제 회차가 끝나는 판단은 `moveToNextQuestion()` 안에 있다.

### ② `gradeAll()` 을 지우고 두 함수로 나눈다

`private func gradeAll() async { ... }` 전체를 지우고, 그 자리에 아래 둘을 넣는다.

```swift
    private func gradeCurrent(_ item: QuizItem, answer: String) async {
        // 직접입력만 모델이 판정해 시간이 걸린다. 선다·O/X 는 곧바로 다음 문제로 이어진다.
        if item.mode == .typing { isGrading = true }

        let isCorrect = await judge(item, answer: answer)
        isGrading = false

        saveObsRecord(for: item, isCorrect: isCorrect)
        applyProgress(for: item, isCorrect: isCorrect)
        try? modelContext.save()

        if isCorrect {
            moveToNextQuestion()
        } else {
            feedback = IncorrectCommentary(
                selectedAnswer: answer,
                correctAnswer: item.question.answer,
                commentary: "해설을 준비 중이에요."
            )
        }
    }

    private func applyProgress(for item: QuizItem, isCorrect: Bool) {
        guard let progress = fetchProgressByID()[item.id] else { return }

        progress.countAttempt(correct: isCorrect)

        if item.affectsProgress {
            progress.moveLadder(correct: isCorrect)
        } else {
            progress.nudgeLadder(correct: isCorrect)
        }
    }
```

- `isGrading` 은 **직접입력일 때만** 켠다. 그래서 선다형·O/X 에는 「채점 중이에요」 화면이 **뜨지 않는다** — 「다음」을 누르면 곧바로 다음 문제다. 끌 때 조건을 안 보는 이유 — 켠 적이 없으면 이미 `false` 다.
- 순서가 중요하다 — `saveObsRecord` 가 `applyProgress` **앞**이다. `countAttempt` 가 `isIntroduced` 를 켜 버리기 때문에, 뒤로 가면 `wasFirstEver` 가 망가진다. (기존 `gradeAll()` 의 순서를 그대로 지킨 것)
- `commentary` 의 고정 문구는 **2단계에서 모델이 만든 해설로 바뀐다.** 지금은 자리만 잡는다.

### ③ 창을 닫는 함수를 새로 만든다

`moveToNextQuestion()` 바로 아래에 넣는다.

```swift
    func dismissFeedback() {
        guard feedback != nil else { return }

        feedback = nil
        moveToNextQuestion()
    }
```

`guard` 가 있는 이유 — 화면 쪽에서 **두 번 불릴 수 있다.** 없으면 한 번 눌러 두 문제가 넘어간다.

`private` 이 아니다 — 창의 「다음 문제」 버튼(화면)이 불러야 한다.

### ④ `moveToNextQuestion()` 끝에 두 줄 추가

`firstTouchAt = nil` 다음에 이어 붙인다.

```swift

        if isFinished { uploadObservations() }
```

회차가 끝나는 순간이 이제 여기 하나뿐이다. 업로드는 문항마다가 아니라 **회차 끝에 한 번**만.

### ⑤ `clearAnswers()` 에 한 줄 추가

`results = [:]` 다음 줄에.

```swift
        feedback = nil
```

새 회차가 지난 회차의 창을 물고 들어가지 않게.

---

## QuizView.swift

### ⑥ `screen(for:)` 의 분기 순서를 바꾼다

`isGrading` 이 이제 **회차 중간에도** 켜지므로, `isFinished` 안쪽에 있으면 안 된다.

```swift
    @ViewBuilder
    private func screen(for session: QuizSession) -> some View {
        if session.isGrading {
            GradingScreen()
        } else if session.isFinished {
            ResultScreen(
                session: session,
                onRestart: { restart(session) },
                onEraseProgress: { erase(session) }
            )
        } else {
            QuestionScreen(session: session, sessionMode: sessionMode) {
                readAloud(session.current)
            }
            .onChange(of: session.currentIndex) { _, _ in
                readAloud(session.current)
            }
        }
    }
```

### ⑦ 임시 확인용 알림 (1단계에서만)

**이걸 넣지 않으면 틀렸을 때 앱이 멈춘 것처럼 보인다.** `feedback` 은 채워지는데 그걸 그리는 화면이 아직 없기 때문이다. 3단계에서 진짜 창(`FeedbackSheet`)으로 교체하고 이건 지운다.

`body` 의 `Group { ... }` 다음, `.background(Color.white)` **위**에 붙인다.

```swift
        .alert(
            "다시 볼까요",
            isPresented: Binding(
                get: { session?.feedback != nil },
                set: { _ in }
            )
        ) {
            Button("다음 문제") { session?.dismissFeedback() }
        } message: {
            if let feedback = session?.feedback {
                Text("고르신 것 : \(feedback.selectedAnswer)\n이 문제의 답 : \(feedback.correctAnswer)\n\n\(feedback.commentary)")
            }
        }
```

---

## 다 넣고 확인할 것

1. **선다형을 맞힌다** → 대기 화면 없이 바로 다음 문제. 깜빡임이 없어야 한다.
2. **선다형을 틀린다** → 알림이 뜨고, 「다음 문제」를 눌러야 넘어간다.
3. **직접입력을 낸다** → 「채점 중이에요」가 1~3초 뜨고 그다음 결과.
4. **다섯 문제를 다 푼다** → 결과 화면의 O/X 표시와 맞힌 개수가 예전과 같은가.
5. **Supabase** → 회차당 다섯 줄. 중복 없이. `chosen`·`reason` 이 예전처럼 들어오는가.

---

## 남겨둔 것 (1단계에서 안 한다)

- **해설** — `commentary` 는 고정 문구. 2단계에서 모델이 쓴다.
- **O/X 의 「이 문제의 답」** — 지금은 `question.answer`(예: 「단군왕검」) 가 그대로 뜬다. 「맞아요/아니에요」와 어긋나 보일 수 있다. 2단계에서 해설과 함께 다듬는다.
- **진짜 창** — 시안 C, 3초 지연, 「다음 문제」 버튼. 3단계.
- **`secOnFeedback`** — 창을 얼마나 보고 있었나. 4단계.
- **`ResultScreen` 의 문항별 목록** — 창이 생기면 중복이다. 뺄지는 3단계에서 결정.
