# PROGRESS

**새 세션은 이 파일만 읽고 시작합니다.** 여기에 없는 것이 필요할 때만 아래 [파일 지도](#파일-지도)를 보고 그 파일의 **필요한 부분만** 엽니다.

- 갱신 시점 : 2026-08-25
- 이 파일의 상한 : **100줄**. 넘으면 오래된 것을 각자의 집(LEARNING_PLAN·GUIDE·Q&A)으로 보내고 여기서 지웁니다.

---

## 지금

이 네 줄만 읽어도 작업을 이어갈 수 있어야 합니다.

- **현재 단계** — **4차 프로토타입 0단계(관찰) 준비 중.** 리팩토링 5단계(테스트 타겟)는 **뒤로 미뤘습니다** — 지금 급한 것은 코드 품질이 아니라 **어머니에게서 숫자를 얻는 것**입니다
- **마지막으로 끝낸 것** — `QuestionProgress.record()` 를 **`countAttempt`(세기) + `moveLadder`(사다리)** 로 분리. 격려용 슬롯도 이제 "지금까지 맞힌 문제"에 셉니다
- **다음 할 일** — ① `nudgeLadder(correct:)` 추가 ② **`KCT.docc` 3곳 수정**(죽은 링크) ③ `ObsEvent` 계측
- **막힌 것** — 없음

**⚠️ `docbuild` 경고 3건이 열려 있습니다** (2026-08-25 확인). 전부 없어진 심볼 `QuestionProgress/record(correct:now:)` 를 가리킵니다 — `TwoAxes.md:36`, `TwoAxes.md:63`(「격려용 슬롯」 절 **제목부터 틀린 말이 됨**), `GradingPath.md:65`.

**4차 프로토타입 0단계 — 관찰이 먼저다** (사용자 합의됨)

| 단계 | 내용 | 상태 |
|---|---|:---:|
| 0-a | 격려용 슬롯 3규칙 — 세기 ✅ · 사다리 제외 ✅ · 바닥 한 칸 ✅ | ✅ |
| 0-b | 관찰 기록 (UI 변경 0) — `ObsRecord` · 시각 훅 · Supabase 업로드 · 재시도 큐 | ✅ |
| 0-c | 관찰 1~2회차 · 10분씩 | 🔶 1~4회차 완료 (08-26, 종이 기록) |
| 0-d | Supabase 업로드 (스키마가 굳은 뒤) | ⬜ |
| 0-e | 관찰 3~5회차 | ⬜ |
| 1~10 | 4차 기능 11단계 — **0단계 결과가 순서를 바꾼다** | ⬜ |

계획 전문 — 아티팩트 **[「먼저 재고, 그다음 짓는다」](https://claude.ai/code/artifact/9cf4bab8-5325-481e-ad4e-97f7dc9747d2)**

---

## 파일 지도

소스는 `KCT/KCT/<폴더>/`, 문서는 저장소 루트. **의존성은 위에서 아래로 한 방향**입니다. **파일별 역할은 [LEARNING_PLAN.md](LEARNING_PLAN.md) 의 「파일 구성」** 에 있습니다 — 여기 옮겨 적지 않습니다.

| 폴더 | 책임 | 파일 수 |
|---|---|:---:|
| `App/` | 앱 시작점 | 2 |
| `Content/` | 문제 콘텐츠 (고정) + `questions.json` — **지금 10문항뿐** | 3 +1 |
| `Progress/` | 학습 진척 (변동) | 2 |
| `Session/` | 이번 회차 출제 구성 | 3 |
| `Grading/` | 채점 | 2 |
| `Focus/` | 묻는 대상 하이라이트 (**2층·3층은 꺼져 있음**) | 4 |
| `Screens/` | 회차 상태(`QuizSession`) + 화면 4개 | 5 |
| `DesignSystem/` | 색·조판·낭독 + 재사용 부품 (도메인을 모른다) | 6 |
| `KCT.docc/` | 문서 카탈로그 — 랜딩 + 개념 아티클 5개. **`.docc` 는 앱 번들에 안 실린다** | 6 `.md` |

**폴더가 생기거나 파일 수가 바뀌면 이 표를, 파일이 생기면 LEARNING_PLAN 을 고칩니다.**

---

## 결정 기록

되돌리려는 유혹이 생길 만한 것만 남깁니다. 한 줄씩, 최대 10개.

- **격려용 슬롯은 세 규칙을 따른다** — ① 맞힌 개수에 **센다**(어머니가 맞힌 건 맞힌 것) ② 사다리 승급·강등에서 **뺀다** ③ 단 **바닥 칸에서만 한 칸**(2지선다→O/X) 올려 준다. ③이 없으면 격려용에 자주 걸리는 문항이 2지선다에 갇힌다
- **`isIntroduced` 는 `countAttempt` 에 있다** — "한 번이라도 나왔는가" 는 사다리가 아니라 **일어난 사실**이다. `moveLadder` 에 두었더니 격려용 문항이 계속 신규 줄로 돌아갔다
- **관찰 상한은 15분이 아니라 10분** — CBL 기록에 두 번 잰 값이 있다(p.36·38). 컨디션이 좋은 날에도 10분이었다
- **단서는 계단이 아니라 시계** — 도움을 여러 단계로 쌓으면 마지막 칸만 쓰인다(Aleven 2001, 81%). 사용자도 같은 결론에 먼저 도착해 있었다(CBL p.57)
- **규칙 1 은 "AI 가 `.swift` 를 직접 고치지 않는다"** — `RULES.md` 가 반대로 적혀 있던 것을 2026-08-25 에 맞췄다
- **문서는 저장소 루트에, 소스 폴더 안에 두지 않는다** — file-system synchronized group 이라 앱 번들로 복사된다 (Q&A 참조)
- **`DifficultyMode` → `AskingMode`** — 난이도가 아니라 **묻는 방식**이다. `Question.difficulty`(축 A)와 겹쳐 두 축이 헷갈렸다
- **`FocusStore.usesModelAnalysis = false`** — 모델이 질문 전체를 돌려줘 지문이 통째로 형광펜 처리됐다. `FocusAnalyzer`·`QuestionFocusRecord` 는 **지금 안 불리는 코드**

---

## 열린 질문

답이 나오면 지우고, 개념 질문이면 GUIDE.md·Q&A.md 로 옮깁니다.

- **마스터 복습 칸이 격려용에 먹힐 수 있다** — `fillSlots` 가 예약한 1칸이 셔플 후 양 끝에 오면 2지선다로 나가고 진척에도 안 남는다. 확률 **2/5**. 마스터 문항이 아직 0개라 관측 불가 — **생기는 날 정한다**
- **격려용 문항의 `nextDueAt` 이 `nil` 로 남는다** — 복습 줄 정렬이 `nextDueAt ?? .distantPast` 라 **맨 앞에 선다.** 10문항일 땐 티가 안 나고 **늘리면 드러난다**
- **어머니가 종료 화면 숫자를 어떻게 읽는가** — 3차에서 한 번도 확인된 적 없다. 관찰 5회차 질문이 답한다

---

## 세션 로그

최근 3개만 남깁니다. 넷째가 생기면 가장 오래된 것을 지웁니다.

- **2026-08-25** — ① CBL 기록(60쪽)과 Xcode 소스를 대조해 **아티팩트의 전제 7개를 고침.** 가장 큰 것 — 문제집이 1000이 아니라 **10문항**이고, 누적 카운터는 버그가 아니라 `affectsProgress:false` 설계가 화면으로 샌 것(15×3/5=9, 기록과 정확히 일치) ② `record()` 분리 적용 ③ **규칙 14~20 신설** — 「만들면서 알아가기」(『Make to Know』 근거)와 「`KCT.docc` 동기화」. 규칙 1 모순 해소 ④ `docbuild` 로 죽은 링크 3건 확인
- **2026-08-24** — 4차 사전 리서치. 아하 효과(Auble 1979 → Kizilirmak 2016), 도움 남용(Aleven 2001), 프로테제 효과(Kobayashi 2024, 예고 g=0.48 / 예고 없으면 −0.02), 노년 인지 피로(Gilsoul 2024 — **노년은 시간에 따른 붕괴가 없었다**). 아티팩트 2개로 정리
- **2026-08-18** — 리팩토링 3·4단계. `STUDY_GUIDE.md` + `KCT.docc`(랜딩 + 개념 아티클 5개). `docbuild` 심볼 링크 경고 0 달성, 27/27 요약 블록 완료

---

## 어디에 무엇을 쓰나

같은 사실을 두 곳에 쓰지 않습니다. 중복은 낡은 쪽이 거짓말을 하게 만듭니다.

| 성격 | 파일 |
|---|---|
| 지금 어디까지 왔나, 다음에 뭘 하나 | **PROGRESS.md** (이 파일) |
| 코드를 어떤 순서로 읽을까 | [STUDY_GUIDE.md](STUDY_GUIDE.md) |
| 설계 원칙, 파일별 역할, 단계별 계획 | [LEARNING_PLAN.md](LEARNING_PLAN.md) |
| 용어의 뜻 | [GUIDE.md](GUIDE.md) |
| 막혔던 것과 그 해결 | [Q&A.md](Q&A.md) |
| AI 작업 규칙 — 목록 | [CLAUDE.md](CLAUDE.md) |
| 규칙의 취지와 지키는 방법 | [RULES.md](RULES.md) |
| 어머니 관찰 계획 · 4차 단계 | 아티팩트 「먼저 재고, 그다음 짓는다」 |

## 2026-08-26 — 같은 문제만 나오던 것을 고치고, 로그를 넣기 시작

- **출제기 결함 발견** — 복습 줄이 회차 크기만큼 차면 신규가 영영 못 들어왔다. 어머니가 4회차 내내 같은 다섯 문항(1·2·9·11·17)만 푸셨고, 코드로 계산한 「첫 회차 다섯」과 실제가 정확히 일치해 구조임이 확인됐다. → `fillSlots` 가 **신규 2칸·마스터 1칸을 먼저 떼어 두는** 방식으로 바뀌었다. `Queues.learning`(복습 절대 우선)은 삭제.
- **`nextDueAt` 이 아무도 채우지 않는 상태** — `record()` 를 셋으로 나눌 때 예정 시각을 잡는 줄이 함께 빠졌다. 간격 반복은 지금 **동작하지 않는다.** 주석·DocC 를 사실대로 고쳐 두었고, 되살리는 것은 로그 뒤로 미뤘다 (한 번에 하나만 바꾸기 위해).
- **규칙 1 의 경계가 확장자 → 줄 단위로** 바뀌었다. 실행되는 줄은 사용자, `//`·`///`·최상단 요약은 AI. 새 타입은 AI 가 **주석만 든 뼈대 파일**을 먼저 만든다.
- **규칙 21 신설** — 「다 넣었어」는 코드 리뷰 요청이다.
- **0-b-1·2 완료** — `ObsRecord`(관찰 기록 한 줄) + `QuizSession` 의 시각 훅. `secToFirstTouch`(생각한 시간)와 `secToSubmit`(누르기까지)을 따로 잰다.

### 열린 질문 (2026-08-26 추가)

- **`nextDueAt` 을 되살릴 때, 격려용으로만 나온 문항의 `nil` 을 맨 앞으로 볼 것인가 맨 뒤로 볼 것인가.**
- **`saveObsRecord` 가 조용히 건너뛴 줄을 어떻게 알아채나.** 5줄이 나와야 할 회차에 4줄만 있어도 지금은 아무도 모른다.
- **`eraseAllProgress()` 는 `ObsRecord` 를 지우지 않는다.** 그래서 기록을 지운 뒤 `wasFirstEver` 가 다시 `true` 로 찍힌다 — 「진짜 처음」과 구분이 안 된다.

## 2026-08-26 (저녁) — 로그가 실제로 흐르기 시작했다

**0-b 끝.** 어머니가 문항 하나를 풀 때마다 한 줄이 남고, 회차가 끝나면 Supabase 로 올라갑니다. **화면은 한 픽셀도 안 바뀌었습니다.**

- `ObsRecord` — `sessionID` · `askedAt` · `questionID` · `secToFirstTouch` · `secToSubmit` · `isCorrect` · `modeRaw` · `wasFirstEver` · `affectsProgress` · `uploadedAt`
- **두 시각을 따로 잽니다** — 처음 손댄 순간(생각한 시간)과 「다음」을 누른 순간. 그 차이가 「고르고 나서 망설인 시간」
- `wasFirstEver` 는 `start()` 에서 **미리** 읽습니다 — `countAttempt()` 가 `isIntroduced` 를 켜 버리기 때문
- Supabase — `obs_record` 표 + **입력만 허용하는 RLS**. 읽기 정책은 일부러 없음. 값의 모양까지 `with check` 로 검사
- 키는 `.gitignore` 된 `SupabaseConfig.swift` 에. 저장소가 공개라 자동 수집기를 막기 위한 것이고, **비밀이 되지는 않는다**(바이너리에 실림). 진짜 방어선은 RLS
- 재시도 큐 — `uploadedAt == nil` 인 줄을 회차 시작마다 다시 보냄. 실패를 화면에 알리지 않는다

**첫 데이터가 두 가지를 증명했습니다.**

- **신규 2칸(A)이 작동합니다.** 2회차에 12번·3번이 새로 들어왔습니다. 어제 어머니가 4회차 내리 같은 다섯 문항만 푸시던 결함이 사라졌습니다
- **사다리가 로그만으로 검증됩니다.** 1회차에 격려용으로 맞힌 1·9번이 2회차에 `mode_raw=1`(O/X)로 나왔습니다 — `nudgeLadder` 가 딱 한 칸만 올린 것

**막혔던 것 셋은 [Q&A.md](Q&A.md) 에** — 401 은 키가 아니라 업서트였던 것, `@MainActor` 도 재진입은 못 막는다는 것, 누적 카운터가 버그가 아니라 설계가 샌 것.

### 열린 질문 (추가)

- **신규 문항이 격려용 자리에 앉으면 사다리를 못 탄다.** `fillSlots` 가 섞은 뒤 `shapeRound` 가 양 끝을 잡으므로 5분의 2 확률. 신규 2칸의 효과가 그만큼 깎인다. 어머니 데이터를 보고 정한다
- **한 회차의 다섯 줄이 다 왔는지 알 방법이 아직 없다.** `saveObsRecord` 는 시간을 못 잰 문항을 조용히 건너뛴다

