# 어려운 낱말 설명 — 계획 (하단 고정 패널 방향)

> 아직 적용 전입니다. 코드는 그대로 두고, 무엇을 어떻게 바꿀지만 정리했어요.
> 데모 3개 중 **2번 하단 고정 패널**로 정하셨고, 손잡이를 위로 쓸어올리면
> 예문까지 펼쳐지는 것으로 확장했습니다. (이전에 말풍선으로 짰던 계획을 이 문서로 대체합니다.)

## 전체 그림

여섯 조각으로 나눠집니다. 1~5번은 지금 바로 만들 수 있고, 6번(Foundation Models)은
그 위에 나중에 얹어도 되는 독립적인 레이어예요.

1. 낱말 사전 데이터 — `questions.json`
2. 낱말 탭 감지 — `KoreanText.swift` 확장
3. 하단 패널 UI (접힘/펼침 포함) — 새 파일
4. 화면 연결 — `QuestionScreen.swift`
5. 닫기 규칙
6. Foundation Models로 설명·예문 만들기 — 새 파일

말풍선 방식과 비교하면 **3·4번이 훨씬 단순해집니다.** 말풍선은 탭한 낱말의 화면 좌표를
기준으로 매번 위치를 계산해야 했는데, 하단 패널은 항상 화면 맨 아래에 붙어 있어서
그 계산이 통째로 필요 없어져요.

---

## 1. 낱말 사전 데이터 (`questions.json`)

`facts` 배열과 같은 자리에, 문제마다 `glossary` 배열을 추가합니다. 이번엔 접혔을 때
보일 **짧은 뜻**과, 쓸어올렸을 때 보일 **예문**을 나눠서 담습니다.

```json
{
  "id": 12,
  "question": "대한민국 헌법을 만들어 공포한 것을 기념하는 국경일은 무엇입니까?",
  "glossary": [
    {
      "word": "헌법",
      "definition": "나라를 다스리는 가장 중요한, 으뜸이 되는 법이에요.",
      "extra": "예문 — 대한민국 헌법 제1조는 '대한민국은 민주공화국이다'예요."
    },
    {
      "word": "공포한",
      "definition": "여러 사람에게 널리 알렸다는 뜻이에요. (무섭다는 '공포'와는 다른 글자예요!)",
      "extra": "예문 — 새로 정한 법을 공포하면, 그날부터 모든 국민이 그 법을 지켜야 해요."
    },
    {
      "word": "국경일",
      "definition": "온 나라가 함께 기념하도록 법으로 정해 놓은 특별한 날이에요.",
      "extra": "예문 — 삼일절·광복절·개천절·한글날도 모두 국경일이에요. 국경일에는 태극기를 답니다."
    }
  ]
}
```

`word`는 지문 안에 실제로 나오는 글자 그대로 적습니다 — `marker`·`highlight`가 이미
`(displayed as NSString).range(of: target)`으로 지문 안에서 위치를 찾는 방식을 그대로
재사용할 수 있어서, 범위를 따로 계산해 넣을 필요는 없어요.

---

## 2. 낱말 탭 감지 (`KoreanText.swift` 확장)

지금 `KoreanText`는 `marker`(형광펜 배경) · `highlight`(굵은 밑줄) 두 종류를 문자열
하나씩만 받고 있어요. 여기에 세 번째로 `glossary: [(word: String, definition: String, extra: String)]`를
추가하고, 각 낱말 자리에 **점선 밑줄**을 입힙니다.

```swift
if let glossary {
    for entry in glossary {
        let target = Self.keepingNumbersWithUnits(entry.word)
        let range = (displayed as NSString).range(of: target)
        if range.location != NSNotFound {
            attributed.addAttributes([
                .underlineStyle: NSUnderlineStyle.patternDot.rawValue,
                .underlineColor: UIColor(AppColor.textMuted),
            ], range: range)
        }
    }
}
```

탭한 자리가 어느 낱말 범위인지는 `UITapGestureRecognizer` + `NSLayoutManager`로
화면 좌표를 글자 인덱스로 바꿔서 알아냅니다 (하단 패널 방식이어도 "어느 낱말을 탭했나"는
똑같이 알아내야 해서, 이 부분은 말풍선 계획과 같아요).

```swift
let layoutManager = NSLayoutManager()
let textStorage = NSTextStorage(attributedString: label.attributedText ?? NSAttributedString())
textStorage.addLayoutManager(layoutManager)
let textContainer = NSTextContainer(size: label.bounds.size)
textContainer.lineFragmentPadding = 0
layoutManager.addTextContainer(textContainer)

let charIndex = layoutManager.characterIndex(
    for: tapPoint, in: textContainer, fractionOfDistanceBetweenInsertionPoints: nil
)
```

콜백은 이제 위치(`CGRect`)가 필요 없어서 더 단순해집니다.

```swift
var onTapWord: ((_ word: String, _ definition: String, _ extra: String) -> Void)?
```

---

## 3. 하단 패널 UI (새 파일, 예: `DesignSystem/GlossaryPanel.swift`)

데모 2와 같은 모양 — 화면 맨 아래 고정, 손잡이(작은 알약 모양 막대) + ✕ + 낱말 이름표 +
짧은 뜻. 손잡이를 위로 쓸어올리면 그 아래로 예문이 펼쳐지며 패널 높이가 커집니다.

```swift
struct GlossaryPanel: View {
    let word: String
    let definition: String
    let extra: String
    @Binding var isExpanded: Bool
    let onClose: () -> Void

    var body: some View {
        VStack(spacing: 0) {
            Capsule()
                .fill(AppColor.softBackground)
                .frame(width: 44, height: 5)
                .padding(.bottom, 12)
                .contentShape(Rectangle().inset(by: -12)) // 손가락이 살짝 벗어나도 잡히게 터치 영역을 넓힌다
                .gesture(dragOrTapGesture)

            HStack {
                Text(word).font(.system(size: 14, weight: .heavy)).foregroundStyle(AppColor.signature)
                Spacer()
                Button(action: onClose) {
                    Image(systemName: "xmark.circle.fill")
                        .foregroundStyle(AppColor.textMuted.opacity(0.5))
                }
            }
            Text(definition)
                .font(.system(size: 20, weight: .semibold))
                .padding(.top, 4)

            if isExpanded {
                Divider().padding(.vertical, 10)
                Text(extra)
                    .font(.system(size: 18, weight: .medium))
                    .foregroundStyle(AppColor.textMuted)
            } else {
                Text("▲ 위로 쓸어올리면 예문이 더 나와요")
                    .font(.system(size: 13, weight: .semibold))
                    .foregroundStyle(AppColor.textMuted)
                    .frame(maxWidth: .infinity)
                    .padding(.top, 10)
            }
        }
        .padding(20)
        .frame(maxWidth: .infinity, alignment: .leading)
        .frame(height: isExpanded ? 260 : 160)
        .background(AppColor.softBackground.opacity(0.4), in: RoundedRectangle(cornerRadius: 20, style: .continuous))
        .animation(.easeInOut(duration: 0.22), value: isExpanded)
    }

    /// 손잡이 하나로 "쓸어올리기"와 "그냥 탭하기"를 함께 받는다.
    /// minimumDistance: 0 으로 어떤 손짓이든 일단 받은 뒤, 놓을 때 움직인 거리로 구분한다.
    private var dragOrTapGesture: some Gesture {
        DragGesture(minimumDistance: 0)
            .onEnded { value in
                let moved = value.translation.height
                if abs(moved) < 8 {
                    isExpanded.toggle()          // 거의 안 움직였으면 탭으로 본다
                } else if moved < 0 {
                    isExpanded = true            // 위로 쓸어올림
                } else {
                    isExpanded = false           // 아래로 쓸어내림
                }
            }
    }
}
```

**판단이 갈리는 지점 — 펼쳤을 때 높이(260pt)가 화면을 얼마나 가릴지**
문제 지문이 긴 편이라 260pt를 펼치면 "다음" 버튼이 완전히 가려질 수 있어요. 대안은
(a) 지금처럼 260pt까지 펼치되 뒤의 화면을 살짝 어둡게 해서 "지금은 이것만 본다"는
느낌을 주거나, (b) 펼침 높이를 더 낮게 잡아 버튼이 항상 조금은 보이게 하는 것. 실제
기기에서 문제 하나가 몇 줄쯤 되는지 보고 정하는 게 정확할 것 같아요.

---

## 4. 화면 연결 (`QuestionScreen.swift`)

상태 두 개면 됩니다 — 말풍선 계획에 있던 `frame: CGRect`가 빠져서 더 단순해졌어요.

```swift
@State private var openGlossary: (word: String, definition: String, extra: String)?
@State private var isGlossaryExpanded = false
```

`KoreanText`의 `onTapWord`에서 `openGlossary`를 채우고 `isGlossaryExpanded = false`로
초기화합니다(새 낱말은 항상 접힌 채로 시작 — 데모와 동일). 패널은 화면 하단에 그냥
쌓으면 되므로, 말풍선처럼 `GeometryReader`로 낱말 위치를 계산할 필요가 없습니다.

```swift
ZStack(alignment: .bottom) {
    // 기존 화면 내용 (questionArea, nextButton 등)

    if openGlossary != nil {
        Color.black.opacity(0.001) // 뒤쪽 탭을 받기 위한 투명 캐처
            .onTapGesture { closeGlossary() }
    }

    if let g = openGlossary {
        GlossaryPanel(
            word: g.word, definition: g.definition, extra: g.extra,
            isExpanded: $isGlossaryExpanded,
            onClose: { closeGlossary() }
        )
        .transition(.move(edge: .bottom))
    }
}

private func closeGlossary() {
    openGlossary = nil
    isGlossaryExpanded = false
}
```

---

## 5. 닫기 규칙

세 가지 방법으로 닫힙니다 — ✕ 버튼, 같은 낱말 다시 탭, 화면 다른 곳 탭. 마지막 것만
판단이 필요해요.

**판단이 갈리는 지점 — 화면 다른 곳(보기 버튼 포함)을 탭했을 때**

| | ⓐ 첫 탭은 닫기만 | ⓑ 닫기 + 그 아래 동작도 같이 |
|---|---|---|
| **동작** | 보기 버튼 위라도 그 탭은 패널만 닫고, 버튼은 안 눌림 | 패널 닫히면서 버튼도 같이 눌림 |
| **좋은 점** | 실수로 답이 선택될 위험이 없음 | 손이 한 번 덜 감 |
| **나쁜 점** | 어쩌다 두 번 눌러야 할 수 있음 | 패널 열어둔 채 이것저것 누르다 답이 잘못 걸릴 수 있음 |

지난번과 같은 이유로 **ⓐ**를 추천합니다 — 위 코드의 투명 캐처가 그 역할을 합니다.
ⓑ를 원하시면 캐처를 없애고 `.simultaneousGesture(TapGesture().onEnded { closeGlossary() })`를
바깥 `VStack`에 다는 쪽으로 바꾸면 됩니다.

---

## 6. Foundation Models로 설명·예문 만들기 (새 파일, 예: `Data/GlossaryComposer.swift`)

패널이 "짧은 뜻 / 예문" 두 단으로 나뉘어 있어서, 이번엔 Foundation Models 한 번 호출로
**둘 다** 만들어달라고 하기 좋은 모양이 됐습니다.

**가장 중요한 원칙은 그대로입니다 — 모델이 새 사실을 지어내면 안 됩니다.** JSON의
`definition`을 "이미 확정된 사실"로 주고, 그 사실 안에서 표현만 다듬거나 예문을
만들라고 못 박습니다.

```swift
import FoundationModels

@Generable
struct GlossaryExplanation {
    @Guide(description: "한 문장, 짧고 쉬운 뜻풀이")
    let short: String

    @Guide(description: "한두 문장, 지금 질문 맥락에 맞는 구체적인 예문")
    let example: String
}

func composeGlossary(
    word: String, baseDefinition: String, questionText: String
) async -> (definition: String, extra: String) {
    guard case .available = SystemLanguageModel.default.availability else {
        return (baseDefinition, "") // 기기가 지원 안 하면 JSON 원문만, 예문 칸은 비운다
    }

    let session = LanguageModelSession(instructions: {
        """
        당신은 70대 어르신에게 낱말을 설명하는 다정한 선생님입니다.
        아래 '확정된 뜻'의 사실 내용은 절대 바꾸지 마세요. 그 사실 안에서만
        표현을 다듬고, 지금 질문과 자연스럽게 이어지는 예문을 하나 만들어 주세요.
        """
    })

    let prompt = "질문: \(questionText)\n낱말: \(word)\n확정된 뜻: \(baseDefinition)"

    do {
        let response = try await session.respond(to: prompt, generating: GlossaryExplanation.self)
        return (response.content.short, response.content.example)
    } catch {
        return (baseDefinition, "")
    }
}
```

**기기 요구사항** — iOS 26 이상 + Apple Intelligence를 지원하는 기기에서만 됩니다.
온디바이스라 인터넷이 필요 없고 밖으로 안 나가요. 어머니 폰이 이 조건을 만족하는지는
직접 확인이 필요해요 — 안 되면 위 코드가 자동으로 JSON 원문(`definition`)만 보여주는
쪽으로 빠지고, 예문 칸("▲ 위로 쓸어올리면...")은 건너뜁니다.

**판단이 갈리는 지점 — 매번 새로 생성할지, 한 번 만들어 두고 재사용할지**

| | 매번 새로 생성 | 처음 한 번 생성 후 저장 |
|---|---|---|
| **좋은 점** | 코드가 단순함 | 패널 열 때 기다릴 필요 없음, 문구가 항상 같음 |
| **나쁜 점** | 열 때마다 잠깐 기다려야 함, 볼 때마다 표현이 조금씩 달라짐 | (question_id, 낱말)을 키로 하는 작은 캐시가 하나 더 필요 |

어머니가 같은 문제를 여러 번 다시 만나실 텐데, 그때마다 설명이 미묘하게 달라지면
오히려 헷갈리실 수 있어서 **저장해서 재사용**하는 쪽을 추천해요.

**참고 — 2026년 여름 WWDC에서 애플이 같은 API에 Anthropic·Google 같은 외부 모델도
연결할 수 있게 열었습니다.** 온디바이스가 안 되는 기기에서 클라우드로 대체하는 것도
이제 가능은 한데, 개발자가 직접 어떤 모델을 쓸지 골라 연결해야 하는 구조라 손이 많이 가요.
지금은 온디바이스만으로 먼저 만들고, 필요해지면 그때 확장하는 걸 추천합니다.

---

## 진행 순서 제안

1. `questions.json`에 낱말 사전 몇 개만 먼저 채워보기 (제헌절 문제 3개 낱말로 테스트, `extra` 예문도 손으로 채워서)
2. 점선 밑줄 + 탭 감지 + 하단 패널(접힘/펼침 포함, Foundation Models 없이 JSON 원문 그대로) — 이 상태로 어머니와 한 번 테스트해도 됩니다
3. 잘 동작하면 그 다음에 6번(Foundation Models로 짧은 뜻·예문 만들기)을 얹기 — 독립적인 레이어라 순서를 미뤄도 안전해요

## 판단이 갈리는 지점 — 한눈에

| 지점 | ⓐ | ⓑ | 제 의견 |
|---|---|---|---|
| 펼쳤을 때 패널 높이 | 260pt까지 크게(버튼이 가려질 수 있음) | 더 낮게(버튼이 항상 조금은 보임) | 실제 화면에서 지문 길이를 보고 결정 |
| 새 낱말 탭할 때 | 항상 접힌 채로 시작 | 이전 상태(펼침 여부) 유지 | ⓐ — 매번 예문부터 보이면 오히려 산만함 |
| 화면 다른 곳 탭 | 첫 탭은 닫기만 | 닫기 + 아래 동작도 같이 | ⓐ — 실수 방지 |
| 설명·예문 생성 시점 | 열 때마다 새로 생성 | 처음 한 번 생성 후 저장 | ⓑ — 대기시간·일관성 |
| 기기 미지원 시 | 기능을 아예 숨김 | JSON 원문 뜻만 보여줌(예문 칸 생략) | ⓑ — 뭐라도 보여주는 게 나음 |

## 참고 자료

- [Getting Started with Foundation Models in iOS 26 (appcoda.com)](https://www.appcoda.com/foundation-models/)
- [Exploring the Foundation Models framework (createwithswift.com)](https://www.createwithswift.com/exploring-the-foundation-models-framework/)
- [WWDC26 — Bring an LLM Provider to the Foundation Models Framework (developer.apple.com)](https://developer.apple.com/videos/play/wwdc2026/339/)

> **확인 못 한 것** — 어머니 폰이 실제로 Apple Intelligence를 지원하는 기종인지, 그리고
> 펼쳤을 때 260pt가 실제 화면에서 "다음" 버튼을 얼마나 가리는지는 여기서 확인할 수
> 없었습니다. 둘 다 실제 기기로 봐야 압니다.
