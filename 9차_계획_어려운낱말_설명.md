# 어려운 낱말 설명 — 계획 (iOS 기본 시트 방향, 최종)

> **1·2·6단계는 적용 완료, 3~5단계는 이번 판에서 iOS 기본 시트로 새로 바뀌어 다시
> 적용이 필요합니다.** 코드는 그대로 두고, 무엇을 어떻게 바꿀지만 정리했어요.
> 처음엔 데모의 하단 고정 패널(손수 그린 손잡이·펼침 애니메이션)로 정했었는데, 다
> 넣고 실기기로 보니 두 가지 문제가 있어 이번 판에서 고쳤습니다.

## 이번에 바뀐 것 네 가지 (지난 판 대비)

1. **낱말 사전 필드 이름** — 처음엔 `word`/`definition`/`extra`로 계획했는데, 이미 있던
   `glossary.json`(하나의 뜻을 여러 문항이 같이 쓰는 구조)에 맞춰 **`word`/`gloss`/`examples`**로
   실제 반영했습니다. `examples`는 예문 문장이 아니라 **비슷한 낱말 목록**입니다(예: 국경일 →
   삼일절·제헌절·광복절·개천절·한글날).
2. **짧은 뜻은 이미 다 손으로 다듬어 놓았습니다** — 30개 문항, 39개 낱말을 전부 검토해서
   10~20자 이내 쉬운 말로 이미 채웠습니다(1단계 완료). 그래서 6단계 Foundation Models가
   짧은 뜻까지 다시 다듬을 필요는 없어졌고, **예문 만드는 일만** 맡기면 됩니다.
3. **탭 감지에 조각 하나가 빠져 있었습니다** — `KoreanText`는 지금 제스처를 하나도 안 받는
   구조라, 계획에 있던 `NSLayoutManager` 코드만으론 동작하지 않습니다. `Coordinator`를
   새로 만드는 부분을 넣었고, 다 넣은 뒤 실기기 검토에서 **점선 밑줄이 안 보이는 버그**를
   하나 더 찾아 2단계에 고쳐 뒀습니다(`patternDot`은 `.single`과 같이 써야 그려집니다).
4. **하단 패널을 직접 그리지 않고, iOS 기본 시트(`.sheet`)로 바꿨습니다** — 손잡이·펼침
   애니메이션·투명 탭 캐처를 전부 우리가 만들 필요 없이 iOS가 대신 해 주고, iOS 26부터는
   **Liquid Glass**(반투명 유리) 모양이 자동으로 붙습니다. 3~5단계가 이번에 다 바뀐 이유입니다.

## 전체 그림

1. ~~낱말 사전 데이터 — `questions.json`~~ **완료** (30문항, 39낱말)
2. ~~낱말 탭 감지 — `KoreanText.swift` 확장~~ **거의 완료** — 점선 밑줄 버그 한 줄만 마저 고치면 끝
3. 낱말 뜻 모달 — iOS 기본 시트(`GlossaryPanel.swift`) ← **다음 작업**
4. 화면 연결 — `QuestionScreen.swift`
5. 닫기 규칙 (이제 대부분 시스템이 처리)
6. ~~Foundation Models로 예문 만들기~~ **완료**

---

## 1. 낱말 사전 데이터 (`questions.json`) — 완료

`facts` 배열과 같은 자리에, 문제마다 `glossary` 배열이 이미 들어가 있습니다. 실제로
반영된 모습(12번 문항, 손으로 그대로 옮긴 것):

```json
{
  "id": 12,
  "question": "대한민국 헌법을 만들어 공포한 것을 기념하는 국경일은 무엇입니까?",
  "glossary": [
    { "word": "헌법", "gloss": "나라의 가장 중요한 법" },
    { "word": "공포한", "gloss": "세상에 널리 알린" },
    {
      "word": "기념하는", "gloss": "기억하며 축하하는"
    },
    {
      "word": "국경일",
      "gloss": "나라가 정한 큰 기념일",
      "examples": ["삼일절", "제헌절", "광복절", "개천절", "한글날"]
    }
  ]
}
```

`word`는 지문 안에 실제로 나오는 글자 그대로 적혀 있습니다 — `marker`·`highlight`가 이미
`(displayed as NSString).range(of: target)`으로 지문 안에서 위치를 찾는 방식을 그대로
재사용할 수 있어서, 2단계에서 범위를 또 계산해 넣을 필요는 없습니다.

**같은 낱말은 여러 문항에 걸쳐 정의가 하나로 통일돼 있습니다** — 예를 들어 '국경일'은
11~15번 문항에 다 나오는데 뜻이 전부 같습니다. `GlossaryCatalog`가 하려던 일("낱말은
되풀이되니 뜻은 한 벌만")을 데이터 자체에서 이미 만족한 셈입니다.

`examples`는 선택 항목이라 없는 낱말도 많습니다(39개 중 5개만 있음). 6단계에서 이걸
어떻게 쓸지는 아래에 정리했습니다.

---

## 1-A. 빠졌던 자리 — `Question`·`QuizItem`에 `glossary` 연결하기

> **다 넣은 뒤 검토에서 발견.** `questions.json`엔 `glossary`가 있지만, 이걸 읽어서
> `QuizItem`까지 전달하는 코드를 계획에 넣는 걸 제가 빠뜨렸습니다. 지금 `Question.swift`엔
> `glossary` 자리가 아예 없고, `QuizItem.swift`에도 없어서 `QuestionScreen.swift`의
> `glossary: item.glossary` 줄이 **컴파일 오류**를 냅니다("QuizItem에 그런 게 없다").
> 이 항목을 2단계보다 먼저 넣어야 합니다.

**① `Data/Question.swift` — `QuestionFact` 바로 위나 아래에 새 구조체 추가**
(``kind``·``examples`` 없이 뜻 하나만 담는, 지금 있는 ``QuestionFact``와 같은 성격의 작은 타입입니다.)

```swift
/// 지문 속 어려운 낱말 하나의 뜻. 1단계에서 `questions.json`에 이미 채워 뒀습니다.
struct GlossaryEntry: Codable, Hashable {
    /// 지문에 실제로 나오는 글자 그대로.
    let word: String

    /// 짧고 쉬운 뜻. 10~20자로 이미 다듬어 놓았습니다.
    let gloss: String

    /// 비슷한 낱말 목록. 대부분 비어 있습니다(39개 중 5개만 있음).
    var examples: [String] = []

    init(from decoder: Decoder) throws {
        let box = try decoder.container(keyedBy: CodingKeys.self)
        word     = try box.decode(String.self, forKey: .word)
        gloss    = try box.decode(String.self, forKey: .gloss)
        examples = try box.decodeIfPresent([String].self, forKey: .examples) ?? []
    }
}
```

**② 같은 파일, `struct Question` 안 — `facts` 프로퍼티(`var facts: [QuestionFact] = []`) 바로 아래에 한 줄 추가**

```swift
/// 지문 속 어려운 낱말들의 뜻. 없는 문항이 더 많습니다(30문항 중 11개).
var glossary: [GlossaryEntry] = []
```

**③ 같은 파일, `init(from decoder:)` 안 — `facts = try box.decodeIfPresent(...)` 줄 바로 아래에 한 줄 추가**

```swift
glossary = try box.decodeIfPresent([GlossaryEntry].self, forKey: .glossary) ?? []
```

**④ `Session/QuizItem.swift` — `actionGuide` 계산 프로퍼티 바로 아래에 새 계산 프로퍼티 추가**
`KoreanText.glossary`가 받는 모양(이름 있는 튜플)으로 여기서 바로 바꿔 주면, `QuestionScreen.swift`는
고칠 필요가 없습니다.

```swift
/// 지문에 점선 밑줄을 입힐 낱말들. `KoreanText`가 받는 모양으로 미리 바꿔 둔다.
var glossary: [(word: String, gloss: String, examples: [String])] {
    question.glossary.map { (word: $0.word, gloss: $0.gloss, examples: $0.examples) }
}
```

**다 넣고 확인할 것** — 이 네 개만 넣으면 `QuestionScreen.swift`는 지금 그대로 컴파일됩니다
(아래 2·4단계 코드는 이미 이 모양을 기대하고 짜여 있었습니다). 빌드가 되면, 12번 문항
("헌법을 만들어 공포한...")을 열어서 점선 밑줄이 뜨는지 확인하면 됩니다.

---

## 2. 낱말 탭 감지 (`KoreanText.swift` 확장) — 최종

> **실기기 검토에서 발견된 버그 — 점선 밑줄이 아예 안 보임.** 지금 타이핑된 코드의
> `.underlineStyle: NSUnderlineStyle.patternDot.rawValue` 이 한 줄이 원인입니다.
> `patternDot`(점선 무늬)은 그 자체로는 그림을 안 그리고, `.single`(실선) 같은
> **밑줄 종류와 같이 있어야만** 화면에 그려지는 애플 UIKit의 특성입니다. 지금
> `KoreanText.swift`의 128번째 줄(`updateUIView` 안, 점선 밑줄 입히는 자리)을 아래처럼
> **바꿔주세요** — 이 한 줄만 고치면 됩니다.
>
> ```swift
> // 고치기 전
> .underlineStyle: NSUnderlineStyle.patternDot.rawValue,
> // 고친 뒤
> .underlineStyle: NSUnderlineStyle.single.rawValue | NSUnderlineStyle.patternDot.rawValue,
> ```

### 어디에 무엇을 넣는지

지금 `KoreanText`의 실제 구조(중요한 줄 번호만):

```
struct KoreanText: UIViewRepresentable {
    let text: String                                    // 40줄
    var font, color, lineSpacing                        // 41~43줄
    var highlight: String?, highlightColor              // 46~47줄
    var marker: String?, markerColor                    // 50~51줄
                                                          ← ① 여기에 glossary, onTapWord 추가
    func makeUIView(context:) -> UILabel { ... }         // 53~61줄
                                                          ← ② 여기서 탭 제스처를 라벨에 붙임
    func updateUIView(_:context:) {
        ... marker 처리 ...                              // 81~87줄
        ... highlight 처리 ...                           // 90~103줄
                                                          ← ③ 여기에 점선 밑줄 + 범위 저장
        label.attributedText = attributed                // 105줄
    }
    func sizeThatFits(...) { ... }                       // 111~116줄
    private static func keepingNumbersWithUnits(_:) { ... } // 123~136줄
}
                                                          ← ④ 여기(파일 맨 아래)에 Coordinator 클래스 추가
```

지금 파일엔 제스처를 받는 장치가 전혀 없습니다(라벨만 만들고 끝). SwiftUI가 UIKit의
탭 이벤트를 다시 받으려면 `Coordinator`가 있어야 하고, 문항 하나에 낱말이 여러 개(최대
8개, 6번 문항) 붙을 수 있으니 **"어느 범위가 어느 낱말인지"를 탭할 때마다 다시 찾지 않고
`updateUIView`가 계산할 때 미리 저장해 둡니다.**

### ① 프로퍼티 추가 (구조체 상단, `marker` 옆)

```swift
/// 점선 밑줄로 표시하고, 탭하면 뜻을 보여줄 낱말들.
var glossary: [(word: String, gloss: String, examples: [String])] = []

/// 낱말을 탭했을 때 호출. 위치(CGRect)가 필요 없어 하단 패널 쪽이 훨씬 단순합니다.
var onTapWord: ((_ word: String, _ gloss: String, _ examples: [String]) -> Void)?
```

### ③ 점선 밑줄 입히기 + 범위 저장 (`updateUIView`, highlight 처리 뒤·105줄 앞)

```swift
// 어려운 낱말은 점선 밑줄. 탭 판정을 위해 범위도 함께 기억해 둔다.
var hits: [(range: NSRange, word: String, gloss: String, examples: [String])] = []
for entry in glossary {
    let target = Self.keepingNumbersWithUnits(entry.word)
    let range = (displayed as NSString).range(of: target)
    if range.location != NSNotFound {
        attributed.addAttributes([
            .underlineStyle: NSUnderlineStyle.single.rawValue | NSUnderlineStyle.patternDot.rawValue,
            .underlineColor: UIColor(AppColor.textMuted),
        ], range: range)
        hits.append((range, entry.word, entry.gloss, entry.examples))
    }
}
context.coordinator.glossaryHits = hits
context.coordinator.onTapWord = onTapWord

label.attributedText = attributed
```

`Coordinator`에 저장해 두는 이유는, 탭 제스처가 호출하는 함수는 `Coordinator` 안에
있는데 `KoreanText` 구조체 값 자체는 SwiftUI가 매번 새로 만들어 버리기 때문입니다.
`Coordinator`는 뷰가 살아있는 동안 하나만 유지되니 여기에 최신 범위를 매번 갱신해
두는 것입니다.

### ② + ④ 탭 제스처 + `Coordinator` (새로 추가하는 부분)

`makeUIView` 안, 라벨을 만든 직후(59~60줄 사이)에 추가:

```swift
label.isUserInteractionEnabled = true
label.addGestureRecognizer(
    UITapGestureRecognizer(target: context.coordinator, action: #selector(Coordinator.handleTap))
)
context.coordinator.label = label
```

`makeUIView` 함수 바로 위(또는 아래)에 추가:

```swift
func makeCoordinator() -> Coordinator { Coordinator() }
```

파일 맨 아래, `private extension Character { ... }` 옆에 새 클래스로 추가:

```swift
/// 탭 이벤트를 받아 어느 낱말이 눌렸는지 알아내는 다리 역할.
/// `updateUIView` 가 매번 최신 범위(glossaryHits)를 여기 채워 둔다.
final class Coordinator: NSObject {
    weak var label: UILabel?
    var glossaryHits: [(range: NSRange, word: String, gloss: String, examples: [String])] = []
    var onTapWord: ((_ word: String, _ gloss: String, _ examples: [String]) -> Void)?

    @objc func handleTap(_ recognizer: UITapGestureRecognizer) {
        guard let label, let attributedText = label.attributedText else { return }

        let layoutManager = NSLayoutManager()
        let textStorage = NSTextStorage(attributedString: attributedText)
        textStorage.addLayoutManager(layoutManager)
        let textContainer = NSTextContainer(size: label.bounds.size)
        textContainer.lineFragmentPadding = 0
        textContainer.maximumNumberOfLines = label.numberOfLines
        layoutManager.addTextContainer(textContainer)

        let tapPoint = recognizer.location(in: label)
        let charIndex = layoutManager.characterIndex(
            for: tapPoint, in: textContainer, fractionOfDistanceBetweenInsertionPoints: nil
        )

        guard let hit = glossaryHits.first(where: { NSLocationInRange(charIndex, $0.range) }) else { return }
        onTapWord?(hit.word, hit.gloss, hit.examples)
    }
}
```

이렇게 하면 문항 하나에 낱말이 몇 개가 붙든(6번 문항처럼 8개라도) 탭 하나로 정확히
어느 낱말인지 구분됩니다.

> ⚠️ **이 절의 밑줄 코드는 더는 안 씁니다 — 바로 아래 2-A를 보세요.** 실기기에서
> O/X 강조 밑줄과 사전 점선 밑줄이 받침(글자 아래쪽)과 겹쳐 보이는 문제가 있었습니다.
> `.underlineStyle` 자체를 버리고 직접 그리는 방식(`UnderlineLabel`)으로 바꿨습니다.

---

## 2-A. 밑줄이 글자와 겹쳐 보임 — 직접 그리는 밑줄로 교체

**증상** — O/X 강조(굵은 실선)와 낱말 사전(점선) 밑줄이 받침이 있는 글자("국", "정"
같은) 아래쪽과 겹쳐 보인다.

**원인** — `NSAttributedString.underlineStyle`은 **폰트가 정한 자리**에만 그려지고,
그 자리를 얼마나 띄울지 조절하는 공개 API가 없습니다. 한글은 받침 때문에 로마자보다
아래쪽으로 튀어나오는 부분이 있어서, 영문 기준으로 정해진 밑줄 위치와 자주 겹칩니다.

**고치는 법** — `.underlineStyle`/`.underlineColor` 속성을 아예 쓰지 않습니다. 대신
`UILabel`을 상속한 `UnderlineLabel`이 글자를 다 그린 뒤(`drawText(in:)`), 각 강조·사전
구간의 실제 화면 위치를 `NSLayoutManager`로 다시 계산해서, **원하는 만큼 아래로 띄운
자리에 선을 직접 그립니다.** 점선은 `CGContext`의 line dash로, 실선은 두께만 다르게
그립니다.

**바뀐 것 — `KoreanText.swift` 전체 구조:**

| | 전 | 후 |
|---|---|---|
| 밑줄을 누가 그리나 | `UILabel`이 `.underlineStyle` 속성으로 자동으로 | `UnderlineLabel.drawText(in:)`이 직접 |
| 간격 조절 | 안 됨 | `underlineGap`(기본 2pt — 처음엔 4였다가 너무 떨어져 보여 줄임) |
| `makeUIView`/`updateUIView`/`sizeThatFits`의 뷰 타입 | `UILabel` | `UnderlineLabel`(`UILabel`의 하위 클래스) |
| `updateUIView`에서 하는 일 | 밑줄 속성을 attributedString에 직접 추가 | 색만 attributedString에 넣고, 범위·색·점선 여부는 `label.underlines`에 따로 전달 |

`Coordinator`의 탭 판정(`glossaryHits`)은 그대로입니다 — 탭 위치를 글자 범위로 바꾸는
로직은 밑줄을 누가 그리는지와 무관합니다.

**간격을 더 벌리거나 줄이려면** — `UnderlineLabel.underlineGap`(기본 2, 처음엔 4로 시작했다가 너무 떨어져 보여 줄임) 값만 바꾸면
됩니다. 실선 두께는 `underlineThickness`(기본 2.5), 점선 굵기·간격은
`drawText(in:)` 안의 `setLineWidth(1.6)`·`setLineDash(phase:lengths:)` 값입니다.

**다 넣고 확인할 것** — O/X 문항과 낱말 사전이 있는 문항을 열어, 밑줄이 받침과 안
겹치고 적당히 떨어져 보이는지 확인합니다. 글자 크기(접근성 설정)를 키워도 여전히
안 겹치는지 함께 봅니다.

> ⚠️ 이 수정이 새 버그를 하나 만들었습니다 — 아래 **2-B**를 봐 주세요.

---

## 2-B. 밑줄 여유가 글자를 세로로 밀어냄 — 위쪽 정렬을 다시 강제

**증상** — 2-A를 적용한 뒤, 사용자가 `drawText(in:)` 안의 다음 부분을 짚으며
"에러나 재검토"를 요청했습니다.

```swift
let box = layoutManager.boundingRect(forGlyphRange: intersection, in: textContainer)
                let y = bounds.origin.y + box.maxY + underlineGap
                let startX = bounds.origin.x + box.minX
                let endX = bounds.origin.x + box.maxX
```

이 환경(`device_bash`, macOS 위 Linux VM)에는 `xcodebuild`가 없어 실제 컴파일러
에러 메시지를 확인할 수는 없었습니다. 대신 코드를 직접 추적해 두 가지를 짚었습니다.

**원인 ① — 세로 가운데 정렬과 밑줄 계산의 좌표계가 어긋남 (진짜 문제)**

2-A에서 `sizeThatFits`가 `fitting.height + underlineGap + 2`를 반환하도록 늘렸습니다.
그런데 `UILabel`의 기본 `drawText(in:)`는 **주어진 rect가 글자에 필요한 실제 높이보다
크면, 그 안에서 글자를 세로 가운데 정렬**합니다. 즉 늘어난 만큼 글자가 아래로
밀려서 그려지는데, 밑줄 위치를 계산하는 `NSTextContainer(size: bounds.size)`는 이
사실을 모르고 **항상 위쪽 정렬**을 가정합니다. 그 결과 글자는 가운데로, 밑줄은
"위쪽 정렬 기준" 자리로 — 서로 다른 좌표계를 보게 됩니다.

**원인 ② — 클로저 안에서 `self.` 없이 프로퍼티를 참조 (확인은 못 했지만 방어적으로 정리)**

`enumerateLineFragments(forGlyphRange:using:)`의 클로저가 escaping인지 non-escaping인지
문서로 확정하지 못했습니다. escaping이라면 `bounds`·`underlineGap`처럼 `self`의
프로퍼티를 클로저 안에서 그냥 쓸 때 `self.`를 요구할 수 있습니다. 확정할 수 없으니
**클로저 밖에서 지역 상수로 미리 꺼내 두는 것**으로 두 경우 모두를 피해 갑니다.

**고치는 법 — `drawText(in:)`를 다시 씀:**

```swift
override func drawText(in rect: CGRect) {
    let textHeight = super.sizeThatFits(CGSize(width: rect.width, height: .greatestFiniteMagnitude)).height
    let topAlignedRect = CGRect(x: rect.minX, y: rect.minY, width: rect.width, height: textHeight)
    super.drawText(in: topAlignedRect)

    guard let attributedText, !underlines.isEmpty else { return }
    guard let context = UIGraphicsGetCurrentContext() else { return }

    let textStorage = NSTextStorage(attributedString: attributedText)
    let layoutManager = NSLayoutManager()
    textStorage.addLayoutManager(layoutManager)
    let textContainer = NSTextContainer(size: topAlignedRect.size)
    textContainer.lineFragmentPadding = 0
    textContainer.maximumNumberOfLines = numberOfLines
    layoutManager.addTextContainer(textContainer)

    let originX = topAlignedRect.minX
    let originY = topAlignedRect.minY
    let gap = underlineGap
    let thickness = underlineThickness

    for underline in underlines {
        let glyphRange = layoutManager.glyphRange(forCharacterRange: underline.range, actualCharacterRange: nil)
        layoutManager.enumerateLineFragments(forGlyphRange: glyphRange) { _, _, container, effectiveGlyphRange, _ in
            let intersection = NSIntersectionRange(glyphRange, effectiveGlyphRange)
            guard intersection.length > 0 else { return }

            let box = layoutManager.boundingRect(forGlyphRange: intersection, in: container)
            let y = originY + box.maxY + gap
            let startX = originX + box.minX
            let endX = originX + box.maxX
            // ... (선 그리는 부분은 그대로)
        }
    }
}
```

**바뀐 것 — 세 가지:**

| | 전 | 후 |
|---|---|---|
| `super.drawText`에 넘기는 사각형 | 받은 `rect` 그대로 (가운데 정렬 위험) | `textHeight`만큼만 위쪽에 붙인 `topAlignedRect` |
| 밑줄 계산용 `NSTextContainer` 크기 | `bounds.size` (늘어난 값) | `topAlignedRect.size` (글자 실제 높이) |
| 클로저 안 프로퍼티 참조 | `bounds`·`underlineGap`을 클로저 안에서 직접 | `originX`/`originY`/`gap`/`thickness`로 미리 지역 상수화 + 클로저 자신의 `container` 매개변수 사용 |

**검증 상태** — 위 스위프트 코드를 손으로 타입까지 추적했고(각 API의 매개변수 순서·
타입 확인, 중괄호/괄호/대괄호 균형 확인) 구조적으로는 문제가 없습니다. 하지만 이
환경에는 Xcode 툴체인이 없어 **실제로 컴파일해 본 것은 아닙니다.** Xcode에서 빌드해
사용자가 봤던 에러/경고가 사라졌는지, 그리고 글자와 밑줄이 실제로 같은 자리에
그려지는지 확인이 필요합니다.

> ⚠️ 실기기에서 간격을 실측해 보니 또 두 가지가 걸렸습니다 — 아래 **2-C**를 봐 주세요.

---

## 2-C. `underlineGap`의 0이 원하는 자리가 아니었고, 음수를 주면 형광펜이 잘렸다

**증상 ①** — `underlineGap`을 `-10`으로 주면 정확히 원하는 자리(받침 바로 아래)에
밑줄이 그려지는데, 의미상 이건 `0`이어야 할 자리다.

**증상 ②** — `underlineGap`을 `-10`처럼 음수로 크게 주면, 밑줄만 올라가는 게 아니라
**형광펜(`marker`, 연노랑) 배경의 아래쪽이 잘려 나간다.**

**원인 ①** — `NSLayoutManager.boundingRect(forGlyphRange:in:)`가 돌려주는 사각형은
글자가 놓인 **줄 한 칸 전체**(line fragment)다. `KoreanText`는 문단에
`lineSpacing = 8`을 주고 있는데, 이 줄 간격은 각 줄의 line fragment 아래쪽에
포함되어 있어서 `box.maxY`가 실제 글자(받침 포함) 아래쪽보다 이미 8~10pt 정도 더
아래에 있습니다. 그래서 `underlineGap = 0`을 줘도 밑줄이 그만큼 아래로 처져 보였고,
`-10`을 줘야 그 여분이 상쇄되어 원하는 자리가 나온 것입니다. TextKit은 이 여분을
공개 API로 알려주지 않아서, 실측(0일 때와 -10일 때의 차이)으로 값을 정했습니다.

**원인 ②** — `KoreanText.sizeThatFits(...)`가 라벨의 전체 높이를
`fitting.height + (underlineGap + 2)`로 계산하고 있었습니다. `underlineGap`이
`-10`처럼 크게 음수면 이 합이 **자연스러운 글자 높이(`fitting.height`)보다 작아져
버립니다.** SwiftUI는 이 값을 그대로 믿고 라벨에 그만큼 작은 자리를 주기 때문에,
그 화면 안에 다 못 들어간 아래쪽 — 마지막 줄의 형광펜 배경이나 글자 자체 — 가
잘려 나갑니다. **밑줄 간격 조절이 형광펜과 전혀 상관없는 값(라벨 전체 높이)에
직접 영향을 준 것**이 진짜 문제였습니다.

**고치는 법**

1. `UnderlineLabel`에 보정 상수 `lineFragmentPadding: CGFloat = 10`을 추가하고,
   밑줄 Y좌표 계산에서 `box.maxY`를 쓰기 전에 이만큼 먼저 뺍니다.
   ```swift
   let y = originY + box.maxY - lineFragmentPadding + gap
   ```
   이제 `underlineGap = 0`이 바로 "받침 바로 아래" 자리입니다.
2. `sizeThatFits`가 반환하는 높이가 **자연스러운 높이보다 작아지지 않게** `max(0, …)`로
   막습니다.
   ```swift
   let underlineAllowance = uiView.underlines.isEmpty
       ? 0
       : max(0, uiView.underlineGap - uiView.lineFragmentPadding + uiView.underlineThickness / 2 + 2)
   ```
   `underlineGap`을 얼마로 조절하든(음수 포함) 라벨은 절대 원래 필요한 높이보다
   작아지지 않으므로, 형광펜 배경도 글자도 다시는 잘리지 않습니다.

**바뀐 것 — 기본값과 의미:**

| | 전 | 후 |
|---|---|---|
| `underlineGap` 기본값 | 4 → 2 (튜닝 중) | **0** — 이제 0 자체가 "받침 바로 아래" |
| `underlineGap`이 라벨 전체 높이에 미치는 영향 | 그대로 더해져서 음수면 높이가 줄어듦 | `max(0, …)`로 절대 자연스러운 높이 밑으로는 안 내려감 |
| 형광펜(marker) 배경 | 밑줄 간격을 크게 조절하면 잘릴 수 있었음 | 밑줄 간격과 무관하게 항상 안전 |

**검증 상태** — 이번에도 컴파일은 못 해 봤습니다. `lineFragmentPadding = 10`은
사용자가 실기기에서 실측한 값을 그대로 옮긴 것이라 근거가 있지만, 폰트 크기나
줄 간격이 달라지면(예: 접근성 큰 글씨) 이 상수도 같이 안 맞을 수 있습니다 — 그런
경우까지는 검증하지 못했습니다.

---

## 3. 낱말 뜻 모달 (iOS 기본 시트 + Liquid Glass) — 최종

> **디자인 방향이 바뀌었습니다.** 데모처럼 직접 그린 하단 고정 패널 대신, SwiftUI의
> **기본 시트(`.sheet`)** 를 씁니다. iOS 26부터는 `presentationDetents`로 띄운 시트가
> **자동으로 Liquid Glass**(화면 위에 둥둥 뜨는 반투명 유리 느낌)로 그려집니다 —
> 우리가 배경을 직접 그릴 필요가 없다는 뜻이고, 오히려 **`.presentationBackground(...)`를
> 달면 이 자동 효과가 꺼집니다.** 그래서 아래 코드엔 배경 관련 코드가 없습니다.
> (iOS 26 미만 기기에서는 그냥 평범한 흰 시트로 보입니다 — 기능은 똑같이 동작합니다.)
>
> 시트로 바꾸면서 **손잡이(드래그 제스처)·펼침 상태(`isExpanded`)·투명 탭 캐처가 전부
> 필요 없어졌습니다** — iOS가 스스로 드래그·확장·바깥 탭 닫기를 다 해 줍니다. 그만큼
> `GlossaryPanel`이 훨씬 짧아졌습니다.

```swift
//
//  GlossaryPanel.swift
//  KCT
//

import SwiftUI

/// 낱말 뜻을 보여주는 시트 안 내용.
///
/// `.presentationDetents`로 iOS 26의 Liquid Glass 모양을 그대로 받습니다.
/// 손잡이·펼침 상태는 시트 자체가 대신 해 주므로 여기서는 다루지 않습니다.
struct GlossaryPanel: View {
    /// 이 시트를 닫는 시스템 동작. ✕ 버튼이 이것만 부르면 됩니다.
    @Environment(\.dismiss) private var dismiss

    let word: String
    let gloss: String
    /// 6단계에서 채워지는 생성 예문. 기기 미지원이거나 아직 생성 중이면 nil.
    let generatedExample: String?
    /// JSON에 원래 있던 비슷한 낱말 목록. 없으면 빈 배열.
    let relatedWords: [String]

    /// 뜻 아래에 더 보여줄 게 있는지 — 없으면 구분선·문구를 아예 안 그린다.
    private var extra: String? {
        if let generatedExample, !generatedExample.isEmpty { return generatedExample }
        if !relatedWords.isEmpty { return "비슷한 낱말 — " + relatedWords.joined(separator: "・") }
        return nil
    }

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 12) {
                HStack {
                    Text(word).font(.system(size: 14, weight: .heavy)).foregroundStyle(AppColor.signature)
                    Spacer()
                    Button(action: { dismiss() }) {
                        Image(systemName: "xmark.circle.fill")
                            .foregroundStyle(AppColor.textMuted.opacity(0.5))
                    }
                }
                Text(gloss)
                    .font(.system(size: 22, weight: .semibold))

                if let extra {
                    Divider()
                    Text(extra)
                        .font(.system(size: 18, weight: .medium))
                        .foregroundStyle(AppColor.textMuted)
                }
            }
            .padding(20)
            .frame(maxWidth: .infinity, alignment: .leading)
        }
        // 작게 열렸다가, 사용자가 위로 끌면 .medium까지 커진다. 내용이 넘치면 ScrollView가 받아 준다.
        .presentationDetents([.height(180), .medium])
        .presentationDragIndicator(.visible)
    }
}
```

> ⚠️ 이 코드는 최초 버전입니다. 그 뒤 흰 배경 고정·뒤 배경 안 어둡게(2-A~2-C 위쪽 기록,
> `Q&A.md`)에 이어 **글자 크기·생성 예문 표시 방식**까지 바뀌었습니다 — **3-A**를 봐 주세요.

---

## 3-A. 글자를 지문 크기로, 생성 예문은 위로 쓸어 올려야 보이게

**요청** — ① 모달 글씨가 전체적으로 작아 보인다. 질문 지문과 같은 크기였으면 좋겠다.
② Foundation Models가 만든 예문("해설")은 처음부터 보이지 말고, **위로 쓸어 올려야**
나오게 해 달라.

**① 글자 크기** — `QuestionScreen`의 지문(`KoreanText`)이 `.systemFont(ofSize: 30,
weight: .bold)`를 쓰고 있어, 낱말 뜻(`gloss`)도 똑같이 `.system(size: 30, weight:
.bold)`로 맞췄습니다. 낱말 이름표(`word`)는 14 → 18로, 비슷한 낱말·생성 예문 문구는
18 → 20으로 함께 키웠습니다 — 전체적으로 큰 지문 옆에서도 작아 보이지 않게.

**② 생성 예문 감추기** — `extra`(뜻 아래에 보여줄 것 하나로 합쳐 두었던 계산 프로퍼티)를
없애고 둘로 나눴습니다.

- `relatedWordsText` — JSON에 원래 있던 비슷한 낱말 목록. **그대로 바로 보여줍니다.**
  모델이 만든 게 아니라 이미 정해진 정보라 감출 이유가 없습니다.
- `generatedExample` — Foundation Models가 만든 문장. `@State private var
  showsExample`이 `true`가 되기 전까지는, 문장 대신 **"위로 쓸어 올려 예문 보기"**
  안내(`revealExampleHint`)만 보여줍니다.

```swift
private var revealExampleHint: some View {
    HStack {
        Spacer()
        Label("위로 쓸어 올려 예문 보기", systemImage: "chevron.up")
            .font(.system(size: 15, weight: .medium))
            .foregroundStyle(AppColor.textMuted)
        Spacer()
    }
    .padding(.vertical, 6)
    .contentShape(Rectangle())
    .gesture(
        DragGesture(minimumDistance: 12)
            .onEnded { value in
                guard value.translation.height < -12 else { return }
                withAnimation(.easeOut(duration: 0.25)) { showsExample = true }
            }
    )
    .onTapGesture {
        withAnimation(.easeOut(duration: 0.25)) { showsExample = true }
    }
}
```

**판단이 갈리는 지점 — 탭도 함께 받게 한 것.** 이 안내 문구가 `ScrollView` 안에 있어서,
짧은 위로 스와이프가 스크롤 제스처에 먼저 먹힐 가능성이 있습니다(iOS가 두 제스처 중
무엇을 우선할지는 실기기에서 봐야 확실합니다). 그래서 스와이프가 안 먹혀도 예문을 볼
방법이 있도록 **눌러도 똑같이 펼쳐지게** 안전장치를 뎠습니다. 스와이프만 되게 하고
싶으시면 `.onTapGesture` 블록을 지우면 됩니다.

**낱말이 바뀔 때 다시 접기** — `GlossarySelection.id`가 고정값(`"glossary"`, 4-A 기록
참고)이라, 시트가 열린 채로 다른 낱말을 탭해도 `GlossaryPanel` 뷰가 새로 안 만들어지고
`@State`가 그대로 남습니다. 그래서 `.onChange(of: word)`로 낱말이 바뀔 때마다
`showsExample`을 다시 `false`로 되돌렸습니다.

**검증 상태** — 컴파일도, 스와이프가 스크롤과 실제로 어떻게 부딪히는지도 실기기에서
확인 못 했습니다. 예문을 펼쳤을 때 `.height(180)` detent 에서 내용이 넘치면
`ScrollView`가 받아 주긴 하지만, 그럴 땐 `.medium`으로 손잡이를 끌어야 편할 수 있습니다.

> ⚠️ "로딩 중" 문구가 실제로는 안 나오던 버그를 찾아 고쳤습니다 — **3-B**를 봐 주세요.
> 그리고 진단용으로 형광펜을 잠시 꺼 뒀습니다 — **2-D**를 봐 주세요.

---

## 2-D. (진단용, 임시) 형광펜을 잠시 끔

밑줄 관련 문제를 형광펜과 분리해서 보려고, `KoreanText`에 `showsMarkerHighlight`
스위치를 추가하고 `false`로 꺼 뒀습니다.

```swift
private let showsMarkerHighlight = false
// ...
if showsMarkerHighlight, let marker, !marker.isEmpty { ... }
```

`marker` 값 자체(무엇을 칠할지)는 그대로 `QuestionScreen`에서 넘어오고 있고, 그리는
부분만 건너뜁니다. 다시 켜려면 `showsMarkerHighlight`를 `true`로 되돌리면 됩니다.
`PROGRESS.md`의 "임시로 꺼 둔 것"에도 적어 뒀습니다 — 다음 세션이 이 파일만 읽어도
지금 형광펜이 꺼져 있다는 걸 알 수 있게.

---

## 2-E. 지문의 진짜 마지막 줄에서만 밑줄이 글자와 겹침

**증상** — 실기기 스크린샷으로 확인: "목숨을"·"기리는"(점선, 사전 낱말) 밑줄은 정상인데,
"현충일"(실선, O/X 강조 밑줄)은 글자와 겹쳐 보입니다. 차이점은 하나 — "현충일"은
그 지문의 **진짜 마지막 줄**에 있고, 나머지 둘은 그 앞 줄들에 있습니다.

**원인** — 2-C에서 넣은 `lineFragmentPadding`(10) 보정은 "모든 줄의 line fragment
아래쪽에 `lineSpacing`만큼 여분이 붙어 있다"는 가정으로 만든 값입니다. 그런데
`lineSpacing`은 **문단 스타일상 "다음 줄과의 사이"에만 들어가는 값**이라, 뒤에 이어지는
줄이 없는 **진짜 마지막 줄의 line fragment에는 애초에 이 여분이 없습니다.** 그 줄에서도
그대로 10을 빼 버리니, 있지도 않은 여분을 뺀 만큼 밑줄이 위로 더 올라가 글자와
겹친 것입니다. 앞 두 줄은 다음 줄이 있어서 여분이 실제로 있었고, 그래서 정상으로
보였습니다.

**고치는 법** — 밑줄을 그릴 줄마다, 그 줄이 전체 글자의 마지막 줄인지 먼저 확인해서
마지막 줄이면 보정을 안 합니다.

```swift
let isLastLine = effectiveGlyphRange.location + effectiveGlyphRange.length >= layoutManager.numberOfGlyphs
let correction = isLastLine ? 0 : padding
let y = originY + box.maxY - correction + gap
```

`effectiveGlyphRange`는 `enumerateLineFragments`가 주는, 지금 줄 전체의 글자 범위입니다.
그 끝이 `layoutManager.numberOfGlyphs`(전체 글자 수) 이상이면 더 이어지는 줄이 없다는
뜻이므로 마지막 줄로 봅니다.

**검증 상태** — 컴파일도, 실제로 마지막 줄에서 밑줄이 이제 안 겹치는지도 실기기에서
다시 확인해야 합니다. 이 진단은 스크린샷 두 장(점선은 정상, 실선만 겹침)에서 나온
추론이라 논리적으로는 앞뒤가 맞지만, TextKit 내부 동작을 직접 들여다본 것은 아닙니다.

---

## 3-B. "로딩 중" 문구가 안 보이던 버그 — String? 하나로 두 상태를 겹쳐 썼다

**증상** — 예문을 위로 쓸어 올리면, 모델이 문구를 만드는 동안 "모델 문구가 로딩
중입니다"가 나오고 다 되면 실제 예문으로 바뀌어야 하는데, 로딩 문구 자체가 아예
안 나옵니다.

**원인** — `generatedExample: String?` 하나로 "완성된 예문"을 넘기고 있었습니다. 이
타입은 값이 있으면 완성, 없으면(`nil`) 그걸로 끝이라 **"아직 만드는 중"과 "기기가
지원 안 해서 끝내 못 만듦"이 똑같이 `nil`** — 서로 구분할 방법이 없었습니다.
`GlossaryPanel`도 `if let generatedExample`로만 게이트를 걸어서, 값이 생기기 **전에는
스와이프 안내조차 안 보였고**, 값이 생긴 뒤에는 항상 완성된 예문만 그릴 수
있었습니다 — "로딩 중"을 그릴 자리가 코드에 없었던 것입니다.

**고치는 법** — `String?` 대신 상태 셋을 가진 열거형을 새로 만들었습니다.

```swift
enum GlossaryExampleState {
    case loading
    case unavailable   // 기기 미지원 또는 생성 실패
    case ready(String)
}
```

- `QuestionScreen`은 낱말을 탭하는 순간 `exampleState = .loading`으로 시작하고,
  `composeExample(...)` 결과에 따라 `.ready(text)` 또는 `.unavailable`로 바꿉니다
  (애니메이션과 함께 — 이미 펼쳐 본 상태라면 로딩 문구에서 실제 예문으로 부드럽게
  바뀝니다).
- `GlossaryPanel`은 `.unavailable`이면 스와이프 안내 자체를 안 보여주고,
  `.loading`/`.ready`면 안내를 보여주다가 펼치면 각 상태에 맞는 내용(로딩
  스피너+문구, 또는 실제 예문)을 `exampleContent`가 그립니다.

**바뀐 것:**

| | 전 | 후 |
|---|---|---|
| 예문 상태 타입 | `String?` (두 가지 "없음"이 겹침) | `GlossaryExampleState` (loading/unavailable/ready 셋) |
| 로딩 중 화면 | 아무것도 없음(스와이프 안내조차 안 보임) | 안내는 바로 보이고, 펼치면 "모델 문구가 로딩 중입니다" |
| 완성 후 갱신 | — | 이미 펼쳐 놨어도 자동으로 실제 예문으로 바뀜(애니메이션) |

**교훈** — "값이 없다"는 뜻이 하나가 아닐 수 있습니다. `nil`을 "아직 모른다"와
"확인했는데 없다"라는 서로 다른 의미로 같이 쓰면, 그 둘을 다르게 보여줘야 하는 UI는
절대 만들 수 없습니다. 상태가 셋 이상이면 `Optional`을 겹쳐 쓰지 말고 그 수만큼
케이스가 있는 열거형을 쓰는 편이 낫습니다.

**검증 상태** — 컴파일 확인은 못 했습니다. `switch`를 SwiftUI `ViewBuilder` 안에서
바로 쓰는 것(iOS 16+ 지원) 자체는 이 프로젝트가 이미 iOS 26을 타깃으로 하므로
문제없을 것으로 보이지만, 실기기 빌드로 확인이 필요합니다.

---

## 3-C. 안내를 쓸어 올려도 시트 크기는 그대로였다 — detent를 직접 바꾸게 함

**증상** — 안내 문구를 위로 쓸어 올리면 내용은 바뀌는데, **시트(모달) 자체는 커지지
않고 같은 크기 안에서 그냥 표시**됩니다. 원하는 동작은 "쓸어 올리면 모달이 실제로
펼쳐지고, 그 커진 자리에 해설(로딩 중이면 안내, 끝나면 실제 해설)이 나오는 것"입니다.

**원인** — `revealExampleHint`의 제스처가 하던 일은 `showsExample = true`뿐이었습니다.
`.presentationDetents([.height(180), .medium])`는 **어떤 크기를 쓸지 바깥에서 정할
방법이 없는 형태**라, 시트 크기는 오직 사용자가 손잡이를 직접 끌 때만 바뀌었습니다.
콘텐츠는 안에서 바뀌어도 시트 틀은 그대로였던 이유입니다.

**고치는 법** — `.presentationDetents(_:selection:)`로 지금 크기를 `@State`에
묶고, 안내를 쓸어 올리는 제스처가 그 값을 직접 `.medium`으로 바꾸게 했습니다.

```swift
@State private var selectedDetent: PresentationDetent = .height(180)
...
.presentationDetents([.height(180), .medium], selection: $selectedDetent)
...
private func reveal() {
    withAnimation(.easeOut(duration: 0.3)) {
        selectedDetent = .medium
        showsExample = true
    }
}
```

이제 "모달이 펼쳐지는 것"과 "안에 해설이 나오는 것"이 `reveal()` 한 곳에서 같이
일어납니다. 사용자가 손잡이를 직접 끌어도 여전히 되고, 낱말이 바뀌면(`onChange(of:
word)`) `showsExample`과 함께 `selectedDetent`도 `.height(180)`으로 되돌립니다.

**문구도 정확히 맞춤** — 안내는 "쓸어 올려서 해설을 확인하세요.", 로딩 중엔 "해설
준비 중입니다."로 사용자가 준 문구 그대로 바꿨습니다.

**판단이 갈리는 지점** — 낱말이 바뀔 때 `selectedDetent`도 `.height(180)`으로
되돌리는 것은 요청에 없던 부분입니다. `showsExample`을 되돌리는 것과 짝을 맞추는
게 자연스럽다고 보고 그렇게 했는데, "이전 낱말에서 이미 펼쳐 봤으면 다음 낱말도
계속 크게 유지"를 원하시면 이 두 줄 중 `selectedDetent = .height(180)`만 빼면 됩니다.

**검증 상태** — 컴파일도, 실기기에서 스와이프가 스크롤 제스처와 안 부딪히고 시트가
실제로 부드럽게 커지는지도 확인 못 했습니다.

> ⚠️ 실기기에서 안내 자체가 아예 안 보이는 버그를 하나 더 찾았습니다 — **3-D**를 봐 주세요.

---

## 3-D. 낱말을 클릭해도 스와이프 안내가 아예 안 보임 — .unavailable 이 통째로 숨기고 있었다

**증상** — 낱말을 탭했는데 "쓸어 올려서 해설을 확인하세요." 안내 자체가 나오지 않습니다.

**원인** — `GlossaryPanel.body`가 `switch exampleState { case .unavailable: EmptyView()
... }`로 짜여 있어서, **`exampleState`가 `.unavailable`이면 안내를 포함한 구획 전체가
사라졌습니다.** 그런데 `composeExample(...)`은 기기가 Foundation Models 를 지원 안
하면(`SystemLanguageModel.default.availability != .available`) **모델을 부르지도
않고 곧바로 `nil`을 돌려줍니다** — 즉 `.loading`에서 `.unavailable`로 사실상
즉시(거의 한 프레임 안에) 넘어갑니다. 시트가 화면에 자리 잡기도 전에 이미
`.unavailable`이 되어 있었을 가능성이 높고, 그러면 안내가 뜰 틈도 없이 사라진
것처럼 보입니다. (지원하는 기기라도 모델이 준비되지 않은 시점엔 같은 경로를 탑니다.)

**고치는 법** — 안내를 숨기는 조건에서 `.unavailable`을 뺐습니다. 이제 `exampleState`
가 무엇이든 **안내는 항상 먼저 보이고**, 사용자가 실제로 펼쳤을 때만
`exampleContent`가 그 상태에 맞는 내용을 보여줍니다 — 완성된 해설, "해설 준비
중입니다.", 또는(지원 안 하는 기기라면) "이 기기에서는 해설을 만들 수 없어요."

```swift
Divider()
if showsExample {
    exampleContent
} else {
    revealExampleHint
}
```

`exampleContent`의 `.unavailable` case 도 `EmptyView()` 대신 안내 문구로 바꿨습니다.

**바뀐 것:**

| | 전 | 후 |
|---|---|---|
| `.unavailable`일 때 안내(쓸어 올리기) | 통째로 숨김 | 그대로 보여줌 |
| `.unavailable`일 때 펼친 뒤 내용 | (안내 자체가 없어서 볼 수 없었음) | "이 기기에서는 해설을 만들 수 없어요." |

**교훈** — "값이 없다"를 화면에서 완전히 숨기는 선택은, 그 상태로 넘어가는 속도가
빠를수록(여기서는 거의 즉시) **사용자에게는 "원래 없던 기능"처럼 보입니다.** 기능이
있다는 신호(안내)는 실패 상태와 무관하게 먼저 보여주고, 실패는 그 신호를 눌렀을 때
설명하는 편이 — "왜 안 보이지"라는 혼란을 줄입니다.

**검증 상태** — 컴파일 확인은 못 했습니다. 사용자의 실기기가 Foundation Models 를
지원하는지/Apple Intelligence 가 켜져 있는지는 직접 확인할 방법이 없어서, 이번
증상이 정말 "기기 미지원 → 즉시 unavailable" 경로였는지는 추정입니다 — 고쳐도 안내가
안 보이면 다른 원인(예: `SystemLanguageModel` 예외, `composeExample`이 아예 안
불림)일 수 있습니다.

> ⚠️ 실기기 확인 결과 이 추정은 틀렸습니다 — 안내는 나오지만 **스와이프 자체**가 안
> 먹혔습니다. 실제 원인과 수정은 **3-E**에 있습니다.

---

## 3-E. 안내는 보이는데 쓸어 올려도 안 펼쳐짐 — ScrollView 가 제스처를 먼저 가져감

**증상** — 3-D를 고친 뒤 안내("쓸어 올려서 해설을 확인하세요.")는 이제 보입니다.
그런데 그 위에서 쓸어 올려도 아무 일도 안 일어나고, **문구를 그냥 누르면(탭)** 펼쳐집니다.

**원인** — `revealExampleHint`는 `GlossaryPanel.body`의 `ScrollView` 안에 있습니다.
`.gesture(DragGesture(...))`로만 제스처를 달면, **조상 뷰(ScrollView)가 세로 드래그의
우선권을 먼저 가져가는 것이 SwiftUI의 기본 동작**입니다. 그래서 안내 위에서 위로
쓸어 올리는 손짓은 우리가 만든 `DragGesture`가 아니라 **ScrollView 자신의 스크롤**로
흡수돼 버렸습니다. `.onTapGesture`(탭)는 스크롤과 겹칠 일이 없는 별개의 제스처라
원래부터 잘 됐던 것이고요.

**고치는 법** — `.gesture(...)`를 `.highPriorityGesture(...)`로 바꿨습니다. 이러면
이 작은 안내 영역에서 시작된 드래그는 **ScrollView보다 우리 제스처가 먼저** 가져갑니다.

```swift
.highPriorityGesture(
    DragGesture(minimumDistance: 12)
        .onEnded { value in
            guard value.translation.height < -12 else { return }
            reveal()
        }
)
```

탭 안전장치는 그대로 뒀습니다 — `highPriorityGesture`로도 혹시 기기·iOS 버전에 따라
스와이프가 여전히 애매하게 인식될 가능성이 있어서입니다.

**판단이 갈리는 지점** — `.highPriorityGesture`는 이 안내 영역 위에서 시작되는
드래그라면 **방향에 상관없이**(아래로 끌어도) 우리 제스처가 먼저 가져가고, 그 안에서
`guard value.translation.height < -12`로 위쪽만 걸러냅니다. 즉 이 좁은 영역 안에서는
아래로 끄는 스크롤도 살짝 막힐 수 있는데, 그 영역이 시트 맨 아래 작은 줄 하나뿐이라
실사용에 지장은 없을 것으로 보입니다.

**교훈** — `ScrollView` 안에 있는 자식 뷰에 별도의 드래그 제스처를 달 때는
`.gesture(...)`가 아니라 `.highPriorityGesture(...)`(또는 필요에 따라
`.simultaneousGesture(...)`)를 써야 한다 — 그냥 `.gesture`는 조상의 스크롤 제스처에
거의 항상 밀린다. 그리고 **탭처럼 스크롤과 절대 안 겹치는 제스처를 안전장치로 같이
두면, 새로 만든 제스처가 실기기에서 예상과 다르게 동작해도 기능 자체는 살아 있다** —
이번에 탭 안전장치 덕분에 "완전히 안 됨"이 아니라 "스와이프만 안 됨"으로 증상이
좁혀졌습니다.

**검증 상태** — 컴파일도, `highPriorityGesture`로 바꾼 뒤 스와이프가 실제로 스크롤과
안 부딪히고 매끄럽게 인식되는지도 실기기 확인이 필요합니다.

> ⚠️ 안내 문구를 탭하는 경로만 고쳐져 있었습니다 — 손잡이로 끌거나 다른 데를 쓸어
> 올려도 해설이 안 나오는 문제가 남아 있었습니다. **3-F**에서 이 세 경로를 하나로
> 묶었습니다.

---

## 3-F. 손잡이로 끌거나 다른 곳을 쓸어 올려도 해설이 안 나옴 — "커진 결과"를 봐야 했다

**증상** — 안내 문구를 탭하면 모달이 펼쳐지며 해설이 잘 나옵니다. 그런데 **시트 위쪽
손잡이를 직접 끌어서** `.medium`으로 키우거나, **안내 문구가 아닌 다른 빈 자리를
쓸어 올려도** 해설이 안 나옵니다. 지금은 문구를 직접 터치(탭/스와이프)하는 경로에만
반응이 걸려 있었습니다.

**원인** — 3-E까지의 코드는 `revealExampleHint`에 달아 둔 제스처(탭, 그 위에서의
스와이프)가 **직접** `showsExample = true`와 `selectedDetent = .medium`을 같이
호출하는 구조였습니다. 그런데 시트가 `.medium`으로 커지는 경로는 그것 말고도 둘 더
있습니다 — 시스템 손잡이를 직접 끄는 것, 그리고 (스크롤할 내용이 없을 때) 시트
아무 데서나 위로 쓸어 올리는 것. 이 둘은 **iOS가 알아서 처리하는 제스처**라 우리
코드를 전혀 거치지 않고 `selectedDetent`만 `.medium`으로 바뀝니다. `showsExample`을
그 경로들에서 켜 주는 코드가 없었으니 안 나온 것입니다.

**고치는 법** — "무엇이 시트를 키웠는가"를 하나하나 처리하는 대신, **"시트가
커진 결과"인 `selectedDetent` 하나만 지켜보게** 바꿨습니다.

```swift
.onChange(of: selectedDetent) { _, newValue in
    guard newValue == .medium else { return }
    withAnimation(.easeOut(duration: 0.25)) { showsExample = true }
}
```

`reveal()`(안내 탭/스와이프가 부르는 함수)은 이제 `selectedDetent = .medium`만
하고, 실제로 해설을 펴는 일은 이 `onChange` 하나가 도맡습니다. 그러면 손잡이로
끌든, 빈 자리를 쓸든, 안내를 탭하든 — **어느 경로로 `.medium`이 되든 똑같이**
해설이 나옵니다.

**바뀐 것:**

| | 전 | 후 |
|---|---|---|
| 해설을 펴는 조건 | `revealExampleHint`의 제스처가 직접 호출 | `selectedDetent == .medium`이 될 때(원인 무관) |
| 손잡이로 직접 끌어서 키운 경우 | 해설 안 나옴 | 해설 나옴 |
| 안내 문구가 아닌 다른 곳을 쓸어 올린 경우 | 해설 안 나옴 | 해설 나옴(스크롤할 게 없어 iOS 가 시트를 키우는 경우) |

**교훈** — 사용자가 어떤 결과("모달이 커졌다")에 다다르는 경로가 여러 개일 수 있을 때
("손잡이로 끈다", "빈 데를 쓴다", "버튼을 누른다"), 그 경로마다 똑같은 후속 동작을
심는 것보다 **그 경로들이 공통으로 만들어내는 상태 하나("커졌다")를 지켜보고 거기서
후속 동작을 한 번만 심는 편**이 낫다. 특히 이번처럼 그중 일부 경로가 시스템이 대신
처리해서 우리 코드가 아예 안 불리는 경우엔, 상태를 지켜보는 방법이 유일한 답이다.

**검증 상태** — 컴파일도, 세 경로(손잡이, 빈 자리 스와이프, 안내 탭/스와이프) 전부
실기기에서 다시 확인이 필요합니다.

> ⚠️ 기능은 다 됐지만 스타일이 남아 있었습니다 — 낱말 이름표가 뜻(gloss)보다 작았고,
> 안내 문구도 작고 구분선에 바짝 붙어 있었습니다. **3-G**에서 폰트 크기를 맞추고
> 안내 문구 위치를 내렸습니다.

---

## 3-G. 낱말 이름표를 뜻과 같은 크기로, 안내 문구는 더 아래로

**요청** — "쓸어 올려서 해설을 확인하세요. 문장을 조금 더 밑으로 내려서 정렬해주고
폰트를 조금 키워줘. 낱말도 폰트를 glossary와 맞춰줘."

**바뀐 것**

| 요소 | 전 | 후 |
|---|---|---|
| 낱말 이름표(`word`) 폰트 | `18pt heavy` | `30pt bold` (`gloss`와 완전히 동일) |
| 안내 문구 폰트 | `15pt medium` | `18pt medium` |
| 안내 문구 위/아래 여백 | `.padding(.vertical, 6)` (양쪽 동일) | `.padding(.top, 16)` + `.padding(.bottom, 8)` (위를 더 넉넉히 둬 아래로 내려와 보이게) |

낱말 이름표는 색만 시그니처 컬러(`AppColor.signature`)로 구분하고, 크기·굵기는
`gloss`와 정확히 맞췄습니다.

**검증 상태** — 컴파일 확인은 이 환경에서 못 합니다(구조적 중괄호/괄호 균형만
확인). 빌드해서 비율이 실제로 원하시는 대로 보이는지 확인이 필요합니다.

> ⚠️ 낱말 이름표를 30pt로 키우면서 옆 ✕ 닫기 버튼 아이콘 크기는 그대로 뒀더니
> 상대적으로 작아 보일 수 있었습니다. **3-H**에서 이 버튼을 아예 없앴습니다 —
> 마침 배경을 탭해도 닫히지 않는다는 별도 요청과 맞물렸습니다.

---

## 3-H. 닫는 ✕ 버튼을 없애고, 배경을 탭하거나 다른 행동을 하면 저절로 닫히게

**요청** — "X 표시는 없애줘도 될것 같아. 그리고 지금은 배경을 터치해도 모달이
안사라지는데, 낱말이 아니고 다른 기능이다 — 답을 고르거나 다시 읽기를 누르거나
바탕화면을 터치하거나 하면 모달을 닫게 해줬으면 해."

**전** — `GlossaryPanel`은 `@Environment(\.dismiss)`를 받아 ✕ 버튼이 눌리면 스스로
닫았습니다. 시트는 `.presentationBackgroundInteraction(.enabled)`라 뒤 배경(지문·
보기·다시 읽기 버튼)을 탭할 수는 있었지만, 그 탭이 시트를 닫지는 않았습니다 —
"뜻만 확인하고 뒤도 계속 보게" 하려던 원래 의도였는데, 실제로 써 보니 배경을
만지면 낱말 볼 일이 끝났다는 뜻이라 오히려 안 닫히는 쪽이 불편했습니다.

**바뀐 것**

1. **`GlossaryPanel.swift`** — ✕ 버튼과 `@Environment(\.dismiss)`를 지웠습니다.
   이제 낱말 이름표(`Text(word)`) 하나만 남습니다. 시트를 닫는 책임은 전부
   `QuestionScreen`으로 옮겼습니다 — 시트 내용(`GlossaryPanel`)은 자기 자신을
   닫을 방법이 없고, 부르는 쪽(`QuestionScreen`)이 `glossarySelection`을 `nil`로
   되돌려야 닫히기 때문입니다.
2. **`QuestionScreen.swift`** — `dismissGlossaryIfNeeded()`를 새로 만들어 아래
   세 곳에서 부릅니다.
   - 답을 고를 때: `content(for:)`에 `.onChange(of: session.userAnswer)`를 달아,
     보기를 고르거나 O/X를 누르면 자동으로 닫습니다.
   - "다시 읽기"를 누를 때: 그 버튼의 `action`에서 `onReadAloud()`를 부르기
     전에 먼저 닫습니다.
   - 배경(지문·여백)을 탭할 때: `content(for:)` 전체에 `.contentShape(Rectangle())`
     로 빈 여백까지 탭 판정 범위를 넓히고 `.onTapGesture`를 달았습니다. 보기
     버튼·텍스트필드처럼 자기 탭을 이미 가진 자식 뷰는 원래 자기가 먼저
     가져가므로 이 배경 제스처와 부딪히지 않습니다.

**검증 상태** — 컴파일 확인은 이 환경에서 못 합니다. 실기기에서 ✕ 버튼이 잘
없어졌는지, 그리고 답 고르기·다시 읽기·배경 탭 세 가지 전부 시트를 닫는지
확인이 필요합니다.

> ⚠️ 배경 탭을 감지하는 자리가 하필 지문(`KoreanText`)을 감싸고 있어서, **같은
> 손가락 탭 한 번**이 "낱말을 탭해서 사전을 여는 것"과 "배경을 탭해서 사전을
> 닫는 것" 둘 다로 잡힐 위험이 있었습니다. 실기기에 올리기 전에 **3-I**에서
> 먼저 손을 봤습니다.

---

## 3-I. (사전 대비) 낱말을 탭해서 여는 순간, 배경 탭 감지가 같이 걸려 바로 닫힐 뻔함

이 항목은 사용자가 겪은 버그가 아니라, 3-H를 만들면서 **코드만 보고 미리 짚어낸**
위험입니다. 실기기 확인 전에 고쳐 두는 편이 낫다고 판단해 먼저 손을 봤습니다.

**문제가 될 뻔한 이유** — `KoreanText`(지문)의 낱말 탭은 `UIViewRepresentable`
안에서 `UILabel.addGestureRecognizer(UITapGestureRecognizer(...))`로, **UIKit
제스처를 직접** 답니다. 3-H에서 배경 탭 감지는 그 지문을 감싸는 조상 뷰에
**SwiftUI `.onTapGesture`**로 달았습니다. 이 둘은 같은 뷰 계층 안의 서로 다른
`UITapGestureRecognizer`인데, 둘 사이에 "하나가 실패해야 다른 하나가 인식된다"는
관계를 따로 걸어 두지 않으면 — **iOS는 어느 쪽이 먼저 인식될지 보장하지
않습니다.** 만약 배경 쪽이 낱말 탭 직후(또는 그 사이)에 처리되면, 방금 낱말을
탭해서 새로 연 시트를 배경 탭 처리가 그 자리에서 바로 닫아 버릴 수 있습니다 —
"낱말을 눌러도 사전이 안 열린다(사실은 열렸다가 바로 닫힘)"는 버그로 보일
것입니다.

**고치는 법** — 어느 제스처가 먼저 실행되는지에 기대지 않고, **한 틱 뒤에 다시
확인하는 방식**으로 경합 자체를 없앴습니다.

```swift
@State private var glossaryOpenToken = 0   // 낱말을 탭할 때마다 +1

// 낱말 탭 — 먼저 "낱말 탭이 있었다"는 표시를 남긴다
onTapWord: { word, gloss, examples in
    glossaryOpenToken += 1
    ...
}

// 배경 탭 — 바로 닫지 않고, 다음 실행 루프 틱에 토큰이 그대로인지 보고 판단한다
private func dismissGlossaryFromBackgroundTap() {
    guard glossarySelection != nil else { return }
    let tokenAtTap = glossaryOpenToken
    DispatchQueue.main.async {
        guard glossaryOpenToken == tokenAtTap else { return }   // 그 사이 낱말 탭이 있었다 → 닫지 않는다
        dismissGlossaryIfNeeded()
    }
}
```

같은 탭에서 낱말 탭도 같이 걸렸다면, `DispatchQueue.main.async`가 실행되는
시점엔 이미 `glossaryOpenToken`이 올라가 있어 토큰이 달라지고, 배경 탭 쪽은
아무 일도 하지 않습니다. 순수 배경 탭이었다면 토큰이 그대로라 정상적으로
닫힙니다. "답을 고르거나 다시 읽기를 누르면 닫는다"는 각각 자기 버튼의
탭이라 이 경합이 없어서, 그 둘은 `dismissGlossaryIfNeeded()`를 그대로
바로 부릅니다 — `dismissGlossaryFromBackgroundTap()`을 거치는 건 배경 탭
하나뿐입니다.

**검증 상태** — 이 시나리오 자체가 코드 검토로 예상한 것이라, 실기기에서
"낱말을 탭했을 때 사전이 열리자마자 다시 닫히지 않는지"를 꼭 확인해 주세요.
만약 그래도 그런 증상이 보이면 토큰 방식 대신 제스처 delegate로 명시적인
우선순위를 거는 방법으로 바꿔야 합니다.

---

## 3-J. 비슷한 낱말 표시 위치를 낱말칸 오른쪽으로

**요청** — "하단 모달에서 비슷한 낱말의 표시 위치를 낱말칸 오른쪽으로 옮겨줄래?"

**전** — 뜻(gloss) 아래, 구분선과 함께 따로 한 줄을 차지했습니다.

**1차 시도(수정됨)** — 낱말 이름표와 같은 줄에 두되, `Spacer`를 낱말과 비슷한
낱말 "사이"에 넣어 비슷한 낱말이 **모달 오른쪽 끝**으로 밀려났습니다. "낱말칸
오른쪽"이라는 요청과는 달리 너무 멀리 떨어져 보였습니다.

> ⚠️ "오른쪽에 둔다"를 "행의 오른쪽 끝에 둔다"로 잘못 읽었습니다. 요청은 **낱말
> 바로 옆(오른쪽)**을 뜻했습니다 — 바로 다음 수정에서 고쳤습니다.

**후(고침)** — `Spacer`를 낱말과 비슷한 낱말 **사이가 아니라 그 뒤**로 옮겼습니다.
그러면 둘이 작은 간격(8pt)만 두고 붙어서 왼쪽에 함께 있고, 남는 공간만 오른쪽으로
밀립니다 — "낱말칸 바로 오른쪽"이 됩니다.

```swift
HStack(alignment: .firstTextBaseline, spacing: 8) {
    Text(word)...
    if let relatedWordsText {
        Text(relatedWordsText)...
    }
    Spacer(minLength: 0)   // 남는 공간만 여기서 흡수 — 둘을 서로 떼어놓지 않는다
}
```

**검증 상태** — 컴파일 확인은 못 합니다. 비슷한 낱말 목록이 길 때(예: 서너 개
이상) 낱말칸과 겹치거나 줄바꿈이 어색하지 않은지 실기기에서 확인이 필요합니다.

---

## 3-K. 시트 손잡이를 시스템 알약 대신 넓적한 "^" 모양으로

**요청** — "하단 모달에 위에 손잡이처럼 끌고 올리는 부분의 모양을 ^ 이 문자가
넓어진 모양으로 바꿔줄 수 있어?"

**원리** — `.presentationDragIndicator(...)`은 보이는 모양(알약 하나)만 정하는
스위치이지, 손잡이 자체를 우리가 그릴 수 있게 해 주지는 않습니다. 원하는 모양을
쓰려면 시스템 손잡이는 `.hidden`으로 숨기고, 그 자리에 우리가 원하는 모양을
직접 그려 얹어야 합니다.

**바뀐 것**

1. `WideChevronShape`(파일 맨 위, `GlossaryExampleState` 바로 아래)를 새로
   만들었습니다 — `Path`로 왼쪽 아래 → 가운데 위 → 오른쪽 아래, 세 점만 이어
   "^"를 그립니다. 폭과 높이는 그리는 쪽(`dragHandle`)의 `frame`이 정하므로,
   `frame(width: 56, height: 10)`처럼 폭에 비해 높이를 낮게 주면 "옆으로
   넓어진 ^"가 됩니다.
2. `dragHandle`(새 computed view)이 이 모양을 `.stroke(...)`로 그려 `ScrollView`
   바로 위, 시트 맨 위쪽에 둡니다. 이 때문에 시트 몸 전체를 `VStack(spacing: 0)
   { dragHandle; ScrollView { ... } }`로 한 겹 더 감쌌습니다 — 손잡이가
   스크롤을 따라 같이 움직이면 안 되니 `ScrollView` 밖에 있어야 합니다.
3. `.presentationDragIndicator(.visible)` → `.hidden`으로 바꿨습니다.

**끌어서 크기 바꾸는 동작은 그대로입니다** — 시스템 손잡이를 숨겨도, 시트를
끌어서 `.medium`으로 키우는 기능 자체(3-F에서 `onChange(of: selectedDetent)`로
경로 무관하게 잡아 두던 것)는 손잡이의 "보이는 모양"과 무관하게 계속 동작합니다.
이번 변경은 순전히 그리는 모양만 바꾼 것입니다.

**검증 상태** — 컴파일 확인은 못 합니다. 다음을 실기기에서 확인해 주세요.
- 넓적한 "^" 모양이 원하시던 느낌인지(폭 56 / 높이 10 / 선 굵기 3 로 시작했습니다 —
  더 넓게·굵게 하고 싶으시면 말씀해 주세요)
- 손잡이가 안 보여도 여전히 그 자리를 끌면 시트가 커지는지

---

## 3-L. 안내 문장을 없애고, 접힌 높이를 150으로

**요청** — "쓸어 올려서 해설을 확인하세요를 지워줘 문장을. 또 접혔을 때 하단
모달의 높이를 150으로 해주고."

**바뀐 것**

1. **안내 문장 삭제** — `Label("쓸어 올려서 해설을 확인하세요.", ...)`를
   `Image(systemName: "chevron.up")` 하나로 바꿨습니다. 글자는 없어졌지만
   그 자리(탭·위로 쓸기로 해설을 펴는 영역)와 제스처는 그대로 남겨 뒀습니다 —
   3-K에서 맨 위에 넣은 넓적한 "^" 손잡이가 이미 "여기서 더 나온다"는 힌트를
   주고 있어서, 화살표 아이콘 하나만으로도 최소한의 표시는 남긴 것입니다.
   > ⚠️ **판단 지점** — "문장을 지워달라"는 요청이 아이콘까지 없애라는
   > 뜻인지, 문장만 없애라는 뜻인지 정확히는 모릅니다. 아이콘까지 완전히
   > 없애고 싶으시면 말씀해 주세요.
2. **접힌 높이 150** — 여기저기 흩어져 있던 `.height(180)` 세 곳(초기값 ·
   `.presentationDetents` · 낱말이 바뀔 때 되돌리는 값)을 `private static let
   collapsedHeight: CGFloat = 150` 상수 하나로 합치고 값을 150으로 바꿨습니다.
   앞으로 이 높이를 또 바꿀 일이 있으면 이 상수 하나만 고치면 됩니다.

**검증 상태** — 컴파일 확인은 못 합니다. 접힌 높이 150이 실제로 원하시는
느낌인지, 그리고 문장이 빠진 자리에서도 탭·위로 쓸기가 여전히 잘 되는지
실기기에서 확인해 주세요.

> ⚠️ 여기서 만든 `collapsedHeight` 상수가 바로 다음 항목(**3-M**)의 컴파일
> 에러 원인이 됐습니다 — 실제로 Xcode에서 빌드해서 잡힌 첫 에러입니다.

---

## 3-M. 컴파일 에러 — "Static member 'collapsedHeight' cannot be used on instance of type 'GlossaryPanel'"

**증상** — Xcode에서 빌드하니 `.presentationDetents([.height(collapsedHeight), ...])`
줄에서 정확히 이 에러가 났습니다.

**원인** — `collapsedHeight`는 `private static let`(타입 자체에 딸린 값, 인스턴스가
아니라 `GlossaryPanel`이라는 타입 하나에만 존재)로 선언했습니다. 그런데
`body`(인스턴스 프로퍼티)나 `@State` 기본값, `onChange` 클로저처럼 **인스턴스
문맥**에서는 같은 타입의 static 멤버라도 이름만 써서(`collapsedHeight`) 바로
꺼내 쓸 수 없습니다 — `Self.collapsedHeight`나 `GlossaryPanel.collapsedHeight`처럼
어디 것인지 밝혀 줘야 합니다. static 상수를 새로 만들면서 이 규칙을 놓쳐,
세 군데(`@State` 기본값 · `.presentationDetents` · `onChange` 안) 전부 이름만
써서 에러가 셋 다 났을 것입니다.

**해결** — 세 곳 전부 `Self.collapsedHeight`로 고쳤습니다.

```swift
private static let collapsedHeight: CGFloat = 150

@State private var selectedDetent: PresentationDetent = .height(Self.collapsedHeight)
...
.presentationDetents([.height(Self.collapsedHeight), .medium], selection: $selectedDetent)
...
.onChange(of: word) { _, _ in
    showsExample = false
    selectedDetent = .height(Self.collapsedHeight)
}
```

고치는 김에, 이 근처 주석 하나가 이전 patch 과정에서 깨져 있던 것도 함께
바로잡았습니다 — `.presentationDragIndicator(.hidden)` 위 주석("손잡이 자체는
숨긴다")에 엉뚱하게 코드 한 줄이 섞여 들어가 있었는데, 원래 문장으로 되돌렸습니다.
(이 환경엔 실시간으로 diff를 보여줄 방법이 마땅치 않아, 어쩌다 섞였는지는
정확히 못 짚었습니다 — 다만 실행에는 영향 없는 주석 줄이었습니다.)

**교훈** — 인스턴스 안에서 타입의 `static` 상수를 참조할 땐 이름만으로는 안
되고 `Self.` 를 붙여야 합니다. 반대로 static 멤버의 "선언" 자리나, 다른
static 멤버를 참조하는 자리(예: 다른 static 프로퍼티의 기본값)에서는 이름만
써도 됩니다 — 헷갈리기 쉬우니 새 `static let` 상수를 만들 때마다 이 규칙을
한 번 더 확인하는 편이 낫습니다.

**검증 상태** — 사용자가 실제로 Xcode 빌드 로그에서 확인해 준 에러라, 이
수정으로 그 특정 에러는 없어져야 합니다. 다만 이 파일에 다른 에러가 더
있었는지는 이 세션에서 컴파일해 보지 못해 알 수 없습니다 — 다시 빌드해서
확인해 주세요.

---

## 3-N. 손잡이를 더 두껍고 살짝 더 넓게, 낱말·뜻 글자는 더 작게(그리고 서로 같게)

**요청** — "WideChevronShape 표시 두께를 2배로 해주고 살짝만 더 넓게 해줄래.
그리고 낱말과 그 아래 의미 글자 크기를 조금만 줄여줘 둘의 크기는 같아야해,
크기는 답을 골라보세요의 크기와 같으면 될 것 같아."

**바뀐 것**

| 요소 | 전 | 후 |
|---|---|---|
| 손잡이(`WideChevronShape`) 선 굵기 | `lineWidth: 3` | `lineWidth: 6`(2배) |
| 손잡이 프레임 | `width: 56, height: 10` | `width: 64, height: 16`(살짝 더 넓게, 두꺼워진 선이 안 잘리게 높이도 같이 늘림) |
| 낱말(`word`) · 뜻(`gloss`) 폰트 크기 | `30pt bold`(둘 다) | `21pt bold`(둘 다) |

**둘의 크기가 항상 같아야 한다는 요구를 코드로 강제** — `wordGlossFontSize`라는
`private static let` 상수 하나를 만들어 `word`·`gloss`가 똑같이 이 값을
씁니다. 우연히 같은 숫자를 두 군데 따로 적어 두는 대신, 나중에 둘 중 하나만
바뀌는 실수 자체가 안 나게 만들었습니다. 값은 요청하신 대로 `QuestionScreen`의
행동 안내 문구("답을 골라보세요" 등, `item.actionGuide`)와 같은 21pt로
맞췄습니다.

```swift
private static let wordGlossFontSize: CGFloat = 21
...
Text(word).font(.system(size: Self.wordGlossFontSize, weight: .bold))...
...
Text(gloss).font(.system(size: Self.wordGlossFontSize, weight: .bold))...
```

3-M에서 배운 대로, 이 상수도 `static`이라 인스턴스 문맥에서 쓸 때는 처음부터
`Self.wordGlossFontSize`로 적었습니다.

**같이 정리한 것** — 글자 크기를 질문 지문(30pt)에 맞췄다고 적어 두었던 옛
주석 두 곳이 이제 사실과 달라져서(21pt로 줄었고, 기준도 행동 안내 문구로
바뀌었으므로), 그 내용에 맞게 고쳤습니다.

**검증 상태** — 컴파일 확인은 못 합니다. 손잡이 두께·폭, 그리고 낱말/뜻 글자
크기가 원하시는 만큼인지 실기기에서 확인해 주세요.

---

## 3-O. 손잡이를 더 낮게, 아래쪽 ^ 아이콘은 삭제

**요청** — "WideChevronShape 의 높이를 살짝 낮춰줄래 지금 크기에서 한 80%
정도로 만들어주면 될거 같고. 하단에 ^ 마크는 삭제해주고."

**바뀐 것**

1. **손잡이 높이** — `dragHandle`의 `frame(height:)`를 16 → 13(≈80%)으로
   낮췄습니다. 폭(64)과 선 굵기(6)는 그대로 뒀습니다.
2. **아래쪽 ^ 삭제** — 3-L에서 안내 "문장"만 지우고 남겨 뒀던 `chevron.up`
   아이콘(펼치기 영역, `revealExampleHint`)을 이번엔 완전히 없앴습니다. 그
   자리는 이제 아무것도 그리지 않는 투명한 `Color.clear`이고, 탭·위로 쓸어
   올리는 제스처(`highPriorityGesture`·`onTapGesture`)는 그대로 남아 있어서
   기능은 똑같이 동작합니다. 이걸로 지난 3-L에서 남겨 뒀던 판단 지점("문장만
   지울지 아이콘까지 지울지")이 해소됐습니다 — 아이콘까지 지우는 쪽이었습니다.

**검증 상태** — 컴파일 확인은 못 합니다. 손잡이가 낮아진 비율이 원하시는
느낌인지, 그리고 아이콘 없이도 그 자리를 탭하거나 위로 쓸면 여전히 해설이
펼쳐지는지 실기기에서 확인해 주세요.

---

## 3-P. 손잡이를 얇고 짧고 좁고 옅게

**요청** — "두께를 80% 줄여주고 높이를 10으로 해주고 회색을 약간 더 옅은
회색으로 변경하고 넓이를 75%로 줄여줘."

**바뀐 것**

| 요소 | 전 | 후 |
|---|---|---|
| 선 굵기(`lineWidth`) | `6` | `4.8`(80%로) |
| 높이(`frame height`) | `13` | `10`(요청하신 값 그대로) |
| 폭(`frame width`) | `64` | `48`(75%로) |
| 색(투명도) | `opacity(0.6)` | `opacity(0.4)`(조금 더 옅게) |

> ⚠️ **판단 지점** — "두께를 80% 줄여주고"를 "지금 값의 80%로 만든다"(6→4.8)로
> 읽었습니다. "80%만큼 깎는다"(6→1.2, 20%만 남김)로 읽을 수도 있는 문장이라,
> 뒤에 이어지는 "넓이를 75%로"(명시적으로 "~로")와 같은 뜻으로 통일해
> 해석했습니다. 원하신 게 후자(1.2)라면 말씀해 주세요.

`AppColor`에는 미리 정해진 "옅은 회색" 토큰이 없어서(``textMuted``는 어르신
대비를 위해 일부러 진한 회색으로 잡아 둔 색입니다 — `AppColor.swift` 참고),
투명도를 낮추는 방식으로 더 옅게 보이게 했습니다. 이 손잡이는 읽는 글자가
아니라 장식적인 UI 표시라 대비 원칙과는 무관합니다.

**검증 상태** — 컴파일 확인은 못 합니다. 실기기에서 두께·높이·폭·색 네
가지가 원하시는 만큼인지, 그리고 위 판단 지점("80%"의 뜻)이 제 해석과
맞는지 확인해 주세요.

---

## 3-Q. 낱말칸·설명칸 글자 크기를 선다 보기 버튼과 맞춤

**요청** — "하단 모달 낱말칸과 설명칸의 폰트 크기를 선다 문제의 보기 글자 크기와 맞춰줄래?"

**바뀐 것** — `wordGlossFontSize`가 가리키던 기준을 QuestionScreen 의 행동
안내 문구(21pt)에서 `ChoiceButton`(선다 문제 보기 버튼)의 글자 크기로
바꿨습니다.

| 기준 | 크기 |
|---|---|
| 전 — 행동 안내 문구("답을 골라보세요") | 21pt |
| 후 — 선다 보기 버튼(`ChoiceButton`) 글자 | **22pt** |

이번에도 `wordGlossFontSize` 상수 하나만 고쳤으므로, 낱말·뜻 두 글자
크기는 여전히 항상 같습니다.

**검증 상태** — 컴파일 확인은 못 합니다. 22pt가 보기 글자와 실제로 나란히
놓았을 때 정확히 같은 크기로 보이는지 실기기에서 확인해 주세요.

---

## 3-R. 낱말칸·설명칸 글자 크기를 28pt로 직접 지정

**요청** — "28pt로 변경해줄래?"

**바뀐 것** — 바로 앞(3-Q)에서 선다 보기 버튼(22pt)에 맞췄던 `wordGlossFontSize`를
28pt로 직접 지정했습니다. 이제 특정 다른 요소를 따라가는 값이 아니라
고정값입니다. `word`·`gloss`는 여전히 이 상수 하나를 같이 쓰므로 둘의
크기는 계속 같습니다.

**검증 상태** — 컴파일 확인은 못 합니다. 28pt가 실기기에서 원하시는
크기인지 확인해 주세요.

---

## 3-S. 선택한 낱말의 배경색을 "다시 읽기"와 같게 (열려 있는 동안만)

**요청** — "이제 질문에서 낱말을 선택하면 하단 모달이 올라오잖아? 선택한
낱말 영역의 배경색을 다시 읽기의 배경색으로 변경해줘. 선택되어서 하단
모달이 올라온 낱말만 배경색을 변경할거야. 그리고 하단 모달을 닫으면 글자는
검정색으로 다시 돌아올거야."

**설계** — 지문(`KoreanText`)에 새 속성 `selectedWord`를 추가했습니다. 이
값과 이름이 같은 낱말에만 배경색(`selectedWordColor`, 기본값이 "다시
읽기" 버튼과 같은 `AppColor.secondaryBackground`)을 칠합니다. 이미 있던
`marker`(형광펜) 배경색 적용과 같은 방식 — `NSAttributedString`의
`.backgroundColor` 속성 — 을 그대로 재사용했습니다.

`QuestionScreen`은 이 값을 `glossarySelection?.word`로 그대로 넘깁니다.
그러니:
- 낱말을 탭해 시트가 열리면 → `glossarySelection`이 그 낱말로 채워지고 →
  `selectedWord`도 그 낱말이 되어 배경이 칠해집니다.
- 시트가 닫히면(답 고르기·다시 읽기·배경 탭·아래로 쓸어내리기 등 **어느
  경로로 닫히든** — 전부 `glossarySelection`을 nil로 되돌립니다) →
  `selectedWord`도 자동으로 nil이 되어 배경이 사라집니다.

**"글자는 검정색으로 돌아온다"는 부분은 따로 코드를 안 넣었습니다** — 글자
색은 애초에 계속 검정(`color: UIColor = .black`, 기본값)이었고, 이번에
바꾼 건 배경색뿐입니다. 배경이 사라지면 원래부터 검정이던 글자만 남으므로,
요청하신 "돌아온다"는 결과가 저절로 따라옵니다.

```swift
// KoreanText.swift
var selectedWord: String?
var selectedWordColor: UIColor = UIColor(AppColor.secondaryBackground)
...
if entry.word == selectedWord {
    attributed.addAttribute(.backgroundColor, value: selectedWordColor, range: range)
}
```

```swift
// QuestionScreen.swift — KoreanText 호출부
selectedWord: glossarySelection?.word,
```

> ⚠️ **패치 중 잡은 실수** — 처음엔 `selectedWord:` 인자를 실수로
> `glossary:` 뒤에 적었습니다. `KoreanText`는 프로퍼티 선언 순서가 `marker`
> → `markerColor` → `selectedWord` → `selectedWordColor` → `glossary` →
> `onTapWord`인데, Swift는 이름 붙은 인자라도 **구조체 선언 순서를 따라
> 늘어놓아야** 컴파일됩니다(순서를 건너뛸 수는 있어도 거슬러 올라갈 수는
> 없습니다). `glossary` 뒤에 `selectedWord`를 두면 이 규칙을 어겨 컴파일
> 에러가 났을 것 — 실제 실기기 확인 전에, 코드를 다시 훑다가 발견해
> `selectedWord`를 `glossary` 앞으로 옮겼습니다.

**검증 상태** — 컴파일 확인은 못 합니다. 낱말을 탭했을 때 그 낱말에만
배경이 칠해지는지, 시트를 닫는 네 가지 경로(답 고르기·다시 읽기·배경
탭·아래로 쓸어내리기) 전부에서 배경이 사라지는지 실기기에서 확인해
주세요.

---

## 3-T. 시트를 다시 접으면 펼쳤던 해설도 같이 접기

**요청** — "모달을 펼쳤다가 다시 접으면 Foundation Model로 만든 해설이 남아있는데
접으면 해설이 사라지게 해줘."

**원인** — `showsExample`을 `true`로 만드는 곳은 `onChange(of: selectedDetent)`
안에서 `newValue == .medium`일 때뿐이었습니다. 시트를 다시 `collapsedHeight`로
내렸을 때(`newValue`가 `.medium`이 아닐 때)는 이 `onChange`가 아무 것도 하지
않았으므로, 한 번 `true`가 된 `showsExample`이 계속 `true`로 남아 — 해설이
이미 만들어져 있으면(캐시된 `exampleState`) 다시 펼쳤을 때 스와이프/탭 없이도
바로 보여 버렸습니다.

**해결** — `guard`로 `.medium`일 때만 반응하던 것을, 두 경우를 다 반응하도록
바꿨습니다 — `showsExample`을 `newValue == .medium` 그 자체로 대입합니다.

```swift
.onChange(of: selectedDetent) { _, newValue in
    withAnimation(.easeOut(duration: 0.25)) {
        showsExample = (newValue == .medium)
    }
}
```

이제 `.medium`이 되면 펴고, `collapsedHeight`로 돌아오면 접습니다. `exampleState`
(로딩 중/완성/불가) 자체는 그대로 남아 있으므로 — 다시 펼치면 해설을 새로
만들지 않고 이미 만든 걸 다시 보여줍니다. 사라지는 건 "펼쳐진 상태"뿐이고,
만들어 둔 해설 내용 자체를 버리는 건 아닙니다.

**검증 상태** — 컴파일 확인은 못 합니다. 해설을 펼친 뒤 손잡이를 내리거나
아래로 스와이프해 시트를 접었을 때 해설 구획이 다시 감춰지는지, 그 뒤 다시
펼치면(같은 낱말) 해설이 다시 보이는지(재생성 없이) 실기기에서 확인해 주세요.

---

## 3-U. 세이프티 가드레일에 걸린 프롬프트를 Supabase 로 남기기

**요청** — "Safety guardrails were triggered" 에러를 보고, 어떤 프롬프트가 입력됐길래
걸렸는지 Supabase 로그로 확인하고 싶다는 요청.

**있던 것** — 이 앱에는 이미 같은 목적의 장치가 있었습니다. 오답 해설을 만드는
`CommentaryWriter`가 모델 호출에 실패하면(세이프티 필터 포함) `Writing(text: nil,
failure: ModelFailureDraft(...))`을 돌려주고, `QuizSession`이 그 초안을
`ModelFailure`(SwiftData)로 저장했다가, `ObsUploader.uploadPendingFailures()`가
Supabase `model_failure` 표(`job` · `questionID` · `reason` · `instructions` ·
`prompt` 컬럼)로 올립니다. **낱말 사전의 생성 예문(`GlossaryComposer.composeExample`)만
이 장치를 안 쓰고 있었습니다** — 실패하면 그냥 `nil`을 돌려주고 이유를 버렸습니다.

**한 것** — 새 표나 새 SDK 없이, 있던 장치에 한 곳만 연결했습니다.

1. `composeExample(...)`의 반환 타입을 `String?`에서 `Writing`으로 바꿨습니다.
   기기가 Apple Intelligence 를 지원 안 하면(시도조차 안 하므로 남길 프롬프트가
   없음) `.empty`를, 모델을 실제로 불렀다가 실패하면 `CommentaryWriter`와 똑같이
   `instructions`·`prompt`·`String(describing: error)`를 담은 `ModelFailureDraft`를
   `job: "glossary_example"`로 채워 돌려줍니다. `questionID` 파라미터를 새로 받아
   실패 기록에 어느 문항에서 탭한 낱말인지 남깁니다.
2. `QuestionScreen`의 `onTapWord`가 이 `Writing`을 받아 `writing.text`로 화면
   상태(`exampleState`)를 갱신하고, `writing.failure`를 `session.saveFailures([...])`
   로 넘깁니다.
3. `QuizSession.saveFailures(_:)`가 `private`이라 화면(`QuestionScreen`)에서 못
   불렀으므로, `private`만 뗐습니다 — `modelContext`는 세션만 들고 있으므로 화면이
   직접 넣지 않고 항상 이 메서드를 거치게 한 구조는 그대로 유지했습니다.

```swift
// GlossaryComposer.swift
func composeExample(
    word: String, gloss: String, relatedWords: [String] = [], questionText: String,
    questionID: Int
) async -> Writing {
    guard case .available = SystemLanguageModel.default.availability else {
        return .empty
    }
    ...
    do {
        let session = LanguageModelSession(instructions: instructions)
        let response = try await session.respond(to: prompt, generating: GlossaryExample.self)
        return Writing(text: response.content.example, failure: nil)
    } catch {
        return Writing(text: nil, failure: ModelFailureDraft(
            job: "glossary_example",
            questionID: questionID,
            reason: String(describing: error),
            instructions: instructions,
            prompt: prompt))
    }
}
```

```swift
// QuestionScreen.swift — onTapWord 안
let writing = await composeExample(
    word: word, gloss: gloss, relatedWords: examples,
    questionText: item.displayText, questionID: item.id)
withAnimation(.easeOut(duration: 0.25)) {
    exampleState = writing.text.map(GlossaryExampleState.ready) ?? .unavailable
}
session.saveFailures([writing.failure])
```

**어떻게 확인하나** — 세이프티에 걸린 뒤 다음 회차가 시작되거나 끝나면(`ObsUploader`가
그 시점에 밀어 올립니다) Supabase 대시보드 → `model_failure` 표에서
`job = 'glossary_example'`로 걸러 보면, 그 줄의 `prompt`·`instructions`가 실제로
보낸 글 그대로이고 `reason`에 에러 문구("Safety guardrails were triggered...")가
그대로 남아 있습니다.

**넣지 않은 것** — 에러 문구가 함께 알려 준 `LanguageModelSession.logFeedbackAttachment(sentiment:issues:desiredOutput:)`으로 Apple 자체 피드백 첨부파일을 만들어
feedbackassistant.apple.com 에 신고하는 절차는 넣지 않았습니다. 그건 Apple 에게
이 사례를 알리는 별도 절차(우리 Supabase 로그와 무관)라, 필요하시면 말씀해 주세요.

**검증 상태** — 컴파일 확인은 못 합니다. 실기기에서 세이프티에 걸릴 만한 낱말을
탭해 "이 기기에서는 해설을 만들 수 없어요."가 뜨는지, 그리고 그 뒤 Supabase
`model_failure` 표에 `job = 'glossary_example'` 줄이 실제 프롬프트와 함께
올라오는지 확인해 주세요.

---

## 3-V. 세이프티에 걸리면 애플 Feedback Assistant 첨부 파일도 남기기

**요청** — 3-U 에서 남긴 에러 문구가 "이게 예상 밖이면
`LanguageModelSession.logFeedbackAttachment(sentiment:issues:desiredOutput:)`로
첨부파일을 만들어 feedbackassistant.apple.com 에 신고해 달라"고 알려줬는데,
이 신고용 연결도 넣어 달라는 요청.

**막혔던 것** — 이 API는 iOS 26 FoundationModels 의 아주 최근 API라, 애플 공식
문서 페이지가 자바스크립트로 렌더링돼 있어 웹 검색 도구로는 정확한 타입을 확인하지
못했습니다. 이 환경엔 Xcode 가 없어 컴파일 확인도 안 되는데, 이런 최신 API는
타입 이름(특히 enum 케이스)을 잘못 짚으면 바로 컴파일 에러가 나므로, 짐작만으로
적지 않고 사용자에게 Xcode Quick Help 화면을 직접 봐 달라고 요청했습니다.

**Quick Help 로 확인된 실제 서명** — 스크린샷으로 아래가 확인됐습니다.

```swift
// LanguageModelSession 의 인스턴스 메서드다 (static 아님!)
func logFeedbackAttachment(
    sentiment: LanguageModelFeedback.Sentiment?,
    issues: [LanguageModelFeedback.Issue],
    desiredOutput: Transcript.Entry?
) -> Data
```

예상과 다르게 확인된 것 두 가지 — ① 타입 이름이 `LanguageModelFeedbackAttachment`가
아니라 `LanguageModelFeedback`, ② `static` 메서드가 아니라 **그 세션 인스턴스에서
불러야 하는 인스턴스 메서드**(실패한 바로 그 `session`에서 불러야 함).

**한 것** — `Sentiment`·`Issue` 의 실제 케이스 이름까지는 Quick Help 로도 안 보였지만,
셋 다 비워도 되는 타입(`Sentiment?`→`nil`, `[Issue]`→`[]`, `Transcript.Entry?`→`nil`)이라
케이스 이름을 몰라도 컴파일되는 값으로 채웠습니다 — 첨부파일 안에는 그래도 그 세션이
실제로 주고받은 내용(트랜스크립트)이 담겨 나올 것으로 봅니다.

1. `session`을 `do` 블록 밖으로 뺐습니다 — `catch` 블록에서도 그 세션의
   `logFeedbackAttachment(...)`를 불러야 하는데, 원래 `let session = ...`이
   `do` 블록 안에 있어 `catch`에서는 안 보였습니다. `LanguageModelSession(instructions:)`
   초기화 자체는 던지지 않으므로 밖으로 빼도 안전합니다.
2. `catch` 블록에 `#if DEBUG`로 감싼 뒤 `session.logFeedbackAttachment(sentiment: nil,
   issues: [], desiredOutput: nil)`을 부르고, 그 `Data`를 파일로 저장하는
   `saveFeedbackAttachment(_:questionID:word:)`를 새로 추가했습니다 — 문서 폴더에
   `feedback_glossary_q<id>_<시각>.data`로 저장됩니다.

**DEBUG 로만 가둔 이유(판단 지점)** — 이 파일을 꺼내려면 Xcode → Window → Devices
and Simulators → 기기 선택 → 이 앱 → Download Container 로 기기에 직접 연결해야
합니다. 어머니가 쓰는 실기기(RELEASE)에서는 그렇게 꺼낼 일이 없으니 계속 쌓아 둘
이유가 없다고 보고, `#if DEBUG`로 개발 중(Xcode 로 실행할 때)에만 남게 했습니다.
실기기(RELEASE)에서도 남기고 싶으시면 말씀해 주세요 — `#if DEBUG` 두 곳만 지우면
됩니다.

**검증 상태** — 컴파일 확인은 못 합니다. `Sentiment`/`Issue` 의 정확한 케이스
이름은 확인 안 된 채로 `nil`/`[]`로만 채웠으므로(타입 자체는 Quick Help 로 확정
됐으니 이 값들 자체는 컴파일됩니다), 세이프티에 걸릴 만한 낱말을 실기기(Xcode
디버그 빌드)에서 탭해 콘솔에 `📎 Feedback Assistant 첨부 저장:` 로그가 뜨는지,
그리고 Xcode 의 기기 컨테이너 다운로드에서 `feedback_glossary_...data` 파일이
실제로 있는지 확인해 주세요.

> ⚠️ 이 판단은 이때 애플 공식 문서를 못 읽어서 내린 임시 조치였다. 3-W 에서
> 문서 원문을 직접 읽어 더 정확하게 다시 고쳤다.

---

## 3-W. 3-V 를 애플 공식 문서 원문으로 다시 확인하고 고치기

**요청** — 사용자가 `logFeedbackAttachment`의 정확한 공식 문서 URL을 직접 보내주며,
"문서를 참고해서 로그를 애플이 참고해서 수정을 잘할 수 있게 함수에 인자들을 잘 채워달라"고
요청.

**막혔던 것을 뚫은 방법** — 애플 문서 페이지는 자바스크립트로 렌더링돼 웹 검색 도구로는
못 읽지만, 사용자가 준 URL 뒤에 `.md`를 붙이면 마크다운 원문이 그대로 나온다는 걸
발견했습니다. 이 방법으로 `logFeedbackAttachment` 본문서, `LanguageModelSession` 전체
개요(Topics) 문서, `GenerationError.guardrailViolation(_:)` 문서, 그 associated
value 인 `Context` 구조체 문서까지 차례로 확인했습니다.

**3-V 에서 틀렸거나 확인 안 된 채 남겨 뒀던 것들:**

1. **`sentiment: nil`로 뒀던 것** — 공식 문서에 `Sentiment`가 positive/negative/
   neutral 세 케이스라고 명시돼 있고, "문제 있는 응답"의 예시 코드에 실제로
   `.negative`를 씁니다. 우리 상황(원치 않게 막힘)에 정확히 맞는 값이라 이제
   `.negative`로 채웠습니다.
2. **모든 종류의 실패에 다 반응하던 것** — 문맥 초과, 응답 형식 오류 같은 것도
   `catch`에 걸리는데, 이런 건 애플 세이프티 팀이 볼 대상이 아닙니다. 문서에서
   `LanguageModelSession.GenerationError.guardrailViolation(_:)`가 정확히
   "세이프티 가드레일이 걸렸다"는 뜻의 케이스임을 확인해서, 이제 이 케이스일
   때만 애플 신고 자료를 남기도록 좁혔습니다.
3. **파일 확장자 `.data`** — 문서 원문이 "Data 는 JSON 인코딩된 자료이니 `.json`
   파일로 저장해서 첨부하라"고 명시적으로 알려줘서 `.json`으로 바꿨습니다.
4. **`Transcript.Entry?`를 손으로 만들어야 하는 줄 알았던 것** — 문서를 보니
   `desiredResponseText: String?`을 바로 받는 오버로드가 따로 있어서, 그걸 쓰면
   `Transcript.Entry`를 직접 조립할 필요가 없습니다(어차피 우리는 "이랬으면
   좋았을 정답"이 없어 `nil`을 넘기지만, 있다면 이 오버로드가 훨씬 간단합니다).

**여전히 확인 못 한 것** — `LanguageModelFeedback.Issue.Category`의 전체 케이스
목록은 애플 사이트 검색으로도 못 찾았습니다. 예시 코드에 `.incorrect`(사실이 틀림)
케이스 하나만 나오는데, 우리 상황(세이프티 차단)엔 맞지 않는 분류라서 **억지로
끼워 맞추지 않고 `issues: []`로 비워 뒀습니다.** 문서에 "실패로 롤백된 항목은
첨부자료에 자동으로 포함된다"고 나와 있어서, `issues`를 안 채워도 막힌 프롬프트
자체는 첨부파일에 담겨 나옵니다 — 그 판단 근거입니다.

```swift
// LanguageModelSession.GenerationError (문서 확인된 케이스)
case guardrailViolation(LanguageModelSession.GenerationError.Context)

// Context (문서 확인)
struct Context {
    var debugDescription: String
}

// GlossaryComposer.swift catch 블록 안
if let generationError = error as? LanguageModelSession.GenerationError,
   case .guardrailViolation(let context) = generationError {
    let attachment = session.logFeedbackAttachment(
        sentiment: .negative, issues: [], desiredResponseText: nil)
    saveFeedbackAttachment(
        attachment, questionID: questionID, word: word,
        debugDescription: context.debugDescription)
}
```

**검증 상태** — 컴파일 확인은 못 합니다. 이번엔 애플 공식 문서 원문(.md)으로
타입·케이스 이름을 직접 확인했으므로 3-V 때보다 확신은 훨씬 높지만, 그래도
실기기(Xcode 디버그 빌드)에서 세이프티에 걸릴 낱말을 탭해 컴파일이 되는지,
콘솔에 `📎 Feedback Assistant 첨부 저장:`과 `가드레일 상세:` 로그가 뜨는지,
기기 컨테이너에서 `feedback_glossary_....json` 파일을 실제로 꺼낼 수 있는지
확인해 주세요.

---

## 3-X. Supabase 에 낱말 해설 실패가 안 올라가던 진짜 이유 — save() 누락과 업로드 시점

**요청** — "supabase에 기록이 안되는 것 같은데 확인해줄래? 낱말 해설도 model_failure에
담겨서 올라가면 좋겠는데"라는 요청과, 이어서 실제 Supabase `model_failure` 표를
CSV로 내려받아 보내주며 "낱말 해설은 따로 안들어가고 있어"라는 확인.

**증거로 확인된 사실** — 사용자가 보내준 CSV엔 7줄이 있었는데, **전부
`job = "note"`**(오답 선택 시 설명, `CommentaryWriter`)였고 같은 세이프티 에러
("Response may contain sensitive or unsafe content")로 걸린 것들이 2026-09-11부터
2026-09-15까지 꾸준히 잘 올라와 있었습니다. **`job = "glossary_example"`(낱말 해설)
행은 단 하나도 없었습니다.** 이건 Supabase 파이프라인 자체는 멀쩡하고, 낱말 사전
경로에만 있는 버그라는 뜻입니다.

**막혔던 것 — 직접 Supabase 를 열어볼 수 없었다** — 클라우드 컨테이너의 `Bash`와
Mac 의 `device_bash` 양쪽 다 조직의 아웃바운드 프록시가 Supabase 도메인
(`abaxaguvacsrithcnstb.supabase.co`)으로 가는 `curl`을 403 으로 막았습니다.
그래서 라이브 진단 대신 **코드를 처음부터 다시 따라가며** 원인을 찾았고,
사용자의 CSV 가 그 결론을 뒷받침해 줬습니다.

**찾은 원인 두 가지:**

1. **`QuizSession.saveFailures(_:)`가 `modelContext.insert(...)`만 하고 `save()`를
   안 불렀습니다.** 문제 채점 경로(`gradeCurrent()`)에서는 바로 뒤이어
   `saveObsRecord()`가 어차피 저장을 하기 때문에 "우연히" 잘 됐던 건데, 낱말
   사전 탭은 그런 뒤따르는 저장이 전혀 없는 독립된 경로라, 저장 안 된 채로
   남아 있다가 앱이 종료되면 그 기록만 조용히 사라질 수 있었습니다.
2. **`ObsUploader.uploadPendingFailures()`를 부르는 곳이 회차 시작/종료
   (`QuizSession.uploadObservations()`) 뿐이었습니다.** 낱말 사전 실패는 회차
   중간 아무 때나 생기는데, 그 회차를 끝내거나 새로 시작하기 전까지는 저장은
   됐어도 업로드 자체가 한 번도 시도되지 않았습니다.

**한 것:**

1. `saveFailures(_:)` 끝에 `try? modelContext.save()`를 추가해, 뒤따르는 저장에
   기대지 않고 이 메서드가 스스로 책임지게 했습니다.
2. `QuizSession`에 `uploadFailuresNow()`를 새로 추가해, 회차 경계를 기다리지
   않고 그 자리에서 바로 `ObsUploader.uploadPendingFailures()`를 부르게
   했습니다.
3. `QuestionScreen`의 `onTapWord`가 `session.saveFailures([writing.failure])`
   뒤에, 실패가 있었을 때만(`writing.failure != nil`) `session.uploadFailuresNow()`를
   부르게 했습니다.
4. `ObsUploader.uploadPendingFailures()`에 `uploadPending()`과 같은 방식의
   진단 로그(`📤 안 올라간 실패 기록:`, 성공 시 `✅`, 실패 시 `❌`)를 추가해,
   다음에 문제가 생기면 콘솔만 보고도 어디서 막혔는지 바로 알 수 있게 했습니다.

```swift
// QuizSession.swift
func saveFailures(_ drafts: [ModelFailureDraft?]) {
    let toInsert = drafts.compactMap { $0 }
    guard !toInsert.isEmpty else { return }

    for draft in toInsert {
        modelContext.insert(ModelFailure(draft: draft))
    }
    try? modelContext.save()
}

func uploadFailuresNow() {
    Task {
        await ObsUploader(modelContext: modelContext).uploadPendingFailures()
    }
}
```

```swift
// QuestionScreen.swift — onTapWord 안
session.saveFailures([writing.failure])
if writing.failure != nil {
    session.uploadFailuresNow()
}
```

**검증 상태** — 컴파일 확인은 못 합니다. 다시 빌드해서, 세이프티에 걸릴 만한
낱말(예: "8월 15일", "광복절"처럼 이미 `note`에서 걸린 것과 비슷한 역사·날짜
관련 낱말)을 탭해 보면서: (1) Xcode 콘솔에 `📤 안 올라간 실패 기록: 1`과
`✅ 실패 기록 업로드 성공: 1`이 뜨는지, (2) 회차를 끝내지 않아도 몇 초 안에
Supabase `model_failure` 표에 `job = 'glossary_example'` 줄이 새로 생기는지
확인해 주세요. 이전엔 회차를 끝내야만 확인할 수 있었지만, 이제는 탭한 직후
바로 확인될 것입니다.

**실제로 확인해 보니 — 콘솔에 `📤 안 올라간 실패 기록: 11`은 찍혔지만
`❌ 실패 기록 업로드 실패 — 다음 기회에 다시 시도`가 함께 떴다.** save()는
이제 되니 그동안 못 올라가고 쌓여 있던 것까지 11개가 한꺼번에 잡힌 것으로
보이는데, 정작 Supabase 로 보내는 마지막 단계(`send(_:to:)`)가 실패하고
있었다. 그런데 `send(_:to:)`는 처음부터 서버 응답 본문을 아예 버리고
(`let (_, response) = ...`) 상태 코드가 2xx 인지만 `Bool`로 돌려주고
있어서, "실패했다"는 것만 알 뿐 **왜** 실패했는지는 로그에 전혀 안 남고
있었다. 이건 `job = "note"`쪽에도 원래 있던 사각지대였는데, 그쪽은 지금까지
한 번도 실패한 적이 없어서 안 드러났을 뿐이다.

**한 것** — `send(_:to:)`가 실패할 때 HTTP 상태 코드와 응답 본문을 그대로
콘솔에 남기도록 고쳤다. 다음 실패부터는 Supabase 가 왜 거절했는지 그 이유
문구가 그대로 보인다.

```swift
do {
    let (data, response) = try await URLSession.shared.data(for: request)
    guard let http = response as? HTTPURLResponse else { return false }
    let ok = (200...299).contains(http.statusCode)
    if !ok {
        let body = String(data: data, encoding: .utf8) ?? "(본문을 텍스트로 못 읽음)"
        print("❌ Supabase 응답 \(http.statusCode):", body)
    }
    return ok
} catch {
    print("❌ Supabase 요청 자체가 실패:", error)
    return false
}
```

**의심되는 원인(아직 확인 전) — Postgres 쪽 제약 조건.** `job = "glossary_example"`은
이번이 Supabase 로 처음 보내는 값이다. `note`만 지금까지 계속 성공해 왔다는
점을 보면, `model_failure` 표의 `job` 컬럼에 특정 값만 허용하는 CHECK 제약이나
비슷한 규칙이 걸려 있고 `"glossary_example"`이 그 목록에 없을 가능성이 있다 —
이건 코드가 아니라 Supabase 쪽 테이블 정의를 봐야 확인되는 부분이라, 이 환경에서
직접 열어볼 수 없다. Supabase 대시보드의 Table Editor 에서 `model_failure` 표의
`job` 컬럼 제약을 확인하거나, 위 로그로 실제 응답 본문(예: `new row for relation
"model_failure" violates check constraint ...`)을 보면 바로 판가름 난다.

**검증 상태** — 다시 빌드해서 같은 상황을 재현한 뒤, 이번엔 `❌ Supabase 응답
<코드>: <본문>` 로그의 실제 내용을 확인해 주세요. 그 본문 문구를 그대로
알려 주시면 원인을 정확히 짚어 고칠 수 있습니다.

**실제 응답 본문으로 원인이 확정됐다 — 이건 앱 버그가 아니라 Supabase 쪽
설정 문제다.**

```json
{"code":"42501","details":null,"hint":null,
 "message":"new row violates row-level security policy for table \"model_failure\""}
```

Postgres 에러 코드 `42501`은 RLS(Row Level Security) 위반이다. 즉 앱은
`glossary_example` 을 올바르게 만들어 정확한 주소로 정확하게 보냈는데,
Supabase 의 `model_failure` 표에 걸린 **INSERT 정책(policy)**이 이 줄을
막았다는 뜻이다. `note`는 지금까지 계속 성공해 왔으므로, 그 정책이 모든
`INSERT`를 막는 게 아니라 **`job` 컬럼 값에 따라 허용 여부가 갈리는 조건
(`WITH CHECK`절)**을 갖고 있고, 그 조건에 `"glossary_example"`이 빠져
있을 가능성이 매우 높다 — 예를 들어 `WITH CHECK (job IN ('note',
'commentary'))` 같은 식이다.

이 저장소 안에는 Supabase 마이그레이션·정책을 담은 `.sql` 파일이 없다
(`SupabaseConfig.swift`에는 프로젝트 URL·anon key 만 있다) — 즉 이 정책은
Supabase 대시보드에서 직접 만들어져 코드로는 추적되지 않고 있다. 이
환경에선 Supabase 에 직접 접속할 수 없어(아웃바운드 프록시가 막음) 정책
원문을 볼 수 없으므로, 다음은 사용자가 Supabase 대시보드에서 직접 확인해야
한다.

**확인·수정 방법** — Supabase 대시보드 → 왼쪽 메뉴 **Authentication → Policies**
(또는 **Database → Tables → model_failure → RLS policies**)에서
`model_failure` 표의 `INSERT` 정책을 열어 `WITH CHECK` 조건을 본다. 거기에
`job` 값을 제한하는 목록이 있다면 `'glossary_example'`을 추가한다. SQL
Editor 에서 아래처럼 현재 정책을 먼저 확인해 볼 수도 있다.

```sql
select policyname, cmd, qual, with_check
from pg_policies
where tablename = 'model_failure';
```

**검증 상태** — 코드 쪽은 이제 할 일이 없다 — `send(_:to:)`가 실제 서버
응답을 그대로 보여주는 것으로 이미 확인이 끝났다. Supabase 쪽 정책을
고친 뒤 같은 낱말을 다시 탭해서 이번엔 `✅ 실패 기록 업로드 성공:`이
찍히고 `model_failure` 표에 `job = 'glossary_example'` 줄이 실제로
생기는지 확인해 주세요. 정책 원문을 알려 주시면(위 SQL 결과를 붙여
주셔도 된다) 정확히 어느 줄을 고쳐야 하는지 짚어 드릴 수 있습니다.

**정책 원문이 실제로 왔다 — 예상이 정확히 맞았다.**

```json
{
  "policyname": "app can insert only",
  "cmd": "INSERT",
  "with_check": "(((length(device_id) >= 8) AND (length(device_id) <= 64))
    AND (job = ANY (ARRAY['commentary'::text, 'note'::text]))
    AND (question_id >= 1) AND (question_id <= 100000)
    AND (occurred_at > (now() - '30 days'::interval))
    AND (occurred_at < (now() + '1 day'::interval))
    AND (length(reason) <= 500) AND (length(instructions) <= 4000)
    AND (length(prompt) <= 2000))"
}
```

`job = ANY (ARRAY['commentary'::text, 'note'::text])` — 정확히 이 한 줄
때문이었다. `model_failure`에 넣을 수 있는 `job` 값을 `'commentary'`·
`'note'` 두 가지로만 못 박아 둔 정책이고, `'glossary_example'`은 아예
목록에 없었다. `device_id`·`question_id`·`occurred_at`·`reason`·
`instructions`·`prompt`의 길이·범위 제약은 낱말 사전 쪽 값도 다 같은
모양(문자열 길이, 1~100000 사이 문항 번호, 최근 30일 이내 시각)이라
그대로 통과한다 — 걸리는 건 `job` 값 하나뿐이다.

**Supabase 대시보드 SQL Editor 에서 실행해야 할 것** — 이건 앱 코드가 아니라
데이터베이스 정책이라 이 환경에서 대신 실행해 줄 수 없다. 아래를 그대로
SQL Editor 에 붙여 넣고 실행하면 된다.

```sql
alter policy "app can insert only" on model_failure
with check (
  (length(device_id) >= 8) and (length(device_id) <= 64)
  and (job = any (array['commentary'::text, 'note'::text, 'glossary_example'::text]))
  and (question_id >= 1) and (question_id <= 100000)
  and (occurred_at > (now() - '30 days'::interval))
  and (occurred_at < (now() + '1 day'::interval))
  and (length(reason) <= 500)
  and (length(instructions) <= 4000)
  and (length(prompt) <= 2000)
);
```

바뀐 건 `array[...]` 안에 `'glossary_example'::text`를 추가한 것 하나뿐이고,
나머지 조건은 원래 정책 그대로 옮겨 적었다 — `ALTER POLICY ... WITH CHECK`는
조건 일부만 바꿀 수 없고 전체를 다시 써야 하기 때문이다.

**검증 상태** — 위 SQL을 실행한 뒤 앱에서 같은 낱말을 다시 탭해서
`✅ 실패 기록 업로드 성공:`이 찍히고 `model_failure` 표에
`job = 'glossary_example'` 줄이 실제로 생기는지 확인해 주세요. 이번엔
코드도, 정책도 둘 다 맞는 값을 쓰고 있으니 이대로 될 것으로 보이지만,
실제 확인은 늘 그렇듯 실기기·Supabase 양쪽을 직접 봐야 압니다.

---

## 4. 화면 연결 (`QuestionScreen.swift`) — 최종



> **시트로 바꾸면서 3단계 지침도 함께 바뀌었습니다.** 이전에 `content(for:)`를
> `ZStack`으로 감싸라고 안내했던 부분은 **이제 필요 없습니다.** 시트는 `.sheet(...)`
> 한 줄을 뷰 아무 데나(형제 뷰로 겹쳐 쌓지 않고) 붙이면 되기 때문에, `VStack`을
> 그대로 두고 그 위에 `.sheet(...)` modifier만 답니다.
>
> **이미 `ZStack` 버전으로 옮겨 적으셨다면** — 아래 최종 버전으로 통째로 바꿔치기
> 하시면 됩니다 (`ZStack`·투명 캐처·`GlossaryPanel` 호출부를 전부 걷어내고 `.sheet`
> 하나로 교체).

**상태 세 개(41~43번째 줄) — `isGlossaryExpanded`는 이제 필요 없어 지웁니다:**

```swift
@State private var openGlossary: (word: String, gloss: String, examples: [String])?
@State private var isGlossaryPresented = false
@State private var generatedExample: String?   // 6단계가 채워 줌. 없으면 nil
```

**`content(for:)` — 원래 있던 평범한 `VStack` 그대로에, `.sheet(...)`만 붙입니다:**

```swift
private func content(for item: QuizItem) -> some View {
    VStack(spacing: 0) {
        header(for: item)
        questionArea(for: item)

        if session.needsAnswerHint {
            hintBanner
        }

        nextButton
    }
    .padding(24)
    .animation(.easeInOut(duration: 0.2), value: session.needsAnswerHint)
    .sheet(isPresented: $isGlossaryPresented, onDismiss: closeGlossary) {
        if let g = openGlossary {
            GlossaryPanel(
                word: g.word, gloss: g.gloss,
                generatedExample: generatedExample,
                relatedWords: g.examples
            )
        }
    }
}
```

**`questionArea(for:)` 안의 `KoreanText(...)` — `onTapWord`에서 패널을 "열기"만 하던 것을
"시트를 띄우기"로 바꿉니다 (매개변수 이름은 그대로):**

```swift
KoreanText(
    text: item.displayText,
    font: .systemFont(ofSize: 30, weight: .bold),
    highlight: item.highlightText,
    marker: sessionMode.showsFocusHighlight ? item.markerText : nil,
    glossary: item.glossary,
    onTapWord: { word, gloss, examples in
        openGlossary = (word, gloss, examples)
        generatedExample = nil
        isGlossaryPresented = true
        Task { generatedExample = await composeExample(word: word, gloss: gloss, relatedWords: examples, questionText: item.displayText) } // 6단계
    }
)
.padding(.top, 32)
```

**`closeGlossary()` — `showAnswerHint()` 옆, 그대로 둡니다:**

```swift
private func closeGlossary() {
    isGlossaryPresented = false
    openGlossary = nil
    generatedExample = nil
}
```

**다 넣고 확인할 것** — 12번 문항을 열어 점선 밑줄(2단계 버그 수정 포함)이 보이는지,
탭하면 아래에서 시트가 올라오는지, iOS 26 시뮬레이터라면 그 시트가 화면 위에 살짝 뜬
듯한 반투명 유리 느낌으로 보이는지 확인하면 됩니다.

> ⚠️ **이 버전에는 버그가 하나 있었습니다 — 낱말을 처음 탭하면 시트가 아주 크게
> 뜹니다.** 원인과 고친 코드는 바로 아래 **4-A**를 보세요. 이 절의 코드를 이미
> 입력하셨다면, 4-A의 코드로 통째로 바꿔치기하시면 됩니다.

---

## 4-A. 새로 생긴 버그 — 낱말을 처음 탭하면 시트가 아주 크게 뜬다

**증상** — 어떤 낱말이든 **이번 화면에서 처음** 탭하면 시트가 화면을 거의 다 채울
만큼 크게 떴다가, 그 시트를 내리고 **다시** 탭하면 그다음부터는 원래 의도한 작은
높이(`.height(180)`)로 정상적으로 뜬다.

**원인** — `.sheet(isPresented:)`와 그 내용을 결정하는 `openGlossary`가 **서로 다른
`@State` 두 개**로 나뉘어 있었습니다.

```swift
openGlossary = (word, gloss, examples)   // ① 내용을 먼저 채운다
isGlossaryPresented = true               // ② 그다음 시트를 연다
```

SwiftUI 입장에서는 ①과 ②가 **같은 순간에 함께 반영된다는 보장이 없습니다.** 시트가
처음 만들어지는 순간, 내용 쪽(`if let g = openGlossary`)이 아직 `nil`인 채로 한 번
평가되면 — `GlossaryPanel`도, 그 안의 `.presentationDetents`도 없는 **빈 화면**으로
시트가 열리는 셈이 되고, 그러면 iOS는 높이를 정할 근거가 없어 기본값인 큰 화면으로
엽니다. 한 박자 뒤에 `openGlossary` 값이 반영돼 내용이 채워지지만, **시트 크기는
이미 그 순간에 정해진 뒤라 바뀌지 않습니다.**

**처음 한 번만 이 일이 생기는 이유** — 시트를 감싸는 뷰(호스팅 컨트롤러)는 같은
`.sheet(...)` 자리에서 **한 번 만들어지면 계속 재사용됩니다.** 두 번째 탭부터는
이미 만들어져 있던 그 뷰가 다시 나타나기만 하므로, 첫 탭에서 있었던 "빈 화면 →
채워짐" 순서가 다시 발생하지 않습니다.

**고치는 법** — 내용과 "열림 여부"를 하나로 합칩니다. 두 `@State` 대신, **낱말
정보 자체가 있으면 열리고 없으면 닫히는** 값 하나만 쓰면 이 틈이 사라집니다.

**상태 — `openGlossary`·`isGlossaryPresented` 두 개를 아래 하나로 바꿉니다:**

```swift
private struct GlossarySelection: Identifiable {
    // 낱말마다 다른 값(예: UUID())을 쓰면 안 된다 — 시트가 열려 있는 동안 다른 낱말을
    // 또 탭했을 때 id가 바뀌어 "새 시트"로 보이고, 닫혔다 다시 열리며 또 크게 뜬다.
    // 이 화면엔 낱말 사전 시트 자리가 하나뿐이므로 항상 같은 값으로 고정한다.
    let id = "glossary"
    let word: String
    let gloss: String
    let examples: [String]
}

@State private var glossarySelection: GlossarySelection?
@State private var generatedExample: String?   // 6단계가 채워 줌. 없으면 nil
```

**`content(for:)` — `.sheet(isPresented:)` 대신 `.sheet(item:)`을 씁니다:**

```swift
private func content(for item: QuizItem) -> some View {
    VStack(spacing: 0) {
        header(for: item)
        questionArea(for: item)

        if session.needsAnswerHint {
            hintBanner
        }

        nextButton
    }
    .padding(24)
    .animation(.easeInOut(duration: 0.2), value: session.needsAnswerHint)
    .sheet(item: $glossarySelection) { selection in
        GlossaryPanel(
            word: selection.word, gloss: selection.gloss,
            generatedExample: generatedExample,
            relatedWords: selection.examples
        )
    }
}
```

`.sheet(item:)`은 **바인딩한 값이 `nil`이 아닐 때만 열리고, 내용 클로저는 항상 그
값을 이미 채워진 채로 받습니다.** "빈 화면으로 잠깐 열리는" 순간 자체가 없어집니다.

**`questionArea(for:)` 안 `KoreanText(...)`의 `onTapWord`:**

```swift
onTapWord: { word, gloss, examples in
    generatedExample = nil
    glossarySelection = GlossarySelection(word: word, gloss: gloss, examples: examples)
    Task { generatedExample = await composeExample(word: word, gloss: gloss, relatedWords: examples, questionText: item.displayText) } // 6단계
}
```

**`closeGlossary()` — 더는 필요 없어 지웁니다.** `.sheet(item:)`은 닫힐 때 스스로
`glossarySelection`을 `nil`로 되돌립니다. (`generatedExample`은 다음 탭에서 매번
`nil`로 다시 시작하므로 굳이 닫을 때 정리하지 않아도 됩니다.)

**다 넣고 확인할 것** — 이번에는 **이번 화면에서 처음 탭하는 낱말**로 확인해야
의미가 있습니다. 화면을 새로 열고(또는 앱을 재시작하고) 처음 탭한 낱말도 바로
작은 높이로 뜨는지 확인합니다.

---

## 5. 닫기 규칙 — 이제 대부분 시스템이 합니다

시트로 바꾸면서 이 항목의 판단이 갈리는 지점이 사라졌습니다. `.sheet`는 iOS가
**바깥 탭·아래로 쓸어내리기로 자동으로 닫히고**, 열려 있는 동안은 뒤 화면이 자동으로
비활성화되어 실수로 "다음" 버튼이 눌리는 일도 원천적으로 없습니다. 우리가 챙길 건
✕ 버튼(위 `GlossaryPanel`의 `dismiss()`)과, 닫힐 때 상태를 정리하는 `onDismiss: closeGlossary`
뿐입니다 — 둘 다 4번 항목에 이미 들어 있습니다.

---

## 6. Foundation Models로 예문 만들기 (새 파일, 예: `Data/GlossaryComposer.swift`)

**바뀐 점** — 짧은 뜻(`gloss`)은 1단계에서 이미 사람이 손으로 다듬어 놓았기 때문에,
Foundation Models가 할 일은 **예문 하나를 새로 짓는 것**뿐입니다. 뜻풀이 자체를
다시 쓰게 하지 않습니다.

**가장 중요한 원칙은 그대로입니다 — 모델이 새 사실을 지어내면 안 됩니다.** JSON의
`gloss`를 "이미 확정된 사실"로 주고, 그 사실 안에서만 지금 질문 맥락에 맞는 예문을
지으라고 못 박습니다. `examples`(비슷한 낱말 목록)가 있으면 참고 재료로 같이 줍니다.

```swift
import FoundationModels

@Generable
struct GlossaryExample {
    @Guide(description: "한두 문장, 지금 질문 맥락에 맞는 구체적인 예문")
    let example: String
}

func composeExample(
    word: String, gloss: String, relatedWords: [String] = [], questionText: String
) async -> String? {
    guard case .available = SystemLanguageModel.default.availability else {
        return nil // 기기가 지원 안 하면 nil — 패널은 JSON의 examples(있으면)로 대신 보여준다
    }

    let session = LanguageModelSession(instructions: {
        """
        당신은 70대 어르신에게 낱말을 설명하는 다정한 선생님입니다.
        아래 '확정된 뜻'의 사실 내용은 절대 바꾸지 마세요. 뜻을 다시 쓰지 말고,
        그 뜻 안에서만 지금 질문과 자연스럽게 이어지는 짧은 예문 하나만 만들어 주세요.
        """
    })

    var prompt = "질문: \(questionText)\n낱말: \(word)\n확정된 뜻: \(gloss)"
    if !relatedWords.isEmpty {
        prompt += "\n비슷한 낱말: \(relatedWords.joined(separator: ", "))"
    }

    do {
        let response = try await session.respond(to: prompt, generating: GlossaryExample.self)
        return response.content.example
    } catch {
        return nil
    }
}
```

**기기 요구사항** — iOS 26 이상 + Apple Intelligence를 지원하는 기기에서만 됩니다.
온디바이스라 인터넷이 필요 없고 밖으로 안 나가요. 어머니 폰이 이 조건을 만족하는지는
직접 확인이 필요해요 — 안 되면 위 함수가 `nil`을 돌려주고, `GlossaryPanel`은 그 낱말에
`examples`(비슷한 낱말 목록)가 있으면 그것만, 그마저 없으면 펼치는 힌트 자체를 숨깁니다.

**판단이 갈리는 지점 — 매번 새로 생성할지, 한 번 만들어 두고 재사용할지**

| | 매번 새로 생성 | 처음 한 번 생성 후 저장 |
|---|---|---|
| **좋은 점** | 코드가 단순함 | 패널 열 때 기다릴 필요 없음, 문구가 항상 같음 |
| **나쁜 점** | 열 때마다 잠깐 기다려야 함, 볼 때마다 표현이 조금씩 달라짐 | (question_id, 낱말)을 키로 하는 작은 캐시가 하나 더 필요 |

어머니가 같은 문제를 여러 번 다시 만나실 텐데, 그때마다 예문이 미묘하게 달라지면
오히려 헷갈리실 수 있어서 **저장해서 재사용**하는 쪽을 추천해요.

**판단이 갈리는 지점 — 예문 소스: AI 생성 우선 vs JSON의 examples 목록 우선**

| | ⓐ AI 예문을 우선, 없으면 JSON 목록 | ⓑ JSON 목록이 있으면 그것만, AI는 없을 때만 |
|---|---|---|
| **좋은 점** | 문장 형태라 더 자연스럽게 읽힘 | 기기 지원 여부와 상관없이 항상 같은 내용 |
| **나쁜 점** | 같은 낱말이라도 기기에 따라 보이는 내용이 다름 | 문장이 아니라 낱말 나열이라 다소 딱딱함 |

**ⓐ를 추천합니다** — 39개 낱말 중 `examples`가 있는 건 5개뿐이라(대부분 국경일 관련),
목록을 우선하면 나머지 34개는 어차피 AI에만 의존하게 돼 일관성이 별 의미가 없습니다.

**참고 — 2026년 여름 WWDC에서 애플이 같은 API에 Anthropic·Google 같은 외부 모델도
연결할 수 있게 열었습니다.** 온디바이스가 안 되는 기기에서 클라우드로 대체하는 것도
이제 가능은 한데, 개발자가 직접 어떤 모델을 쓸지 골라 연결해야 하는 구조라 손이 많이 가요.
지금은 온디바이스만으로 먼저 만들고, 필요해지면 그때 확장하는 걸 추천합니다.

---

## 6-A. 질문 문맥을 빼고, 낱말·뜻을 나란히 비교하는 두 문장으로 바꿈

**요청** — 세이프티에 자주 걸리는 걸 보고, 사용자가 직접 `GlossaryComposer`의
`instructions`·`prompt`를 고쳤다. 문제 문장(`questionText`)을 프롬프트에서 아예
빼고, 대신 "낱말을 그대로 넣은 문장"과 "확정된 뜻을 넣은 문장"을 낱말·뜻 자리만
빼고 같은 문장으로 나란히 만들게 했다(예: "햇곡식으로 송편을 만들었어요." /
"그 해에 처음난 곡식으로 송편을 만들었어요."). 그 뒤 두 문장이 출력으로 나와
해설에 표시되도록 `@Generable` 구조체에 프로퍼티 두 개를 만들고, 하단 모달에도
두 문장이 잘 나오게 반영해 달라는 요청.

**이 방향이 3-X 논의와 정확히 맞아떨어진다** — `HISTORY.md`에 이미 2026-09-06에
"문제 지문을 넣지 않는다 — 「일제의 지배」·「목숨을 바친」 같은 낱말이 안전
필터에 걸린다"고 기록돼 있고, `CommentaryWriter`는 그 교훈대로 문제 지문 대신
`facts`·`displayAnswer`만 쓴다. `GlossaryComposer`는 이 교훈을 안 물려받고
`questionText`(문제 문장 그대로)를 프롬프트에 넣고 있었는데, 사용자가 이번에
그 문맥 자체를 프롬프트에서 없애 같은 교훈을 스스로 적용한 것이다.

**한 것:**

1. `@Generable struct GlossaryExample`을 프로퍼티 하나(`example`)에서
   `wordSentence`·`glossSentence` 두 개로 나눴다.
2. 성공했을 때 두 문장을 `"1. \(wordSentence)\n\n2. \(glossSentence)"` 모양의
   한 문자열로 합쳐 `Writing(text:)`에 담는다 — `Writing`·`GlossaryExampleState`
   등 기존 타입은 하나도 안 건드리고, 이미 있던 `String?` 파이프라인을 그대로 탄다.
3. `GlossaryPanel.exampleContent`의 `.ready` 갈래를 고쳐, 그 문자열을 빈 줄
   기준으로 나눠 두 문장을 각각 다른 문단으로 보여준다 — 번호("1."/"2.")는
   텍스트 자체에 이미 들어 있어 따로 그릴 필요가 없다.

```swift
// GlossaryComposer.swift
@Generable
struct GlossaryExample {
    @Guide(description: "낱말을 그대로 적용한 문장 (예: 햇곡식으로 송편을 만들었어요.)")
    let wordSentence: String

    @Guide(description: "확정된 뜻을 적용한 문장. wordSentence 와 낱말·뜻 자리만 다르고 나머지는 같은 문장을 공유한다")
    let glossSentence: String
}
```

```swift
// GlossaryPanel.swift — exampleContent 의 .ready 갈래
let sentences = text.components(separatedBy: "\n\n")
VStack(alignment: .leading, spacing: 14) {
    ForEach(Array(sentences.enumerated()), id: \.offset) { _, sentence in
        Text(sentence)
            .font(.system(size: 20, weight: .medium))
            .foregroundStyle(AppColor.textMuted)
    }
}
```

**아직 남은 것** — `questionText` 매개변수 자체는 함수 시그니처에 그대로 남아
있다(안 쓰이기만 할 뿐 컴파일에는 지장 없다). 지우고 싶으면 `QuestionScreen`의
호출부까지 같이 고쳐야 한다 — 지금은 사용자가 판단을 미뤄 둔 상태라 손대지 않았다.

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 낱말을 탭해 보고 (1) 하단
모달에 두 문장이 문단으로 나뉘어 잘 보이는지, (2) "천제인 환인의 손자..."·
"목숨을 바친..." 같은 예전에 걸리던 낱말도 이제 걸리지 않는지 확인해 주세요.

---

## 6-B. 1번 문장이 낱말 그 글자를 안 쓰고 다른 말로 바꿔 나온 것을 고침

**문제** — 실기기 스크린샷으로 확인됨: 낱말 "최초"·뜻 "제일 먼저"에서, 2번 문장은
"제일 먼저 자란 곡식으로..."처럼 뜻을 그대로 썼는데, **1번 문장은 "그 해에
처음난 곡식으로..."로 나와 낱말 "최초"가 아예 안 들어갔다** — "처음난"은
비슷한 말이지 낱말 그 글자가 아니다. instructions 안의 예시 문장("햇곡식으로
송편을 만들었어요")만으로는 "낱말 그 글자를 반드시 그대로 써야 한다"는 것이
강제되지 않았던 것으로 보인다.

**한 것** — 예시 문장은 그대로 두고, 그 위에 `[반드시 지킨다]` 규칙 세 개를
명시적으로 추가했다. `@Guide` 설명도 같은 방향으로 강화했다 — 스키마 단
설명은 호출마다 달라지는 낱말·뜻 값을 직접 넣을 수는 없어(모든 호출이 같은
타입 스키마를 공유하므로) "프롬프트의 '낱말' 글자를 조사만 붙여 그대로
넣은 문장"처럼 일반적인 문구로 못박았다.

```swift
[반드시 지킨다]
1. wordSentence 에는 낱말로 받은 글자를 정확히 그대로 넣습니다. 조사(을/를,
   으로 등)만 붙일 수 있고, 비슷한 다른 말로 바꾸거나 풀어 쓰지 않습니다.
2. glossSentence 에는 확정된 뜻으로 받은 글자를 정확히 그대로 넣습니다.
   요약하거나 다른 말로 바꾸지 않습니다.
3. 낱말·뜻이 문장 안에서 자연스러운 자리(주어·목적어·수식어 등 어디든)에
   오면 됩니다 — 문장 맨 앞 고정 위치가 아니어도 됩니다.
```

**검증 상태** — 컴파일 확인은 못 한다. 이건 온디바이스 소형 모델에게 지침으로
강제하는 것이라 100% 확실하지는 않다 — 다시 빌드해서 "최초"처럼 그대로 넣기
애매한 낱말 몇 개를 더 탭해 보고, 1번 문장에 낱말 글자가 실제로 그대로 들어가는지
확인해 주세요. 계속 어긋나면 예시 문장 자체를 더 다양하게(부사·수식어 등 여러
품사) 보여주는 쪽으로 더 손볼 수 있습니다.

---

## 6-C. 규칙이 복잡해질수록 더 어긋나서, 확 줄임

**문제** — 6-B 로 규칙을 늘렸더니(예시 문장 + [반드시 지킨다] 세 항목) 오히려
더 이상해졌다. 낱말 "목숨을 바친" · 뜻 "나라를 위해 돌아가신"에서 1번은
"목숨을 바친 나라를 위해 돌아가셨어요"(뜻이 수식하는 대상이 뒤바뀜)로,
2번은 "나라를 위해 돌아가신 목숨을 바쳤어요"(**2번인데 낱말 "목숨을 바쳤어요"가
들어감**, 섞이면 안 된다는 규칙이 그대로 어겨짐)로 나왔다. 온디바이스 소형
모델에게 규칙을 촘촘히 쌓을수록(예시 + 번호 규칙 세 개) 오히려 지침 자체를
못 따라가는 것으로 보여, 사용자가 **"심플하게" — instructions 5줄, prompt
3줄, @Guide 는 2~3줄로** 명시적으로 줄여 달라고 요청했다.

**한 것** — 예시 문장을 통째로 뺐다("같은 문장을 공유해야 한다"는 요구 자체가
낱말·뜻의 문법 구조가 다를 때 부자연스러움의 원인 중 하나로 보여, "비슷한
상황"으로만 완화했다). 번호 매긴 규칙 목록도 없애고 짧은 문장 네 개로 줄였다.

```swift
let instructions = """
    당신은 70대 어르신에게 낱말을 설명하는 선생님입니다.
    낱말을 넣은 자연스러운 문장(1번), 그 자리에 뜻만 넣은 자연스러운 문장(2번)을 만듭니다.
    1번에는 낱말만, 2번에는 뜻만 들어가야 하며 서로 섞이지 않습니다.
    두 문장은 비슷한 상황을 담아 자연스럽게 이어지게 씁니다.
    """

var prompt = """
    낱말: \(word)
    뜻: \(gloss)
    위 낱말과 뜻으로 두 문장을 만드세요.
    """
```

```swift
@Generable
struct GlossaryExample {
    @Guide(description: "낱말만 넣은 자연스러운 문장. 뜻은 넣지 않는다")
    let wordSentence: String

    @Guide(description: "뜻만 넣은 자연스러운 문장. 낱말은 넣지 않는다")
    let glossSentence: String
}
```

instructions 4줄(요구대로 5줄 이내), prompt 3줄(비슷한 낱말이 있으면 한 줄
더 붙는 건 그대로 둠 — 항상 있는 건 아니라 기본 3줄로 본다), `@Guide` 각각 한
줄로 줄였다.

**검증 상태** — 컴파일 확인은 못 한다. "완전히 같은 문장을 공유"하라는 요구를
빼서 "1번=목숨을 바쳤어요, 2번=목숨을 바쳤어요" 식으로 다시 섞일 가능성은
여전히 남아 있다 — 규칙이 짧아진 대신 그만큼 모델의 자체 판단에 맡기는 부분이
늘었다. 다시 빌드해서 "목숨을 바친"·"최초"처럼 전에 이상하게 나오던 낱말들을
다시 탭해 보고, (1) 문장이 자연스러운지, (2) 1번엔 낱말만, 2번엔 뜻만
들어갔는지 확인해 주세요. 여전히 섞이면 예시를 다시 넣기보다, `@Guide`
설명에 "다른 문장에 있는 말을 넣지 않는다" 같은 한 줄을 추가하는 쪽이
지침 줄 수를 안 늘리면서 시도해볼 만합니다.

---

## 6-D. 문장을 두 줄 이상으로 길게, "기리다" 같은 낱말의 어색함도 줄임

**요청** — 문장이 한 줄짜리 짧은 것 말고 **둘 다 두 줄 이상**으로 나오게 해
달라는 요청과, "기리다"처럼 사전형(기본형) 그대로인 낱말을 문장에 넣을 때
여전히 어색하니 맞춤법·문법에 더 신경 써서 정교하게 써 달라는 요청.

**한 것** — 6-C 에서 줄인 지침에 두 줄을 더했다(총 5줄).

```swift
let instructions = """
    당신은 70대 어르신에게 낱말을 설명하는 선생님입니다.
    낱말을 넣은 문장(1번)과 그 자리에 뜻만 넣은 문장(2번)을 만듭니다.
    낱말·뜻의 조사나 어미는 문장에 자연스럽게 맞춰 바꿔도 됩니다.
    각 문장은 상황이나 배경을 덧붙여 두 줄 이상 되도록 자세히 씁니다.
    1번에는 낱말만, 2번에는 뜻만 들어가야 하며, 맞춤법과 문법에 맞게 씁니다.
    """
```

- **"조사나 어미는 자연스럽게 맞춰 바꿔도 된다"**를 새로 넣었다 — "기리다"
  같은 낱말은 사전형 그대로("~다")는 문장 안에 자연스럽게 못 들어가고
  반드시 활용(기리는·기립니다·기려요 등)해야 하는데, 6-B 에서 "글자를
  정확히 그대로 넣으라"고 강하게 못박았던 것이 오히려 이런 낱말에서 어색함의
  원인이었을 수 있다고 보고 명시적으로 허용했다.
- **"두 줄 이상 되도록 자세히 씁니다"**를 넣어 상황·배경을 덧붙이게 했다 —
  전에는 짧은 한 문장으로 끝나 화면에서 한 줄로 보였다.
- `@Guide` 설명에도 같은 길이 요구를 반영했다(각각 한 줄 그대로 유지).

```swift
@Guide(description: "낱말을 자연스럽게 넣은 문장. 상황을 덧붙여 두 줄 이상 길게 쓴다. 뜻은 넣지 않는다")
let wordSentence: String

@Guide(description: "뜻을 자연스럽게 넣은 문장. 상황을 덧붙여 두 줄 이상 길게 쓴다. 낱말은 넣지 않는다")
let glossSentence: String
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 "기리다"를 다시 탭해
문장이 자연스러운 활용형으로 나오는지, 그리고 두 문장 다 화면에서 실제로
두 줄 이상으로 보이는지 확인해 주세요. 5줄까지 늘어난 지침이 6-C 에서
줄인 목적(단순화)과 다시 부딪히는지도 같이 봐 주시면 좋겠습니다 — 여전히
섞이거나 어색하면, 짧게 유지하기와 품질 사이에서 다시 조율이 필요합니다.

---

## 6-E. "두 줄 이상"이 너무 길게 나와, 글자 수 상한으로 뒤집음

**요청** — 6-D 에서 "두 줄 이상"으로 늘렸더니 문장이 너무 길어졌다는 피드백.
아래쪽을 늘리는 것("최소 두 줄")이 아니라 **위쪽을 막는 것("50자 이내")**으로
방향을 뒤집어 달라는 요청.

**한 것** — instructions·`@Guide` 두 곳 모두 "두 줄 이상 되도록 자세히"를
"50자 이내로 짧고"로 바꿨다. 줄 수(5줄)는 그대로다.

```swift
let instructions = """
    당신은 70대 어르신에게 낱말을 설명하는 선생님입니다.
    낱말을 넣은 문장(1번)과 그 자리에 뜻만 넣은 문장(2번)을 만듭니다.
    낱말·뜻의 조사나 어미는 문장에 자연스럽게 맞춰 바꿔도 됩니다.
    각 문장은 50자 이내로 짧고 자연스럽게 씁니다.
    1번에는 낱말만, 2번에는 뜻만 들어가야 하며, 맞춤법과 문법에 맞게 씁니다.
    """
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 문장 길이가 이번엔
적당한지(50자 안팎), "기리다" 같은 낱말도 여전히 자연스러운 활용형으로
나오는지 확인해 주세요. 온디바이스 모델에게 글자 수 제한은 "지침"일 뿐
강제되는 값은 아니라, 가끔 넘길 수도 있습니다 — 계속 많이 넘으면 알려주세요.

---

## 6-F. 상한만 있던 글자 수에 하한도 두고, "구체적인 예시"임을 못박음

**요청** — 6-E 의 "50자 이내"(상한만 있음)를 **"30자 이상 50자 이내"**로
범위를 두고, 이전 지침(6-D 이전)이 예시를 구체적으로 요구했던 것이 결과가
좋았다고 보고 **"구체적인 예시"임을 지침에 다시 명시**해 달라는 요청.

**한 것** — instructions 넷째 줄과 `@Guide` 설명 둘 다에 "구체적인 예시/예문"과
"30자 이상 50자 이내"를 같이 넣었다. 줄 수는 여전히 5줄, `@Guide`도 한 줄씩
그대로다.

```swift
let instructions = """
    당신은 70대 어르신에게 낱말을 설명하는 선생님입니다.
    낱말을 넣은 문장(1번)과 그 자리에 뜻만 넣은 문장(2번)을 만듭니다.
    낱말·뜻의 조사나 어미는 문장에 자연스럽게 맞춰 바꿔도 됩니다.
    각 문장은 실생활에서 있을 법한 구체적인 예시로, 30자 이상 50자 이내로 씁니다.
    1번에는 낱말만, 2번에는 뜻만 들어가야 하며, 맞춤법과 문법에 맞게 씁니다.
    """
```

```swift
@Guide(description: "낱말을 자연스럽게 넣은 구체적인 예문. 30~50자로 쓴다. 뜻은 넣지 않는다")
let wordSentence: String

@Guide(description: "뜻을 자연스럽게 넣은 구체적인 예문. 30~50자로 쓴다. 낱말은 넣지 않는다")
let glossSentence: String
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 문장이 30~50자 범위
안에서 나오는지, "구체적인 예시"라는 말대로 실생활 장면이 담기는지(추상적인
설명으로 흐르지 않는지) 확인해 주세요.

---

## 6-G. 글자 수 범위를 30~50자에서 50~80자로 다시 늘림

**요청** — 30자 이상 50자 이내였던 범위를 **50자 이상 80자 이내**로 올려
달라는 요청. instructions·`@Guide` 세 곳의 숫자만 바꿨다.

```swift
각 문장은 실생활에서 있을 법한 구체적인 예시로, 50자 이상 80자 이내로 씁니다.
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 문장이 50~80자 범위
안에서 나오는지 확인해 주세요.

---

## 6-H. "기리는"에서 1번 문장이 낱말 없이 뜻만으로 나온 것을 고침

**문제** — 낱말 "기리는" · 뜻 "고맙게 생각하는"에서, 1번 문장(wordSentence)이
낱말 "기리는"을 안 쓰고 **뜻 "고맙게 생각하는"만 갖다 썼다.** "1번에는
낱말만, 2번에는 뜻만"이라는 규칙이 있었지만("들어가야 하며"라는 약한
표현), "기리다"처럼 활용이 필요한 동사에서 모델이 낱말과 뜻을 헷갈린 것으로
보인다.

**한 것** — instructions 마지막 줄과 `wordSentence`의 `@Guide`를 "반드시
그대로 들어가야 한다"는 더 강한 표현으로 바꿨다. `glossSentence` 쪽은 그대로
뒀다(이번엔 1번만 문제였다).

```swift
@Guide(description: "낱말이 반드시 그대로 포함된 구체적인 예문. 50~80자로 쓴다. 뜻은 넣지 않는다")
let wordSentence: String
```
```swift
1번에는 낱말이 반드시 그대로 들어가야 하고, 2번에는 뜻만 들어가야 하며, 맞춤법과 문법에 맞게 씁니다.
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 "기리는"을 다시 탭해
1번 문장에 낱말이 실제로 들어가는지 확인해 주세요. 6-B 때도 비슷한 시도를
했다가 다른 부작용(문장이 부자연스러워짐)이 났던 적이 있어서, 이번에도
1번뿐 아니라 2번·전체 문장의 자연스러움에 새 문제가 생기지 않는지 같이
봐 주시면 좋겠습니다.

---

## 6-I. 어려운 낱말은 questions.json 에 참고 예문을 미리 적어 둘 수 있게 함

**요청** — "기리다"처럼 Foundation Models 가 활용형을 잘 못 만드는 낱말은,
`questions.json` 의 낱말 사전 항목에 사람이 미리 예문 하나를 적어 두고
있으면 그걸 참고해서 만들게 하고, 없으면(대부분) 지금처럼 모델이 알아서
만들게 해 달라는 요청. 사용자가 예시로 든 모양:

```json
"glossary": [
  { "word": "기리는", "gloss": "고맙게 생각하는",
    "exampleSenteces: "우리는 우리를 구해준 분들을 기립니다" }
]
```

**먼저 발견한 것 — 이 예시를 그대로 `questions.json` 에 넣어 보신 상태였는데,
따옴표가 하나 빠져 JSON 문법 자체가 깨져 있었다** (`"exampleSenteces: "..."` —
키 문자열이 콜론 앞에서 안 닫힘). `python3 -m json.tool` 같은 걸로도 바로
파싱 에러가 나는 상태라, **고치지 않았으면 questions.json 전체가 안 읽혀서
어머니가 쓰시는 30문항 전부가 로드 실패했을 것**이다. 다른 무엇보다 먼저
이 오타부터 고쳤다.

**이름을 그대로 안 쓰고 `referenceSentence`로 바꾼 이유** — `GlossaryEntry`에
이미 `examples: [String]`(비슷한 낱말 목록, 「기리다/기억하다」 같은 낱말들)
필드가 있다. 사용자가 제안한 `exampleSenteces`(예문 하나, 문자열)는 이름이
`examples`와 너무 비슷해서 나중에 "낱말 목록"과 "문장 하나"를 헷갈리기 쉽다고
보고, 의미가 분명한 `referenceSentence`(단수, `String?`)로 바꿨다.

**한 것:**

1. `Question.swift` — `GlossaryEntry`에 `var referenceSentence: String? = nil`을
   추가하고 `decodeIfPresent`로 해독한다(없으면 nil, 대부분의 경우).
2. `GlossaryComposer.swift` — `composeExample(...)`에 `referenceSentence: String? = nil`
   매개변수를 추가했다. 있으면 프롬프트에 `참고 예문: ...` 한 줄을 더 붙이고,
   instructions 에도 "참고 예문이 주어지면 그 문장을 최대한 활용해 1번을
   만듭니다"를 한 줄 추가했다(총 6줄 — 5줄 기준을 살짝 넘겼다).
3. `QuestionScreen.swift` — `onTapWord` 안에서 `item.glossary`를 낱말로 찾아
   `referenceSentence`를 꺼내 `composeExample(...)`에 넘긴다.
4. `questions.json` — 문항 16번 "기리는"에 `referenceSentence: "우리는 우리를
   구해준 분들을 기립니다"`를 넣었다(오타 고치고 이름도 바꿔서).

```swift
// GlossaryComposer.swift
if let referenceSentence {
    prompt += "\n참고 예문: \(referenceSentence)"
}
```

**검증 상태** — 컴파일 확인은 못 한다. 다시 빌드해서 "기리는"을 탭했을 때
1번 문장이 참고 예문("우리는 우리를 구해준 분들을 기립니다")과 비슷한
모양으로 나오는지, 그리고 참고 예문이 없는 다른 낱말(예: "최초")은 전처럼
잘 만들어지는지 확인해 주세요. `questions.json` 이 이번에 문법 깨진 채로
저장돼 있었던 적이 있었으니, 앞으로 이 파일을 직접 고치실 때는 저장 전에
`python3 -m json.tool KCT/Data/questions.json > /dev/null`(또는 아무
JSON 검사기)로 한 번 확인해 보시길 권합니다.

---

## 진행 순서 제안

1. ~~`questions.json`에 낱말 사전 채우기~~ **완료** (30문항, 39낱말 전부)
2. ~~점선 밑줄 + 탭 감지 + Foundation Models 예문~~ **거의 완료** — 2단계의 밑줄 버그
   한 줄만 고치면 끝
3. 3~5단계(iOS 기본 시트로 낱말 뜻 모달 띄우기)를 새로 넣기 — 이 상태로 어머니와
   한 번 테스트해도 됩니다

## 판단이 갈리는 지점 — 한눈에

| 지점 | ⓐ | ⓑ | 제 의견 |
|---|---|---|---|
| 시트 높이 | `.height(180)` 하나만 | `[.height(180), .medium]` 두 단계 | ⓑ — 예문이 길면 위로 끌어 더 볼 수 있게 |
| 예문 생성 시점 | 열 때마다 새로 생성 | 처음 한 번 생성 후 저장 | ⓑ — 대기시간·일관성 |
| 예문 소스 | AI 생성 우선, 없으면 JSON 목록 | JSON 목록 있으면 그것만 | ⓐ — 목록 있는 낱말이 5개뿐이라 |
| 기기 미지원 시 | 기능을 아예 숨김 | JSON의 gloss(+examples 있으면 그것도) 보여줌 | ⓑ — 뭐라도 보여주는 게 나음 |

> 「펼쳤을 때 패널 높이」·「화면 다른 곳 탭」은 3~5단계를 iOS 기본 시트로 바꾸면서
> 판단할 필요 자체가 없어졌습니다(시스템이 정합니다) — 그래서 이 표에서 빠졌습니다.

## 참고 자료

- [Getting Started with Foundation Models in iOS 26 (appcoda.com)](https://www.appcoda.com/foundation-models/)
- [Exploring the Foundation Models framework (createwithswift.com)](https://www.createwithswift.com/exploring-the-foundation-models-framework/)
- [WWDC26 — Bring an LLM Provider to the Foundation Models Framework (developer.apple.com)](https://developer.apple.com/videos/play/wwdc2026/339/)

> **확인 못 한 것**
> - 어머니 폰이 실제로 Apple Intelligence를 지원하는 기종인지
> - 펼쳤을 때 260pt가 실제 화면에서 "다음" 버튼을 얼마나 가리는지
> - `Coordinator`가 `NSLayoutManager`로 다중 낱말(최대 8개, 6번 문항)의 탭을 실제
>   기기에서 정확히 구분해내는지 — 로직상으로는 맞지만 실기기 테스트 전까지는 추정입니다
>
> 셋 다 실제 기기로 봐야 압니다.
