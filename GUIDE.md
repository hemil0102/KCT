# GUIDE — 용어집

`CLAUDE.md` 규칙 11에 따라, ⭐️GUIDE⭐️ 로 안내한 용어를 모아둔 곳입니다.

무엇을 하고 싶은지는 아는데 그것을 뭐라고 부르는지 몰라 검색이 막힐 때 찾아보는 사전입니다. 용어를 모르면 질문도 검색도 어려워지므로, **한글 뜻과 영문 공식 명칭을 함께** 적습니다. 새 용어가 나오면 해당 분류에 추가합니다.

---

## 이 앱의 학습 설계

KCT 고유의 개념입니다. 프레임워크 용어가 아니라 **우리가 정한 규칙**이므로, 여기가 유일한 정의 장소입니다.

| 용어 | 뜻 |
|---|---|
| **두 개의 축 (two axes)** | 이 앱 설계의 뼈대. **축 A = 문제 고유 난이도**(고정, `Question.difficulty`), **축 B = 학습 레벨**(변동, `QuestionProgress.mode`). 섞으면 설계가 무너집니다 |
| **난이도 사다리 (ladder)** | 축 B의 단계. 2지선다 → O/X → 4지선다 → 직접입력 → 마스터. 모든 문제가 같은 사다리를 탑니다 |
| **묻는 방식 (asking mode)** | 사다리의 각 칸. 같은 문제를 얼마나 쉽게 묻는지이지 문제가 어려운 정도가 아닙니다 |
| **승급 · 강등 (promotion / demotion)** | 맞히면 사다리 한 칸 위, 틀리면 한 칸 아래. 세션(날짜)을 넘어 유지됩니다 |
| **마스터 (mastered)** | 직접입력까지 맞힌 상태. 단 **마스터 후에도 랜덤으로 계속 복습 출제**합니다 |
| **도입 (introduced)** | 한 번이라도 출제된 적 있는지. 신규 문제와 복습 문제를 가르는 기준 |
| **간격 반복 (spaced repetition)** | 복습 간격을 정답 여부에 따라 늘리거나 줄이는 학습법. 틀리면 즉시, 맞으면 하루 뒤, 마스터는 멀리 |
| **다음 출제 예정 시점 (due date)** | 그 간격을 저장하는 값 (`nextDueAt`). 스케줄러가 이른 순으로 우선 출제합니다 |
| **단원 라운드로빈 (round-robin)** | 신규 문제를 쉬운 순으로 두되 단원을 번갈아 도입하는 것. 한 단원만 몰아서 나오지 않게 합니다 |
| **라운드 셰이핑 (round shaping)** | 회차의 **첫·마지막 문제를 격려용 2지선다**로 고정. 쉽게 시작하고 쉽게 끝나야 계속하고 싶어집니다. 진척에 반영하지 않습니다 |
| **오답 보기 (distractor)** | 선다형에서 정답이 아닌 선택지. **다른 문제의 정답에서 뽑아 오므로** 문제집이 바뀌면 자동으로 따라 바뀝니다 |
| **진술문 (statement)** | O/X에서 의문문 대신 보여주는 평서문. `statementFormat` 의 `{답}` 자리에 답이 들어갑니다. 의문문을 자동 변환하면 어색해서 문제마다 직접 적어 둡니다 |
| **묻는 대상 (focus)** | 질문이 무엇을 묻고 있는지. 형광펜으로 칠해 어르신이 질문의 핵심을 놓치지 않게 합니다 |
| **스토리 게이트 (story gate)** | 앞으로 "읽은 콘텐츠의 문제만 개방"하려고 미리 뚫어둔 자리. 지금은 `isUnlocked` 가 항상 `true` |
| **이음새 (seam)** | 나중에 구현을 갈아끼우려고 미리 만들어 둔 경계. 스토리 게이트와 `QuestionSource` 가 그 예입니다 |

---

## SwiftUI와 상태

| 용어 | 뜻 |
|---|---|
| **뷰 (View)** | 화면의 한 조각. SwiftUI에서는 **값(struct)** 이라서 수시로 다시 만들어집니다. 그래서 상태를 뷰 안에 그냥 두면 사라집니다 |
| **`@State`** | 뷰가 다시 만들어져도 살아남는 값. 그 뷰가 **소유하는** 상태입니다 |
| **`@Observable`** | 클래스를 관찰 가능하게 만드는 매크로. 이 안의 값이 바뀌면 그것을 읽는 뷰만 다시 그려집니다 |
| **`@Environment`** | 위에서 아래로 내려보낸 값을 꺼내 쓰는 것. 뷰마다 손으로 넘기지 않아도 됩니다 |
| **`@ViewBuilder`** | 여러 뷰를 묶어 하나처럼 돌려주게 해주는 표시. `if`·`switch` 로 화면을 갈라 쓸 때 필요합니다 |
| **`.task`** | 뷰가 나타날 때 비동기 작업을 시작하고, 사라질 때 **자동 취소**해 주는 수식어 |
| **`UIViewRepresentable`** | SwiftUI가 아직 못 하는 일을 UIKit 뷰를 감싸서 해결하는 통로. 이 앱에서는 한글 줄바꿈 때문에 `UILabel` 을 감쌉니다 |
| **햅틱 (haptic)** | 진동으로 알려주는 것. 소리를 못 듣거나 화면을 놓쳤을 때의 보조 신호 |

---

## SwiftData (영구 저장)

| 용어 | 뜻 |
|---|---|
| **`@Model`** | 이 클래스를 **디스크에 저장되는 것**으로 만드는 매크로. 앱을 껐다 켜도 값이 남습니다 |
| **모델 컨테이너 (ModelContainer)** | 저장소 자체. 앱 시작할 때 "어떤 타입들을 저장할지" 알려주며 만듭니다 |
| **모델 컨텍스트 (ModelContext)** | 저장소와 대화하는 창구. `insert`·`delete` 하고 `save()` 로 확정합니다 |
| **`@Query`** | 저장소에서 값을 읽어와 뷰에 꽂아주는 것. 데이터가 바뀌면 화면이 따라 바뀝니다 |
| **`@Attribute(.unique)`** | 이 값이 겹치는 행을 만들지 못하게 하는 제약. 문제 하나당 진척 하나를 보장합니다 |
| **인메모리 (inMemory)** | 디스크에 쓰지 않고 메모리에만 두는 모드. 미리보기와 테스트에 씁니다 |

> **⚠️ 함정** — `@Query` 는 지우고 바로 다시 만드는 흐름에서 **즉시 갱신되지 않을 수 있습니다.** 그럴 때는 직접 만든 값을 쓰고 `@Query` 결과를 믿지 않습니다.

---

## FoundationModels (온디바이스 모델)

| 용어 | 뜻 |
|---|---|
| **온디바이스 모델 (on-device model)** | 기기 안에서 도는 언어 모델. 네트워크가 필요 없고 데이터가 기기를 떠나지 않습니다 |
| **`@Generable`** | "모델이 이 구조체 모양으로 답을 만들어라" 는 표시. 문자열을 직접 파싱하지 않아도 됩니다 |
| **`@Guide`** | 각 항목을 어떻게 채울지 모델에게 알려주는 설명 |
| **유도 생성 (guided generation)** | 위 둘을 써서 **타입 안전하게** 결과를 받는 방식 |
| **지침 (instructions)** | 세션 전체에 걸리는 규칙. 개별 프롬프트보다 우선 적용됩니다 |
| **가용성 (availability)** | 이 기기에서 모델을 쓸 수 있는지 (`isAvailable`). **`false` 인 기기가 있습니다** |
| **환각 (hallucination)** | 모델이 없는 것을 지어내는 것. 하이라이트처럼 "지문에 실제로 있어야 하는" 값은 반드시 검증합니다 |
| **폴백 (fallback)** | 모델이 실패했을 때 대신 동작하는 안전망. 이 앱에서는 규칙 기반 추출과 결정적 채점 |
| **결정적 채점 (deterministic grading)** | 모델 없이 값을 그대로 비교해 판정하는 것. 선다형·O/X는 이걸로 충분하고 훨씬 빠릅니다 |
| **의미 채점 (semantic grading)** | 표현이 달라도 뜻이 같으면 정답으로 보는 것. 직접입력에만 모델을 씁니다 |
| **워밍 (warming)** | 필요해지기 전에 미리 계산해 캐시에 채워 두는 것. 화면을 막지 않도록 백그라운드에서 합니다 |
| **캐시 무효화 (cache invalidation)** | 원본이 바뀌면 저장된 결과를 버리는 것. 이 앱은 질문 텍스트의 SHA256 해시를 비교합니다 |

| **제약 해독 (constrained decoding)** | 모델이 토큰을 뽑을 때 **문법에 맞는 후보만 남기는** 것. 유도 생성이 실제로 동작하는 방식입니다 |
| **`@Guide(.anyOf([...]))`** | 값의 후보를 **런타임에** 목록으로 정하는 것. 정답·문항 id 처럼 문제집에서 나오는 값에 씁니다 |
| **`DynamicGenerationSchema`** | 구조 자체를 런타임에 정하는 것. 컴파일 때 모양을 모를 때만 씁니다 |

> **⚠️ 함정** — Swift의 `hashValue` 는 **실행할 때마다 값이 달라져** 저장용으로 쓸 수 없습니다. 앱을 껐다 켜도 같아야 하므로 SHA256을 씁니다.

### 무엇을 모델에게 맡기고 무엇을 코드가 하나

**기준은 「어렵나」가 아니라 「답이 하나로 정해지나」다.**

| | 코드가 한다 | 모델이 한다 |
|---|---|---|
| 답 | **하나뿐이다** | 여럿이다 |
| 예 | 글자가 같은가 · 몇 글자인가 · 날짜 계산 | 뜻이 통하는가 · 무슨 종류인가 · 문장 쓰기 |
| 틀리면 | 안 틀린다 | 다시 만나 회복된다 |

답이 하나뿐인 일은 **95%를 맞혀도 못 씁니다.** 나머지 5%가 언제 나올지 모르고, 어제 맞힌 것을 오늘 틀려도 원인을 못 찾습니다.

#### 한글은 특히 그렇다

모델은 글자가 아니라 **조각(token)** 으로 읽습니다.

```
사람이 보는 것    고 / 죠 / 선
모델이 보는 것    [고죠] [선]      ← 「죠」를 따로 꺼낼 수가 없다
```

「몇 번째 글자가 다른가」는 **물어볼 수단 자체가 없습니다.** 능력이 부족한 것이 아니라 입력이 그렇게 생기지 않았습니다.

> **실제로 겪은 것 (2026-09-10)** — 「고죠선」·「태국가」·「단검신화」·「단검왕검」. 전부 **한 글자만** 다른데 지침을 여섯 번 고치는 동안 계속 통과했습니다. 반면 아예 다른 답(「홍길동/안익태」·「매화/무궁화」)은 **10건 모두** 제대로 걸렀습니다. **글자는 못 보고 뜻은 봅니다.**

#### 그러면 계산은 누가 — tool 이다

ChatGPT가 계산을 잘하는 것은 모델이 계산해서가 아니라 **파이썬을 부르기 때문**입니다. tool calling 은 **모델에게 손을 붙여 주는 것**입니다.

다만 갈림길이 하나 있습니다.

```
모델이 "지금 계산이 필요하다"를 스스로 판단해야 할 때   →  tool
우리가 이미 알고 있을 때                            →  그냥 코드
```

직접입력 채점이면 **무조건** 글자 비교가 필요하다는 것을 우리가 미리 압니다. 그러니 도구를 쥐여 줄 이유가 없습니다 — 코드가 먼저 하고 애매한 것만 넘깁니다.

대화형(6차 7단계)은 다릅니다. 어머니가 무엇을 물을지 모르므로 **모델이 스스로 골라야** 하고, 거기가 tool 이 필요한 자리입니다. **갈림길은 「대화형이냐」가 아니라 「범위가 닫혀 있느냐」입니다.**

### `@Generable` 이 정확히 무엇을 하나

**매크로입니다.** 붙이면 컴파일할 때 코드가 자동으로 생깁니다. `Codable` 이 `init(from:)` 을 만들어 주는 것과 같은 종류의 일입니다.

| 생기는 것 | 하는 일 |
|---|---|
| `static var generationSchema` | 이 타입의 **모양 설명서**. 모델에게 보낸다 |
| `init(_ content: GeneratedContent)` | 모델이 뱉은 것을 **이 타입으로 되돌린다** |
| `PartiallyGenerated` | 스트리밍용 — 아직 다 안 온 중간 상태 |

앞의 둘이 `Codable` 의 `encode` / `decode` 와 정확히 같은 짝입니다.

#### 방향 — 나가는 것은 값이 아니라 모양이다

여기를 뒤집어 이해하기 쉽습니다. JSON 으로 나가는 것은 **우리 데이터가 아니라 타입의 모양**입니다.

```
① 요청할 때
   AnswerCheck.generationSchema  →  JSON Schema 로 바뀌어 모델의 문맥 창에 들어간다
     { "type":"object",
       "properties": { "isCorrect": {"type":"boolean"},
                       "basis":     {"enum":["exactMatch","differentName",...]} } }

② 모델이 답할 때
   토큰을 하나 뽑을 때마다 이 스키마를 만족시킬 수 있는 것만 후보로 남는다  ← 제약 해독
     →  GeneratedContent 로 받아  →  init(_:) 이 인스턴스로 되돌린다

③ 우리 손에는
   let check: AnswerCheck        ← 문자열 파싱이 한 줄도 없다
```

> **스키마는 문맥 창을 먹습니다.** Apple 문서의 표현 — *"the framework converts its type and format information to a JSON schema and provides it to the model. **This contributes to the available context window size.**"*
>
> 그래서 프로퍼티 이름은 짧고 명확하게, `@Guide(description:)` 은 **품질이 나아질 때만** 붙입니다. 타입이 커지면 `LanguageModelError.contextSizeExceeded` 가 납니다.

#### 안 붙이면

```swift
let text = try await session.respond(to: prompt).content   // String
// "정답입니다. 근거는 exactMatch 입니다"  ← 이번엔 이렇게 왔다
// "네, 맞아요!"                          ← 다음엔 이렇게 올 수 있다
```

붙이면 `respond(to:generating: AnswerCheck.self)` 로 **타입이 그대로** 옵니다. **파싱을 잘하는 것이 아니라 파싱이 필요 없게 만드는 것**입니다.

#### `@Guide` 는 그 설명서에 주석을 다는 것

`@Generable` 이 뼈대를 만들고, `@Guide` 가 칸마다 설명이나 제약을 붙입니다. `description:` 은 **부탁**이고, `.count(3)` · `.anyOf([...])` · 열거형은 **문법**입니다.

#### `Codable` 과 나란히

| | `Codable` | `Generable` |
|---|---|---|
| 상대 | JSON 파일 | 언어 모델 |
| 내보내는 것 | **값** (`{"id":1}`) | **모양** (`{"type":"object",...}`) |
| 들여오는 것 | JSON → 타입 | 모델 출력 → 타입 |
| 자동 생성 | `init(from:)` · `encode(to:)` · `CodingKeys` | `generationSchema` · `init(_:)` · `PartiallyGenerated` |
| 손으로 덮어쓰기 | 자주 한다 (`decodeIfPresent` 건) | 거의 안 한다. 필요하면 `DynamicGenerationSchema` |
| 틀리면 | `throws` | **모양은 안 틀린다.** 내용은 여전히 틀릴 수 있다 |

**마지막 줄이 이 앱에서 가장 중요합니다.** `@Generable` 은 **모양을 보장하지 내용을 보장하지 않습니다.** `isCorrect: Bool` 에는 반드시 `true`/`false` 가 오지만 **그 판정이 맞는지는 별개**입니다. 「고죠선」이 통과된 것은 모양 문제가 아니라 지침 문제였습니다.

### `@Generable` 을 열거형에 붙인다는 것

`@Generable` 은 **구조체만이 아니라 열거형에도** 붙습니다. 붙이면 모델은 **그 케이스 중 하나만** 고를 수 있게 됩니다.

```swift
@Generable
enum CheckBasis {
    /// 글자가 완전히 같다
    case exactMatch
    /// 소리는 같은데 글자가 다르다 — 「고죠선」
    case phoneticMatch
    /// 아예 다른 이름이다 — 「단군신화」
    case differentName
    /// 못 정하겠다
    case undecided
}
```

**「검사」가 아니라 「불가능하게 만들기」입니다.** 문자열로 받고 나서 값을 확인하는 것과 다릅니다. 제약 해독이 걸려 목록 밖의 값을 만드는 토큰이 애초에 후보에서 빠집니다.

| | 문자열 + 지침 | 열거형 `@Generable` |
|---|---|---|
| 성격 | **부탁** | **문법** |
| 목록 밖의 값 | 나올 수 있다 | 나올 수 없다 |
| 받는 쪽 | `if` 로 비교, 오타는 `default` 로 샌다 | `switch` — 빠뜨린 케이스를 컴파일러가 잡는다 |

**왜 이 앱에서 중요한가** — 지금까지 겪은 문제가 전부 「모델이 없는 값을 지어낸다」였습니다. 케이스 이름 자체(`differentName`)를 모델이 읽으므로 **지침이 짧아지는 덤**도 있습니다.

**`undecided` 를 반드시 둡니다.** 「모른다」를 말할 자리가 없으면 모델은 아무거나 고릅니다. 환각이 난 자리는 전부 **빈칸을 채우라고 시킨 곳**이었습니다.

**쓰는 자리** — 자유 문장과 고정 선택을 한 타입에서 나눕니다.

```swift
@Generable
struct AnswerCheck {
    let isCorrect: Bool
    let reason: String      // 자유 — 로그에 남겨 사람이 읽는다
    let basis: CheckBasis   // 고정 — 코드가 switch 로 분기한다
}
```

**연관값도 됩니다** (iOS 26.0~). WWDC25 「Deep dive into the Foundation Models framework」의 예시입니다.

```swift
@Generable
enum Encounter {
    case orderCoffee(String)
    case wantToTalkToManager(complaint: String)
}
```

모델이 **케이스를 고르고 딸린 값까지 채웁니다.** 「무엇을 물었나 + 무엇에 대해」를 한 번에 받고 싶을 때 쓸 수 있습니다. 다만 케이스마다 스키마가 갈라져 **문맥 창을 더 씁니다.** 지금 이 앱에 필요한 것은 전부 연관값 없는 단순 케이스라 굳이 쓰지 않습니다.

> **열거형이 안 맞는 자리** — 케이스는 **컴파일 때 정해져 있어야** 합니다. 정답 후보처럼 문제집에서 나오는 값은 `@Guide(.anyOf(catalog.answerPool))` 로 런타임에 목록을 줍니다. 제약 해독이 걸리는 것은 똑같고, 받는 타입만 `String` 입니다.

**참고** — [Generable](https://developer.apple.com/documentation/foundationmodels/generable) · [Deep dive into the Foundation Models framework (WWDC25 301)](https://developer.apple.com/videos/play/wwdc2025/301/)

---

## 한국어 조판과 낭독

어르신 접근성의 핵심입니다. 기능이 아니라 **읽을 수 있느냐**의 문제입니다.

| 용어 | 뜻 |
|---|---|
| **줄바꿈 전략 (line break strategy)** | 어디서 줄을 끊을지 정하는 규칙 |
| **`hangulWordPriority`** | Apple이 한국어 UI에 권장하는 줄바꿈 전략. **단어 중간에서 끊지 않습니다.** SwiftUI `Text` 는 지원하지 않아 `UILabel` 을 감싸야 합니다 |
| **워드 조이너 (WORD JOINER, U+2060)** | 폭이 없는 특수 문자. 두 글자 사이에 끼우면 그 자리에서 줄이 갈라지지 않습니다. "2333년" 이 숫자와 한글 경계에서 끊기는 것을 막습니다 |
| **어트리뷰티드 문자열 (NSAttributedString)** | 글자마다 색·굵기·밑줄을 따로 줄 수 있는 문자열. 형광펜과 강조를 이걸로 그립니다 |
| **받침 (final consonant)** | 한글 음절의 마지막 자음. "을/를", "이/가" 를 고르는 기준이라 코드로 판별해야 합니다 |
| **음성 합성 (speech synthesis)** | 글자를 소리로 읽어주는 것. `AVSpeechSynthesizer` |
| **음질 (voice quality)** | `default` < `enhanced` < `premium`. **좋은 음질은 사용자가 따로 내려받아야** 기기에 존재합니다 |
| **오디오 세션 (AVAudioSession)** | 앱이 소리를 어떻게 쓸지 시스템에 알리는 설정. `.playback` 으로 해야 **무음 스위치와 무관하게** 들립니다 |
| **더킹 (ducking)** | 다른 앱의 소리를 잠시 줄이는 것 (`.duckOthers`) |
| **동적 타입 (Dynamic Type)** | 사용자가 기기에서 정한 글자 크기에 맞춰 글씨가 커지는 것 |
| **터치 타깃 (touch target)** | 손가락으로 누를 수 있는 최소 영역. 어르신에게는 56pt 이상을 씁니다 |

---

## 문서화와 주석

| 용어 | 뜻 |
|---|---|
| **DocC (Documentation Compiler)** | Apple의 문서화 도구. `Word`의 `.docx` 와는 무관합니다 |
| **문서화 주석 (documentation comment)** | `//` 대신 `///` 세 개로 쓰는 주석. Xcode가 문서로 인식합니다 |
| **퀵 헬프 (Quick Help)** | 심볼을 `Option+클릭` 하면 뜨는 설명 팝업. `///` 주석이 여기 표시됩니다 |
| **요약 / 설명 (summary / discussion)** | 문서화 주석의 첫 문장 / 빈 줄 뒤 문단. Xcode가 둘을 구분해 취급합니다 |
| **콜아웃 (callout)** | `- Note:`, `- Important:` 같은 표시. 색깔 상자로 강조됩니다 |
| **심볼 링크 (symbol link)** | 백틱 두 개로 감싼 타입 이름. DocC가 실제 링크로 만들어 줍니다 |
| **DocC 카탈로그 (`.docc`)** | 문서의 랜딩 페이지와 아티클을 담는 폴더. 코드 주석만으로는 못 하는 **읽는 순서**를 여기서 정합니다 |
| **아티클 (article)** | 특정 타입에 매이지 않은 설명 글. 학습 경로를 놓는 데 씁니다 |
| **마크 (`// MARK: -`)** | 코드를 구획하는 주석. Xcode 점프바에 목차가 생깁니다 |

> **⚠️ 함정** — `private` 멤버는 **DocC 문서에 나오지 않습니다.** 좋은 설명이 private 함수에만 있으면 문서에서 사라집니다.

---

## Xcode와 프로젝트

| 용어 | 뜻 |
|---|---|
| **타겟 (target)** | 빌드 결과물 하나. 앱 본체, 테스트 묶음이 각각 타겟입니다 |
| **타겟 멤버십 (Target Membership)** | 이 파일을 어느 타겟에 포함할지 정하는 것 |
| **파일 시스템 동기화 그룹 (file-system synchronized group)** | Xcode 16부터의 방식. 폴더에 있는 파일이 자동으로 프로젝트에 포함됩니다. **KCT가 이 방식입니다** |
| **번들 (bundle)** | 앱으로 포장된 결과물(`KCT.app`). 리소스는 폴더 구조와 무관하게 **루트에 평평하게** 놓입니다 |
| **`project.pbxproj`** | Xcode 프로젝트의 설정이 담긴 파일. 직접 편집하면 깨지기 쉽습니다 |
| **파생 데이터 (DerivedData)** | 빌드 중간 산출물이 쌓이는 폴더. Git에 올리지 않습니다 |
| **미리보기 (`#Preview`)** | 코드 일부만 띄워 보는 것. 저장소를 인메모리로 두면 실제 기록을 건드리지 않습니다 |
| **Swift Testing** | `@Test`·`#expect` 를 쓰는 새 테스트 방식. XCTest 를 대체합니다 |

> **⚠️ 함정** — 동기화 그룹이라 `KCT/KCT/` 안의 파일은 확장자를 Xcode가 모르면 **앱 번들로 복사**됩니다. 문서·메모는 저장소 루트에 둡니다.

---

## AI 협업과 세션

`CLAUDE.md` 규칙 12·13이 다루는 것들입니다. Swift 용어는 아니지만, 이 저장소에서 AI와 작업할 때 반복해서 나옵니다.

| 용어 | 뜻 |
|---|---|
| **토큰 (token)** | 모델이 글을 세는 단위. 대략 한글 한 글자가 1개쯤 됩니다. 파일을 열면 그 분량만큼 토큰을 씁니다 |
| **컨텍스트 윈도우 (context window)** | 한 세션에서 모델이 한 번에 볼 수 있는 대화와 파일의 총량. 여기가 꽉 차면 앞부분이 밀려납니다 |
| **압축 (compaction)** | 대화가 길어질 때 앞부분을 요약으로 갈아 끼우는 것. 이때 세부가 사라지므로 `PROGRESS.md` 같은 파일이 안전망이 됩니다 |
| **프롬프트 캐싱 (prompt caching)** | 같은 세션에서 반복 전송되는 앞부분을 재사용해 비용을 낮추는 구조. 긴 세션이 계산만큼 비싸지지는 않는 이유입니다 |
| **재진입 비용** | 새 세션이 프로젝트를 다시 파악하는 데 드는 비용. `PROGRESS.md` 는 이걸 줄이려고 있습니다 |
| **인수인계 파일 (handoff)** | 세션이 끊겨도 다음 세션이 이어받을 수 있게 현재 상태를 적어 두는 파일. 이 저장소에서는 `PROGRESS.md` |
| **서브에이전트 (subagent)** | 넓은 조사를 대신 시키는 별도의 AI. 파일을 여럿 뒤져도 **결론만** 돌아오므로 본 대화가 무거워지지 않습니다 |
| **MCP 도구** | AI가 Xcode를 직접 조작하는 통로. `XcodeRead`·`XcodeGrep`·`BuildProject` 등. 셸 명령보다 싸고 승인 절차가 없습니다 |
