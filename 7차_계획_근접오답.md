# 7차 계획 — 선다형 오답을 헷갈리게 만든다

> 2026-09-14. 2지선다·4지선다가 너무 쉽다는 것에서 시작. 원인은 이미 알려져 있었다 —
> `answerPool(excludingCategory:)`가 정답과 **다른 계열**에서만 오답을 뽑아, 소거법만으로
> 풀리기 때문이다. 이번엔 반대로, **닮은 답끼리 겨루게** 만든다.

---

## 1. 지금 무슨 일이 일어나고 있나

`SessionBuilder.shapeRound()`가 2지선다·4지선다 두 방식 모두에 같은 오답 풀을 건넨다.

```swift
answerPool: catalog.answerPool(excludingCategory: question.category)
```

`QuestionCatalog.answerPool(excludingCategory:)`는 **정답과 같은 계열(category)을 통째로
뺀** 정답 모음을 돌려준다. 예를 들어 "역사" 문제라면 오답 후보는 "대한민국"·"문화" 계열에서만
나온다 — 애초에 다른 주제라 소거법만으로 골라낼 수 있다.

이건 버그가 아니라 **의도한 설계**였다. `KCT.docc/SessionPlanning.md`에 이미 이유가 적혀
있다 — 예전엔 전체 풀에서 무작위로 뽑았더니 같은 문제도 회차마다 난이도가 들쭉날쭉했다.
2026-08-28 관찰에서 오답 8개 중 5개가 "같은 계열" 오답이 걸렸을 때 나왔고, 새로 나온
계열의 문항은 스무 개를 다 맞히셨다. **그래서 "같은 계열은 아예 빼서" 난이도를
고정시켰는데, 그 결과가 지금 보시는 "너무 쉬움"이다.**

## 2. 무엇이 참이어야 어머니에게 원하는 일이 일어나나

① **이걸 지나간 어머니에게 무엇이 일어나야 하나** — 오답을 볼 때 "그냥 훑어도 하나만
튀어서 바로 골라진다"가 아니라, **진짜로 답을 아는지 확인하고 골라야** 한다.

② **그러려면 무엇이 참이어야 하나** — 오답 후보가 정답과 **같은 종류의 답**(예: 사람 이름
문제엔 다른 사람 이름, 기념일 문제엔 다른 기념일)이어야 소거법이 안 통한다.

③ **지금 이 작업이 ②에 닿나** — 문제집에 이미 `AnswerKind`(사람·장소·기념일·물건·
문화유산·숫자 — 여섯 가지)가 문항마다 붙어 있다. 이 축으로 오답을 우선 뽑으면 ②에 바로
닿는다. 새로 만들 데이터가 없다.

## 3. 숫자로 먼저 확인한 것

지금 문제집(30문항)을 `AnswerKind`별로 세어 봤다.

| kind | 문항 수 | 자기 자신 빼면 |
|---|---:|---:|
| day (기념일) | 10 | 9 |
| person (사람) | 6 | 5 |
| number (숫자) | 5 | 4 |
| thing (물건) | 4 | 3 |
| place (장소) | 3 | 2 |
| heritage (문화유산) | 2 | 1 |

4지선다는 오답 3개가 필요하다. day·person·number·thing 넷은 **같은 kind 안에서 충분히
뽑힌다.** place·heritage 둘은 자기 자신을 빼면 1~2개뿐이라 **부족하다** — 이 둘은 지금처럼
다른 kind에서도 끌어와야 한다.

(`tags`는 30문항 전부 비어 있어서 이번엔 재료로 못 쓴다.)

## 4. 뭘 더하고 뭘 뺄까

**더하기** — `answerPool(excludingCategory:)` 대신, **같은 `AnswerKind`를 우선** 뽑는
`answerPool(preferringKind:atLeast:)`(가칭)를 새로 만든다. 같은 kind가 최소 개수
(4지선다면 3개) 이상이면 그 안에서만 뽑고, 모자라면(place·heritage) 지금처럼 전체 풀로
돌아간다 — 이 "모자라면 전체로" 규칙은 기존 `atLeast minimum` 을 그대로 재사용한다.

**빼기** — "같은 계열을 통째로 배제"하던 규칙을 없앤다. 대신 "닮은 것부터 우선, 모자라면
전체"로 바뀌니, 예전에 걱정했던 "회차마다 난이도가 들쭉날쭉함"은 그대로 안 생긴다 —
무작위가 아니라 **kind로 결정되는 규칙**이기 때문이다.

## 5. 판단이 갈리는 지점 — 정해 주세요

**2지선다(`binaryChoice`)도 똑같이 어렵게 할까?** 지금 2지선다는 사다리 맨 처음 칸이고,
**모든 신규 문제가 여기서 시작**한다. 그리고 회차의 **첫 문제·마지막 문제는 항상
2지선다로 고정**돼 있는데, 이건 어려우라고 넣은 게 아니라 "쉽게 시작하고 쉽게 끝나서
계속하고 싶어지게" 만든 격려용 자리다(`affectsProgress: false`).

- **A안 — 2지선다·4지선다 둘 다 어렵게.** 말씀하신 그대로 적용한다. 다만 회차 첫·마지막
  (격려용) 자리까지 어려워지면 그 자리의 원래 목적(쉽게 시작·쉽게 끝내기)이 흔들릴 수 있다
- **B안 — 4지선다만 어렵게, 2지선다(특히 회차 첫·마지막)는 지금처럼 쉽게 유지.** 사다리
  낮은 칸·격려용 자리의 "쉬움"은 의도된 것이니 건드리지 않고, 이미 어느 정도 아는 상태에서
  만나는 4지선다만 헷갈리게 만든다
- **C안 — 2지선다·4지선다 다 어렵게 하되, 회차 첫·마지막(격려용) 자리만 예외로 쉽게.**
  `affectsProgress` 플래그를 그대로 재사용해 나눌 수 있다

**정해야 할 것 하나만 여쭤볼게요 — A / B / C 중 어느 쪽으로 할까요?**

## 6. 건드릴 파일 (승인 후)

| 파일 | 무엇을 |
|---|---|
| `Data/QuestionCatalog.swift` | `answerPool(excludingCategory:)` 옆에(또는 대신) `answerPool(preferringKind:atLeast:)` 추가 |
| `Session/SessionBuilder.swift` | `shapeRound()`에서 새 함수를 부르도록. 5번 결정에 따라 2지선다·4지선다 분기 여부가 갈림 |

`Data/Question.swift`의 `makeChoices(count:answerPool:)` 자체는 안 바뀐다 — 오답을
"어디서" 뽑는지만 바뀌고, "몇 개 섞는지"는 그대로다.

## 7. 다 넣고 확인할 것

1. 빌드가 되는지
2. "역사·person" 문제(예: 세종대왕)의 4지선다 오답 3개가 전부 사람 이름인지, 실제로 몇 번
   출제해서 눈으로 확인
3. place·heritage처럼 항목이 적은 kind도 오류 없이 여전히 출제되는지(전체 풀로 잘
   돌아가는지)
4. 어머니께 보여드리고 — 정말 헷갈려 하시는지, 아니면 너무 어려워서 지치시는지. **이건
   코드로 예측이 안 되는 부분**이라 관찰이 필요하다(규칙 16)

## 모르는 것

- 같은 kind로 좁혀도 실제로 어머니가 "헷갈려서 틀리는" 정도가 될지, 아니면 여전히 쉽거나
  반대로 너무 어려워질지는 **써봐야 안다**
- `tags`가 비어 있어 지금은 kind까지만 손댈 수 있다. category 안에서 더 세밀한 근접
  오답(예: 같은 "국경일"이라도 날짜가 비슷한 것끼리)을 원하면 tags를 먼저 채워야 하는데,
  이건 이번 작업 범위 밖이다

---

승인해 주시면 이 계획대로 코드를 준비해서, 지난번처럼 `.swift`에 옮겨 적을 부분과 제가
바로 고칠 주석·DocC를 나눠서 드릴게요.
