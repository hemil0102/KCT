# 4차 프로토타입 · 2단계 — 해설을 만들되, 화면에는 아직 안 띄운다

> 1단계 : 틀리면 창이 뜨고 고른 답과 정답이 보인다. 해설 자리에는 「해설을 준비 중이에요.」 고정 문구.
> 2단계 : 모델이 **진짜 해설을 쓴다.** 다만 **로그에만 남긴다.**
>
> 어머니에게 보여주기 전에 20문항의 해설을 **전부 눈으로 본다.** 어르신 앞에
> 이상한 문장이 뜨는 것보다 며칠 늦게 뜨는 편이 낫다.

---

## 넣는 순서 — 이대로 하지 않으면 업로드가 통째로 막힌다

```
① Supabase 컬럼 추가        ✅ 2026-09-04 완료
② ObsRecord 에 필드 추가
③ ObsUploader Payload 에 추가     ← encodeIfPresent 아니고 encode
④ CommentaryWriter 새 파일
⑤ QuizSession 에서 부르기
⑥ 20문항 돌려서 해설 눈으로 검토
```

**서버 컬럼이 앱보다 먼저다.** 앱이 먼저 나가면 `PGRST102` 로 회차 다섯 줄이 전부 거부된다 (`Q&A.md` 「`nil` 이면 키가 사라진다」).

②③은 함께 넣는다 — ②만 넣고 빌드하면 서버로 안 나가는 필드가 하나 생길 뿐 깨지지는 않지만, 두 파일이 짝이라 나눌 이유가 없다. ④⑤도 짝이다(④ 없이 ⑤를 넣으면 빌드가 깨진다).

---

## ① Supabase — 완료

`explanation text` 컬럼을 넣고, 정책의 `with check` 마지막에 한 줄을 더했다.

```sql
and (explanation is null or length(explanation) <= 500)
```

`is null or` 로 감싼 이유 — 맞힌 문항은 `explanation` 이 `null` 이다. `length(null)` 은 `null` 이고, `with check` 에서 `null` 은 **통과가 아니라 거부**다. 감싸지 않았으면 선다형 줄이 전부 막혔다.

---

## ② `KCT/Observation/ObsRecord.swift`

세 곳에 한 줄씩.

**ⓐ `var reason: String?` 아래**

```swift
    var explanation: String?
```

**ⓑ `init` 의 `reason: String?` 아래** (윗줄 끝에 쉼표를 붙이고)

```swift
        explanation: String?
```

**ⓒ `self.reason = reason` 아래**

```swift
        self.explanation = explanation
```

`uploadedAt` 처럼 `init` 밖에 두지 않는 이유 — `uploadedAt` 은 「나중에 채워지는 값」이라 `nil` 로 시작하는 게 맞지만, `explanation` 은 **줄을 만들 때 이미 정해져 있다.**

---

## ③ `KCT/Observation/ObsUploader.swift`

`Payload` 는 손으로 쓴 `Encodable` 이라 **네 군데를 다 고쳐야** 한다. 하나라도 빠지면 조용히 안 나간다.

**ⓐ 프로퍼티 — `let reason: String?` 아래**

```swift
        let explanation: String?
```

**ⓑ `CodingKeys` — 마지막 줄을 통째로 교체**

```swift
            case wasFirstEver, affectsProgress, chosen, reason, explanation
```

**ⓒ `encode(to:)` — `try container.encode(reason, forKey: .reason)` 아래**

```swift
            try container.encode(explanation, forKey: .explanation)
```

> **`encodeIfPresent` 를 쓰지 않는다.** 값이 `nil` 이면 키를 아예 안 써서, 한 회차 다섯 줄 중 어떤 줄만 키가 하나 더 많아진다. PostgREST 는 그걸 `400 PGRST102` 로 **배열 전체**를 거부한다. 2026-08-31 에 이것 때문에 일주일 치가 막혔다.

**ⓓ `uploadPending()` 안의 `Payload(...)` — `reason: record.reason,` 아래**

```swift
                explanation: record.explanation,
```

> 원래 `reason` 이 마지막 인자라면 그 줄 끝에 쉼표를 붙이고 이 줄을 넣는다. 순서는 `Payload` 프로퍼티 선언 순서와 같아야 한다.

---

## ④ `KCT/Grading/CommentaryWriter.swift` — 새 파일

`MeaningGrader.swift` 와 뼈대가 같다. 다른 점은 셋이다.

| | `MeaningGrader` | `CommentaryWriter` |
|---|---|---|
| 묻는 것 | 맞았나 틀렸나 | 왜 그게 답인가 |
| 실패하면 | `throw` — 부르는 쪽이 오답 처리 | **`nil`** — 창은 그대로 뜬다 |
| 결과의 쓰임 | 사다리를 움직인다 | **2단계에서는 로그에만** |

`throw` 하지 않는 이유 — 해설이 없다고 창이 안 뜨면 안 된다. 해설은 **있으면 좋은 것**이지 창의 조건이 아니다.

```swift
//
//  CommentaryWriter.swift
//  KCT
//
//  역할 : 틀린 문항에 대해 "왜 그것이 답인지" 한두 문장을 만든다
//  요점 : 나무라지 않고, 정답을 한 번 더 만나게 한다. 못 만들면 조용히 nil
//
//  ── 구성 ──────────────────────────────────────────────
//  Commentary (@Generable)   모델이 만들어 주는 해설 한 덩어리
//  └─ text                   두 문장 이내의 한국어
//
//  CommentaryWriter
//  └─ write(for:chosen:) async -> String?
//
//  ── 흐름 ──────────────────────────────────────────────
//  QuizSession.gradeCurrent() 가 오답을 만나면
//    → 창(feedback)을 먼저 띄우고
//    → write(for:chosen:) 호출
//    → 지침으로 문체를 세운 세션에 문제·정답·고른 답을 넘긴다
//    → 실패하면 nil — 창은 이미 떠 있으므로 아무 일도 일어나지 않는다
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuizSession.gradeCurrent()
//  기대는 것    : FoundationModels, QuizItem(문제와 정답)
//  건드리지 않는 것 : 화면 - 2단계에서는 만든 해설을 표시하지 않는다.
//                    ObsRecord 에만 남기고 20문항을 눈으로 검토한 뒤 3단계에서 띄운다
//

import Foundation
import FoundationModels

/// 오답 해설 한 덩어리. 모델이 이 구조체 모양으로 직접 만들어 준다.
///
/// 문장을 나눠 받지 않고 `text` 하나로 받는 이유 — 어머니가 창에서 읽는 것은
/// **이어지는 두 문장**입니다. 쪼개 받아 다시 붙이면 이음매가 어색해집니다.
@Generable
struct Commentary {
    @Guide(description: "정답이 무엇인지 알려주는 한국어 설명. 두 문장 이내. 나무라는 말을 쓰지 않는다")
    let text: String
}

/// 틀린 문항의 해설을 쓰는 쪽. 온디바이스 모델을 쓴다.
///
/// ## 무엇을 지키게 하는가
///
/// | 지킬 것 | 왜 |
/// |---|---|
/// | 두 문장 이내 | 어머니는 창을 3~5초 봅니다. 세 문장이면 다 못 읽습니다 |
/// | 고른 답을 부정하지 않는다 | 8/28·8/31 기록에서 어머니는 **이미 자기가 틀린 걸 압니다.** 한 번 더 말할 이유가 없습니다 |
/// | 정답을 다시 말한다 | 창의 목적은 정답을 한 번 더 만나게 하는 것입니다 |
/// | 새로운 어려운 말을 꺼내지 않는다 | 「시조」를 설명하려고 「건국신화」를 꺼내면 모르는 말이 하나 늡니다 |
///
/// - Important: 실패해도 **던지지 않습니다.** ``MeaningGrader`` 는 판정을 못 하면
///   회차의 진행이 막히지만, 해설은 없어도 창이 뜹니다. 실패를 `nil` 로 돌려주면
///   ``ObsRecord/explanation`` 이 `null` 로 남고, 나중에 **몇 번 실패했는지 셀 수 있습니다.**
struct CommentaryWriter {

    /// 틀린 문항의 해설을 만든다. 못 만들면 `nil`.
    ///
    /// - Parameters:
    ///   - item: 방금 틀린 문항 (지문과 정답을 함께 넘긴다)
    ///   - chosen: 어머니가 고르거나 입력한 답
    /// - Returns: 두 문장 이내의 해설. 모델을 쓸 수 없거나 빈 문장이면 `nil`
    func write(for item: QuizItem, chosen: String) async -> String? {
        // 문체 규칙은 매번 같으므로 프롬프트가 아니라 지침에 둔다.
        // MeaningGrader 에서 배운 것 - 작은 온디바이스 모델은 규칙 문장보다
        // 예시를 훨씬 잘 따른다. 그래서 규칙마다 좋은 예와 나쁜 예를 붙인다.
        let instructions = """
            당신은 어르신에게 한국 역사와 제도를 알려주는 사람입니다.
            방금 문제를 틀린 분에게 정답을 다시 알려주는 짧은 글을 씁니다.
            읽는 분은 70대이고 한국어를 나중에 배우셨습니다.

            [반드시 지킨다]
            1. 두 문장을 넘기지 않는다.
            2. 정답을 문장 안에 그대로 넣는다.
            3. 쉬운 말만 쓴다. 문제에 없던 어려운 말을 새로 꺼내지 않는다.

            [쓰지 않는다]
            4. 나무라는 말. "틀렸습니다", "아닙니다", "잘못" 은 쓰지 않는다.
               고르신 답을 굳이 다시 말하지 않는다.
            5. 격려하는 말. "괜찮아요", "다음에는" 은 군더더기다.
            6. 확실하지 않은 연도나 숫자. 모르면 넣지 않는다.

            [예]
            문제 "고조선을 세운 왕은 누구입니까?" 정답 "단군왕검"
            좋은 글: "고조선을 세운 첫 임금은 단군왕검입니다. 단군신화는 그 이야기를 담은 옛이야기입니다."
            나쁜 글: "틀렸습니다. 정답은 단군왕검입니다."          (규칙 4 위반)
            나쁜 글: "단군왕검입니다."                              (규칙 2 는 지켰으나 알려주는 것이 없다)
            """

        let prompt = """
            문제: \(item.question.question)
            정답: \(item.question.answer)
            고르신 답: \(chosen)

            정답이 무엇인지 알려주는 두 문장 이내의 글을 쓰세요.
            """

        do {
            let session = LanguageModelSession(instructions: instructions)
            let response = try await session.respond(to: prompt, generating: Commentary.self)

            let text = response.content.text.trimmingCharacters(in: .whitespacesAndNewlines)
            return text.isEmpty ? nil : text
        } catch {
            // 화면에는 아무것도 알리지 않는다. 창은 이미 떠 있다.
            return nil
        }
    }
}
```

---

## ⑤ `KCT/Screens/QuizSession.swift`

**ⓐ `meaningGrader` 아래에 한 줄**

```swift
    private let commentaryWriter = CommentaryWriter()
```

**ⓑ `gradeCurrent` 의 뒷부분 교체**

`let isCorrect = await judge(...)` 아래를 통째로 이걸로 바꾼다.

```swift
        isGrading = false

        applyProgress(for: item, isCorrect: isCorrect)

        if isCorrect {
            saveObsRecord(for: item, isCorrect: isCorrect, explanation: nil)
            moveToNextQuestion()
        } else {
            feedback = IncorrectCommentary(
                selectedAnswer: answer,
                correctAnswer: item.question.answer,
                commentary: "해설을 준비 중이에요.")

            let text = await commentaryWriter.write(for: item, chosen: answer)
            saveObsRecord(for: item, isCorrect: isCorrect, explanation: text)
        }
        try? modelContext.save()
```

두 가지가 바뀌었다.

- **창이 `await` 보다 먼저다.** 반대로 두면 어머니는 아무것도 없는 화면을 3초 본다.
- **`saveObsRecord` 가 `applyProgress` 뒤로 내려갔다.** 1단계에서는 앞이었다. 해설을 기다린 뒤에 줄을 남겨야 하기 때문인데, `wasFirstEver` 는 `start()` 에서 떠 둔 `wasFirstEverByID` 를 읽으므로 진척이 먼저 바뀌어도 값이 달라지지 않는다. **확인하고 옮기는 것이지 순서가 아무래도 좋아진 게 아니다.**

**ⓒ `saveObsRecord` 가 해설을 받게 한다**

선언 줄을 교체.

```swift
    private func saveObsRecord(for item: QuizItem, isCorrect: Bool, explanation: String?) {
```

그리고 `ObsRecord(` 안, `reason: reasonForLog(item.id)` 아래 (윗줄 끝에 쉼표를 붙이고)

```swift
                explanation: explanation
```

---

## 다 넣고 확인할 것

1. **빌드가 되는가** — ④⑤는 짝이라 ④를 먼저 넣어야 한다.
2. **선다형을 틀린다** → 창이 **곧바로** 뜨는가. 3초 기다렸다 뜨면 ⑤ⓑ의 순서가 뒤집힌 것이다.
3. **회차를 끝낸다** → Supabase 에 다섯 줄. `explanation` 이 오답 줄에만 차 있는가.
4. `explanation` 이 **모든 줄에서 `null`** 이면 → ③의 네 군데 중 하나가 빠졌다.
5. **업로드가 통째로 안 되면** → ③ⓒ 에서 `encodeIfPresent` 를 쓴 것이다.

---

## ⑥ 해설 검토 — 이번 단계의 진짜 목적

20문항을 **4지선다와 직접입력 양쪽으로** 일부러 틀려 가며 돌린 뒤, 표를 읽는다.

```sql
select question_id, mode_raw, chosen, explanation
from obs_record
where is_correct = false and explanation is not null
order by question_id;
```

보는 것 셋.

1. **두 문장을 넘겼나**
2. **나무라는 말이 있나** (「틀렸습니다」, 「아닙니다」)
3. **문제에 없던 어려운 말이 새로 나왔나**

하나라도 걸리면 지침을 고치고 다시 돌린다.

실패 횟수도 같이 센다.

```sql
select count(*) from obs_record where is_correct = false and explanation is null;
```

---

## 3단계로 넘길 것

- 만든 해설을 **화면에 띄우기** — `IncorrectCommentary.commentary` 에 넣는다
- 시안 C 의 실제 창, 3초 딜레이, 「다음 문제」 버튼
- **해설이 늦게 오는 문제** — 지금은 안 띄우니 괜찮지만, 띄우는 순간 「준비 중이에요」가 잠깐 보였다가 바뀐다. 그게 괜찮은지, 아니면 창 안에서만 기다리게 할지 그때 정한다
- 임시 `alert` 을 지우고 `FeedbackSheet` 로 교체
