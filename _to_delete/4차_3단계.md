# 4차 프로토타입 · 3단계 — 임시 알림을 모달로, 해설을 화면에

> 2단계까지 : 틀리면 iOS 기본 `alert` 이 뜨고, 해설은 로그에만 남았다
> 3단계 : **시안 C 의 모달**이 뜨고, **모델이 쓴 해설이 그 안에 보인다**
>
> 원래는 20문항을 표로 먼저 검토한 뒤 띄우려 했다. 어머니가 아직 보지 않으므로
> **화면에서 직접 검토하는 쪽으로 바꾼다.** 표보다 눈에 잘 들어온다.

---

## 넣는 순서

```
① IncorrectCommentary 에 placeholder 와 isReady 추가
② gradeCurrent 에서 해설이 오면 창을 갈아 끼운다
③ FeedbackSheet.swift 새 파일
④ QuizView 의 alert 을 sheet 로 교체
```

①②는 `QuizSession.swift`, ③은 새 파일, ④는 `QuizView.swift`. ③ 없이 ④를 넣으면 빌드가 깨지므로 순서를 지킨다.

---

## ① `QuizSession.swift` — `IncorrectCommentary` 를 이렇게

```swift
    struct IncorrectCommentary {
        /// 해설이 아직 안 왔을 때 창에 넣어 두는 문구.
        static let placeholder = "해설을 준비 중이에요."

        let selectedAnswer: String
        let correctAnswer: String
        let commentary: String

        /// 해설이 도착했는가.
        var isReady: Bool { commentary != Self.placeholder }
    }
```

**왜 `isReady` 가 필요한가** — 모달의 「다음 문제」 버튼은 3초 뒤에 켜진다. 그런데 그 3초를 **창이 뜬 순간부터** 재면, 해설이 4초 만에 왔을 때 **읽기도 전에 버튼이 켜져 있다.** 3초는 **해설이 도착한 다음부터** 세야 한다.

문구를 `static let` 으로 뽑는 이유 — 같은 문자열이 두 곳(만드는 곳, 비교하는 곳)에 있으면 한쪽만 고쳐질 때 `isReady` 가 영영 `false` 가 된다.

## ② `QuizSession.swift` — `gradeCurrent` 의 `else` 안

통째로 교체.

```swift
            feedback = IncorrectCommentary(
                selectedAnswer: answer,
                correctAnswer: item.question.answer,
                commentary: IncorrectCommentary.placeholder)

            let text = await commentaryWriter.write(for: item, chosen: answer)

            if let text, feedback != nil {
                feedback = IncorrectCommentary(
                    selectedAnswer: answer,
                    correctAnswer: item.question.answer,
                    commentary: text)
            }

            saveObsRecord(for: item, isCorrect: isCorrect, explanation: text)
```

- **`feedback != nil` 이 없으면 안 된다.** 해설을 기다리는 1~3초 사이에 이미 「다음 문제」를 눌렀을 수 있고, 그때 `feedback` 을 다시 채우면 **닫았던 창이 혼자 다시 뜬다.**
- 해설을 못 만들면(`text == nil`) 창은 「해설을 준비 중이에요.」 그대로 남는다. 그리고 로그에는 `null` 로 남아 **실패 횟수를 셀 수 있다.**

## ③ `KCT/Screens/FeedbackSheet.swift` — 새 파일

```swift
//
//  FeedbackSheet.swift
//  KCT
//
//  역할 : 틀렸을 때 정답과 해설을 보여주는 모달
//  요점 : 정답이 가장 크다. 버튼은 3초 뒤에 켜진다 - 읽기 전에 넘어가지 않도록
//
//  ── 구성 ──────────────────────────────────────────────
//  FeedbackSheet
//  ├─ feedback        고른 답 · 정답 · 해설 (QuizSession 이 만든다)
//  ├─ onNext          「다음 문제」를 눌렀을 때 위에 알린다
//  ├─ secondsLeft     남은 대기 초. 0 이 되면 버튼이 켜진다
//  └─ .task           해설이 도착한 뒤부터 3초를 센다
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuizView - session.feedback 이 있는 동안
//  기대는 것    : QuizSession.IncorrectCommentary, AppColor, PrimaryActionButton
//  건드리지 않는 것 : 다음 문제로 넘기는 일 - onNext 로 위에 부탁한다
//

import SwiftUI

/// 오답 직후에 올라오는 모달.
///
/// 화면 순서가 곧 읽는 순서입니다 — **고른 답(작게) → 정답(가장 크게) → 해설.**
/// 고른 답을 맨 위에 작게 두는 이유는 「내가 무엇을 골랐더라」를 한 번 확인하고
/// 넘어가기 위해서지, 틀린 것을 강조하기 위해서가 아닙니다.
///
/// - Note: 버튼이 3초 뒤에 켜지는 이유 — 창이 뜨자마자 누르면 정답을 안 보고
///   넘어갑니다. 이 창의 목적은 **정답을 한 번 더 만나는 것**이므로,
///   그 시간을 화면이 확보합니다.
struct FeedbackSheet: View {
    let feedback: QuizSession.IncorrectCommentary
    let onNext: () -> Void

    /// 「다음 문제」가 켜지기까지 남은 초.
    @State private var secondsLeft = 3

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {

            Text("고르신 것 · \(feedback.selectedAnswer)")
                .font(.title3.weight(.medium))
                .foregroundStyle(AppColor.textMuted)
                .padding(.top, 36)

            Text("이 문제의 답")
                .font(.system(size: 21, weight: .semibold))
                .foregroundStyle(AppColor.textMuted)
                .padding(.top, 28)

            Text(feedback.correctAnswer)
                .font(.system(size: 40, weight: .bold))
                .foregroundStyle(AppColor.signature)
                .fixedSize(horizontal: false, vertical: true)
                .padding(.top, 8)

            ScrollView {
                Text(feedback.commentary)
                    .font(.system(size: 24, weight: .medium))
                    .foregroundStyle(.black)
                    .lineSpacing(8)
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .opacity(feedback.isReady ? 1 : 0.4)
            }
            .padding(.top, 24)

            Spacer(minLength: 16)

            PrimaryActionButton(
                title: secondsLeft > 0 ? "\(secondsLeft)" : "다음 문제  →",
                isReady: secondsLeft == 0
            ) {
                if secondsLeft == 0 { onNext() }
            }
            .padding(.bottom, 24)
        }
        .frame(maxWidth: .infinity, alignment: .leading)
        .padding(.horizontal, 28)
        .background(Color.white)
        .presentationDetents([.fraction(0.75)])
        .presentationDragIndicator(.hidden)
        .interactiveDismissDisabled()
        // 해설이 도착한 뒤부터 센다. isReady 가 바뀌면 이 블록이 처음부터 다시 돈다.
        .task(id: feedback.isReady) {
            guard feedback.isReady else { return }

            secondsLeft = 3
            while secondsLeft > 0 {
                try? await Task.sleep(for: .seconds(1))
                secondsLeft -= 1
            }
        }
    }
}

#Preview {
    FeedbackSheet(
        feedback: .init(
            selectedAnswer: "단군신화",
            correctAnswer: "단군왕검",
            commentary: "고조선을 세운 첫 임금은 단군왕검입니다. 단군신화는 그 이야기를 담은 옛이야기입니다."),
        onNext: {})
}
```

**읽을 때 볼 것 넷**

| | 무엇 | 왜 |
|---|---|---|
| `presentationDetents([.fraction(0.75)])` | 화면의 3/4 높이 | 「좀 더 높으면 좋겠다」 — 숫자만 바꾸면 된다 |
| `interactiveDismissDisabled()` | 쓸어내려 닫기 금지 | 닫는 길이 **버튼 하나뿐**이어야 3초가 뜻이 있다 |
| `opacity(isReady ? 1 : 0.4)` | 준비 중이면 흐리게 | 글자가 눈앞에서 바뀌는 것을 **미리 알린다** |
| `.task(id:)` | 해설 도착 후 3초 | `id` 가 바뀌면 이전 것을 취소하고 다시 시작한다 |

**카운트다운은 지금 숫자다** (`"3"` → `"2"` → `"1"` → `"다음 문제 →"`). 채워지는 막대로 바꾸고 싶으면 `PrimaryActionButton` 자리만 갈아 끼우면 된다.

## ④ `QuizView.swift` — `alert` 을 지우고 `sheet` 로

`body` 의 `.alert(...)` **덩어리 전체를 지우고** 그 자리에 넣는다.

```swift
        .sheet(
            isPresented: Binding(
                get: { session?.feedback != nil },
                set: { _ in }
            )
        ) {
            if let session, let feedback = session.feedback {
                FeedbackSheet(feedback: feedback) {
                    session.dismissFeedback()
                }
            }
        }
```

`set: { _ in }` 과 `dismissFeedback()` 안의 `guard` 는 **그대로 둔다.** 9/4 에 「한 번 눌렀는데 두 칸」이 났던 이유가 그것이다 (`Q&A.md`).

---

## 다 넣고 확인할 것

1. **선다형을 틀린다** → 아래에서 모달이 올라온다. 버튼에 `3` 이 보인다.
2. **3·2·1 이 지나면** 버튼이 켜진다. 그전에는 눌러도 아무 일이 없다.
3. **쓸어내려도 안 닫힌다.**
4. **직접입력을 틀린다** → 해설이 흐리게 있다가, 도착하면 진해지고 **그때부터** 3초가 시작된다.
5. **해설이 3초 넘게 걸리는 문항** → 버튼 숫자가 그동안 `3` 에 머물러 있는가.
6. **Supabase** → `explanation` 이 화면에 뜬 글과 같은가.

---

## 검토 — 이번 단계의 목적

20문항을 **4지선다와 직접입력 양쪽으로** 일부러 틀려 가며 돌리고, **화면에서** 해설을 본다.

1. 두 문장을 넘겼나
2. 나무라는 말이 있나 (「틀렸습니다」, 「아닙니다」)
3. 문제에 없던 어려운 말이 새로 나왔나
4. **3초가 짧은가 긴가** — 직접 읽어 보고 정한다
5. **글자가 바뀌는 순간이 거슬리는가** — 거슬리면 해설이 올 때까지 창을 늦게 띄우는 쪽으로 바꾼다

걸리는 게 있으면 `CommentaryWriter` 의 지침을 고치고 다시 돌린다.

---

## 4단계로 넘길 것

- `secOnFeedback` — 창을 얼마나 보고 있었나를 잰다
- `ResultScreen` 의 문항별 목록을 뺄지 (창이 생겼으니 중복이다 — 규칙 15)
- 어머니와 실제로 돌려보기
