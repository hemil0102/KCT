# 6차 계획 — 지식층을 만든다

> 2026-09-08. 문제집·재료·낱말을 **한 입구**로 모으고, 모델에게는 **필요한 만큼만** 넘긴다.
> 지금은 JSON 으로 읽고, 나중에 서버에서 갈아 끼우고, 그다음에 tool 로 대화한다.
> **부르는 쪽 코드는 그 셋 내내 바뀌지 않는다.**

---

## 1. 전체 구조

```
        ┌─────────────────────────────────────────────┐
        │  번들 JSON            서버 (6단계)            │
        │  questions.json       Supabase              │
        │  glossary.json        같은 모양의 JSON        │
        └──────────────┬────────────────┬─────────────┘
                       │  앱과 함께     │  받아서 Documents 에 저장
                       ▼               ▼
        ╔═════════════════════════════════════════════╗
        ║          ContentFile<Payload>               ║   ← 파일을 읽고 쓰는 하나
        ║  load()             받아 둔 것 먼저, 없으면 번들 ║
        ║  loadDownloaded()   받아 둔 것                ║
        ║  loadBundled()      앱과 함께 나온 것          ║
        ║  saveDownloaded()   받아 온 것을 적어 둔다      ║
        ╚══════════╤════════════════════╤═════════════╝
                   │                    │
                   ▼                    ▼
        ┌──────────────────┐   ┌──────────────────┐
        │ QuestionCatalog  │   │ GlossaryCatalog  │
        │ 문제집 창구        │   │  낱말 사전 창구    │
        │ questions·facts  │   │  gloss·examples  │
        │ tags·answerPool  │   │                  │
        └────────┬─────────┘   └─────────┬────────┘
                 │                       │
        ┌────────┴────────┐   ┌──────────┴──────────────┐
        ▼                 ▼   ▼                         ▼
┌────────────────┐  ┌──────────────────┐      ┌──────────────────┐
│ SessionBuilder │  │  KnowledgeKit    │      │  QuestionScreen  │
│   출제 계획      │  │ 프롬프트에 넣을    │      │ 질문 아래 낱말 풀이 │
└────────────────┘  │ 재료를 골라 다듬음  │      └──────────────────┘
                    └─────────┬────────┘
                    ┌─────────┼────────────┐
                    ▼         ▼            ▼
              AnswerChecker Commentary  KnowledgeTool
                 (채점)      Writer      (대화형·7단계)
                            (해설)
```

### 각 층이 하는 일

| 층 | 하는 일 | 왜 갈랐나 |
|---|---|---|
| **번들 JSON / 서버** | 원본을 담는다 | 앱과 함께 나가되, 나중에 서버가 이길 수 있어야 한다 |
| **`ContentFile`** | **파일을 읽고 쓰는 곳이 여기 하나뿐** | 번들이든 받아 둔 것이든 같은 길이다. 문제집과 사전이 같은 타입을 쓴다 |
| **`QuestionCatalog` · `GlossaryCatalog`** | 앱이 쓰는 **창구** | 화면과 출제기는 여기서만 묻는다 |
| **`KnowledgeKit`** | 재료를 **골라서** 문자열로 만든다 | **토큰이 쓰이는 자리를 한 곳에 모은다.** 여기만 보면 무엇이 모델에게 가는지 안다 |
| **`AnswerChecker`** | 채점 | 사실이 필요 없다. 정답과 답변만 |
| **`CommentaryWriter`** | 해설 | 그 문항의 재료 두세 조각만 |
| **`KnowledgeTool`** | 대화형 | `QuestionCatalog` 과 `GlossaryCatalog` 을 뒤지는 **얇은 껍데기** |

> **`KnowledgeTool` 이 얇은 이유** — tool 은 「모델이 낱말을 정해서 우리 함수를 부르는 것」일 뿐이고, **찾는 일은 창구가 이미 한다.** 그래서 나중에 붙일 때 새로 짤 것이 거의 없다.

---

## 2. 데이터의 모양

### `questions.json`

```json
{
  "version": 3,
  "questions": [
    {
      "id": 14,
      "category": "대한민국",
      "unit": "국경일",
      "question": "단군왕검이 고조선을 세운 것을 기념하는 국경일은 무엇입니까?",
      "answer": "개천절",
      "kind": "day",
      "statementFormat": "…는 {답}이다.",
      "difficulty": 1,

      "tags": ["개천절", "고조선", "단군왕검", "10월 3일", "국경일"],
      "facts": [
        { "kind": "date",         "weight": 3, "text": "10월 3일" },
        { "kind": "event",        "weight": 3, "text": "단군왕검이 고조선을 세운 것을 기리는 날" },
        { "kind": "wordplay",     "weight": 2, "text": "'하늘이 열린 날'이라는 뜻" },
        { "kind": "confusedWith", "weight": 1, "text": "광복절(8월 15일)과 헷갈리기 쉽다" }
      ]
    }
  ]
}
```

> **`shape` 는 왜 안 보이나** — `.word` 가 기본값이라 **911문항(81%)은 적지 않는다**(2단계). `list` 45개와 `sentence` 174개에만 `"shape": "list"` 처럼 적는다. 「적지 않는 것이 다수」가 되게 기본값을 골랐다.
>
> ```json
> { "id": 9, "answer": "고구려, 백제, 신라", "kind": "place", "shape": "list", ... }
> ```

### `glossary.json`

```json
{
  "version": 1,
  "words": [
    { "word": "국경일",
      "gloss": "나라의 큰 일을 기념하려고 법으로 정한 날",
      "examples": ["삼일절", "제헌절", "광복절", "개천절", "한글날"] },
    { "word": "시조",   "gloss": "그 나라나 집안을 처음 세운 사람" },
    { "word": "도읍",   "gloss": "나라의 수도" },
    { "word": "건국이념", "gloss": "나라를 세울 때 내세운 뜻" }
  ]
}
```

### 셋을 왜 갈랐나 — 각자 다른 물음에 답한다

```
   facts   "개천절에 대해 무엇을 아나"      →  답에 붙는다
   tags    "이 문항이 어느 무리에 속하나"    →  문항을 묶는다
 glossary  "국경일이라는 말이 무슨 뜻인가"   →  낱말에 붙는다 · 여러 문항이 함께 쓴다
```

**태그는 묶는 것이지 설명하는 것이 아니다.** 「국경일이 뭐야?」에 태그로 답하려 하면, 문항 다섯이 나오는데 **국경일의 뜻은 아무 데도 없다.** 그래서 사전이 따로 있어야 한다.

낱말은 **되풀이된다.** 「국경일」은 여섯 문항에 나오지만 뜻은 하나면 된다. 문항마다 붙이면 여섯 벌이 되고 고칠 때 여섯 군데를 고쳐야 한다.

> 사전은 **원래 질문을 읽게 해주려고** 만들려던 것이다(`낱말_구상.md`). 여기서 보니 **어머니의 물음에 답하는 데도 그대로 쓰인다.** 한 번 만들어 두 곳에서 쓴다.

---

## 3. 토큰을 아끼는 구조

**어디서 토큰이 쓰이는지 한 곳에서 보이게** 하는 것이 요점이다. `KnowledgeKit` 이 모델에게 갈 문자열을 **전부** 만든다.

| 쓰임 | 넣는 것 | 안 넣는 것 | 어림잡아 |
|---|---|---|---|
| **채점** | 정답 · 답변 · 짧은 절차 지침 | **문제 지문**, 재료, 사전 | 250 토큰 |
| **해설** | 답 · 재료 **상위 3조각** · 유형 지침 **한 덩어리** | 문제 지문, 다른 유형 지침 열 개 | 400 토큰 |
| **대화형** | 회차 5문항의 **답과 태그만** · 필요하면 tool 로 재료 | 처음부터 모든 재료 | 300 + α |

### 채점에 문제 지문을 안 넣는 이유 둘

1. **필요 없다.** 글자를 비교하는 데 지문이 왜 필요한가
2. **안전 필터에 걸린다.** 「일제의 지배」, 「목숨을 바친」 같은 낱말이 들어가면 `May contain unsafe content` 로 거부된다 (2026-09-06)

### 재료를 셋만 넣는 이유

`weight` 높은 순으로 셋. 조각이 다섯이어도 **해설은 세 문장**이라 다 못 쓴다. 다 넣으면 모델이 **고르느라 헤매고** 토큰만 는다.

그리고 **매번 같은 셋을 넣지 않는다.** 같은 무게끼리는 섞어서 고른다 — 그래야 같은 문항이라도 해설이 달라진다. 형님이 원하던 「고정 힌트는 싫다」가 여기서 풀린다. **사실은 늘 같고 고르는 조각만 달라진다.**

---

## 4. 서버로 갈아 끼우는 길

```
앱을 켠다
   ↓
① Documents 에 저장된 파일이 있나?
   있다 → 그것을 읽는다        ← 서버에서 받아 둔 최신본
   없다 → 번들 JSON 을 읽는다   ← 앱과 함께 나온 것
   ↓
② 조용히 서버에 버전을 물어본다 (실패해도 그냥 넘어간다)
   ↓
③ 서버 버전이 더 높으면 받아서 Documents 에 저장한다
   ↓
④ 다음에 앱을 켤 때부터 새 것을 쓴다
```

**지금은 ②③을 안 만든다.** 다만 ①의 「Documents 먼저, 없으면 번들」 순서만 **지금 넣어 둔다.** 그러면 나중에 ②③을 붙일 때 `ContentFile` 밖은 하나도 안 바뀐다.

**회차 도중에는 절대 갈아 끼우지 않는다.** 어머니가 문제를 푸는 중에 문항이 바뀌면 진척과 로그가 어긋난다. **다음에 앱을 켤 때부터**가 규칙이다.

**진척(`QuestionProgress`)은 건드리지 않는다.** 문항 id 로만 이어져 있어서, 문제집을 갈아 끼워도 어머니의 사다리는 그대로 남는다. **id 는 절대 재사용하지 않는다.**

---

## 5. 단계 — 한 번에 하나씩

| 단계 | 무엇 | 무엇이 달라지나 | 상태 |
|---|---|---|---|
| 1단계 | `ContentFile` | 읽는 길을 하나로 모은다 | ✅ **적용 완료** (2026-09-09) |
| 2단계 | `AnswerMatcher` | 코드로 채점을 거른다 | ✅ 적용됨 (2026-09-10) |
| 3단계 | `AnswerChecker` | 모델 채점을 다시 만든다 | ✅ 적용됨 (2026-09-10) — 로그까지 |
| 4단계 | `facts` | 해설이 사실을 지어내지 않는다 | ✅ 적용됨 (2026-09-10) — 30문항 |
| 5단계 | `glossary` | 질문 속 낱말을 읽게 한다 | ⬜ — 사전은 읽히지만 **화면에 안 쓰인다** |
| 6단계 | 서버 갱신 | 앱 업데이트 없이 문항을 늘린다 | ⬜ |
| 7단계 | `KnowledgeTool` | 「왜요?」에 답한다 | ⬜ |

**1~3 이 지금 겪는 문제를 고친다.** 4~5 는 어머니가 실제로 배우게 하는 것이고, 6~7 은 그다음이다.

### 1단계 — 읽는 길을 넓힌다  ✅ 적용됨

> **적용 현황 (2026-09-10 기준, 실제 코드와 대조함) — 코드는 다 들어갔다**
>
> | 항목 | 계획 | 실제 | |
> |---|---|---|---|
> | `Data/ContentFile.swift` | `let name` | **`let fileName`** | ✅ 아래 코드를 실제에 맞춰 고쳤다 |
> | `QuestionSource.swift` · `StoredQuestionSource.swift` | 지운다 | 없음 | ✅ |
> | `QuestionPayload` | `Question.swift` 로 옮긴다 | 옮겨져 있음 | ✅ |
> | `QuestionCatalog.bundled()` → `loaded()` | | `loaded()` | ✅ |
> | `Question.tags` · `.facts` | 더한다 | 있음 | ✅ |
> | `Question.init(from:)` 해독기 | 1단계 보완 | `Question.swift:86` | ✅ |
> | `QuestionFact.init(from:)` | 1단계 보완 | `Question.swift:131` | ✅ |
> | `GlossaryEntry.init(from:)` | 1단계 보완 | `Glossary.swift:38` | ✅ |
> | 낱말 사전 | `Glossary` 한 파일 | **`Glossary.swift`(값) + `GlossaryCatalog.swift`(창구) 두 파일** | ✅ 쪼갠 쪽이 낫다 |
> | `KCTApp` 에 사전 싣기 | | `GlossaryCatalog.loaded()` | ✅ |
>
> **남은 것은 머리말 주석 넷뿐이다** (규칙 20 — 문서는 코드를 따라간다). 동작에는 영향이 없지만, 없는 타입을 가리키고 있어 다음에 읽는 사람이 찾으러 간다.
>
> ```
> App/KCTApp.swift:17         → QuestionCatalog.bundled() : …     ← loaded()
> Data/QuestionCatalog.swift:14  ├─ bundled()  앱에 들어 있는…      ← loaded()
> Data/QuestionCatalog.swift:20    → bundled() : BundledQuestionSource 가…
> Data/QuestionCatalog.swift:26  기대는 것 : QuestionSource(어디서 읽을지), Question
> Data/QuestionCatalog.swift:36  /// 고칠 곳은 이 파일과 ``QuestionSource`` 뿐입니다
> ```
>
> `QuestionCatalog.swift:36` 의 DocC 링크 ``` ``QuestionSource`` ``` 는 **없는 타입을 가리켜 문서 빌드 경고가 난다.** 「고칠 곳은 이 파일과 ``ContentFile`` 뿐입니다」로 바꾼다.

> **정정 둘 (2026-09-08)**
>
> **①** 문항을 담아 두는 창구를 새로 만들지 않는다. `QuestionCatalog` 이 이미 그 역할이고, 새로 만드는 것은 파일을 읽고 쓰는 `ContentFile` 하나뿐이다.
> **②** `QuestionSource`(protocol) · `BundledQuestionSource` · `StoredQuestionSource` **셋을 하나로 합친다.**

#### 왜 프로토콜이 이제 안 맞나

`QuestionSource` 는 「번들에서 읽기 / **서버에서 읽기**」를 가르려던 이음새였다. 그런데 서버가 하는 일이 달라졌다.

```
옛 생각   서버에서 곧바로 읽는다         → 「어디서 읽나」가 갈림길
새 설계   서버에서 받아 파일로 저장한다   → 읽는 것은 늘 파일. 「어느 파일이냐」만 다르다
```

**이음새가 다른 자리로 옮겨갔다.** 남은 차이는 「번들 파일이냐, 받아 둔 파일이냐」 뿐이고 그건 **URL 하나 차이**다. 그리고 낱말 사전도 **똑같은 모양**이라 같은 것을 쓴다.

#### ① `KCT/Data/ContentFile.swift` — 새 파일 (셋을 대신한다)

```swift
//
//  ContentFile.swift
//  KCT
//
//  역할 : 앱이 읽는 내용 파일 하나 - 번들과 받아 둔 것을 같은 방식으로 다룬다
//  요점 : 읽는 것은 늘 파일이다. 다른 것은 어느 파일이냐뿐
//
//  ── 구성 ──────────────────────────────────────────────
//  ContentFile<Payload>
//  ├─ fileName          "questions" · "glossary"
//  ├─ load()            받아 둔 것 먼저, 없으면 번들
//  ├─ loadDownloaded()  서버에서 받아 둔 것. 없으면 nil
//  ├─ loadBundled()     앱과 함께 나온 것. 없으면 오류
//  └─ saveDownloaded()  받아 온 것을 적어 둔다
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuestionCatalog.loaded() · GlossaryCatalog.loaded() · (6단계) 갱신
//  기대는 것    : Codable 한 Payload, 번들 안의 <fileName>.json
//  건드리지 않는 것 : 언제 갈아끼울지 — 부르는 쪽이 정한다
//

import Foundation

/// 앱이 읽는 내용 파일 하나.
///
/// 문제집과 낱말 사전이 **같은 타입을 씁니다.** 둘 다 「받아 둔 것 먼저, 없으면 번들」이고
/// 담는 내용만 다르기 때문입니다.
///
/// - Note: 서버에서 **받아 오는 일**은 여기 없습니다. 받아서 ``saveDownloaded(_:)`` 로
///   적어 두는 것까지가 6단계의 몫이고, 이 타입은 **읽고 쓰기만** 합니다.
struct ContentFile<Payload: Codable> {

    /// 확장자를 뺀 파일 이름.
    let fileName: String

    enum LoadError: Error {
        case notFound(String)
    }

    /// 받아 둔 것 먼저, 없으면 번들. 둘 다 없으면 `nil`.
    ///
    /// **이 순서가 6단계(서버 갱신)의 전부입니다.** 받아서 저장하는 코드를 나중에
    /// 붙이면 이 함수는 손대지 않아도 새 내용이 쓰입니다.
    func load() -> Payload? {
        loadDownloaded() ?? (try? loadBundled())
    }

    /// 서버에서 받아 기기에 적어 둔 것. 없거나 깨졌으면 `nil`.
    ///
    /// 깨졌을 때 오류를 던지지 않는 이유 — 앱이 안 켜지는 것보다
    /// **번들 것으로 도는 편이 낫습니다.**
    func loadDownloaded() -> Payload? {
        guard let url = downloadedURL,
              let data = try? Data(contentsOf: url) else { return nil }

        return try? JSONDecoder().decode(Payload.self, from: data)
    }

    /// 앱과 함께 나온 것. 이것마저 없으면 오류입니다.
    func loadBundled() throws -> Payload {
        guard let url = Bundle.main.url(forResource: fileName, withExtension: "json") else {
            throw LoadError.notFound(fileName)
        }
        return try JSONDecoder().decode(Payload.self, from: Data(contentsOf: url))
    }

    /// 서버에서 받은 것을 적어 둔다.
    func saveDownloaded(_ payload: Payload) throws {
        guard let url = downloadedURL else { return }
        try JSONEncoder().encode(payload).write(to: url, options: .atomic)
    }

    private var downloadedURL: URL? {
        FileManager.default
            .urls(for: .documentDirectory, in: .userDomainMask)
            .first?
            .appendingPathComponent("\(fileName).json")
    }
}
```

#### ② 지운다

| 파일 | 왜 |
|---|---|
| `KCT/Data/QuestionSource.swift` | 프로토콜과 `BundledQuestionSource` 가 `ContentFile` 로 흡수됨 |
| `KCT/Data/StoredQuestionSource.swift` | 같은 이유 |

**`QuestionPayload` 는 살려야 한다.** `QuestionSource.swift` 안에 있으므로, 지우기 전에 이 구조체를 `KCT/Data/Question.swift` 맨 아래로 옮긴다.

```swift
/// 문제집 파일의 내용.
///
/// 배열이 아니라 객체로 감싸 두어야 나중에 칸을 더해도 기존 디코더가 안 깨집니다.
struct QuestionPayload: Codable {
    let version: Int
    let questions: [Question]
}
```

#### ③ `KCT/Data/QuestionCatalog.swift` — `bundled()` 를 `loaded()` 로

`static func bundled()` 를 통째로 교체.

```swift
    /// 앱이 쓸 문제집을 읽어 온다. **받아 둔 것 먼저, 없으면 번들.**
    ///
    /// 둘 다 실패하면 빈 문제집을 돌려줍니다. 앱이 죽는 것보다 빈 화면이 낫고,
    /// 개발 중에는 `assertionFailure` 가 즉시 알려 줍니다.
    static func loaded() -> QuestionCatalog {
        let contentFile = ContentFile<QuestionPayload>(fileName: "questions")

        if let downloaded = contentFile.loadDownloaded(), !downloaded.questions.isEmpty {
            return QuestionCatalog(payload: downloaded)
        }

        do {
            return QuestionCatalog(payload: try contentFile.loadBundled())
        } catch {
            assertionFailure("기본 문제집을 읽지 못했습니다: \(error)")
            return QuestionCatalog(payload: QuestionPayload(version: 0, questions: []))
        }
    }
```

`bundled()` 라는 이름을 남기지 않는 이유 — **이제 번들만 읽지 않는다.** 이름이 거짓이 되면 다음에 읽는 사람이 속는다.

부르는 곳 **셋**을 고친다.

```
App/KCTApp.swift:41         QuestionCatalog.loaded()
App/RootView.swift:37       QuestionCatalog.loaded()
Screens/QuizView.swift:137  QuestionCatalog.loaded()
```

> 뒤의 둘은 `#Preview` 안이다. 미리보기도 같은 길을 타는 것이 맞다.

#### ④ `KCT/Data/Question.swift` — 칸 둘을 미리 연다

`let difficulty: Int` 아래에.

```swift
    /// 이 문항을 묶는 이름들. 낱말로 문항을 찾을 때 쓴다.
    ///
    /// 「국경일」처럼 **답이 아닌 낱말**도 들어갑니다. 다만 태그는 묶는 것이지
    /// 설명하는 것이 아니라, 뜻은 낱말 사전(``GlossaryCatalog``)에 있습니다.
    var tags: [String] = []

    /// 해설에 쓸 사실 조각. 모델은 여기 있는 것만 씁니다.
    var facts: [QuestionFact] = []
```

> **정정 (2026-09-09) — 기본값은 해독을 구해 주지 않는다**
>
> 여기 처음에 「기본값을 꼭 준다. 없으면 문제집을 못 읽는다」고 적었는데 **정확히 반대였다.**
> **Swift 가 자동으로 만들어 주는 `Codable` 은 기본값을 쓰지 않는다.** 자동 생성 코드는
> `decodeIfPresent` 가 아니라 `decode` 라서, `questions.json` 에 `tags`·`facts` 가 없으면
> 그대로 `keyNotFound` 를 던진다. 실제로 「기본 문제집을 읽지 못했습니다」가 떴다.
>
> `= []` 는 **손으로 `Question(...)` 을 만들 때만** 쓰이는 값이다. 파일에서 읽을 때 없어도 되게
> 하려면 **해독기를 손으로 적어야 한다.** 코드는 `6차_1단계_보완.md` (HISTORY.md 로 합침) 에 있고,
> 왜 그런지는 [Q&A.md](Q&A.md) 맨 위 두 항목에 있다.
>
> 같은 함정이 `QuestionFact.weight` 와 `GlossaryEntry.examples` 에도 있었다.

같은 파일 아래쪽(구조체 밖)에.

```swift
/// 해설에 쓸 사실 한 조각.
///
/// 조각으로 쪼개 두는 이유 — 해설은 세 문장이라 다 못 씁니다. **무게 높은 순으로 몇 개만
/// 골라 넣고, 같은 무게끼리는 섞어서** 같은 문항이라도 해설이 달라지게 합니다.
/// 사실은 늘 같고 고르는 조각만 달라집니다.
struct QuestionFact: Codable, Hashable {
    /// 재료의 종류. date · year · event · wordplay · pairsWith · confusedWith · everyday
    let kind: String

    /// 먼저 쓸 순서. 높을수록 먼저.
    var weight: Int = 1

    let text: String
}
```

#### ⑤ 낱말 사전 — **파일 둘로 나뉘었다**

계획에는 `Glossary` 한 파일이었는데, 실제로는 이렇게 나뉘었다. **이쪽이 낫다** — 담는 것(`struct`)과 들고 있는 것(`class`)이 섞이지 않는다. `Question.swift` / `QuestionCatalog.swift` 가 나뉜 것과 같은 모양이다.

| 파일 | 무엇 |
|---|---|
| `Data/Glossary.swift` | `GlossaryEntry` · `GlossaryPayload` — **값** |
| `Data/GlossaryCatalog.swift` | `GlossaryCatalog` — 앱이 쓰는 **창구** (`@Observable`) |

```swift
/// 낱말 하나.
struct GlossaryEntry: Codable, Hashable {
    let word: String
    let gloss: String

    /// 이 낱말에 속하는 것들. 「국경일」의 삼일절·제헌절처럼.
    var examples: [String] = []
}

/// 사전 파일의 내용.
struct GlossaryPayload: Codable {
    let version: Int
    let words: [GlossaryEntry]
}
```

```swift
@Observable
final class GlossaryCatalog {

    private(set) var version: Int
    private(set) var words: [GlossaryEntry]

    init(payload: GlossaryPayload) {
        self.version = payload.version
        self.words = payload.words
    }

    /// 사전을 읽어 온다. 없으면 빈 사전.
    ///
    /// 사전이 비어도 앱은 그대로 돕니다 — 질문 아래 풀이가 안 붙을 뿐입니다.
    /// 그래서 문제집과 달리 `assertionFailure` 를 두지 않습니다.
    static func loaded() -> GlossaryCatalog {
        let payload = ContentFile<GlossaryPayload>(fileName: "glossary").load()
        return GlossaryCatalog(payload: payload ?? GlossaryPayload(version: 0, words: []))
    }

    /// 낱말 하나의 뜻.
    func gloss(_ word: String) -> GlossaryEntry? {
        words.first { $0.word == word }
    }

    /// 문장 안에 들어 있는 낱말들. 질문 아래에 풀이를 붙일 때 씁니다.
    ///
    /// 긴 낱말이 먼저 오게 정렬합니다 — 「건국이념」이 있는데 「이념」만 잡히면 안 됩니다.
    func words(in sentence: String) -> [GlossaryEntry] {
        words
            .filter { sentence.contains($0.word) }
            .sorted { $0.word.count > $1.word.count }
    }
}
```

> ✅ **`GlossaryEntry.init(from:)` 도 들어갔다.** `examples` 가 없는 낱말(시조·도읍·건국이념·신화) 때문에 **소리 없이 빈 사전**이 될 뻔했다. `assertionFailure` 가 없어 아무 신호도 없었을 자리다. 코드는 `6차_1단계_보완.md` (HISTORY.md 로 합침) 에.

#### ⑥ `KCT/Data/glossary.json` — 이미 만들어 두었다

다섯 낱말로 시작한다. 5단계에서 150~250개로 늘린다.

> **Xcode 에 넣을 때 Target Membership 을 확인한다.** 번들에 안 들어가면 빈 사전이 되고, **오류가 하나도 없이 조용히** 아무 일도 안 일어난다.

#### ⑦ `KCT/App/KCTApp.swift` — 사전을 환경에 싣는다

`@State private var catalog` 아래에.

```swift
    /// 앱이 사용할 낱말 사전.
    @State private var glossary = GlossaryCatalog.loaded()
```

`.environment(catalog)` 아래에.

```swift
                .environment(glossary)
```

### 확인

**아무것도 달라지지 않아야 한다.**

1. 앱이 켜지고 문항이 나온다
2. 사다리가 돌고 로그가 올라간다
3. `questions.json` 에 `tags`·`facts` 가 없는데도 문제집을 읽는다 — **손으로 적은 `init(from:)` 의 `decodeIfPresent` 가 일한 것이다**
4. 사전은 아직 아무 데도 안 쓰인다. 읽히기만 한다
5. `GlossaryCatalog.loaded().words.count` 가 **5** 다 — 0 이면 `examples` 해독기가 빠진 것이다

**3번과 5번이 이 단계의 진짜 확인이다.** 둘 다 「기본값은 해독을 구해 주지 않는다」의 같은 얼굴이고, 3번은 시끄럽게(`assertionFailure`) 5번은 조용히 실패한다.

> **미리보기는 아직 사전을 안 싣는다.** `App/RootView.swift:37` · `Screens/QuizView.swift:137` 의 `#Preview` 에 `.environment(QuestionCatalog.loaded())` 만 있다. **5단계에서 질문 화면이 사전을 쓰기 시작하면 미리보기가 깨진다.** 그때 `.environment(GlossaryCatalog.loaded())` 를 나란히 넣는다.

### 2단계 — `AnswerMatcher` (코드로 판정)

**하는 일** : 직접입력 답을 **코드가 먼저** 본다. 확실한 것은 여기서 끝내고, 애매한 것만 모델에게 넘긴다.

```
답 제출  (손으로 침 / 말로 함)
   ↓
① `shape` 가 목록인가?             → 곧바로 모델로     (코드는 손대지 않는다)
   ↓
② 표기 후보 중 하나와 같나         → 정답 확정        exactMatch
   ↓
③ 말로 한 것인가? 발음이 같나      → 정답 확정        phoneticMatch   (음성 붙일 때)
   ↓
④ 모델에게 묻는다                 → 3단계
```

> **정정 (2026-09-09) — 목록은 코드가 판정하지 않는다**
>
> 처음에는 「정답이 목록이면 집합을 비교해 코드가 확정한다」로 잡았다. **뺐다.**
>
> 집합이 같으려면 어머니가 「고구려, 백제, 신라」를 **세 개 다, 글자까지 똑같이** 적어야 한다.
> 직접입력 4문항 중앙값이 17.9초인 분에게 그럴 일이 사실상 없다. **거의 일하지 않는 층이면서
> 「단군 왕검」이 두 항목으로 쪼개지는 위험만 남긴다.**
>
> 그래서 목록형은 **①에서 곧바로 모델로 보낸다.** 어차피 부분 정답(`partialList`)·순서 바뀜을
> 가려야 하는데 그건 코드로 깔끔하지 않다.
>
> **무엇이 목록인지는 쉼표로 정하지 않는다.** 문제집 1,130문항에서 쉼표가 든 정답은 121개인데,
> 그 중 **76개가 `meaning`** 이다 — 목록이 아니라 **쉼표가 든 설명 문장**이다.
>
> ```
> '고구려, 백제, 신라'                                     ← list
> '고구려, 백제, 신라, 3국 중 신라는 가장 늦게 국가로서의…'  ← meaning (정답 칸에 해설이 섞였다)
> ```
>
> 그래서 `check(...)` 에 **`shape: AnswerShape` 를 넘긴다.** 정답 문자열을 보고 짐작하는 대신
> **문항이 스스로 밝힌 것**을 쓴다. 「사실은 우리가 준다」와 같은 방향이고,
> `AnswerKind` 를 1,130문항에 붙여 둔 값어치가 여기서 처음 나온다.

#### 구분자 규칙 — `/` 와 `,` 는 뜻이 다르다

| 구분자 | 뜻 | 예 |
|---|---|---|
| **`/`** | **아무거나 맞다** | `삼일절/3.1절` · `을사조약/을사늑약` |
| **`,`** | **다 대야 맞다** | `고구려, 백제, 신라` |

지금 문제집은 이것을 **괄호**로 적고 있다 — `을사조약(을사늑약)`, `3.1운동(삼일운동)`. 단답형 911개 중 **41개**가 이 모양이고, 통째로 비교하면 **「을사조약」이라고 제대로 답한 것이 안 맞는다.** 목록형과 정반대 방향의 손해다 — 맞힌 것을 모델로 보낸다.

**괄호 41개를 `/` 로 옮기는 정리가 필요하다.** 그러면 코드는 `split(separator: "/")` 하나로 끝나고 정규식이 없다.

> ⚠️ 괄호 중 서넛은 병기가 아니라 **부연**이다 — `9년 (초등학교 6년 + 중학교 3년)`. 이런 것은 `/` 로 바꾸지 말고 괄호째 지워 `9년` 만 남긴다.

**코드가 통과시키는 셋은 전부 확실한 것**이라 모델이 다시 볼 이유가 없다. 반대로 코드가 「다르다」고 한 것에는 **진짜 정답이 섞여 있다**(「음 이건 고조선인 것 같은데요」). 그래서 **모델은 판사가 아니라 마지막 구제 절차**가 된다.

그러면 남는 위험이 하나로 좁혀진다 — **모델이 틀린 것을 구제해 주는 실수**(「고죠선」 통과)뿐이다. 정답을 오답으로 만드는 실수는 코드가 이미 막았다.

#### `KCT/Grading/AnswerTypes.swift` — 답을 보는 낱말들을 **한 파일에** (규칙 24)

채점이 쓰는 짧은 열거형 넷을 한 파일에 둔다. 넷 다 「답을 어떻게 볼 것인가」의 낱말이고, 같이 쓰이고 같이 바뀐다.

```
Grading/AnswerTypes.swift
├─ AnswerKind    주제. 아홉
├─ AnswerShape   모양. 셋
├─ AnswerSource  어디서 왔나. 둘
├─ CheckBasis    모델이 고르는 근거. 일곱
└─ MatchBasis    코드가 정하는 근거. 둘
```

> **파일 이름을 `AnswerKind.swift` 로 두지 않는다.** 형제를 들이는 순간 이름이 거짓말이 된다 — 「`AnswerShape` 어디 있지」로 찾으면 안 나온다. 지금 실제로 그 상태다.

##### `AnswerSource`

```swift
/// 답이 어디서 왔는가.
///
/// 같은 글자라도 **손으로 친 것과 말로 한 것은 다르게 봐야** 합니다.
/// 「고죠선」은 손으로 쳤으면 오타지만, 말로 했으면 **인식기가 잘못 적은 것**입니다.
/// 앞은 오답으로 둬도 곧 다시 만나 회복되고, 뒤는 **맞혔는데 틀렸다고 하는 것**입니다.
enum AnswerSource: String, Codable {
    case typed
    case voice
}
```

##### `CheckBasis`

```swift

/// 무엇을 근거로 판정했는가. 코드가 판정한 것과 모델이 판정한 것이 함께 들어간다.
///
/// 로그에 **낱말이 아니라 값으로** 남기려고 만듭니다. 그래야 셀 수 있습니다 —
/// `afterTrimming` 이 많으면 군더더기 목록을 늘리면 되고,
/// `differentLetters` 인데 정답으로 처리된 줄이 있으면 **모델이 규칙을 어긴 것**입니다.
@Generable
enum CheckBasis {
    /// 글자가 그대로 같다 (코드)
    case exactMatch
    /// 여러 항목을 다 댔다 — 순서는 달라도 된다 (모델)
    case listMatch
    /// 발음이 같다 — 음성 입력에서만 (코드)
    case phoneticMatch
    /// 군더더기를 걷어내니 같다 (모델)
    case afterTrimming
    /// 한 글자 이상 다르다 (모델)
    case differentLetters
    /// 아예 다른 이름이다 (모델)
    case differentName
    /// 답을 둘 이상 대고 정하지 않았다 (모델)
    case undecided
    /// 여러 항목 중 일부만 말했다 (모델)
    case partialList
}
```

로그에 남길 때는 `String(describing: basis)` 를 쓴다. `exactMatch` 같은 이름이 그대로 나온다.

#### `AnswerShape` 와 `AnswerKind` — 축을 나눈다

> **정정 (2026-09-10) — `AnswerKind` 하나에 축이 둘 섞여 있었다**
>
> 처음에는 `AnswerKind.shape` 를 **계산 프로퍼티**로 잡아 `.list` → `.list`, `.meaning` → `.sentence`,
> 나머지 아홉 → `.word` 로 매핑했다. **틀렸다.** 데이터가 보여 준다.
>
> ```
> 고구려, 백제, 신라              ← 목록이면서 place
> 팔만대장경, 직지심체요절, 고려청자  ← 목록이면서 heritage
> 한산도대첩, 행주대첩, 진주대첩     ← 목록이면서 event
> 미혼은 도련님, 기혼은 서방님       ← 목록이면서 procedure
> ```
>
> `list` 로 찍는 순간 **주제를 잃는다.** 그래서 ``CommentaryWriter`` 가 「이건 문화유산이니
> 이렇게 설명하라」를 못 고른다. `meaning` 174개도 정치 51 · 대한민국 46 · 제도와생활 41 ·
> 역사 31로 흩어져 있어 같은 손해를 본다.
>
> **`list` 와 `meaning` 은 「모양」이고 나머지 아홉은 「주제」다.** 한 열거형에 앉히면 안 된다.

#### 두 축으로 나눈다

```
Question
├─ shape : AnswerShape   모양. 셋.   「어떻게 생겼나」  → 채점이 쓴다
└─ kind  : AnswerKind    주제. 아홉.  「무엇이냐」      → 해설이 쓴다
```

**`shape` 를 `kind` 에서 계산하지 않는다.** 문항이 둘 다 들고 다닌다. 서로 독립이기 때문이다.

| | `.word` | `.list` | `.sentence` |
|---|---|---|---|
| `place` | 고조선 | 고구려, 백제, 신라 | — |
| `heritage` | 석굴암 | 팔만대장경, 직지심체요절 | 널리 인간을 이롭게 한다 |
| `event` | 3.1운동 | 한산도대첩, 행주대첩 | 왕건은 후삼국을 통일하고… |
| `system` | 민주주의 | — | 국민의 4대 의무는… |

`Question.difficulty`(축 A) 와 `QuestionProgress.mode`(축 B) 를 나눠 둔 것과 같은 이유다 — **두 축을 섞으면 설계가 무너진다.**

```swift
/// 답의 **모양**. ``AnswerKind`` 가 「무엇이냐」라면 이것은 「어떻게 생겼나」다.
///
/// 축을 나누는 이유 — 「고구려, 백제, 신라」는 목록이면서 동시에 ``AnswerKind/place`` 입니다.
/// 하나의 열거형에 넣으면 둘 중 하나를 잃습니다.
///
/// - Note: 채점은 이것을 보고, 해설은 ``AnswerKind`` 를 봅니다. 쓰는 쪽이 다릅니다.
enum AnswerShape: String, Codable {
    /// 한 낱말 — 고조선, 세종대왕, 태극기
    case word
    /// 여러 항목을 다 대야 한다 — "고구려, 백제, 신라"
    case list
    /// 설명 문장 — "널리 인간을 이롭게 한다"
    case sentence
}
```

##### `AnswerKind` — `list` 와 `meaning` 을 뺀다

남는 아홉은 순수하게 주제다.

```swift
enum AnswerKind: String, Codable, CaseIterable {
    /// 연도·날짜·숫자 — "1950년 6월 25일", "만 18세"
    case number
    /// 사람 — 안익태, 주몽, 세종대왕
    case person
    /// 나라와 땅 — 고조선, 백두산, 서울
    case place
    /// 기념일과 명절 — 개천절, 추석
    case day
    /// 사건과 전쟁 — 3.1운동, 6.25전쟁
    case event
    /// 물건·음식·옷·놀이·상징 — 태극기, 김치, 온돌
    case thing
    /// 문화유산·책·옛이야기, 그리고 **거기서 나온 정신** — 석굴암, 단군신화, 홍익인간
    case heritage
    /// 제도·기관·권리 — 민주주의, 대법원, 기본권
    case system
    /// 생활 절차·서류·호칭 — 혼인신고, 등기부등본, 아주버님
    case procedure
}
```

> 「널리 인간을 이롭게 한다」는 **`heritage`** 다. 건국이념이고, 「단군신화」와 같은 서랍에 있는 것이 자연스럽다. 그래서 이념을 위한 열째 주제는 만들지 않는다.

#### ⚠️ `questions.json` 9번을 같이 고친다 — 안 고치면 앱이 안 켜진다

`AnswerKind` 에서 `list` 를 빼는 순간, 아직 `"kind": "list"` 인 문항이 **해독에서 던진다.** 키가 없는 게 아니라 **값이 열거형에 없는** 경우다(`dataCorrupted`).

```json
      { "id": 9, "answer": "고구려, 백제, 신라",
        "kind": "place",     ← "list" 에서
        "shape": "list" }    ← 새로
```

25문항 중 **9번 하나뿐이다.** 나머지는 day · person · thing · place · heritage 라 그대로 산다.

#### `KCT/Data/Question.swift` — 칸 하나를 더한다

`let kind: AnswerKind` 아래에.

```swift
    /// 답의 모양. 채점이 이것을 본다. 대부분은 ``AnswerShape/word`` 다.
    var shape: AnswerShape = .word
```

그리고 손으로 적은 해독기에 한 줄 (1단계 보완에서 만든 `init(from:)` 안, `tags` 옆).

```swift
        shape = try box.decodeIfPresent(AnswerShape.self, forKey: .shape) ?? .word
```

**911문항(81%)은 JSON 에 아무것도 안 적어도 된다.** `.word` 가 기본이라 `list` 45개와 `sentence` 174개에만 적는다.

#### 함께 나아지는 것 — `CommentaryWriter`

지금 지침이 11개 블록으로 평평한데, 축이 둘이면 갈린다.

```
주제 지침 (9)   「사람은 글자를 풀지 않는다」 「제도는 글자를 푸는 게 가장 좋다」
       +
모양 한 줄 (3)  .list     → 「항목을 빠짐없이 말한다」
                .sentence → 「짧게 줄이지 말고 뜻을 살린다」
                .word     → (덧붙일 것 없음)
```

**「팔만대장경, 직지심체요절」이 문화유산 지침을 받게 된다.** 지금은 `list` 지침을 받아 「셋을 다 대세요」밖에 못 한다.

#### 옮겨야 하는 데이터

| 무엇 | 얼마나 |
|---|---|
| `AnswerShape` 열 신설 · `list` 로 찍기 | 45행 |
| `AnswerShape` 열 신설 · `sentence` 로 찍기 | 174행 |
| 그 219행에 **주제를 새로 매기기** | 219행 |
| 나머지 | 911행 — 손대지 않는다 |

**219행 재분류는 따로 하는 작업이다.** 「번호 / 정답 / 지금 kind / 제안 shape / 제안 kind」 표를 뽑아 눈으로 훑고 고치는 쪽이 안전하다.

#### `KCT/Grading/AnswerMatcher.swift` — 새 파일

```swift
import Foundation

/// 코드로 판정할 수 있는 것을 먼저 가려낸다. 애매한 것만 모델로 넘긴다.
///
/// 대부분의 정답은 여기서 끝납니다. 그래서 직접입력의 기다림이 줄고,
/// 모델을 부르는 횟수가 줄어 **안전 필터에 걸릴 기회도 그만큼 줄어듭니다.**
struct AnswerMatcher {

    /// 판정 결과. `needsModel` 이면 확실하지 않아 모델에게 넘긴다.
    ///
    /// - Note: 코드가 정하는 근거(``MatchBasis``)만 담습니다. 모델이 고르는 것은
    ///   ``CheckBasis`` 로 따로 있습니다 — 섞으면 로그에서 누가 판정했는지 못 가릅니다.
    enum Outcome {
        case correct(MatchBasis)
        case needsModel
    }

    /// - Parameter shape: 답의 모양. **글자를 보고 짐작하지 않고 문항이 밝힌 것을 쓴다.**
    ///   주제(``AnswerKind``)는 채점이 쓰지 않으므로 받지 않는다.
    static func check(
        _ answer: String,
        against correctAnswer: String,
        shape: AnswerShape,
        from source: AnswerSource
    ) -> Outcome {
        switch shape {
        case .list, .sentence:
            // 목록  — 부분 정답·순서 바뀜은 코드로 깔끔하지 않다.
            // 설명 문장 — 46자짜리를 글자로 비교해 봐야 늘 다르다. 뜻은 모델이 본다.
            return .needsModel
        case .word:
            break
        }

        let given = tidy(answer)

        if candidates(correctAnswer).contains(given) { return .correct(.exactMatch) }

        if source == .voice,
           candidates(correctAnswer).contains(where: { sounds($0) == sounds(given) }) {
            return .correct(.phoneticMatch)
        }

        return .needsModel
    }

    /// 정답으로 인정할 표기들. `/` 로 나눈 것은 **아무거나 맞다**.
    ///
    /// 「삼일절/3.1절」은 둘 중 하나만 적어도 정답입니다. `,` 와 뜻이 다릅니다 —
    /// 쉼표는 **다 대야** 정답인 목록이고, 그쪽은 애초에 여기까지 오지 않습니다.
    ///
    /// - Note: `/` 가 없으면 후보가 하나라 그냥 글자 비교와 같습니다.
    ///   **특별한 경우를 만들지 않으려고** 이 모양으로 둡니다.
    private static func candidates(_ correctAnswer: String) -> Set<String> {
        Set(correctAnswer.split(separator: "/").map { tidy(String($0)) }.filter { !$0.isEmpty })
    }

    /// 비교용으로 다듬는다. **글자와 숫자만 남긴다.**
    ///
    /// 빼는 쪽이 아니라 남기는 쪽으로 씁니다 — 뺄 것은 목록을 다 셀 수 없지만
    /// 남길 것은 둘뿐입니다. `isLetter` 는 한글·한자를 포함합니다(유니코드 `Lo`).
    ///
    /// - Note: `isPunctuation` 만 빼면 `~` `>` `+` 가 살아남습니다. 유니코드는 이것들을
    ///   문장부호가 아니라 **기호**로 봅니다. 문제집 정답 1,155개에 19군데 있습니다.
    private static func tidy(_ text: String) -> String {
        String(text.filter { $0.isLetter || $0.isNumber })
    }

    /// 발음이 같은 글자를 한 모양으로 모은다.
    ///
    /// - Note: **아직 비어 있습니다.** 음성 입력을 붙일 때 채웁니다.
    ///   그때까지 ``AnswerSource/voice`` 가 들어올 일이 없어 이 자리는 안 쓰입니다.
    private static func sounds(_ text: String) -> String {
        text
    }
}
```

**확인**

| 입력 | 기대 | 어디서 |
|---|---|---|
| `고조선` | 정답 | ② 즉시 |
| `단군 왕검` | 정답 | ② 즉시 (공백을 지운다) |
| `신라 백제 고구려` | → 모델 | ① `.list` 라 비켜선다 (모델이 `listMatch`) |
| `인간을 널리 이롭게 하는 것` | → 모델 | ① `.sentence` 라 비켜선다 (모델이 `sameMeaning`) |
| `고구려, 백제` | → 모델 | ① (모델이 `partialList` 로 오답) |
| `삼일절` (정답 `삼일절/3.1절`) | 정답 | ② 즉시 |
| `3.1절` (정답 `삼일절/3.1절`) | 정답 | ② 즉시 (부호를 지운다) |
| `고죠선` · `태국기` · `조선` | → 모델 | ④ (모델이 오답) |
| `음 이건 고조선이요` | → 모델 | ④ (모델이 `afterTrimming` 으로 정답) |

**②에서 끝나는 것은 기다림이 없어야 한다.** 「채점 중이에요」가 안 떠야 맞다.

목록형과 해설형은 늘 모델을 거치므로 **기다림이 생긴다.** `.list` 45개 + `.sentence` 174개 = **1,130개 중 219개(19%)** 다. 그래도 직접입력까지 올라온 목록형은 어머니가 **셋을 다 적어야** 하므로, 로그에서 `partialList` 가 쌓이면 **목록형은 사다리 꼭대기를 4지선다로 두는 쪽**을 본다.

### 3단계 — `AnswerChecker` (모델은 구제 절차)

**하는 일** : `MeaningGrader` 를 `AnswerChecker` 로 바꾼다. 하는 일이 달라졌기 때문이다 — **뜻을 보던 것이 글자를 보게 됐고, 이제는 코드가 못 가른 것만 본다.**

> **검토 (2026-09-10) — 코드를 열어 보고 여섯 가지를 고쳤다**
>
> | | 무엇 | 어떻게 |
> |---|---|---|
> | ① | `CheckBasis` 에 코드 전용 케이스(`exactMatch`·`phoneticMatch`)가 **모델 선택지**에 있었다 | **열거형을 둘로 나눈다** |
> | ② | `.sentence` 도 모델로 가는데 지침은 「글자가 같으면」이라 **174문항이 무조건 오답** | 지침에 뜻 판정 한 줄 · `sameMeaning` 근거 추가 |
> | ③ | `AnswerChecker.swift` 에 옛 `MeaningGrader` 와 `@Generable GradingResult` 가 남아 있다 | 지운다 |
> | ④ | **시한이 없다.** `withTimeout`·`ModelGate` 가 저장소 어디에도 없다 (5차 9단계 미적용) | `ModelCall.swift` 로 같이 넣는다 |
> | ⑤ | 세션을 매번 새로 만들고 `prewarm()` 이 없다 | 다 쓴 뒤 **새 세션을 미리 데워 둔다** |
> | ⑥ | 머리말·요약이 낡았다 (`AnswerKind.swift`, 「열한 가지」) | 아래 목록대로 |
>
> **②는 코드가 계획서보다 앞서 있었다.** `AnswerMatcher` 가 이미 `.list, .sentence` 를 함께 넘기고 있고, 그쪽이 맞다. 계획서를 코드에 맞췄다.

#### ① `KCT/Grading/AnswerTypes.swift` — 근거를 두 열거형으로 나눈다

`CheckBasis` 를 통째로 교체한다.

```swift
/// **모델이** 고르는 판정 근거.
///
/// 코드가 정하는 것(``MatchBasis``)은 여기 없습니다. `@Generable` 이라 모델은 이 목록에서만
/// 고를 수 있는데, 코드가 이미 거른 뒤 넘어온 건에 「글자가 그대로 같다」를 고를 수 있으면
/// **로그가 거짓말을 합니다.** 3단계의 목적이 근거를 숫자로 보는 것이므로 그 숫자부터 참이어야 합니다.
@Generable
enum CheckBasis {
    /// 여러 항목을 다 댔다 — 순서는 달라도 된다
    case listMatch
    /// 군더더기를 걷어내니 같다
    case afterTrimming
    /// 글자는 다르지만 뜻이 같다 — 설명 문장에서만
    case sameMeaning
    /// 한 글자 이상 다르다
    case differentLetters
    /// 아예 다른 이름이다
    case differentName
    /// 답을 둘 이상 대고 정하지 않았다
    case undecided
    /// 여러 항목 중 일부만 말했다
    case partialList
}

/// **코드가** 정하는 판정 근거. ``AnswerMatcher`` 만 만듭니다.
enum MatchBasis: String {
    /// 글자가 그대로 같다
    case exactMatch
    /// 발음이 같다 — 음성 입력에서만
    case phoneticMatch
}
```

`AnswerMatcher.Outcome` 도 따라 바뀐다.

```swift
    enum Outcome {
        case correct(MatchBasis)
        case needsModel
    }
```

> **로그에는 둘이 한 칸에 들어간다.** 그래서 `GradingResult.basis` 는 열거형이 아니라 `String?` 이다 (아래 ④). 화면은 이 값을 안 쓰고 로그만 쓰므로 타입을 지킬 값어치가 없다.

#### ⑥ 같은 파일의 낡은 머리말

```swift
//
//  AnswerTypes.swift
//  KCT
//
//  역할 : 답을 어떻게 볼 것인가 - 채점이 쓰는 낱말들
//  요점 : 모양은 채점이 보고, 주제는 해설이 본다. 축이 둘이다
//
//  ── 구성 ──────────────────────────────────────────────
//  AnswerShape   답의 모양. 셋   word · list · sentence
//  AnswerKind    답의 주제. 아홉  number · person · place · day · event
//                               thing · heritage · system · procedure
//  AnswerSource  답이 어디서 왔나. 둘  typed · voice
//  CheckBasis    모델이 고르는 근거. 일곱
//  MatchBasis    코드가 정하는 근거. 둘
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : Question · AnswerMatcher · AnswerChecker · CommentaryWriter
//  기대는 것    : FoundationModels (CheckBasis 의 @Generable 만)
//  건드리지 않는 것 : 판정 자체 - 여기는 낱말만 둔다
//
```

`AnswerKind` 의 `- Important:` 문단도 **거짓이 됐다.** 「앞의 둘은 답의 모양으로」는 `list`·`meaning` 이 있던 시절 이야기다.

```swift
/// 정답이 어떤 종류인가. 이 값에 따라 ``CommentaryWriter`` 가 다른 지침을 씁니다.
///
/// 사람 이름에 글자 풀이를 붙이면 거짓이 나오고, 제도 이름은 반대로 글자 풀이가
/// 가장 잘 통하기 때문입니다.
///
/// - Note: 답의 **모양**은 ``AnswerShape`` 가 따로 맡습니다. 「고구려, 백제, 신라」는
///   목록이면서 동시에 ``place`` 이므로 한 열거형에 넣을 수 없습니다.
```

`case number` 에 주석이 빠져 있다.

```swift
    /// 연도·날짜·숫자 — "1950년 6월 25일", "만 18세"
    case number
```

#### ④ `KCT/Grading/ModelCall.swift` — 새 파일 (시한)

모델이 안 돌아오면 「채점 중이에요」에서 **영영 멈춘다.** 9/6 에 실제로 겪었고 아직 안 고쳤다.

```swift
//
//  ModelCall.swift
//  KCT
//
//  역할 : 모델을 부를 때 공통으로 하는 것 - 지금은 시한 하나
//  요점 : 안 돌아오는 것과 실패하는 것을 같게 다룬다. 화면이 멈추지 않게
//
//  ── 구성 ──────────────────────────────────────────────
//  ModelTimeout            시한이 지났다는 오류
//  withTimeout(seconds:_:) 시한 안에 안 끝나면 던진다
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : AnswerChecker · CommentaryWriter
//  기대는 것    : 없음 - Swift Concurrency 뿐
//

import Foundation

/// 모델이 시한 안에 답하지 않았다.
struct ModelTimeout: Error {}

/// 시한 안에 끝나지 않으면 ``ModelTimeout`` 을 던진다.
///
/// **안 돌아오는 것을 실패와 같게** 만듭니다. 부르는 쪽은 `catch` 하나만 쓰면 되고,
/// 화면은 어느 쪽이든 같은 문구를 냅니다 — 어르신에게 「모델이 느립니다」는 아무 뜻이 없습니다.
///
/// - Note: 먼저 끝난 쪽을 받고 나머지는 취소합니다. 모델 쪽이 이기면 잠자기가 취소되고,
///   잠자기가 이기면 모델 쪽이 취소됩니다.
func withTimeout<T: Sendable>(
    seconds: Double,
    _ work: @escaping @Sendable () async throws -> T
) async throws -> T {
    try await withThrowingTaskGroup(of: T.self) { group in
        group.addTask { try await work() }
        group.addTask {
            try await Task.sleep(for: .seconds(seconds))
            throw ModelTimeout()
        }

        let first = try await group.next()!
        group.cancelAll()
        return first
    }
}
```

**몇 초로 두나** — 9/5 로그에서 직접입력 중앙값이 17.9초였고 그중 상당 부분이 모델을 기다린 시간이다. **12초**로 시작한다. 로그의 `basis` 가 비어 있는 줄이 시한에 걸린 것이니, 그 비율을 보고 조정한다.

#### ③⑤ `KCT/Grading/AnswerChecker.swift` — 통째로 다시 쓴다

**지금 이 파일에 옛것이 남아 있다.** 머리말이 `MeaningGrader.swift` 이고, `@Generable struct GradingResult` 와 `struct MeaningGrader` 가 그대로 있다. 아래 내용으로 **파일 전체를 갈아 끼운다.**

```swift
//
//  AnswerChecker.swift
//  KCT
//
//  역할 : 코드가 못 가른 답을 모델에게 묻는다
//  요점 : 모델은 판사가 아니라 마지막 구제 절차다. 확실한 것은 이미 코드가 통과시켰다
//
//  ── 구성 ──────────────────────────────────────────────
//  AnswerCheck (@Generable)   모델이 만들어 주는 판정
//  ├─ isCorrect               같은가
//  ├─ reason                  한국어 한 문장 (로그용)
//  └─ basis                   무엇을 근거로 보았나
//
//  AnswerChecker
//  ├─ prepare()               회차를 시작할 때 모델을 깨워 둔다
//  └─ check(answer:correctAnswer:) async throws -> AnswerCheck
//
//  ── 흐름 ──────────────────────────────────────────────
//  QuizSession.judge() 의 ③층
//    → check(answer:correctAnswer:)
//    → 시한 12초를 걸고 세션에 묻는다
//    → 유도 생성으로 AnswerCheck 를 그대로 받는다
//    → 다 쓴 세션은 버리고 새것을 미리 데워 둔다
//    → 실패하거나 시한이 지나면 throw → QuizSession 이 조용히 오답 처리한다
//
//  ── 연결 ──────────────────────────────────────────────
//  불러 쓰는 곳 : QuizSession.judge()
//  기대는 것    : FoundationModels, CheckBasis, withTimeout
//  건드리지 않는 것 : 문제 지문 - 프롬프트에 넣지 않는다 (안전 필터)
//

import Foundation
import FoundationModels

/// 모델이 만들어 주는 판정.
@Generable
struct AnswerCheck {
    @Guide(description: "정답과 답변이 같으면 true, 다르면 false")
    let isCorrect: Bool

    @Guide(description: "왜 그렇게 보았는지 한국어 한 문장")
    let reason: String

    @Guide(description: "판정의 근거")
    let basis: CheckBasis
}

/// 코드가 못 가른 답만 모델에게 묻습니다. ``AnswerMatcher`` 와 짝을 이룹니다.
///
/// - Important: 판단이 서지 않으면 오답으로 둡니다 — 틀렸는데 맞다고 하면 사다리가 잘못
///   올라가고 아무도 모르지만, 맞았는데 틀렸다고 하면 그 문항을 곧 다시 만나 회복됩니다.
@MainActor
final class AnswerChecker {

    /// 모델이 답하기를 기다리는 한계. 넘으면 오답으로 두고 넘어갑니다.
    private static let timeout: Double = 12

    private var session = LanguageModelSession(instructions: Self.instructions)

    /// 회차를 시작할 때 부릅니다. 첫 직접입력의 기다림이 줄어듭니다.
    func prepare() {
        session.prewarm()
    }

    func check(answer: String, correctAnswer: String) async throws -> AnswerCheck {
        // 다 쓴 세션은 버린다. 남겨 두면 앞선 채점이 문맥에 쌓여
        // 뒤의 채점에 영향을 주고, 문맥 창도 계속 커진다.
        defer { renew() }

        let prompt = """
            정답: \(correctAnswer)
            답변: \(answer)

            답변이 정답과 같은지 채점하세요.
            """

        let session = self.session
        let response = try await withTimeout(seconds: Self.timeout) {
            try await session.respond(
                to: prompt,
                generating: AnswerCheck.self,
                options: GenerationOptions(temperature: 0.1))
        }
        return response.content
    }

    /// 새 세션을 만들고 **미리 데워 둔다.**
    ///
    /// 채점마다 세션을 새로 만드는 이유는 문맥을 깨끗이 하기 위해서인데, 그러면 매번
    /// 모델을 다시 깨우게 됩니다. 다 쓴 **직후에** 만들어 두면 다음 채점 때는 이미 따뜻합니다.
    private func renew() {
        session = LanguageModelSession(instructions: Self.instructions)
        session.prewarm()
    }
}
```

**온도를 낮춘다.** 같은 답은 같게 채점되어야 한다. 「테국기」가 한 번은 통과하고 한 번은 안 된 것이 이것일 수 있다.

#### ② 지침 — 규칙은 여기 한 곳에만

같은 파일 `extension` 에 둔다. `static` 이라 세션을 새로 만들어도 문자열은 한 벌이다.

```swift
extension AnswerChecker {

    /// 채점 규칙. **여기 한 곳에만 적는다** — `@Guide` 에 되풀이하면 스키마가 커진다.
    static let instructions = """
        당신은 한국어 받아쓰기 채점기입니다.
        '정답'과 '답변'의 글자가 같은지 봅니다. 소리가 비슷한지는 보지 않습니다.

        [정답으로 두는 것]
        · 글자가 모두 같다.
        · 띄어쓰기나 문장부호만 다르다.                      "단군 왕검"
        · 답 앞뒤에 말이 붙었을 뿐 답의 글자는 같다.          "그래 고구려야" · "음 이건 고조선이요"
        · 여러 항목을 다 말했다. 순서는 달라도 된다.          "신라 백제 고구려"

        [오답으로 두는 것]
        · 글자가 하나라도 다르다. 소리가 비슷해도 오답이다.   "고죠선" · "태국기"
        · 글자가 빠졌거나 더 붙었다.                        "조선" · "세종"
        · 다른 이름이다.                                    "단군신화"
        · 답을 둘 이상 대고 정하지 않았다.
        · 여러 항목 중 일부만 말했다.                        "고구려하고 백제요"

        [정답이 한 문장일 때만 다르게 본다]
        · 정답이 낱말이 아니라 문장이면, 글자가 아니라 뜻이 같은지 봅니다.
          말의 차례가 바뀌거나 말투가 달라도, 같은 것을 말하고 있으면 정답입니다.
          정답 "널리 인간을 이롭게 한다" / 답변 "인간을 널리 이롭게 하는 것" → 정답
          이때 근거는 sameMeaning 입니다.

        확실하지 않으면 오답으로 둡니다.
        """
}
```

**「군더더기를 걷어낸다」를 안 쓴다.** 무엇이 군더더기인지 모델이 정하게 되고, 그러면 「고죠선」의 「죠」도 군더더기가 될 수 있다. 대신 **실제 말투를 예시로** 박는다 — 「그래 고구려야」는 나중에 붙일 음성 입력에서 그대로 나올 말이다.

**뜻 판정을 맨 아래 따로 둔 이유** — 위의 두 목록은 글자를 보는 규칙이고, 이것 하나만 다르다. 섞어 두면 낱말 문항에까지 뜻 판정이 새어 「조선」이 「고조선」의 정답이 된다. **`.sentence` 문항은 정답이 문장이라 모델이 스스로 알아본다** — `shape` 를 프롬프트로 넘기지 않아도 된다.

**프롬프트에 문제 지문을 안 넣는 이유 둘** — 글자를 비교하는 데 필요 없고, 「일제의 지배」·「목숨을 바친」 같은 낱말이 **안전 필터에 걸린다** (2026-09-06 `May contain unsafe content`).

#### `KCT/Screens/QuizSession.swift` — `judge` 를 세 층으로

```swift
    private func judge(_ item: QuizItem, answer: String) async -> Bool {
        // ① 선다·O/X 는 정답이 명확하다
        if let byRule = RuleGrader.grade(item, userAnswer: answer) {
            results[item.id] = GradingResult(isCorrect: byRule, reason: "", basis: nil)
            return byRule
        }

        // ② 코드로 가릴 수 있는 것은 여기서 끝낸다
        switch AnswerMatcher.check(
            answer,
            against: item.question.answer,
            shape: item.question.shape,
            from: .typed) {
        case .correct(let basis):
            results[item.id] = GradingResult(isCorrect: true, reason: "", basis: basis.rawValue)
            return true
        case .needsModel:
            break
        }

        // ③ 애매한 것만 모델에게 넘긴다
        do {
            let check = try await answerChecker.check(
                answer: answer,
                correctAnswer: item.question.answer)

            results[item.id] = GradingResult(
                isCorrect: check.isCorrect,
                reason: check.reason,
                basis: String(describing: check.basis))

            return check.isCorrect
        } catch {
            // 시한이 지난 것과 실패한 것을 같게 다룬다. 화면에는 조용히 오답.
            print("❌ 채점 실패:", error)
            results[item.id] = GradingResult(isCorrect: false, reason: "", basis: nil)
            return false
        }
    }
```

프로퍼티도 바꾼다.

```swift
    private let answerChecker = AnswerChecker()      // meaningGrader 를 대신한다
```

그리고 회차를 준비하는 곳(`prepareSession()`)에 한 줄.

```swift
        answerChecker.prepare()
```

`GradingResult` 는 이제 **평범한 struct** 다. 모델이 만드는 것은 `AnswerCheck` 이고, 이것은 화면과 로그가 쓴다.

```swift
/// 한 문항의 채점 결과. 화면과 로그가 쓴다.
struct GradingResult {
    let isCorrect: Bool
    let reason: String

    /// 무엇을 근거로 판정했나. 코드가 정한 것과 모델이 고른 것이 함께 들어와
    /// **열거형이 아니라 글자**다. 로그에만 쓰이므로 타입을 지킬 값어치가 없다.
    let basis: String?
}
```

> `@Generable struct GradingResult` 와 `struct MeaningGrader` 는 **지운다.** 옛 파일에 남아 있으면 이름이 겹쳐 컴파일되지 않는다.

#### 로그에 `basis` 를 남긴다

**따라 칠 코드는 `6차_3단계_로그.md` (HISTORY.md 로 합침) 에 있다.** 고칠 곳은 넷 — Supabase 정책 · `ObsRecord` · `ObsUploader` · `QuizSession.saveObsRecord`.

`explanation_source` 도 같은 자리라 함께 넣는다. 서버 정책이 `'cloud'` 와 `'device'` 만 받게 되어 있고, 지금은 전부 온디바이스라 `"device"` 다.

#### 지울 것 · 고칠 것

| | 무엇 |
|---|---|
| 지운다 | `AnswerChecker.swift` 안의 `@Generable struct GradingResult` · `struct MeaningGrader` |
| 고친다 | `KCT.docc/GradingPath.md` — `MeaningGrader` 를 `AnswerMatcher` → `AnswerChecker` 두 층으로 (규칙 20) |
| 더한다 | `AnswerMatcher.swift` 에 파일 최상단 요약 (규칙 5). 지금 `// Created by` 뿐이다 |

#### 확인

```sql
select basis, is_correct, count(*)
from obs_record where mode_raw = 3
group by basis, is_correct order by count(*) desc;
```

| 보이는 것 | 뜻 |
|---|---|
| `exactMatch` 가 대부분 | **코드가 일하고 있다.** 모델을 부르는 횟수가 줄었다 |
| `basis` 가 `null` 인데 `is_correct = false` | **시한에 걸렸거나 실패했다.** 12초를 조정할 근거 |
| `differentLetters` 인데 `is_correct = true` | **모델이 규칙을 어겼다.** 바로 보인다 |
| `sameMeaning` 이 낱말 문항에 붙었다 | 뜻 판정이 **샜다.** 지침의 맨 아래 문단을 조인다 |
| `partialList` 가 쌓인다 | 목록형을 직접입력까지 올릴지 다시 본다 |

**세 번째 줄이 이 단계의 진짜 확인이다.** 지금은 오채점이 나도 왜 통과했는지 알 방법이 없다.

### 4단계 — `facts`

**하는 일** : 20문항에 재료를 손으로 넣고, `CommentaryWriter` 가 **재료에 있는 것만** 쓰게 한다.

```
문제: (넣지 않는다)
답: 개천절
알려진 사실:
  · 10월 3일
  · 단군왕검이 고조선을 세운 것을 기리는 날
  · '하늘이 열린 날'이라는 뜻

위 '알려진 사실' 안에 있는 것만 써서 두 문장 이내로 쓰세요.
사실에 없는 날짜, 숫자, 뜻은 절대 넣지 마세요.
```

**왜** : 9/5 에 오답 다섯 중 **셋의 해설이 사실과 달랐다.** 「고조선은 크고 높은 나라」, 「태극기는 붉은색과 하얀색의 띠」. **어머니가 틀린 자리에서 창이 또 틀린 것을 알려줬다.**

모델의 일이 **「기억해 내기」에서 「고쳐 쓰기」로** 바뀐다. 고쳐 쓰기는 작은 모델도 한다.

#### 지침도 두 축으로 갈린다

2단계에서 `AnswerKind` 가 아홉으로 줄고 `AnswerShape` 가 따로 생기므로, `CommentaryWriter.guide(for:)` 도 따라 바뀐다. **지금은 11개 블록이 평평하다.**

```
주제 지침 (9)   guide(for kind: AnswerKind)
                 「사람은 글자를 풀지 않는다」 「제도는 글자를 푸는 게 가장 좋다」
       +
모양 한 줄 (3)  note(for shape: AnswerShape)
                 .list     → 「항목을 빠짐없이 말한다」
                 .sentence → 「짧게 줄이지 말고 뜻을 살린다」
                 .word     → 덧붙이지 않는다
```

**이것이 두 축으로 나눈 실질적인 이득이다.** 지금은 「팔만대장경, 직지심체요절」이 `list` 지침을 받아 「셋을 다 대세요」밖에 못 한다. 나눈 뒤에는 **문화유산 지침 + 목록 한 줄**을 받는다.

`meaning` 지침 블록은 **없앤다.** 그 174문항은 이제 저마다의 주제 지침을 받고, 「설명 문장이다」는 모양 한 줄이 맡는다.

**확인** : 20문항을 다 틀려 보고 해설을 읽는다. **사실이 아닌 문장이 하나라도 있으면** 재료가 부족하거나 지침이 샌 것이다.

### 5단계 — `glossary`

**하는 일** : 낱말 사전을 만들고, **질문 아래 작은 글씨 한 줄**로 보여준다.

```
고조선의 건국이념은 무엇입니까?
건국이념 = 나라를 세울 때 내세운 뜻
```

**누르지 않아도 보인다.** 8/28·8/31 기록에서 어머니가 스스로 누른 것은 「다시 읽기」뿐이었다.

**얼마나 드나** : 질문에 나오는 낱말 2,265개 중 **자주 나오는 300개가 58%를 덮는다.** 어머니가 이미 아는 것을 빼면 **150~250개.** 한 번 만들면 문항이 늘어도 다시 만들 필요가 없다.

**확인** : 읽는 시간(`secToFirstTouch`)이 긴 문항에서 그 낱말이 사전에 있는가.

### 6단계 — 서버 갱신

**하는 일** : 4번 절의 ②③. Supabase 에 `content_version` 과 JSON 을 두고, 앱이 조용히 확인해 받아 둔다.

**확인** : 비행기 모드에서 앱이 그대로 도는가. 받다가 실패해도 예전 것으로 도는가.

### 7단계 — `KnowledgeTool`

**하는 일** : 모델이 낱말을 정해 `QuestionCatalog` 과 `GlossaryCatalog` 을 뒤지게 한다.

```swift
@Guide(description: "찾고 싶은 낱말. 예: 개천절, 고조선, 단군왕검, 10월 3일")
let keyword: String
```

**문항 번호가 아니라 낱말을 받는다.** 번호를 받으면 어머니가 이미 어느 문항인지 알아야 한다.

**찾는 순서**

```
① 사전에 있나?    → 뜻 + examples
② 문항의 답인가?  → 그 문항의 facts
③ 태그로 걸리나?  → 걸리는 문항들의 답
④ 없다           → "그건 아직 모르겠어요"
```

**④를 지침에 못 박는다** — 「찾은 것이 없으면 모른다고 말한다. 아는 척하지 않는다.」 지금까지 환각이 난 자리가 전부 **빈칸을 채우라고 시킨 곳**이었다.

**미리 알아 둘 것** : 「그때 왕이 누구였지?」에 답하려면 모델이 대화 문맥에서 **「그때 = 고조선」을 읽어내야** 한다. 온디바이스 소형 모델에게 이것이 쉽지 않다. **안 되면 「왜요?」 같은 버튼 몇 개**로 물음을 미리 정해 두는 쪽이 낫다 — 그러면 낱말을 우리가 정하니 모델이 헤맬 일이 없다.

---

## 5-1. 곁가지 — 해설이 도착해도 반짝임이 멈추지 않는다

> 단계와 무관하게 **지금 바로 고친다.** `Screens/FeedbackSheet.swift` 한 파일이다.

### 증상

해설을 기다리는 동안 「잠시만 같이 살펴봐요」가 옅게-진하게 맥동한다. 그런데 **해설이 도착해 글자가 바뀐 뒤에도 계속 맥동한다.** 다 온 글을 읽는데 밝기가 오르내려 읽기가 어렵다.

### 원인

```swift
.opacity(commentaryOpacity)          // 값은 바뀐다
...
.animation(pulseAnimation, value: isPulsing)   // 그런데 감시하는 것은 isPulsing 뿐
```

`.animation(_:value:)` 는 **`value` 가 바뀔 때만** 애니메이션을 다시 정합니다. `isPulsing` 은 `onAppear` 에서 한 번 `true` 가 된 뒤 **다시는 바뀌지 않습니다.**

해설이 도착하면 `feedback.isReady` 가 `true` 가 되어 `commentaryOpacity` 는 `1`, `pulseAnimation` 은 `nil` 이 됩니다. **둘 다 제대로 계산됩니다.** 하지만 그것을 화면에 다시 걸어 줄 계기가 없어, 처음에 걸어 둔 `repeatForever` 가 그대로 돕니다.

**「값은 맞는데 화면이 안 바뀐다」의 전형입니다.** 계산이 틀린 게 아니라 **다시 그릴 신호가 없는 것**입니다.

### 고치는 방향

두 가지를 같이 합니다.

| | 무엇 | 왜 |
|---|---|---|
| ① | 애니메이션을 **`Text` 에만** 건다 | 지금은 `VStack` 전체에 걸려 있어 정답 글자·버튼까지 영향권이다 |
| ② | 해설이 오면 **`isPulsing` 을 내린다** | 감시 대상이 실제로 바뀌어야 다시 걸린다. `pulseAnimation` 은 그때 이미 `nil` 이라 **소리 없이 멈춘다** |

②가 핵심입니다. 「멈춰라」고 시키는 게 아니라 **감시하는 값을 움직여 다시 계산하게** 만드는 것입니다.

---

### 따라 칠 코드

#### 1. `ScrollView` 안의 `Text` — `.opacity` 아래 한 줄을 더한다

```swift
            ScrollView {
                Text(commentaryText)
                    .font(.system(size: 24, weight: .medium))
                    .foregroundStyle(.black)
                    .lineSpacing(8)
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .opacity(commentaryOpacity)
                    .animation(pulseAnimation, value: isPulsing)
            }
            .padding(.top, 24)
```

#### 2. `VStack` 끝의 수식어 두 줄을 바꾼다

**지운다**

```swift
        .animation(pulseAnimation, value: isPulsing)
        .onAppear { isPulsing = true }
```

**대신 넣는다**

```swift
        // 이미 도착한 채로 열렸으면 아예 맥동하지 않는다.
        .onAppear { isPulsing = !feedback.isReady }
        // 도착하면 감시 값을 내린다. 그때 pulseAnimation 은 nil 이라 그 자리에서 멈춘다.
        .onChange(of: feedback.isReady) { _, isReady in
            if isReady { isPulsing = false }
        }
```

#### 3. 파일 맨 위 설명이 낡았다 — 같이 고친다

`secondsLeft` 와 「3초 뒤에 켜진다」는 **5차에서 없앤 것**이다. 지금 남아 있으면 다음에 이 파일을 열 때 없는 것을 찾게 된다.

```swift
//  역할 : 틀렸을 때 정답과 해설을 보여주는 모달
//  요점 : 정답이 가장 크다. 해설을 기다리는 동안만 글자가 맥동한다
//
//  ── 구성 ──────────────────────────────────────────────
//  FeedbackSheet
//  ├─ feedback        고른 답 · 정답 · 해설 (QuizSession 이 만든다)
//  ├─ onNext          「다음 문제」를 눌렀을 때 위에 알린다
//  ├─ isPulsing       해설을 기다리는 중인가. 도착하면 내려간다
//  └─ commentaryText  해설 속 정답을 연두로 칠한 문장
```

그리고 아래 `- Note: 버튼이 3초 뒤에 켜지는 이유 ...` 문단도 지운다. 버튼은 지금 늘 켜져 있다.

---

### 확인할 것

1. 직접입력에서 오답 → 모달이 뜨고 「잠시만 같이 살펴봐요」가 맥동한다
2. **해설이 뜨는 순간 맥동이 멈춘다** — 밝기가 고정된다
3. 선다·O/X 오답처럼 해설이 **거의 바로** 오는 경우, 한 번 깜빡이고 마는 게 아니라 **처음부터 또렷하다**
4. 「동작 줄이기」를 켠 기기에서는 애초에 맥동이 없다 (`reduceMotion`)
5. 해설이 실패해 「다음 문제 버튼을 눌러주세요.」가 뜬 경우 — **이것도 `isReady` 로 쳐서 멈춰야 한다.** 안 멈추면 `IncorrectCommentary.isReady` 가 `placeholder` 하고만 비교하고 있어서다

**5번이 이 고침의 진짜 경계다.** 「도착했다」의 뜻이 「모델이 성공했다」가 아니라 **「더 기다릴 것이 없다」** 여야 한다.

---

## 6. 함께 넣을 만한 것

| 무엇 | 어디에 | 왜 |
|---|---|---|
| **`prewarm()`** | 회차를 시작할 때 | 첫 직접입력의 기다림이 준다. 지금 17.9초의 일부가 **모델을 깨우는 시간**이다 |
| **`streamResponse`** | 해설·대화형 | 첫 글자가 0.5초에 나온다. **3초를 버티는 대신 흐르게** 한다 |
| **`SystemLanguageModel.availability`** | 앱을 켤 때 한 번 | 모델을 못 쓰는 기기인지 **미리** 안다. 지금은 실패한 뒤에야 안다 |
| **`@Guide(.anyOf([...]))`** | 값이 정해진 자리 | 모델이 없는 값을 지어낼 수 없다 |

**`prewarm()` 과 `streamResponse` 가 가장 싸고 크다.** 둘 다 코드가 적고 어머니가 바로 느낀다.

---

## 7. 아직 안 정한 것

- **1,130문항의 `facts` 를 누가 채우나.** 20문항은 손으로 쓴다. 그다음은 `문제집_유형분류.xlsx` 에 칸을 더해 반은 규칙으로 채우고 나머지를 손보는 쪽을 본다
- **대화형을 어디에 두나.** 지금 모달은 「다음 문제」 하나뿐이다. **어머니가 「왜요?」를 누르실지**는 만들기 전에 종이로 물어봐야 한다
- **괄호 병기 41개를 `/` 로 옮기는 정리.** `을사조약(을사늑약)` → `을사조약/을사늑약`. 부연 괄호(`9년 (초등학교 6년 + 중학교 3년)`)는 괄호째 지운다. 이걸 해야 2단계의 `candidates` 가 일한다
- **정답 칸에 해설이 섞인 문항이 있다.** 쉼표가 든 121개를 훑다 발견했다 — `'고구려, 백제, 신라, 3국 중 신라는 가장 늦게…'` 처럼 정답과 설명이 한 칸에 있다. 이런 칸은 **채점에도 해설에도 해가 된다.** `문제집_유형분류.xlsx` 를 훑어 정답 칸을 정답만 남기는 정리가 따로 필요하다
- **`weight` 를 누가 정하나.** 지금은 손으로. 나중에는 **어머니가 자주 틀리는 문항의 `confusedWith` 무게를 올리는** 식으로 로그가 정할 수 있다

---

## 8. 같이 치울 것

> 2026-09-08 에 저장소를 훑어 찾은 것들. **파일이 커지기 전에 지운다.**

### 지울 파일

| 파일 | 왜 | 상태 |
|---|---|---|
| `KCT/Data/QuestionSource.swift` | `ContentFile` 이 대신한다 | ✅ 지워짐 |
| `KCT/Data/StoredQuestionSource.swift` | 같은 이유 | ✅ 지워짐 |
| `_to_delete/` 폴더 | `4차_6단계.md`·`6차_계획_DB와도구.md` | ⬜ **남아 있다.** `git rm -r _to_delete` |

### 지울 죽은 코드

| 어디 | 무엇 | 확인한 것 | 상태 |
|---|---|---|---|
| `Progress/QuestionProgress.swift:149` | `nextDue(mastered:correct:from:)` | 저장소 전체에서 **선언 한 곳뿐** | ⬜ 남아 있다 |
| `Observation/ObsRecord.swift:145` | `hesitationSec` | **선언 한 곳뿐** | ⬜ 남아 있다 |

**`nextDue` 를 지우자는 이유** — 8/31 부터 죽어 있었고, 되살릴 때는 **간격 설계를 다시 한다**고 이미 정했다(어머니 기록과 논문을 보고). 그러면 지금 코드는 어차피 못 쓴다. 남겨 두면 「살아 있는 줄 알고」 읽는 사람만 헷갈린다.

**다만 `nextDueAt` 프로퍼티는 남긴다.** SwiftData 스키마라 지우면 마이그레이션이 필요하고, `SessionBuilder.isDueEarlier` 가 1순위로 아직 읽는다.

**`moveLadder(correct:now:)` 의 `now` 인자도 안 쓰인다.** 다만 DocC 링크가 여러 문서에 박혀 있어 지우면 그 링크들이 깨진다. `nextDue` 를 지우면서 `now` 를 같이 지울지는 **한 번에 하지 말고** 따로 본다.

### 문서 정리 — 루트가 어수선하다

지금 저장소 루트에 `.md` 가 **열네 개**다.

```
루트에 남길 것    CLAUDE.md · RULES.md · PROGRESS.md · Q&A.md
                 (매 세션 읽히거나 계속 쌓이는 것)

문서/ 로 옮길 것  4차_1~5단계 · 4차_보완 ×2 · 4차_7단계_PCC
                 5차_계획 · 5차_구현 · 6차_계획_지식층
                 6차_1단계_보완
                 낱말_구상 · 스토리모드_구상
                 GUIDE · LEARNING_PLAN · STUDY_GUIDE
```

**4차 것들은 다 끝났다.** 지우지 않고 옮기는 이유 — 「왜 그렇게 했나」가 거기 적혀 있고, `Q&A.md` 가 그것을 가리킨다.

### 공개 저장소에 올라가 있는 것 — 확인이 필요하다

**이 저장소는 공개다.** 지금 깃이 추적 중인 것 가운데 이런 것들이 있다.

```
CBL_20260625.key      Keynote 파일
CBL_20260823.pdf
```

그리고 아직 안 올라갔지만 폴더에 있는 것.

```
귀화 문제집 20250518.numbers     1,250문항 원본
문제집_유형분류.xlsx
문제집_유형분류_v2.xlsx           ← 2026-09-10 신설. shape + 재분류 219행
문제집_재분류_219행.xlsx           ← 재분류 초안. v2 에 반영된 뒤에는 지워도 된다
캐릭터.png
```

**어디까지 공개할 생각이신지**에 따라 다릅니다. 문제집 원본과 발표 자료를 공개해도 괜찮다면 그대로 두면 되고, 아니라면 `.gitignore` 에 넣고 `git rm --cached` 로 추적에서 뺍니다. **한 번 올라간 것은 히스토리에 남으므로**, `CBL_*` 두 개는 이미 올라가 있다는 것을 알고 계셔야 합니다.

### 앞으로 합쳐질 것 (지금 말고)

| 언제 | 무엇 |
|---|---|
| 2단계 | `Grading/AnswerKind.swift` 에서 `list`·`meaning` 두 케이스가 빠진다. `AnswerShape.swift` 가 새로 생긴다 |
| 3단계 | `Grading/MeaningGrader.swift` → `AnswerChecker.swift` (하는 일이 달라졌다) |
| 3단계 | `KCT.docc/GradingPath.md` · `KCT.docc/KCT.md` 가 `MeaningGrader`·`QuestionSource`·`BundledQuestionSource` 를 아직 가리킨다. **코드가 바뀔 때 같이 고친다** (규칙 20) |
| 5차 7단계 | `Screens/GradingScreen.swift` 가 되살아난다 (회차 끝의 한 박자) |
