# 10차 계획 — UX/기능 개선 묶음

2026-09-20에 사용자가 우선순위와 함께 정리한 17개 항목을 다룹니다. 9차
문서(`9차_계획_어려운낱말_설명.md`)가 "낱말 사전 모달" 한 기능에 집중했다면,
이 문서는 그 이후 발견된 자잘한 버그·UX 개선과 앞으로의 큰 기능들을 함께
묶어 둡니다.

## 처리 순서 (2026-09-14 대화에서 합의)

**A. 오늘내일 바로 (시각/레이아웃, 새 로직 거의 없음)** — 9, 10, 6, 7, 11
**B. 이번 주 안 (로직은 있지만 범위 작음)** — 2, 8, 5, 1
**C. 리서치 먼저** — 12
**D. 심사 전 필수는 아님 (사후 추가 고려)** — 3, 4
**E. 10월 마감 있는 큰 작업** — 13, 14
**마일스톤** — 15 (10월 중순 첫 심사)
**보류** — 16 (IF-AT, 계속 지켜보기)
**심사 이후 체크포인트** — 17 (리팩토링/문서화)

원래 번호는 사용자가 준 17개 목록 순서 그대로입니다. 이 문서의 각 절 제목은
"10-<번호>" 형식으로 그 번호를 그대로 씁니다.

> **작업 방식 메모 (2026-09-20)** — 9번을 처음에 제가 문제 정의를 스스로
> 넘겨짚고 바로 코드부터 고쳤다가 지적받았습니다. **앞으로는 코드를 고치기
> 전에 먼저 문제를 정확히 같이 정의하고, 여러 안이 있으면 시안으로 먼저
> 보여드린 뒤에 진행합니다.**

---

### 10-9. OX 문제에서 "낱말"과 "답"의 표시가 헷갈리는 문제

**요청 (1차)** — "OX 문제에서 낱말과 답의 밑줄이 겹쳐 보이니 다르게
구분하기, 답을 누를 수 없는 것 처럼 만들기"

**1차 시도 (되돌림)** — 강조(`highlight`, "답")와 사전 낱말(`glossary`,
"낱말")이 같은 글자를 가리킬 때 실선(강조)·점선(사전) 두 밑줄이 겹쳐
그려지는 걸 보고, 제가 임의로 "겹치는 낱말은 점선 밑줄과 탭을 아예
없앤다"는 방향으로 바로 코드를 고쳤습니다. 문제 정의를 사용자와 맞추지
않고 진행한 것이라 지적받았고, 이 변경은 되돌렸습니다.

**요청 (재정의, 2차)** — 실제로 원하신 것은 이렇습니다.

- **낱말**(사전, 점선 밑줄)은 지금 그대로 — 절대 건드리지 않는다.
- **답**(O/X 판단 대상)은 애초에 **밑줄이 아닌 다른 방식**으로 표시한다.
  겹침 자체가 "밑줄끼리" 부딪혀서 생기는 문제이므로, 답 쪽을 밑줄이 아닌
  걸로 바꾸면 낱말의 점선 밑줄과 자리가 겹칠 일이 없어진다.
- 지금 사전 시트가 열려 있는("선택된") 낱말의 표시도, 답 표시와 헷갈리지
  않게 구분되면 좋겠다.

**시안 비교** — 밑줄을 대신할 방법 3가지를 HTML 데모로 만들어 보여드렸습니다.

| 시안 | 표시 방법 |
|---|---|
| **1. 색상 + 굵기만** (선택됨) | 밑줄·배경 없이 답 글자만 시그니처 색 + 더 굵게 |
| 2. 소프트 하이라이트 캡슐 | 답 글자를 옅은 보라 알약(pill) 배경으로 감쌈 |
| 3. 아웃라인 배지 | 답 글자를 채우지 않고 얇은 보라 테두리로만 감쌈 |

세 시안 모두 "답이면서 동시에 낱말인 경우"에 답 표시와 낱말의 점선 밑줄이
서로 다른 자리(글자 자체 vs 글자 아래)를 쓰므로 겹치지 않고, "선택됨"
(사전 열림) 상태는 기존 라벤더 배지 그대로 두어 답 표시와 모양이 달라
구분되게 설계했습니다. **1번(색상+굵기만)으로 결정했습니다.**

**한 것** — `KCT/DesignSystem/KoreanText.swift`의 `updateUIView(_:context:)`에서,
강조(`highlight`) 구간에 밑줄을 더 이상 추가하지 않고, 대신 글자 색은
그대로 두되 폰트 두께만 `.heavy`로 올렸습니다. 지문 글자가 이미 기본
`.bold`라서, 그보다 한 단계 더 굵게 줘야 차이가 보입니다. 밑줄을 안
그리므로 사전 낱말 루프의 "겹치면 건너뛴다" 예외는 필요 없어져 원래대로
되돌렸습니다 — 이제 모든 어려운 낱말은 답과 겹치든 안 겹치든 항상 점선
밑줄 + 탭이 됩니다.

```swift
// 강조 구간("답")은 밑줄이 아니라 색 + 더 굵은 글자로만 표시한다.
if let highlight, !highlight.isEmpty {
    let target = Self.keepingNumbersWithUnits(highlight)
    let range = (displayed as NSString).range(of: target)
    if range.location != NSNotFound {
        attributed.addAttribute(.foregroundColor, value: highlightColor, range: range)
        attributed.addAttribute(
            .font,
            value: UIFont.systemFont(ofSize: font.pointSize, weight: .heavy),
            range: range
        )
    }
}

// 사전 낱말 루프는 원래대로 — 겹침 예외 없이 항상 점선 밑줄 + 탭.
for entry in glossary {
    let target = Self.keepingNumbersWithUnits(entry.word)
    let range = (displayed as NSString).range(of: target)
    if range.location != NSNotFound {
        underlines.append(.init(range: range, color: UIColor(AppColor.textMuted), isDotted: true))
        hits.append((range, entry))
        // ... (선택된 낱말 배경 처리는 그대로)
    }
}
```

**미리 알려드릴 것** — 색과 굵기 두 신호만으로 답을 표시하는 방식이라,
색 구분이 어렵거나 대비에 예민하신 경우 밑줄이 있을 때보다 눈에 덜 띌 수
있습니다. 어머니와 실제로 써보시고 "답이 어디 있는지 잘 안 보인다" 하시면
그때 배경이나 테두리를 더하는 쪽(시안 2·3)으로 보완할 수 있습니다.

**검증 상태** — 다시 빌드해서 (1) O/X 문제의 답 부분에 밑줄 없이 색+굵은
글자만 보이는지, (2) 답이 어려운 낱말이기도 한 문항에서 답 표시(색·굵기)와
낱말의 점선 밑줄이 겹치지 않고 함께 보이는지, (3) 그 낱말을 탭하면 평소처럼
사전 시트가 열리는지 확인해 주세요.


**후속 요청** — "지금 상태에서 답 양 옆에 "" 따옴표로 표시해서 강조하자."
색+굵기만으로는 신호가 약할 수 있다는 우려가 실제로 나와서, 따옴표를 덧붙였습니다.

**한 것** — `updateUIView(_:context:)`에서 `displayed` 문자열을 만든 직후,
강조 대상 앞뒤에 여는/닫는 큰따옴표(“ ”)를 실제로 끼워 넣습니다(지문 원문에는
없는 문자라 화면에 보여줄 때만 추가). 이 삽입을 attributed 문자열을 만들기
**전에** 해야, 그 뒤에 나오는 marker·glossary 검색이 문자 위치가 밀리는 문제
없이 이 문자열을 그대로 기준으로 삼을 수 있습니다. 따옴표를 포함한 범위를
`highlightRange`에 남겨 두고, 색+`.heavy` 굵기를 그 범위(따옴표 포함) 전체에
적용합니다.

```swift
var highlightRange: NSRange?
if let highlight, !highlight.isEmpty {
    let target = Self.keepingNumbersWithUnits(highlight)
    if let swiftRange = displayed.range(of: target) {
        let quoted = "“\(target)”"
        displayed.replaceSubrange(swiftRange, with: quoted)
        highlightRange = (displayed as NSString).range(of: quoted)
    }
}
// ... attributed 생성 이후
if let highlightRange {
    attributed.addAttribute(.foregroundColor, value: highlightColor, range: highlightRange)
    attributed.addAttribute(.font, value: UIFont.systemFont(ofSize: font.pointSize, weight: .heavy), range: highlightRange)
}
```

**판단이 갈리는 지점 (제가 정한 것, 알려드립니다)** — 따옴표 글자로 어떤
걸 쓸지 정해야 했습니다. 사용자가 채팅에 `""`(직선 따옴표)를 치셨지만,
한글 조판에서 인용·강조에 흔히 쓰는 곡선 큰따옴표 "“ ”"(U+201C/U+201D)를
썼습니다. 직선 따옴표(`"..."`)가 더 맞으면 말씀해 주세요, 한 글자만 바꾸면
됩니다. 따옴표도 답과 같은 색+`.heavy` 굵기를 입혀서 한 덩어리로 보이게
했습니다 — 따옴표만 기본 검정으로 두는 안도 가능합니다.

**검증 상태** — 다시 빌드해서 (1) O/X 답 양옆에 따옴표가 실제로 보이는지,
(2) 답이 어려운 낱말이기도 한 문항에서 따옴표+색+굵기와 낱말의 점선 밑줄이
겹치지 않고 함께 보이는지(밑줄은 따옴표 없이 낱말 글자에만 그려져야
합니다), (3) 줄 끝에 답이 걸렸을 때 따옴표 때문에 줄바꿈이 이상해지지
않는지 확인해 주세요.

---

### 10-10. 질문의 낱말 밑줄이 너무 얇아 눈에 안 띄는 문제

**요청 (1차)** — "질문의 밑줄을 조금 둥글게 개선하고 높이를 줘서 눈으로 잘
확인되게 만들기"

**문제 정의 (먼저 맞춘 것)** — "둥글게"와 "높이를 준다"가 여러 갈래로 읽혀
코드부터 고치지 않고 먼저 여쭤봤습니다. 돌아온 답은 **"지금 점선이 너무
얇아서 눈에 안 띈다"** 였고, 그래서 이번 작업의 대상은 **어려운 낱말(사전)
점선 밑줄 하나**로 확정됐습니다. 9차에서 O/X 의 답이 밑줄을 안 쓰게 되면서,
지문에 그려지는 밑줄은 이제 이 점선뿐입니다.

바꾸기 전 상태는 이랬습니다.

| 항목 | 값 |
|---|---|
| 점선 두께 | 1.6pt |
| 점 / 사이 | 1.2pt / 3.4pt (`setLineDash`) |
| 글자와의 간격 | 0pt — 받침 바로 아래에 붙음 |

**시안 비교** — 점 → 파선 → 실선 순으로 진해지는 세 가지를 HTML 데모로
만들어 "지금"과 나란히 놓고 보여드렸습니다. 세 시안 모두 **글자에서 2pt
띄우는 것**은 공통으로 넣었습니다 — 간격 0 때문에 점선이 받침("국"·"웅")과
섞여 보이던 것이 두께와는 별개의 문제였기 때문입니다.

| 시안 | 표시 방법 |
|---|---|
| **1. 또렷한 동그란 점** (선택됨) | 두께 3pt 동그라미, 사이 넓힘 — 점선 성격 유지, 크기만 키움 |
| 2. 굵은 파선 | 2.5pt 짧은 선분 반복 — 점과 실선의 중간 |
| 3. 굵은 실선 | 2.5pt 이어진 줄 — 가장 잘 보임 |

색은 세 시안 모두 지금 그대로(`AppColor.textMuted`) 뒀습니다. 보라색으로
바꾸면 더 튀지만 답 색(`AppColor.signature`)과 같은 계열이라 헷갈릴 수
있어서입니다. **1번(또렷한 동그란 점)으로 결정했습니다.**

**추가 요청** — "저 동그라미가 잘려나가지 않게 간격을 낱말별로 맞춰서 잘
조절해줬으면 좋겠어."

데모에서 낱말 오른쪽 끝의 점이 반쯤 잘려 보이다 것을 짚으신 것이고, 이건
데모만의 문제가 아니라 `setLineDash` 방식 자체의 문제였습니다. dash 패턴은
낱말 왼쪽 끝에서 시작해 일정 주기로 반복할 뿐이라, **낱말 너비가 그 주기로
나눠떨어지지 않으면 오른쪽 끝에서 점이 잘린 채 끝납니다.** 낱말마다 너비가
다르니 어떤 주기를 골라도 일부 낱말에서는 반드시 잘립니다.

**한 것** — `KCT/DesignSystem/KoreanText.swift` 의 `UnderlineLabel`.

1. 점선을 `setLineDash` 로 긋지 않고, **낱말(정확히는 줄에 걸린 조각)마다 점
   개수를 세어 고르게 배분해 `fillEllipse` 로 하나씩 찍습니다.** 첫 점의 왼쪽
   끝이 낱말 시작에, 마지막 점의 오른쪽 끝이 낱말 끝에 정확히 맞습니다.
2. `underlineGap` 기본값 0 → **2** (글자에서 띄우기).
3. 점 모양을 속성으로 뺐습니다 — `dottedDotDiameter = 3`, `dottedDotSpacing = 3`.
4. 뷰 높이 계산(`sizeThatFits`)이 실선 두께가 아니라 실제 밑줄 깊이
   (`underlineDepthBelowLine`)를 보게 고쳤습니다. 이게 없으면 점을 키웠을 때
   마지막 줄 밑에서 점이 잘립니다.

```swift
// 점 "중심"이 놓일 수 있는 구간. 양 끝에서 반지름만큼 안으로 들어와야
// 점이 낱말 밖으로 삐져나가지 않는다.
let centerSpan = max(0, width - diameter)
let count = max(1, Int((centerSpan / (diameter + dotSpacing)).rounded()) + 1)
let step = count > 1 ? centerSpan / CGFloat(count - 1) : 0
let firstCenterX = count > 1 ? startX + radius : startX + width / 2

for index in 0 ..< count {
    let centerX = firstCenterX + step * CGFloat(index)
    context.fillEllipse(
        in: CGRect(x: centerX - radius, y: y - radius, width: diameter, height: diameter)
    )
}
```

**대가 (알고 택한 것)** — 점 간격이 **낱말마다 조금씩 달라집니다.**
`dottedDotSpacing = 3` 은 "이 정도면 좋겠다"는 기준일 뿐이고, 실제 간격은
낱말 너비를 점 개수로 나눈 값입니다. 계산해 보면 실제 낱말 너비 범위
(20~120pt)에서 간격은 **2.2~3.8pt** 사이에 들어옵니다 — 나란히 놓고 비교하면
알아볼 수 있는 차이지만, 점이 잘리는 쪽보다 낫다고 봤습니다.

**확인한 것** — 컴파일은 못 하므로 (1) 중괄호 짝, (2) `setLineDash` 가 점선
경로에 남아 있지 않은지, (3) 같은 계산을 파이썬으로 옮겨 낱말 너비
2~120pt 전 구간에서 첫 점의 왼쪽 끝이 0, 마지막 점의 오른쪽 끝이 너비와
정확히 일치하는지를 확인했습니다. 너비가 점 지름(3pt)보다 좁은 극단적인
조각에서만 0.5pt 삐져나가서, 그 경우 지름을 너비까지 줄이도록 막아 뒀습니다.

**검증 상태** — 다시 빌드해서 (1) 낱말 아래 점이 또렷한 동그라미로 보이는지,
(2) 낱말 양 끝에서 점이 잘리지 않는지, (3) 받침 있는 글자("국"·"웅") 아래에서
점이 글자와 안 섞이는지, (4) 낱말이 줄 끝에 걸려 두 줄로 나뉠 때 각 줄
조각마다 점이 온전히 찍히는지, (5) 사전이 열린 낱말(라벤더 배지)에서 배지
아래에 점이 그대로 보이는지 확인해 주세요.

---

### 10-6. 오답 시 오답 해설 기능 UI 및 색상 시인성 수정

**요청 (1차)** — "오답 시 오답 해설 기능 UI 및 색상 시인성 수정."

**문제 정의 (먼저 맞춘 것)** — 코드부터 고치지 않고, 지금 실제로 뜨는 모달을
색상값 그대로 재현한 HTML을 먼저 보여드리고 어디가 안 보이는지 여쭤봤습니다.
돌아온 답은 예상한 후보(배경색이 옅다, 제목 색이 튄다)와 달랐습니다 —

> "지금 디자인은 낱말 해설과 일체감이 없어. 낱말 해설에 적용된 보라색 모달
> 디자인을 적용하고 싶어. 그리고 높이는 화면 절반만 차지하도록 하되 오답과
> 정답은 둘 다 보여주고 싶고."

즉 색 대비 문제가 아니라 **다른 모달(`GlossaryPanel`, 낱말 사전)과 겉모습이
따로 논다**는 것이 진짜 문제였고, 여기서 세 가지가 새로 정해졌습니다.

| 항목 | 이전 | 새로 정한 것 |
|---|---|---|
| 공개 순서 | "정답 보기"를 눌러야 정답이 나오는 두 박자 | 오답·정답 처음부터 같이 보임 |
| 시트 높이 | `.fraction(0.75)` | `.medium` (화면 절반) |
| 글자 크기 | 22~30pt | 그대로 유지 (노안 대비, 줄이지 않음) |

**1라운드 — 보라 모노크롬 시안 3개, 전부 반려** — `GlossaryPanel`을 읽어
"보라 한 계열 안에서 명도 차이로 위계를 만드는" 원칙을 그대로 가져온 시안
3개(모노크롬 / 보라+주황 포인트 / 압축형)를 만들었습니다. 실제 해설
최대 길이(고른 답 설명 1문장 + 정답 해설 최대 3문장, `CommentaryWriter`
기준)로 채워 넣어 시안 2가 절반 높이를 넘기는 것도 미리 잡아 알려드렸습니다.
그런데 어머니 반응은 —

> "다 마음에 안드는데? 이모지를 빼주고. 위에는 오답 타이틀을 붙여줘. 아래
> 정답에는 정답 타이틀을 붙여주고. 그리고 위는 분홍색 배경, 아래는 하늘색
> 배경으로 해줘."

보라 모노크롬 방향 자체가 반려되고, **이모지(👉/✅) 대신 "오답"/"정답" 글자
제목 + 분홍/하늘색**이라는 완전히 다른 방향이 나왔습니다.

**2라운드 — 색 헤더바형 포함 시안 3개** — 새 방향으로 시안 3개(부드러운
카드형 / 색 헤더바형 / 통짜 2단형)를 다시 만들었습니다. 이번에도 최대 길이
실제 콘텐츠로 채워 확인했는데, **셋 다 절반 높이를 넘겼습니다**(부드러운
카드형 +84pt, 색 헤더바형 +98pt, 통짜 2단형 +72pt) — 처음엔 "셋 다 들어간다"고
잘못 판단했다가, flexbox가 내용을 안 보이게 압축하는 바람에 실제로는 잘리고
있던 것을 다시 화면으로 찍어 확인하고 정정해서 알려드렸습니다. 어머니는
**색 헤더바형**을 골랐습니다: "폴더 탭처럼 두 칸이 확실히 나뉘어 보이는" 구조.

**3라운드 — 낱말 해설과 같은 보라 배경** — "2번째 시안이 좋은데 배경 모달은
낱말 모달과 같은 보라색이면 좋겠어." 색 헤더바형은 그대로 두고, 시트 배경만
`GlossaryPanel`과 똑같은 `AppColor.signature.opacity(0.5)` 반투명 보라로
바꾼 데모를 한 장 더 만들어 확인받았습니다 — 오답(분홍)/정답(하늘색) 카드는
그 위에 그대로 얹히고, 손잡이는 흰 계열로 반전했습니다. "응 좋아, 스크롤하면
이어짐은 삭제해주고 일단 적용해보자 실제 앱에서 봐야 알 것 같아"로 최종
승인받아 코드에 반영했습니다. (데모에만 있던 "↓ 스크롤하면 이어짐 ↓" 안내는
실제 앱 UI가 아니므로 코드에는 처음부터 없습니다 — 삭제 요청은 데모 파일
자체에 대한 것이었습니다.)

**한 것**

1. **`KCT/DesignSystem/AppColor.swift`** — `wrongAccent`/`answerAccent`를
   기존 붉은색·파랑에서 짙은 분홍(`#B23A5C` 계열)·짙은 하늘색(`#1C6FA0` 계열)로
   바꾸고, `wrongBackground`/`answerBackground`를 옅은 분홍·하늘색 카드
   배경으로 바꿨습니다. 새로 생긴 "색 헤더 바" 채움색을 위해
   `wrongHeader`(진한 분홍 `#E85D86` 계열)·`answerHeader`(진한 하늘색
   `#3FA9DE` 계열)를 추가했습니다. 강조 글자색(카드 위)과 헤더 바 채움색(흰
   글자를 얹는 배경)을 분리한 이유 — 하나로 겸용하면 옅은 카드 배경 위
   글자 대비가 부족해집니다(계산상 대비비 약 2.7:1로 WCAG AA 미달).
2. **`KCT/Screens/FeedbackSheet.swift`** — 전면 재작성.
   - `@State showsAnswer`와 "정답 보기" 두 박자를 없애고, `chosenCard`
     (오답)와 `answerCard`(정답)를 처음부터 같이 그립니다.
   - 각 카드는 색 헤더 바("오답"/"정답", 흰 글자 24pt) + 그 아래 카드
     배경(옅은 분홍/하늘색) 위에 낱말(30pt)과 설명(22pt)을 쌓은 구조로
     `clipShape(RoundedRectangle)`로 하나의 카드처럼 클립했습니다.
   - 화면 맨 위에 있던 "오답 해설" 제목(30pt)은 시안들에 한 번도 없었던
     요소라 없앴습니다 — 각 카드 자체의 "오답"/"정답" 제목이 그 역할을
     대신합니다.
   - `.presentationDetents([.fraction(0.75)])` → `.presentationDetents([.medium])`.
   - `.background(Color.white)` → `.presentationBackground(AppColor.signature.opacity(0.5))`
     로 교체 — `GlossaryPanel`과 똑같은 방식(시트의 진짜 배경 자체를 바꾸는
     것이라 `.background`가 아니라 `.presentationBackground`를 씁니다).
   - 글 속 낱말 강조(`highlight(_:word:color:)`)는 그대로 두고 색만
     새 `wrongAccent`/`answerAccent`로 바뀝니다.

**대가 (알고 택은 것)** — 실제 해설 최대 길이를 넣으면 절반 높이를 여전히
넘칩니다(2라운드에서 확인한 약 98pt). 코드에서는 `ScrollView`로 감싸 뒀으니
잘려서 사라지지는 않지만, **"정답"까지 한 화면에 다 안 보이고 아래로
당겨야 할 수 있습니다.** 절반 높이 유지 + 글자 크기 유지 + 최대 길이 해설을
동시에 만족시키는 것은 이번 재설계로도 풀리지 않은 문제라, 실제 기기에서
자주 나오는 길이의 해설로 확인해 보고 필요하면 별도로 다시 다뤄야 합니다.

**확인한 것** — 컴파일은 못 하므로 (1) 중괄호·괄호 짝(33쌍/108쌍, 새 파일과
동일), (2) `QuizView.swift`가 `FeedbackSheet(feedback:onNext:)`를 그대로
호출하고 있어 공개 인터페이스가 안 바뀌었는지, (3) 없앤 `showsAnswer`를
참조하는 곳이 이 파일 밖에 없는지, (4) `wrongAccent`/`wrongBackground`/
`answerAccent`/`answerBackground`가 `FeedbackSheet.swift` 밖에서 쓰이지
않아 이번 색상 값 교체가 다른 화면에 번지지 않는지를 확인했습니다.

**검증 상태** — 다시 빌드해서 (1) 시트가 화면 절반 높이로 뜨는지, (2) 오답
카드(분홍)·정답 카드(하늘색)가 처음부터 같이 보이는지, (3) 시트 배경이
낱말 해설 패널과 같은 보라 반투명으로 보이는지(뒤 문제 화면이 옅게 비치는지),
(4) 해설이 길 때 스크롤로 끝까지 보이는지, (5) "정답 보기" 버튼이 사라지고
"다음 문제" 버튼만 바로 있는지 확인해 주세요.

**정정 (2026-09) — 두 박자를 없앤 건 잘못 알아들은 것이었다** — 위 "한 것"에서
`@State showsAnswer`를 없애고 오답·정답을 처음부터 같이 보여주게 고쳤었는데,
"누가 두 박자를 없애래 오답 먼저 보이고 정답 보기 누르면 정답이 나타나게
해줘" 라는 정정을 받았습니다. "오답과 정답을 둘 다 보여주고 싶고"를
"동시에"로 잘못 넓혀 해석한 것이었습니다 — 원래 뜻은 그저 "결국엔 정답도
보여달라"였던 셈입니다. `showsAnswer`와 「정답 보기」→「다음 문제」 버튼
전환을 그대로 되살렸습니다. 바뀐 채 남은 것은 오직 **색·모양**뿐입니다 —
"오답"/"정답" 헤더 바(분홍/하늘색), 보라 반투명 배경, `.medium` 높이. 1박자엔
오답 카드만, 「정답 보기」를 누르면 정답 카드가 `.transition(.opacity)`로
이어 붙습니다.

**후속 (2026-09-21) — 스크롤이 불편하다 → 「한 줄 대조」로 구조를 다시 짰다**

"지금 모달 구조는 스크롤을 해야해서 불편한데, 스크롤을 하지 않고도 오답과 정답을
절반 사이즈 모달에서 다 보여주려면 어떤 구조가 가능할지 리서치해줘." 라는 요청에서
시작했습니다. 노인 UI 연구를 먼저 확인하고, 공간을 수치로 재고, 시안 3개를 만들어
**시안 1(한 줄 대조)** 로 정했습니다.

**리서치에서 실제로 쓴 것**

| 확인한 것 | 설계에 쓴 방식 |
|---|---|
| 재인은 노화에도 거의 안 떨어지지만 **회상·분할 주의는 떨어진다** (Brain Aging, NCBI) | 오답·정답이 **같은 화면에** 있어야 한다는 근거. 탭·페이지 분리는 후보에서 뺐다 |
| 관련 정보를 **공간적으로 붙이면** 이해가 올라간다 — 58개 비교·2,426명, **g=0.63** (Educational Psychology Review, 2018) | 두 낱말을 **한 줄에** 붙였다. 비교가 눈 한 번 움직임으로 끝난다 |
| 글자는 **너무 커도** 읽기 속도가 떨어지고, 스마트폰 정독 권장은 17~20px (Frontiers in Psychology, 2022) | 22pt를 줄일 여지가 있다고 보고 시작 → **결과적으로 안 줄였다** |
| **줄 간격**을 넓히면 노인 읽기 성능이 올라간다 (같은 문헌) | 높이가 모자랄 때 `lineSpacing(8)` 은 건드리지 않고 **패딩에서만** 뺐다 |
| 터치 목표 **1cm(≈64pt)** 권장 (NN/g) | 버튼을 56 → **64pt** 로 올렸다 |
| 노인은 스크롤 자체보다 **"지금 위치를 잃는 것"** 을 꺼린다 (Behaviour & IT, 2020) | 애초에 스크롤이 필요 없게 만드는 쪽으로 방향을 잡았다 |

**공간 예산 (iPhone 844pt 기준)**

| | 높이 |
|---|---|
| 시트 `.medium` | 422pt |
| 빼는 것 (위 16 + 버튼 64 + 아래 24 + 사이 12) | −116pt |
| **쓸 수 있는 높이** | **≈306pt** |
| 지금 구조 (헤더 바 2개 + 22pt) | 422pt — **120pt 초과** |
| 글자만 20pt 로 줄임 | 351pt — **여전히 49pt 초과** |
| 시안 1 (헤더 칸 없앰, 글자 22pt 그대로) | **여유 +11pt** |

**여기서 나온 가장 중요한 사실 두 가지**

1. **글자만 줄여서는 못 들어간다.** 색 헤더 바 두 개가 제목만으로 약 100pt를 먹고
   있었고, 그게 진짜 원인이었습니다.
2. **그래서 글자를 줄일 필요가 없었다.** 헤더 칸을 없애니 **22pt 그대로** 들어갑니다.
   처음엔 20pt로 내릴 생각이었는데, 재 보고 취소했습니다.

**한 것** — `KCT/Screens/FeedbackSheet.swift` 전면 재작성, `AppColor.swift` 보강.

- 색 헤더 바 두 개 → **흰 카드 한 장**. 맨 위가 낱말 대조 줄(`wordRow`),
  그 아래가 설명 두 줄. 누구 설명인지는 카드 배경색이 아니라 **앞의 세로 색 막대
  5pt**(`noteLine`)가 알린다.
- 낱말 줄은 `개천절 → 추석`. 고른 답은 분홍 + 취소선, 정답은 하늘색, 32pt.
  **1박자에는 정답 자리가 `?`** 로 비어 있다가 채워진다.
- 버튼 `minHeight: 64`. `PrimaryActionButton` 자체는 안 고쳤다 — 호출부에서만 넘긴다.
- `AppColor.pendingText` 추가(`?`·화살표용 흐린 회보라).
  `wrongBackground`/`answerBackground` 는 **미사용**이 됐고, 되돌릴 때를 위해
  표시만 붙여 남겼다.
- **두 박자는 그대로다.** 이번 변경은 색과 배치뿐이다.
- 설명이 아예 없는 문항(오타·O/X 라벨)에서는 흰 칸 자체를 안 그린다 —
  안 그러면 낱말 줄 아래 빈 흰 띠만 남는다.

**대가 (알고 택한 것)**

- **취소선.** 「가리키기만 하고 나무라지 않는다」던 원래 원칙보다 한 발 더 나간
  표시다. `.strikethrough(...)` 한 줄이라 빼기 쉽다.
- **작은 화면.** `.medium` 은 화면 절반이라 iPhone SE(667pt)에서는 쓸 수 있는 높이가
  213pt로 줄어 어떤 시안도 안 들어간다. 이번 계산은 844pt 기준이다.
- **해설이 더 길어지면** 다시 넘친다. 여유가 11pt뿐이라 문장 하나가 늘면 끝이다.
  `ScrollView` 는 그대로 두어 잘려 사라지지는 않는다.
- 뒤 화면 음영을 없애느라 켠 `presentationBackgroundInteraction(.enabled)` 때문에
  **모달이 떠 있는 동안 뒤 문제 화면이 탭된다.** iOS가 "안 어둡게"와 "뒤도 탭 가능"을
  한 세트로만 제공해서 생긴 대가다(GlossaryPanel 주석과 같은 사정).

**확인한 것** — 컴파일은 못 하므로 (1) 중괄호·괄호·대괄호 짝, (2) 쓰는 `AppColor`
토큰 6개가 전부 정의돼 있는지, (3) 없앤 이름(`cardHeader`·`headerSize`·
`wrongBackground` 등)이 남아 있지 않은지, (4) `QuizView` 의 호출
(`FeedbackSheet(feedback:onNext:)`)이 그대로인지, (5) `PrimaryActionButton(title:
minHeight:)` 호출 형태가 그 파일의 기존 Preview 선례와 같은지, (6) 브라우저에서
1pt = 1px 로 그려 **여유 +11pt** 를 실측.

**검증 상태** — 다시 빌드해서 (1) 스크롤 없이 한 화면에 들어가는지, (2) 1박자에
`개천절 → ?` 가 보이고 「정답 보기」를 누르면 `?` 자리에 정답이 들어오는지,
(3) 세로 색 막대만으로 누구 설명인지 구분되는지, (4) 취소선이 과하지 않은지,
(5) 설명 없는 문항(오타 답)에서 빈 흰 띠가 없는지, (6) 버튼이 커진 것이
어색하지 않은지 확인해 주세요.

**후속 2 (2026-09-21) — 오답이 길어지면 「한 줄 대조」가 깨진다 → 타이틀 박스를 없애고 「낱말 배지」로 바꿨다**

시안 1(한 줄 대조, `개천절 → 추석`)을 실제로 넣고 보니, 오답이 **"고구려, 백제,
신라"** 처럼 길어지는 문항에서 한 줄 대조가 줄바꿈되며 어색해졌습니다. 폰트를
줄이거나 뒤를 말줄임표로 자르는 안도 검토했지만, **정답을 자르면 비슷한 오답과
구분이 안 되는 경우**(예: "고구려, 백제, 가야" vs "고구려, 백제, 신라" 가 둘 다
"고구려, 백…"로 잘림)가 있어 채택하지 않았습니다.

대신 "타이틀 박스 없이 해설만 있어도 비교 인지가 될까?"를 시안 3개로 확인했고,
**시안 3(낱말을 배지로 앞세우고 해설이 이어짐)** 으로 정했습니다.

**시안 3을 고른 이유**

1. **길이에 안전하다.** 배지는 폭이 늘어날 뿐 줄 구조가 안 깨진다.
2. **낱말 사전 패널(GlossaryPanel)의 배지와 같은 시각 언어**(코너 반경 7, 굵은
   글자)라 앱 전체가 한 덩어리로 보인다.
3. **구조적으로 더 안전하다.** 해설 문장은 온디바이스 모델이 매번 새로 쓰는
   글이라 정답 낱말이 문장에 아예 안 나오는 경우가 실제로 있습니다(정답
   "고조선", 해설 "고는 옛날이라는 뜻이에요. 도읍은 아사달이었어요." — "고조선"이
   한 번도 안 나옴). 낱말 대조 줄이 있을 때는 그 줄이 정답을 보여줬지만, 시안
   1·2(문장에 이모지·색만 칠하는 안)로 갔다면 이런 문항에서 **정답이 화면 어디에도
   안 보이는** 결함이 생겼을 것입니다. 시안 3은 배지가 문장과 별개로 낱말을
   항상 보여주므로 이 결함이 구조적으로 없습니다.

**공간 재계산 — 어머니 기기 크기를 다시 잡았다**

지난 라운드까지 393pt 폭 기기를 가정하고 있었는데, 실제 화면 스크린샷의 줄바꿈
(장문 오답이 "3줄/3줄"로 잡히는 것)을 재현하는 폭을 역산해 보니 **440pt급
(Pro Max 급, 440×956pt)** 기기였습니다. 이 계산 착오를 사용자에게 그대로
알렸고, 이후 계산은 모두 440pt 기준으로 다시 했습니다.

| 안 | 440pt 기기 | 393pt 기기 |
|---|---|---|
| 시안 1 · 흰 카드 (배지 없음) | +49pt | **−42pt** |
| 시안 2 · 색 바탕 (배지 없음) | +40pt | **−51pt** |
| 시안 3 · 낱말 배지 (채택안) | +17pt | **−73pt** |
| (참고) 시안 1 적용 직전 출시본 | +9pt | −116pt |

440pt 기기에서는 세 안 다 스크롤 없이 들어갑니다. **393pt 기기에서는 세 안 다
넘칩니다** — 특히 시안 3은 배지가 낱말·해설을 한 줄이 아니라 위아래로 나누는
대가로 셋 중 가장 많이 넘칩니다. 실측(`/tmp/levers.py`, 이전 라운드)으로
확인된 가장 효과 큰 여백 확보 수단이 **"오답 설명만 22 → 19pt"**(약 46pt 절약,
3줄→2줄로 접힘)라서, **이 레버를 처음부터 기본값으로 켰습니다** — 393pt에서도
스크롤 없이 들어가도록 안전장치를 먼저 넣은 것입니다. **어머니의 실제 기기
크기는 아직 확인받지 못했습니다.**

**한 것** — `KCT/Screens/FeedbackSheet.swift` 전면 재작성, `AppColor.swift` 주석 보강.

- 낱말 대조 줄(`wordRow`, "개천절 → 추석")을 **없앴다.** 그 자리에 있던 낱말은
  각 설명 줄 머리의 **배지**(`noteRow`)로 옮겼다 — 칸을 하나 더 그리는 게 아니라
  줄의 머리를 낱말로 바꾸는 것이라 높이가 거의 안 늘었다.
- 시트 맨 위에 **제목 한 줄**을 새로 넣었다 — 1박자 "오답 해설", 「정답 보기」를
  누르면 "오답 및 정답 비교 해설"로 바뀐다. 사용자가 요청한 문구 그대로다.
  데모에 있던 "정답은 아래 버튼을 누르면 나와요" 안내 문구는 **넣지 않았다**
  (요청대로 뺐다 — 실제 코드에는 원래도 없었다).
- 오답 설명 글자 크기를 22 → **19pt**로 낮췄다(`wrongNoteSize`). 정답 해설은
  22pt 그대로 — "정답은 더 크게, 오답은 인식만 되면 된다"는 비대칭.
- 문장 속 낱말 색칠(`highlight`)은 **그대로 유지**했다 — 데모는 보기 좋으라고
  문장 앞의 낱말을 손으로 지웠지만, 실제 문장은 모델이 매번 다르게 써서
  "앞 낱말만 지운다" 같은 규칙이 안전하지 않다. 배지 + 문장 색칠 이중 표시로
  남겨, 낱말이 문장에 있으면 두 곳이 같은 색으로 보이고 없으면 배지가 혼자
  그 역할을 떠맡는다.
- `AppColor.wrongHeader`/`answerHeader`— 세로 막대 배경에서 **배지 배경**으로
  역할이 바뀌었다(주석 갱신). `pendingText`는 낱말 대조 줄과 함께
  **미사용**이 됐다(주석 갱신, 값은 남김).

**대가 (알고 택한 것)**

- **393pt 기기에서는 시안 3이 셋 중 가장 여유가 적다.** 19pt 레버를 기본으로
  켜서 완화했지만, 어머니 기기가 그보다도 작으면(iPhone SE급) 추가 조정이
  필요할 수 있다.
- **배지 + 문장 색칠 이중 표시**가 다소 중복으로 보일 수 있다 — 다만 이건
  파일에 이미 있던 원칙("같은 낱말이 두 곳에서 같은 색으로 보이면 눈이 그
  색을 낱말로 배운다")을 그대로 따른 것이라 의도한 중복이다.

**아직 안 한 것** — 이전 라운드(장문 오답 대응 데모)에서 나온 "제목과 모달
사이 여백을 조금 더" · "다음 문제 버튼을 조금 내려서 콘텐츠 공간 확보" 요청은
이번 변경에 포함하지 않았다. 이번 라운드가 그 요청을 대체하는지 아직 확인이
필요하다.

**확인한 것** — 컴파일은 못 하므로 (1) 중괄호·괄호·대괄호 짝(44/44, 130/130,
4/4), (2) 없앤 이름(`wordRow`·`wordSize`·`arrowSize`)이 코드 어디에도 안 남아
있는지, (3) `QuizView`의 호출(`FeedbackSheet(feedback:onNext:)`)이 그대로인지
grep으로 확인.

**검증 상태** — 다시 빌드해서 (1) 스크롤 없이 한 화면에 들어가는지, (2) 1박자에
「오답 해설」 제목 + ❌ 배지·설명만 보이고 「정답 보기」를 누르면 제목이
바뀌며 ✅ 배지·해설이 이어 붙는지, (3) "고구려, 백제, 신라"처럼 긴 정답도
배지가 자연스럽게 늘어나는지, (4) 오답 설명 글자가 정답보다 살짝 작아 보이는
것이 어색하지 않은지, (5) 설명이 없는 문항(`#Preview("설명이 없을 때")`)에서
배지만으로 정답이 보이는지 확인해 주세요. **그리고 어머니 기기가 440pt급인지
더 작은지 알려주시면**, 393pt 기준 여유(-73pt는 19pt 레버 적용 전 수치이며
현재 코드는 이미 레버가 켜져 있어 그보다 여유가 있습니다)까지 마저 확정하겠습니다.

**후속 3 (2026-09-21) — 색·크기·여백 미세조정 3건**

시안 3을 넣고 실제 화면을 보신 뒤 이어진 요청 세 개를 한 번에 반영했습니다.

1. **배지 배경색이 너무 진하다 → 60% 더 옅게.** `AppColor.wrongHeader`/
   `answerHeader`는 원래 진한 분홍·하늘(opacity 0.8)이 흰 배경 위에서
   실제로 보이던 색(약 RGB 0.93·0.49·0.62 분홍, 0.40·0.73·0.90 하늘)을
   기준으로, 흰색 쪽으로 60% 더 섞었다. 결과: 분홍 `(0.97, 0.80, 0.85)`,
   하늘 `(0.76, 0.89, 0.96)`. opacity를 걷어내고 미리 섞은 값을 직접 쓰는
   방식으로 바꿨다 — 배지가 뒤에 뭐가 있든 항상 같은 옅은 색으로 보이게.
2. **폰트를 오답 기준으로 통일.** 정답 해설(22pt)·오답 설명(19pt)이
   다르던 비대칭을 걷어내고, 배지·오답 설명·정답 해설 모두 **19pt**로
   통일했다(`FeedbackSheet.noteSize`). 원래는 "정답은 크게, 오답은 인식만
   되면 된다"는 인지 연구 근거로 일부러 다르게 뒀던 값인데, 실제 화면에서
   비대칭이 어색하다는 판단으로 되돌렸다. 덕분에 정답 해설이 길 때도 이제
   19pt가 적용돼 작은 기기에서의 여유가 더 늘었다(정확한 pt는 재보지
   못했다 — 컴파일 환경이 없다).
3. **「다음 문제」 버튼을 살짝 아래로.** 시트 하단 여백을 24 → 14pt,
   버튼 위 최소 간격을 12 → 8pt로 줄여 버튼을 바닥 쪽으로 옮기고, 그만큼
   위 콘텐츠(`ScrollView`)가 쓸 공간을 늘렸다. 지난 라운드에서 "아직 반영
   안 한 것"으로 남겨 뒀던 요청이다.
4. **카드 배경의 반투명을 없앴다.** `notesCard`의 `Color.white.opacity(0.92)`가
   뒤 보라 배경을 살짝 비치게 해서, 옅어진 배지·오답 해설 글자가 흐릿해
   보인다는 피드백을 받아 **완전 불투명한 흰색**으로 바꿨다.

**확인한 것** — 중괄호·괄호·대괄호 짝(44/44, 137/137, 4/4), 없앤 이름
(`badgeSize`·`wrongNoteSize`)이 안 남았는지, `0.92`가 라이브 코드가 아니라
주석에만 남았는지 grep으로 확인.

**검증 상태** — 다시 빌드해서 (1) 배지 색이 실제로 옅어 보이는지, (2) 오답·
정답 글자 크기가 같아 보이는지, (3) 「다음 문제」 버튼이 이전보다 살짝 아래
있는지, (4) 카드 배경이 또렷한 흰색으로 보이고 뒤 보라가 안 비치는지
확인해 주세요.

**후속 4 (2026-09-21) — 모달 보라색 재질문 + 폰트 2pt 재조정**

**"언제 바뀐거야, 원래 낱말 해설에 적용된 게 좋았는데?"** — 확인해 보니 이
모달의 보라 배경값(`AppColor.signature.opacity(0.5)`) 자체는 이번 라운드
내내 **한 번도 안 바뀌었고**, 지금도 낱말 해설(GlossaryPanel)과 정확히
같은 값입니다. 대신 후속 3에서 카드를 완전 불투명한 흰색으로 바꾸고 배지
색도 옅게 만들면서, 화면에 보이는 보라 면적·대비가 줄어 상대적으로 연하게
느껴지신 것으로 보입니다. 요청대로 **이 모달만** 0.5 → **0.58**로 살짝
더 진하게 올렸습니다 — GlossaryPanel은 이번 요청 대상이 아니라서 0.5
그대로 뒀습니다(둘이 이제 값이 갈립니다, 의도한 것입니다).

**폰트 2pt 인상.** 후속 3에서 오답 기준(19pt)으로 통일했던 배지·오답·정답
공용 글자 크기(`noteSize`)를 **21pt**로 2pt 올렸습니다. 배지·오답 설명·
정답 해설이 전부 같은 값을 쓰므로 셋 다 같이 커집니다(다시 비대칭이
생기지 않습니다). 다만 줄간격(`lineSpacing`)은 19pt·22pt 두 값만 CSS
데모로 실측해 둔 것이라, 21pt는 19pt쪽 값(7)을 그대로 쓴 근사값입니다.
22pt(원래 정답 크기)에 다시 가까워졌으니, 작은 기기(393pt)에서의 여유는
19pt였을 때보다 줄어듭니다 — 정확한 pt는 컴파일 환경이 없어 재지 못했고,
이 부분은 실제 기기에서 봐 주셔야 합니다.

**확인한 것** — 중괄호·괄호·대괄호 짝(44/44, 140/140, 4/4), `noteSize`·
`BodyStyle` 기본값이 전부 21로 일관되는지 grep으로 확인.

**검증 상태** — 다시 빌드해서 (1) 모달 배경 보라가 이전보다 살짝 진해
보이는지(낱말 해설 모달과는 이제 미세하게 달라야 정상), (2) 배지·오답·
정답 글자가 전보다 커 보이면서 여전히 크기가 서로 같은지, (3) 작은 기기
시뮬레이터가 있다면 스크롤 없이 들어가는지 확인해 주세요.

**후속 5 (2026-09-21) — 낱말 해설 쪽도 같이 어둡게, 두 모달을 다시 통일**

후속 4에서 오답 해설 모달(FeedbackSheet)만 0.5 → 0.58로 올리고 낱말 해설
(GlossaryPanel)은 그대로 뒀었는데, 낱말 해설 쪽도 같이 어둡게 해 달라는
요청을 받아 **`GlossaryPanel.swift`도 0.58로 맞췄다.** 이제 두 모달이
다시 같은 보라 배경값을 쓴다 — 갈라졌던 걸 되돌린 게 아니라, 더 어두운
값으로 다시 통일한 것이다.

**확인한 것** — `GlossaryPanel.swift`를 이번 세션에서 처음 건드려서 전체
괄호 짝을 처음부터 셌다. 파일 전체로는 221:220으로 이미 어긋나 있었는데
(git HEAD 버전에도 이미 있던 상태 — 확인해 보니 내가 만든 게 아니다),
주석을 다 걷어내고 코드만 세면 118:118로 정확히 맞는다. 즉 이 어긋남은
주석 속 괄호 하나 때문이고 실제 코드 문법과는 무관하다. 내가 추가한
주석은 괄호가 짝이 맞아서, 편집 전후로 이 어긋남의 정도는 그대로다
(221:220 → 222:221, 둘 다 +1).

**검증 상태** — 다시 빌드해서 (1) 낱말 사전 모달(글자 탭했을 때 뜨는 것)의
보라색이 이전보다 살짝 진해 보이는지, (2) 오답 해설 모달과 색이 다시
같아 보이는지 확인해 주세요.

**후속 6 (2026-09-21) — 보라색을 한 번 더: 0.58 → 0.65**

"투명도를 조금만 더 낮춰주고(=더 불투명하게) 보라색을 조금만 더 진하게"
요청을 받아, 두 모달(`GlossaryPanel`·`FeedbackSheet`)의
`presentationBackground(AppColor.signature.opacity(...))` 값을
0.58 → **0.65**로 올렸다. 지금까지의 진행: 0.5(원래 공용값) → 0.58(후속
4·5) → 0.65(이번). 두 모달은 항상 같은 값을 쓴다.

**확인한 것** — 중괄호·괄호 짝(FeedbackSheet 44/44·140/140·4/4,
GlossaryPanel 39/39·222/221·8/8 — 221:220 대비 +1 오프셋은 후속 5에서
확인한 주석 속 괄호로, 이번 편집으로 새로 생긴 게 아니다).

**검증 상태** — 다시 빌드해서 두 모달의 보라색이 이전보다 한 단계 더
진해 보이는지, 뒤 화면 글씨가 그래도 알아볼 만큼은 비치는지 확인해
주세요. 계속 올라가고 있는 값이라, 너무 진해지면 뒤 화면이 안 보이는
지점이 있을 수 있습니다.

**후속 7 (2026-09-21) — 0.65 → 0.75**

두 모달의 보라 배경을 0.75로 올렸다. 진행: 0.5 → 0.58 → 0.65 → **0.75**.
0.75쯤 되면 뒤 지문이 비치는 정도가 상당히 옅어지므로, 다음에 더 올릴
요청이 오면 `.presentationBackgroundInteraction(.enabled)`의 원래
목적("뒤 화면이 밝게 비쳐 계속 보임")이 무색해지는 지점인지 실제 화면으로
확인해 보는 게 좋겠다.
